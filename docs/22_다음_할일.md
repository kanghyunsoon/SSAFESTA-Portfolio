# 다음 할 일

> **갱신: 2026-08-12** (이전 판은 2026-08-08 Week 1 기준이었고 대부분 완료되어 전면 개정)
> 현재 위치: **SDD 착수 완료** — spec-kit 설치·spec 18종·헌법 v1.2·팀 결정 확정
> 상세 경과: `docs/24_작업일지.md` · 진행 방법: `docs/00_SDD_가이드.md`

---

## 완료된 것 (참고)

| 항목 | 상태 |
|---|---|
| Unity 초기 구조 + POC A~D | ✅ 2026-08-08 |
| Web 빌드 ↔ Docker Dedicated Server 멀티플레이 | ✅ 로컬 실증 |
| Git 저장소 + 기준선 태그 `v0.0.1-poc` | ✅ |
| 팀 결정 (①번 14건 + 파트별) | ✅ 2026-08-12 — `docs/26` |
| spec-kit 설치 (Claude + Codex 공용) | ✅ 동작 검증 완료 |
| spec 18종 작성 | ✅ `specs/` |
| 헌법 v1.2 | ✅ `.specify/memory/constitution.md` |
| Unity 파트 spec 6종 확정 + 013 plan 산출물 | ✅ |

## 7. Booth 통합 다음 작업 (2026-08-14 확정 계약 기준)

- [x] Layout 식별자를 `objectId`, 공개 조회 경로를 `/booths/{boothId}/layouts/published`로 통일
- [x] Unity의 canonical Object Type 10종과 구 POC 별칭 읽기 호환 반영
- [ ] Unity `World_11F`에 물리 임대 슬롯 수만큼 `BoothExteriorSlot`·`BoothInteriorAnchor`·외부 복귀 지점 배치
- [ ] Dedicated Server에 활성 Lease 검증, `currentBoothId`, 내부 Teleport, 만료 강제 퇴장 구현
- [ ] React Booth Studio를 `외부 설정`과 `내부 꾸미기`로 분리
- [ ] Spring Lease 만료 트랜잭션과 Slot/Booth 연결 해제, Draft 보존 구현
- [ ] Publish → 외부 입장 → 내부 Local Spawn → 퇴장/만료 Clear E2E 검증

## 7. Booth 통합 다음 작업 (2026-08-14 확정 계약 기준)

- [x] Layout 식별자를 `objectId`, 공개 조회 경로를 `/booths/{boothId}/layouts/published`로 통일
- [x] Unity의 canonical Object Type 10종과 구 POC 별칭 읽기 호환 반영
- [ ] Unity `World_11F`에 물리 임대 슬롯 수만큼 `BoothExteriorSlot`·`BoothInteriorAnchor`·외부 복귀 지점 배치
- [ ] Dedicated Server에 활성 Lease 검증, `currentBoothId`, 내부 Teleport, 만료 강제 퇴장 구현
- [ ] React Booth Studio를 `외부 설정`과 `내부 꾸미기`로 분리
- [ ] Spring Lease 만료 트랜잭션과 Slot/Booth 연결 해제, Draft 보존 구현
- [ ] Publish → 외부 입장 → 내부 Local Spawn → 퇴장/만료 Clear E2E 검증

## 7. Booth 통합 다음 작업 (2026-08-14 확정 계약 기준)

- [x] Layout 식별자를 `objectId`, 공개 조회 경로를 `/booths/{boothId}/layouts/published`로 통일
- [x] Unity의 canonical Object Type 10종과 구 POC 별칭 읽기 호환 반영
- [ ] Unity `World_11F`에 물리 임대 슬롯 수만큼 `BoothExteriorSlot`·`BoothInteriorAnchor`·외부 복귀 지점 배치
- [ ] Dedicated Server에 활성 Lease 검증, `currentBoothId`, 내부 Teleport, 만료 강제 퇴장 구현
- [ ] React Booth Studio를 `외부 설정`과 `내부 꾸미기`로 분리
- [ ] Spring Lease 만료 트랜잭션과 Slot/Booth 연결 해제, Draft 보존 구현
- [ ] Publish → 외부 입장 → 내부 Local Spawn → 퇴장/만료 Clear E2E 검증

## 7. Booth 통합 다음 작업 (2026-08-14 확정 계약 기준)

- [x] Layout 식별자를 `objectId`, 공개 조회 경로를 `/booths/{boothId}/layouts/published`로 통일
- [x] Unity의 canonical Object Type 10종과 구 POC 별칭 읽기 호환 반영
- [ ] Unity `World_11F`에 물리 임대 슬롯 수만큼 `BoothExteriorSlot`·`BoothInteriorAnchor`·외부 복귀 지점 배치
- [ ] Dedicated Server에 활성 Lease 검증, `currentBoothId`, 내부 Teleport, 만료 강제 퇴장 구현
- [ ] React Booth Studio를 `외부 설정`과 `내부 꾸미기`로 분리
- [ ] Spring Lease 만료 트랜잭션과 Slot/Booth 연결 해제, Draft 보존 구현
- [ ] Publish → 외부 입장 → 내부 Local Spawn → 퇴장/만료 Clear E2E 검증

## 8. Game Studio P2 — 로컬 제작·플레이 완료, 운영 API E2E 대기 (2026-08-23)

> Game Studio는 Unity 미니게임이 아니라 독립 React 제작기/Web 2D Runtime이다.
> Unity는 선택적 부스 진입 trigger만 담당하고 GameProject를 실행하지 않는다.

- [x] `specs/019-game-studio/spec.md` + requirements checklist 작성
- [x] GameProject v1 JSON Schema, API/Portal/Preview/Event Runtime 계약 작성
- [x] Scene/Object/Properties/Event 편집 모델과 Asset reference 정책 문서화
- [x] OVERLAY/FULL_SCREEN DIALOGUE, `CLOSE_DIALOGUE`, `nextNodeId` Schema 위치 보완
- [x] Positive/Negative fixture 계약 검증 **7/7 통과**
- [x] renderer 없는 입력/상태 reference Runtime trace **6/6 통과**
- [x] 공통 `spec.md/contracts`와 `FE/`, `BE/` 실행 문서 5종을 파트별로 분리
- [x] GitHub 파트 이슈 등록: [#20 FE](https://github.com/kanghyunsoon/ssafesta/issues/20), [#21 BE](https://github.com/kanghyunsoon/ssafesta/issues/21), [#22 AI](https://github.com/kanghyunsoon/ssafesta/issues/22)
- [x] #20 Host 답변 반영 — `festa-frontend/src/game-studio/`, `/app/games/:gameId/edit|play`, same-origin Preview
- [x] #21 BE 답변 반영 — Draft/Published 2테이블, revision 409, atomic Publish, Portal no-store
- [x] #22 AI 답변 반영·종료 — **MVP 선행 조건 아님**, P2 candidate/patch + spec 007 Job 정책
- [x] develop에서 ERD가 `020-erd-schema`로 정리된 최신 기준에 맞춰 Game Studio를 `019-game-studio`로 고정
- [x] 남은 결정 분리: [#33 제품 정책](https://github.com/kanghyunsoon/ssafesta/issues/33), [#34 Portal ID](https://github.com/kanghyunsoon/ssafesta/issues/34), [#35 Studio 내부](https://github.com/kanghyunsoon/ssafesta/issues/35)
- [x] #33 합의 반영·이슈 종료 — 신규 REST 진입만 차단, loaded session 완료 허용, soft/hard delete, 이력 유지, Ranking P1 절연
- [x] #34 합의 반영·이슈 종료 — signed Int32 `configId`, 별도 INTEGER 공개 ID, `GAME_PORTAL requiresConfig=true`, BE-first 배포
- [x] `festa-frontend/src/game-studio/` contract/core와 renderer 의존 없는 US1 테스트 구현 — `codex/game-studio-web-runtime-core`
- [x] [#49 Frontend](https://github.com/kanghyunsoon/ssafesta/issues/49) Scene/Object/Event/DIALOGUE Workspace와 local Preview 수직 구현 — PR #63 병합·이슈 종료
- [x] TOP_DOWN·PLATFORMER·DIALOGUE 공통 Runtime, 6종 시각 템플릿, 재료함·간단/고급 Inspector·범용 튜토리얼 구현
- [ ] [#48 Backend](https://github.com/kanghyunsoon/ssafesta/issues/48) `game_drafts`·`game_published_versions` migration과 Draft/Publish integration test 착수
- [x] 독립 URL local Preview와 Frontend Portal Host adapter 구현 — 실제 resolver E2E는 [#56](https://github.com/kanghyunsoon/ssafesta/issues/56) 대기
- [x] PLATFORMER reference renderer·physics·편집·상태 유지 구현
- [ ] [#69](https://github.com/kanghyunsoon/ssafesta/issues/69) 사용자 Asset stable `asset://` 업로드·승격 연결
- [ ] [#73](https://github.com/kanghyunsoon/ssafesta/issues/73) 사람 대상 20분 제작 테스트와 활성 PC 탭 55~60fps 기록

실행 작업은 `specs/019-game-studio/FE/tasks.md` 74개와 `BE/tasks.md` 21개로 분리했다. 총 95개 중
Frontend·공통 계약 **66개를 완료**했고, FE 잔여 8개와 BE 21개는 #48·#56·#69 운영 endpoint 및 #73 사람 QA 경계에서 추적한다.
작업 기록은 `docs/KHS/27_Game_Studio_작업일지.md`, 문제 기록은
`docs/KHS/28_Game_Studio_트러블슈팅.md`에서 일반 Unity 기록과 분리해 관리한다.

---

## 1. 전원 — spec 검토 (지금 여기)

각자 `docs/00_SDD_가이드.md` §3에서 자기 spec을 찾아 **맨 아래 리뷰 3칸**을 채운다.
3칸을 채운 spec부터 `/speckit-plan` → `/speckit-tasks` → 구현.

- [ ] BE — 001 / 003 / 004 (+002·005·009·013·016 협조분)
- [ ] FE 이정헌 — 005 / 016 (+001·013·009·010)
- [ ] AI 김가현 — 007 / 008 (+011)
- [ ] Infra — CI/CD, AWS wss (+017·018 서버 구성)
- [x] Unity — 002·006·013·014·017·018 **확정 완료**

> ⚠️ 검토 없이 바로 plan을 돌리면 리드가 추측으로 쓴 요구사항이 코드까지 내려간다.

## 2. 기다리지 말고 지금 시작 (spec 검토와 병렬)

- [ ] **BE — Google·Kakao OAuth 앱 등록** (개발자 콘솔 승인에 리드타임이 있다. 제일 먼저)
- [ ] **Infra — 파트 브랜치 CI/CD** (`ai`/`back`/`front`/`game` + `develop`)
- [ ] **Infra — AWS wss 실측** ← 헌법 6조의 마지막 빈칸(ALB/NLB)을 채운다
- [ ] **AI — 미니 스파이크**: PDF 1개 → 파싱 → 청킹 → 임베딩 → 격리 검색 → 답변
      GMS 키 전이므로 어댑터 뒤 Mock으로. 실측값이 007/008 clarify를 정확하게 만든다
- [ ] **FE — 공통 구조 + Overlay Platform 계약 확정** (김가현이 여기 얹혀야 착수 가능)
- [ ] **Unity — 013 캐릭터 커스터마이징 UI** (`docs/29` 작업지시서)

## 3. 이번 주 안에 모여서 정할 것

혼자 정할 수 없고 여러 spec이 동시에 대기 중인 것.

| 안건 | 참여자 | 막히는 spec |
|---|---|---|
| **Layout JSON 왕복 검증** (규칙은 확정, 실제 일치 확인이 남음) | FE + Unity | 005 · 006 · 016 |
| Unity → React 상호작용 payload 형식 | FE + Unity | 006 · 008 · 016 |
| SSE 이벤트 payload 상세 필드 | AI + FE | 008 |
| 층별 서버 인스턴스 구성 | Infra + Unity | 018 (2차) |

## 4. 운영

- [ ] **Jira 백로그 구성** — `/speckit-tasks` 산출물을 이슈로 옮긴다 (`/speckit-taskstoissues` 사용 가능)
- [ ] 헌법 29조 준수 시작: 작업일지(날짜별) / 트러블슈팅(날짜 포함) / **매일 종료 시 Jira 1회**
- [ ] 도메인 구매 + 서브도메인 설계 (`app.` / `api.` / `ai.` / `world.`)

---

## 남은 미확정 (AWS 실측 후)

| 항목 | 판단 기준 |
|---|---|
| ALB vs NLB | Health Check 방식 (NLB=TCP 체크로 코드수정 0 / ALB=/healthz 추가) |
| idle timeout 최종값 | heartbeat 실측 (초기 권장 180s) |
| pgvector 공유/분리 | 비용·부하 (권장: 같은 RDS + 별도 스키마) |
| ECS 이관 시점 | Unity Server 부하 실측 |

전체 미결 목록: `docs/26_팀_결정_필요사항.md` ③
