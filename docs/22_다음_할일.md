# 다음 할 일 (Week 1 잔여 + SDD 착수)

> 작성: 2026-08-08 · 현재 위치: Unity 초기 구조 + POC A/B/C/D 완료, **Web 빌드 로컬 ws:// 멀티플레이 실증 완료**
> 상세 경과는 `festa-unity/Docs/poc-status.md` 참조

---

## 1. Linux Dedicated Server 빌드 → Docker 🎯 최우선 (반나절)

- [ ] Build Profiles → **Linux Server** 프로파일 생성 → 빌드 (`Builds/linux-server/`)
- [ ] Dockerfile 작성 → 이미지 빌드 (Docker Desktop 필요)
- [ ] 로컬 Docker 컨테이너로 서버 실행 (`-port 7777`)
- [ ] 브라우저(Web 빌드)가 **Docker 안의 서버**에 접속되는지 확인
- 💡 Dockerfile / 실행 스크립트는 Claude 세션에서 요청하면 작성해줌

## 2. AWS wss 검증 — Week 1 최종 관문 (1~2일)

> 이게 통과되면 ADR 결정 1(Transport)이 실측 확정된다. 로컬에서 됐어도 여기서 막히면 의미 없음.

- [ ] EC2 1대에 Docker로 Unity Server 실행
- [ ] ALB + ACM 인증서 + Route53 도메인 세팅 (Security Group: 443 only)
- [ ] ALB idle timeout **180초 이상**으로 상향 (기본 60초 — NGO 연결 끊김 방지)
- [ ] Web 빌드를 S3 + CloudFront(HTTPS)로 서빙
- [ ] **HTTPS 페이지에서 `wss://`로 접속 성공** ← 성공 판정 기준
- [ ] 60초 이상 무입력 상태에서 연결 유지 확인
- ⚠️ 사전 코드 수정 필요: 현재 HUD는 IP:Port 접속만 지원 → **wss URL 접속 경로를 ConnectionManager에 추가** (AWS 작업 시작할 때 Claude에 요청)
- ⏱️ **타임박스: 5일.** 안 되면 "더 파기"가 아니라 Transport 대안(Unity Relay 등) 전환 판단

## 3. AI 미니 스파이크 (반나절, 병렬 — AI 담당)

- [ ] Embedding API 키 발급 + 결제 수단 확인 ← 의외의 병목, 먼저 처리
- [ ] PDF 1개 → 파싱 → 청킹 → 임베딩 → pgvector 저장
- [ ] `WHERE booth_id = :x AND agent_id = :y` 필터 검색 동작 확인
- [ ] 실측 기록: 벡터 차원, 문서 처리 시간, 비용 → ADR 결정 2의 근거

## 4. 팀 결정 회의 (90분)

> `docs/21_착수전_기술결정_ADR.md`의 6개 결정 빈칸을 실측 데이터로 채운다.

- [ ] 결정 1: Transport — 2번 결과로 사실상 자동 확정
- [ ] 결정 2: Embedding 모델/차원 — 3번 결과 기반
- [ ] 결정 3: LLM Provider (1차 모델 + timeout 수치)
- [ ] 결정 4: SSE + AI 채팅 UI를 React 오버레이로 (Unity 내 한글 입력 금지)
- [ ] 결정 5: Token 4계층 (Access/Refresh/Connection/WS)
- [ ] 결정 6: Docker 필수 + EC2 시작, Unity Server만 Week 6 전 ECS 이관
- [ ] 회의 후: 21번 문서 확정본 전환, 12번 문서 POC 체크리스트에 wss 항목 반영

## 5. SDD 착수 (spec-kit)

- [ ] spec-kit 설치: `uvx --from git+https://github.com/github/spec-kit.git specify init --here --ai claude`
- [ ] `constitution.md` 작성 — 결정사항 + 원칙 (wss만 사용, 벡터 차원 고정, 텍스트 입력은 React, Refresh Token은 Unity 전달 금지, 전 컴포넌트 Docker, AI 장애 격리, Coin은 REST/DB 트랜잭션만)
- [ ] `docs/02_서비스_기능_명세서.md` 기준으로 spec 분할안 작성 (001~00N + 의존관계)
- [ ] 첫 기능 `/speckit.specify` 시작
- [ ] **스파이크 코드 소급 처리**: festa-unity의 Booth Runtime / Connection 구조 중 살아남는 코드는 해당 기능 spec에 소급 반영 (성역화 금지)

## 6. 저장소 / 운영 정리 (짬 날 때)

- [ ] SSAFESTA `git init` → 첫 커밋 (`.gitignore` 2종 배치 완료: 루트=reference 제외, festa-unity=Unity 표준)
- [ ] GitLab 저장소 생성 + `docs/17` 브랜치 전략 적용
- [ ] Jira에 위 작업들 Epic/Task 등록 (`docs/18` 가이드 기준)
- [ ] IdleGameScripts 레퍼런스 삭제 (선택 — BossRoom/VampireSurvivors는 유지)

---

## 미뤄둔 것 (지금 안 함 — 정식 spec 단계 몫)

- 플레이어 충돌/CharacterController 이동
- 재접속 상태 머신 (BossRoom 참고 예정)
- 이동 서버 검증, 30명 부하 테스트
- Active Input Handling `Both` 원복 검토 (DevConnectionHud를 정식 UI로 교체할 때)
- Mock → 실서버(Spring/FastAPI) HTTP/SSE 구현
