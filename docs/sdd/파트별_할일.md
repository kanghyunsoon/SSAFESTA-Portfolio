# SDD — 파트별 지금 할 일

> **작성 기준일**: 2026-08-12 (Unity 리드)
> **이 문서를 읽는 법**: 자기 이름 칸만 보면 된다. 위에서부터 순서대로 하면 된다.
> **공통 원칙**: 모르는 건 혼자 정하지 말고 `docs/26_팀_결정_필요사항.md`에 올린다 (헌법 22조).

---

## 0. 지금 상황 한 줄 요약

팀 결정이 끝나서 SDD를 시작할 수 있는 상태다. **spec 초안 6개(`specs/`)를 리드가 먼저 써놨으니, 각자 자기 것을 검토하고 → 자기 repo에서 plan/tasks를 돌리면 된다.**

```text
[1단계] spec 검토      ← 지금 여기. 전원.
[2단계] spec-kit 설치  ← 각 파트 repo
[3단계] plan / tasks   ← 각자 실행
[4단계] 구현 시작
```

---

## 1. 전원 공통 (먼저 30분)

- [ ] `.specify/memory/constitution.md` 읽기 — **프로젝트 헌법**. 여기 어긋나는 구현은 리뷰에서 막힌다. 특히 자기 파트 관련 조항.
- [ ] `docs/00_SDD_가이드.md` 읽기 — SDD를 어떻게 굴리는지
- [ ] `docs/sdd/parts/<자기파트>.md` 읽기 — 자기 spec 목록과 작성 가이드
- [ ] `specs/README.md` 읽기 — 검토하는 법 (**리뷰 3칸**)

**기록 규칙 (오늘부터 적용, 헌법 20조)**

- [ ] 작업 끝날 때마다 **본인 작업일지**를 날짜별로 갱신
- [ ] 트러블 생기면 **해결 여부와 무관하게** 본인(이름) 트러블슈팅 문서에 날짜 포함해 기록
- [ ] **매일 작업 종료 시 Jira 1회 업데이트** (필수)

형식은 `docs/24_작업일지.md` / `docs/25_트러블슈팅.md`를 그대로 따라하면 된다.

---

## 2. Backend (Spring) 담당 — `황덕`

가장 바쁘다. 001 → 003 → 004가 직렬이라 여기가 늦으면 전체가 늦는다.

### 이번 주

- [ ] **spec 001 (auth-user) 검토** — `specs/001-auth-user/spec.md`
  - C-01~C-08 답하기. 특히 **C-03(SSO/소셜 로그인 여부)** 은 범위가 크게 달라지니 먼저 결정
  - 리뷰 3칸 채우기
- [ ] **spec 002 (world-session) 검토** — `specs/002-world-session/spec.md`
  - BE가 만들 건 사실상 **API 1개 + 토큰 검증**뿐이다. Unity는 이미 돌아간다
  - **C-01 토큰 검증 방식**은 Unity 리드와 같이 결정 (Spring 조회 vs 서명 검증)
- [ ] spec-kit 설치 (`docs/00_SDD_가이드.md` §2)
- [ ] 001 spec을 자기 repo로 복사 → `/speckit.plan` → `/speckit.tasks` → 착수

### 그다음 (자기가 직접 spec 작성)

- [ ] **spec 003 (wallet-coin)** 직접 작성 — 가이드는 `docs/sdd/parts/BE.md`
- [ ] **spec 004 (booth-slot-lease)** 직접 작성
  - ⚠️ `SSAFY_FESTA_임대부스_팀_논의사항.md`의 **D01~D08 결정 결과를 반드시 반영**. 안 정해졌으면 그 회의부터
- [ ] spec 005(Layout) 검토 참여 — FE와 공동
- [ ] spec 013 C-01: **아바타 저장 컬럼을 VARCHAR(32)로 만들면 안 된다** (600자 이상 필요)

### 병목 주의

API Key(GMS)는 아직 없다. **인증·경제·임대는 키와 무관**하니 지금 바로 시작할 수 있다.

---

## 3. Frontend — 이정헌 (Frontend Main / Platform & Booth)

FE 공통 구조를 네가 정해야 다른 사람이 붙을 수 있다. **구조 확정이 최우선.**

### 이번 주 (순서 중요)

- [ ] **FE 공통 구조 확정** — spec보다 먼저. `역할 분담 확정안 §8`의 착수 전 공동 확정 항목
  - 폴더/Feature 구조, 네이밍, API Client, Auth, 공통 타입, Error/Loading/Empty 정책
  - **Overlay Platform 구조와 `openOverlay(type, payload)` 계약** ← 김가현이 AIChatOverlay를 붙이려면 이게 있어야 함
- [ ] **spec 005 (booth-studio-layout) 검토** — `specs/005-booth-studio-layout/spec.md`
  - ⚠️ **이 프로젝트에서 제일 중요한 계약 문서다.** C-02(좌표 원점·단위)가 어긋나면 배치가 통째로 틀어진다
  - Layout JSON 계약에 **3파트 서명** 필요 (FE/BE/Unity)
- [ ] **spec 016 (노트북 홈페이지) 검토** — `specs/016-booth-laptop-homepage/spec.md`
  - ⚠️ **§기술 리스크를 꼭 읽어라.** 유명 사이트는 iframe 삽입이 대부분 막힌다. "새 창 열기"를 예외가 아니라 기본 기능으로 설계해야 함
- [ ] **spec 013 (아바타 커스터마이징) 검토** — React 창이 네 담당
  - Unity 쪽은 이미 동작한다. C-06(브릿지 연결 방식)만 Unity 리드와 맞추면 됨
- [ ] spec-kit 설치 → 001 로그인 UI + 005 Studio 착수 (**Mock API로 BE와 병렬 가능**)

### 합의해야 할 상대

| 상대 | 내용 |
|---|---|
| Unity 리드 | Layout JSON, Bridge 이벤트 payload, 아바타 브릿지 |
| BE | Layout 저장·공개 API, 홈페이지 URL 저장 위치 |
| 김가현 | Overlay Platform 계약, AI Feature 입출력 |

---

## 4. AI / Conversation — 김가현

유일하게 **외부 키에 막힐 수 있는** 파트다. 어댑터부터 만들어두면 키가 늦어도 안 막힌다.

### 이번 주

- [ ] **AI 미니 스파이크 먼저** (spec보다 먼저 권장)
  - PDF 1개 → 파싱 → 청킹 → 임베딩 → 검색 → 답변까지 로컬에서 한 번 관통
  - 여기서 나온 실측값으로 spec의 clarify가 정확해진다
  - ⚠️ **GMS 키는 아직 없다.** LLM/Embedding은 **어댑터 인터페이스 뒤에** 두고 Mock 또는 개인 키로 진행 → 키 나오면 구현만 교체
- [ ] **spec 007 (ai-agent-document) 직접 작성** — 가이드 `docs/sdd/parts/AI.md`에 입력 초안 있음
- [ ] **spec 008 (ai-conversation-rag) 직접 작성**
  - **격리 테스트(Booth A의 Agent가 Booth B 문서를 반환하면 릴리스 불가)** 를 반드시 요구사항에 넣을 것 — 헌법 13조
  - SSE 이벤트 스키마(`start/token/source/done/error`)는 이정헌과 공동 확정
- [ ] spec-kit 설치 → plan → tasks

### 확정된 전제 (2026-08-12)

- Embedding **1536차원 고정**, chunk에 `embedding_model_id` 컬럼 기록
- LLM은 어댑터 뒤 저가 모델부터, timeout **TTFT 15s / 전체 60s**
- 스트리밍은 **SSE**, 채팅 입력 UI는 **React 오버레이** (Unity 한글 입력 회피)

---

## 5. Infra 담당 — `정승욱`

브랜치별 CI/CD가 있어야 다른 파트가 빨리 움직인다. **여기가 먼저 깔려야 한다.**

### 이번 주

- [ ] `docs/sdd/parts/INFRA.md` 읽고 infra-001 spec 작성
- [ ] **파트 브랜치 4개(`ai`/`back`/`front`/`game`) CI/CD 구축**
  - 각 브랜치는 자기 개발환경에 **독립 배포** (front 배포가 back을 재시작시키지 않게)
  - `develop`은 **실사용 환경 기준**으로 통합 CI/CD
  - 상세는 `docs/17_Git_개발_Convention.md §2-1`
- [ ] 환경 분리: dev(파트별) / demo(develop)
- [ ] **AWS wss 실측** — 이게 헌법의 마지막 빈칸(ALB vs NLB)을 채운다
  - 재료는 다 있음: `festa-unity/Docker/Dockerfile`(검증 완료), `festa-unity/Docs/deployment-handoff.md`
  - 성공 판정: 외부 브라우저 2개가 `wss://world.<도메인>`으로 붙어 서로 보임
- [ ] 도메인 구매 진행 (팀 자체 구매 결정) → 서브도메인 설계 (`app.` / `api.` / `ai.` / `world.`)

### 주의

- Unity 빌드는 **캐시(Library) 전략이 없으면** 파이프라인이 빌드 시간에 잡아먹힌다
- Secret은 저장소에 절대 커밋 금지 — CI Variable / Secret 저장소 사용 (헌법 11조)

---

## 6. Unity — 강형순

- [ ] 커스터마이징 파츠 교체 → 타 클라이언트 반영 최종 검증 (T-24/T-25 수정 후)
- [ ] AvatarCatalog 프리셋 재입력 (T-21로 소실된 데이터)
- [ ] spec 002 / 006 / 013 소급 spec 확정
- [ ] 005 Layout 계약 3파트 합의 주도
- [ ] 006 상호작용 이벤트 payload를 이정헌과 확정
- [ ] 016 노트북 3D 에셋 확보 (라이선스 확인)

---

## 7. 이번 주 안에 모여서 정해야 할 것

혼자 정할 수 없는 것들. **한 번에 모아서 30분이면 끝난다.**

| 안건 | 참여자 | 왜 급한가 |
|---|---|---|
| **Layout JSON 좌표 원점·단위** | FE + BE + Unity | 어긋나면 배치가 전부 틀어짐. 005/006/016이 전부 대기 |
| 접속 토큰 검증 방식 | BE + Unity | 001/002 동시 착수 조건 |
| 임대 정책 D01~D08 | 전원 | spec 004를 못 씀 |
| 아바타 문자열 길이·저장 타입 | BE + Unity | DB 스키마가 걸림 |
| SSE 이벤트 스키마 | AI + FE | 008 착수 조건 |

---

## 8. 막히면

- 결정이 필요해서 막힘 → `docs/26_팀_결정_필요사항.md`에 등록하고 리드에게 알림
- 기술 문제로 막힘 → **본인 트러블슈팅 문서에 먼저 기록**하고 공유 (같은 문제를 두 사람이 겪는 걸 막는다)
- spec이 틀린 것 같음 → 리뷰 3칸의 ②에 적어라. **그게 정상이고 그러라고 만든 칸이다**
