# 팀 결정 필요사항 모음

> **목적**: 흩어져 있는 "팀이 정해야 하는 것"을 한 곳에 모은다. 결정되면 이 문서에 결정값을 기록하고 원본 문서에 반영한다.
> **분류 기준**: 언제 정해야 하는가 — ① 지금 회의 ② spec 작성 단계 ③ AWS 실측 후
> 갱신 규칙: 결정 즉시 "결정값" 칸 채우고 날짜 기입. 미결정 항목을 구현자가 임의로 정하지 않는다 (doc 00 원칙).

---

## ① 지금 회의로 확정 (SDD 착수 전 — 여러 기능의 계약에 동시에 박히는 것)

> 상세 트레이드오프와 권장안: **`docs/21_착수전_기술결정_ADR.md`** (회의 자료로 사용)

| # | 항목 | 참조 문서 | 권장안 요약 | 결정값 |
|---|---|---|---|---|
| 1 | Unity Web Transport | docs/21 결정1, festa-unity/Docs/deployment-handoff.md | WebSocket + LB에서 TLS 종료 (로컬 검증 완료, AWS 실측만 남음) | ✅ 권장안 채택 (2026-08-12) |
| 2 | Embedding Provider / **벡터 차원** | docs/21 결정2, docs/09, docs/13 §18 | 관리형 API + 1536d 고정, chunk에 embedding_model_id 컬럼 | ✅ 권장안 채택 (2026-08-12) |
| 3 | LLM Provider (1차 모델 + timeout) | docs/21 결정3, docs/13 §17 | 어댑터 뒤 저가 모델 시작, TTFT 15s/전체 60s | ✅ 권장안 채택 (2026-08-12) |
| 4 | AI Streaming + 채팅 UI 위치 | docs/21 결정4, docs/16 §6, docs/10 | SSE + 텍스트 입력 UI는 React 오버레이 (Unity 한글 IME 회피) | ✅ 권장안 채택 (2026-08-12) |
| 5 | Token 구조 (Refresh/Access/Connection/WS) | docs/21 결정5, docs/12 §7, docs/16 §14 | 4계층 분리, Refresh는 Unity 전달 금지 | ✅ 권장안 채택 (2026-08-12) |
| 6 | Docker/EC2/ECS + Channel 범위 | docs/21 결정6, docs/15 | Day1 Docker 필수, EC2 시작, Unity Server만 Week6 전 ECS | ✅ 권장안 채택 (2026-08-12) |
| 7 | API Key 발급 주체·결제 수단 (LLM/Embedding) | docs/21 결정2·3 | 회의 **전에** 확인 (병목 방지) | ✅ **GMS 키 지급 예정** — 발급 전까지 Mock/어댑터로 개발 (2026-08-12) |
| 8 | Repo 전략 (Mono vs 분리) | docs/17 §1 | — (팀 운영 방식) | ✅ **파트별 자율 결정** (2026-08-12) |
| 9 | Merge 방식 (Squash vs 일반) | docs/17 §9 | Squash 권장 | ✅ Squash 채택 (2026-08-12, 이의 시 재논의) |
| 10 | Unity C# 컨벤션 택일 (_camelCase 등) | docs/17 §15 | 현재 POC 코드는 `_camelCase` 사용 중 | ✅ `_camelCase` 유지 (2026-08-12) |
| 11 | 환경 분리 수준 (dev/demo) | docs/15 §16 | 최소 dev/demo 분리 | ✅ dev/demo 분리 + **개발환경은 파트 브랜치별 개별 배포** (2026-08-12) |
| 12 | 도메인 규칙 | docs/15 §14 | SSAFY 배포 규칙 확인 필요 | ✅ **팀 자체 구매** (비용 갹출) (2026-08-12) |
| 13 | 예산 상한 (AWS) | docs/15 §25 | — | ✅ **AWS 서버 제공됨** — 예산 상한 이슈 해소 (2026-08-12) |
| 14 | MVP Cut Line 재확인 | docs/01 §7, §12 | P0/P1 경계 그대로 갈지 | ✅ **컷 유지 + 캐릭터 커스터마이징 P0(필수) 승격** + 부스 노트북 홈페이지(P0) 신설 (2026-08-12) |
| 15 | FE 1차 통합 마일스톤 범위 | spec 005·006·009·013·016, docs/01 §12 | Booth Studio(외부·내부) + Publish + Unity 반영까지. 016·013·009는 P0이나 이번 범위 제외 | |
| 16 | **파트 브랜치 → develop 통합 방식** | docs/17 §1·§2-1, PR [#3](https://github.com/kanghyunsoon/ssafesta/pull/3) | `front`/`back`/`ai`가 `festa-unity/` 삭제 이력을 가져 **일반 merge 시 1,462개 파일이 충돌 없이 삭제 스테이징됨**(격리 worktree 실측 확인). 제안 ① `festa-unity/`를 별도 저장소로 분리(GitLab 이전 시점에 함께) ② 통합 시 일반 Merge 버튼 금지를 `docs/17`에 규칙화 | ⚠️ **미결 — develop 통합 재시도 전 필수.** 08-18 통합 시도(`f8c47fc`)는 컨벤션 위반으로 되돌림(`95cd845`). **08-21 실증(back)**: 반대 방향(develop→back) 머지에서도 같은 원인으로 festa-unity 1,456개가 **충돌 없이** 삭제 스테이징됨 — 머지 직후 `git diff --name-status`의 D 목록 검증으로 발견·복원 (back T-117). 어느 방향이든 머지 후 삭제 목록 검증이 필수 |
| 17 | **spec 문서 서명 공간** | docs/17 §2, spec 005 리뷰칸, PR [#12](https://github.com/kanghyunsoon/ssafesta/pull/12)·[#13](https://github.com/kanghyunsoon/ssafesta/pull/13) | 현재 spec 문서 원본은 `front`. `back`·`main`은 FE 검토 4칸·C-03 확정이 없는 구버전 | ✅ **"서명만 담은 작은 PR을 develop에 직접"로 확정, 실증 완료** (2026-08-20). PR #12(FE 상태 sync)·PR #13(BE 검토 서명) 둘 다 이 절차대로 develop에 병합됨. `develop`·`front`의 `spec.md`가 diff 0으로 동일. spec 005 리뷰 전 칸 완료 |
| 18 | **내부 서비스 간 API 인증 방식** (서비스 토큰 / SG 제한 / mTLS) | docs/HDD/AI_문서처리_Job_설계_검토.md §7-5, docs/HDD/임대만료_AI대화_종료계약_검토.md §8-5, 헌법 15조 | FastAPI→Spring 내부 endpoint 2종(`PATCH /internal/ai/documents/{id}/status`, `GET /internal/ai/booth-access`)이 **같은 결정 하나에 함께 걸려 있다.** 따로 정하지 말고 한 번에 (2026-08-20 등록) | |
| 19 | **ADMIN 권한 모델** (U-01) | docs/HDD/코인_지갑_구현_정리.md §U-01 | 003 관리자 조정·정합성 점검 REST, 004 C-05 신고·강제 비공개가 전부 여기서 막혀 **기능을 빼는 방향**으로 처리돼 있다. 여는 시점에 한 번에 설계 (2026-08-20 등록) | |

## ② spec 작성 단계에서 결정 (`/speckit.clarify` 대상 — 기능 하나에 갇히는 것)

> 해당 기능 spec을 쓸 때 정한다. 미리 회의에 올리지 않는다.

| 항목 | 참조 문서 | 결정 시점 (해당 spec) | 결정값 |
|---|---|---|---|
| 회원 탈퇴 데이터 보존 기간 | docs/03, docs/00 | Auth/User spec | ✅ 탈퇴 확정 즉시 관련 데이터 전체 hard delete (2026-08-19) |
| 코인 초기·일일 지급 정책 | spec 003 | Wallet/Coin spec | ✅ 초기 200 / 일일 50, KST 날짜당 회원의 첫 인증 접속에서 자동 지급, 게스트 지급 없음, **현금 충전 영구 제외 — 게임 내 가상 코인으로만 순환** (2026-08-19) |
| Booth 임대 연장 상세 정책 | docs/03 | Booth Lease spec | |
| 플랫폼 수수료율 | docs/03 | Economy spec | |
| AI 실패 시 Coin 환불 (Ledger REFUND 타입은 스키마에 선반영) | docs/21 결정3 | AI 이용/결제 spec | |
| 재접속 상세 정책 | docs/12 §9 | Multiplayer 안정화 spec | |
| 이벤트/경쟁관 주기 | docs/00 | Event spec (P2) | |
| AI 문서 허용 형식·최대 크기 | docs/13 §7 | Document Pipeline spec | |
| Conversation 저장 위치·보존 기간 | docs/07 §5, docs/13 §12 | Conversation spec / 008 C-01 (권장: 서비스 PG + 종료 시 삭제). **`ai_conversations` 테이블이 V1에 없고 탈퇴 hard delete 대상에도 없다 — 개인정보 삭제(D11·001)와 연결된 결손** (2026-08-20 확인) | |
| Rate Limit 수치 | docs/13 §25 | 부하 확인 후 | |
| Booth Layout JSON 최종 스키마 (version 포함) | festa-unity BoothLayoutDto, docs/09 §10, docs/08 §4 | Booth Studio↔Runtime spec — **React/Spring/Unity 3파트 합의** | ✅ **2026-08-20 spec 005 리뷰 완료** — `objectId` / `assetCode` / canonical type 10종 / `/api/v1/booths/{id}/layouts/published`. 기능형=`configId`·장식가구=`assetCode`는 docs/09 §10 기재. Unity 정합 완료(canonical 매핑 `e8209bc`, 계약 불일치 3건 `8852861`), 좌표 왕복 검증 통과([#6](https://github.com/kanghyunsoon/ssafesta/issues/6)), BE 검토칸 서명(PR [#13](https://github.com/kanghyunsoon/ssafesta/pull/13)), SC-002 해소(PR [#15](https://github.com/kanghyunsoon/ssafesta/pull/15)). **spec 전체 확정의 마지막 조건은 C-04(기획 승인)뿐.** **미검증 잔여 1건** — `BOOTH_LAPTOP_INTERACT` 브라우저 왕복(WebGL 빌드 후). `assetCode` 지정 케이스는 2026-08-20 종단 검증 완료(변종 프리팹으로 분기 확정, 타입당 프리팹 1개 제약을 임시 엔트리로 우회 — 검증 후 삭제). 미등록 코드 경고 추가(`origin/game 6e03998`). **BE 구현·3파트 통보 완료 (2026-08-21, #17·#19·#36)** — `version`(공개 회차)과 `schemaVersion`(구조 버전)을 분리 구현, spec 005 예시 `"version": 2` → `"schemaVersion": 1` 정정 제안은 #36 합의 대기 |
| **부스 영역 경계 수치** (Layout 좌표 검증 임계값) | `specs/005/data-model.md` §3, `LayoutValidator` | 좌표 검증 임계값 | ~~✅ 부스 6m × 6m × 6m (2026-08-20)~~ → ✅ **2026-08-21 정정: 부스 6m × 6m × 2.72m** — 셸 벽 패널 실측 2.725의 내림([#19](https://github.com/kanghyunsoon/ssafesta/issues/19) ②). `\|x\|,\|z\| ≤ 3`, `0 ≤ y ≤ 2.72`로 구현·반영 완료. "6×6×6"의 세 번째 6은 x·z와 대칭으로 잡은 값이었고 셸 근거가 없었다(리드, [#45](https://github.com/kanghyunsoon/ssafesta/issues/45)). 정면 트러스(z 2.85~3.15, y 1.85↑)는 부스 경계 밖이라 배치 공간을 제약하지 않는다. **§10 기하 계약 신설**(타입별 실측 bounds 10종, 회전 후 AABB 판정 `AREA_OUT_OF_BOUNDS`, 통행 판정 `FRONT_BLOCKED`/`ISOLATED_AREA`) — `contracts/layout-api.md` §9·§10(`origin/feature/booth-layout-geometry`, 미병합) |
| `LAPTOP` 홈페이지 주소 저장 위치 | spec 016 FR-001·FR-011·C-01·C-02, docs/09 §7 | 016 C-01 — **3파트 합동**. 005 schema 동결 전 | FE 의견: 부스 설정에 저장하고 Layout에는 넣지 않는다(`booths`에 URL 컬럼 **신규 추가**, 부스당 1개). 이러면 Layout 계약 변경이 없다. **FR-001의 "여러 개 허용 여부"(C-02)가 미결이라 함께 정해야 함** |
| 부스 외부 설정(Facade) 저장 계약 | docs/08 §3, docs/09 §7, spec 006 §공간 운영 기준 | **spec 005 범위에서 제외** (2026-08-18 판단) — 별도 spec 또는 후속 작업으로 FE 외부 설정 화면 착수 전 확정 | 조회 API(`GET /booths/{id}`)와 DB 컬럼 4종은 이미 있고 **저장 endpoint만 없음**. 정해야 할 것: ⑴ 저장 경로 ⑵ Layout처럼 Draft/Publish를 태우는지, `booths` 컬럼 직접이라 즉시 반영인지 ⑶ `themeCode` 허용값과 `primaryColor` 독립 여부 ⑷ `logoUrl` 업로드 방식 ⑸ `booths.name`과 `facade_sign_text`의 화면상 관계 → ✅ **`PUT /booths/{id}/facade` 신설로 대부분 해소 (2026-08-20 spec 005 구현, #17 통보)**: ⑴ `PUT /booths/{id}/facade` ⑵ Draft/Publish 없이 `booths` 컬럼 직접·즉시 반영 ⑶ `themeCode` 화이트리스트 {DEFAULT, SSAFY_BLUE, WARM, MONO} + `primaryColor` 독립 — 전역 팔레트 1개(A안)와 서버 팔레트 소속 검증은 #17 합의 진행 ⑷ 업로드 아님, https URL 참조(≤2048자) ⑸만 미정(FE 화면 몫). 만료 부스는 편집 거부(`BOOTH_LEASE_EXPIRED`) |
| ~~Booth 오브젝트 크기 조절 허용 여부~~ | spec 005 C-03 | — | ✅ 2026-08-18 확정 — **고정 크기.** `scale` 필드 없음, Layout 계약 변경 없음 |
| ~~부스 템플릿 종수·차이~~ | spec 005 C-06, FR-002·FR-003 | — | ✅ **2026-08-21 확정** — 1종 `PROJECT_EXHIBITION`, `DEFAULT` 제거(V11 이관), `template → 셸 프리팹 1:1`(리드 승인, [#45](https://github.com/kanghyunsoon/ssafesta/issues/45)·[#19](https://github.com/kanghyunsoon/ssafesta/issues/19)). 미리 배치된 오브젝트 세트는 채택 안 함(12개 상한 잠식). 목록은 `GET /booth-layout-templates` 신설로 조회(BE 구현, `contracts/layout-api.md` §9, 미병합). **스냅 간격도 확정** — `SNAP_METERS=0.25`(Unity·BE 권고, 최소 파츠 0.32m보다 작음), 서버 미검증(편집기 UX) |
| ~~C-04 — 콘텐츠 미연결 오브젝트의 공개 차단 여부~~ | spec 005 C-04, FR-007·FR-016 | — | ✅ **2026-08-21 확정 — 선택지 1(경고만, 공개 허용)** (리드 승인, [#45](https://github.com/kanghyunsoon/ssafesta/issues/45)). `LayoutValidator`가 검증 결과를 `errors`(차단)/`warnings`(허용)로 나눠 응답하고, 미연결은 `warnings`(FR-016) — 향후 재논의돼도 서버가 이 행을 `error`로 옮기면 FE는 코드 변경 없이 따라간다. **전제 조건**: Unity `AiNpcInteractable`이 `configId` 미검사로 `agentId=0`(Unity가 null을 0으로 읽음) 호출하던 구멍을 가드로 막음(`9917d4d`) — FE·BE는 `configId: 0`을 유효 ID로 발급하지 않는다 |
| 부스 외부/내부 공간 모델과 임대 만료 처리 | docs/specs/006-booth-runtime | 006 C-06 | ✅ 2026-08-14 확정 — 고정 외부 슬롯 + 동일 Scene 내부 슬롯 풀, 외부 제한형/내부 자유 Layout, 만료 시 슬롯 연결 해제·데이터 Draft 보존 |
| ⚠️ 프리팹 카탈로그 확장 시 `assetCode` 기본 자산 결정 규칙 | Unity `BoothObjectRegistry`, spec 005, [#18](https://github.com/kanghyunsoon/ssafesta/issues/18) | 카탈로그 확장 시 — **Unity + FE**. 판매 카탈로그(#18)와 맞물림 | **타입 기본 자산이 Registry 배열 순서(last-wins)로 결정되고 조회 맵 캐시에 무효화 경로가 없다.** 타입당 자산이 1개인 지금은 드러나지 않으나, 2개 이상이 되는 순간 `assetCode` 오타 시 어느 자산이 나올지 배열 순서에 좌우된다(경고는 `6e03998`로 나오지만 결과는 비결정적). 타입별 기본 자산을 명시하는 방향으로 좁힐 예정 — Unity 내부 구조라 FE 계약에는 영향 없음 (Unity T-148) |
| Chunk size / Top-K | docs/13 §8 | 결정 아님 — 구현 후 튜닝 값 | (튜닝) |
| **아바타 항목 목록 마스터 (Spring vs Unity)** | spec 013 C-07 | 013 — React 창 착수 전 (권장: Unity Catalog가 소유, 썸네일은 정적 자산) | |
| 아바타 카테고리·항목 수 / 썸네일 제작 | spec 013 C-01·C-03·C-05 | 013 — **용량 실측 후 역산** | |
| **아바타 파츠 목록·썸네일 마스터** | festa-unity/Docs/avatar-customization-contract.md §4 | 013 | ✅ Unity `AvatarConfig`·프로젝트 Sprite가 Source of Truth. 별도 React/Spring 카탈로그 없음 (2026-08-16) |
| 아바타 정식 UI 소유권 | specs/013-avatar-customization/spec.md | 013 | ✅ Unity `CharacterLobby` 정식 화면 확정, React 교체 요구 폐기 (2026-08-16) |
| 아바타 구매·보유 개념 도입 여부 | spec 013 C-06, ECON-06/07 | 012 (P1) — 권장: MVP는 전체 개방 | |
| Booth `facade` 스키마 — V1 단일 `facade_code` vs docs/09 4컬럼 (U-05) | `V1__initial_schema.sql:38`, docs/09 | 005 착수 전 — 양쪽 다 구현이 없어 정리 비용 0 | ✅ **docs/08·09 설계 문서대로 4필드 채택 (2026-08-20).** V9에서 rename+3컬럼 추가, 편집은 `PUT /booths/{id}/facade` 신설 (docs/08 갱신 필요) |
| **전역 오류 봉투 통일** — `{code, message, requestId}` (docs/08 §1.3) | docs/08 §1.3, `specs/005/research.md` R-09 | 현재 `ResponseStatusException` 23곳 중 코드를 붙이는 곳이 1곳뿐이라 docs/08 코드 표가 응답에 없다 | ✅ **005에서 `ErrorCode` enum + 전역 핸들러로 구현 (2026-08-20).** Unity는 상태 코드만 보고 테스트 단언 0건, React는 아직 없어 **오류 본문 소비자가 없음을 확인**하고 지금 통일한다. Breaking이 아니라 문서와의 정합 회복 — AI·FE 파트에 통보 |
| 문서 상태 소유 — DocumentStatus=Spring / JobStatus=FastAPI 분리 | docs/HDD/AI_문서처리_Job_설계_검토.md §1 | 007 — 권장: 분리 (헌법 3조) | |
| `GET /ai/v1/documents/{id}/status`의 사용자 노출 여부 | docs/14 §8, 위 검토 §1 | 007 — 현재 계약이 헌법 3조와 충돌. 노출 조회는 Spring이 서빙 권장 | |
| 문서 처리 재시도 상한·백오프 · heartbeat / lease TTL / 스위퍼 주기 | 위 검토 §4 | 007 — 권장 3회 / 1·5·15분, 30s·90s·60s. **임베딩 비용과 직결** | |
| `failure_reason` 사용자 노출 문구 정책 | 위 검토 §7 | 007 — 원문 스택트레이스 노출 금지 | |
| 임대 검증 시점 — 매 질문 vs 대화 생성 1회 + 로컬 deadline | docs/HDD/임대만료_AI대화_종료계약_검토.md §1 | 008 — 권장: 생성 1회 + 로컬 deadline (헌법 14조와 같은 결) | |
| D03 "이미 열린 대화" = 진행 중 응답 1건 vs 대화 세션 | 위 검토 §4 | 008 — 세션으로 읽으면 FR-008과 충돌한다 | |
| 만료 오류 코드 — 기존 `BOOTH_LEASE_EXPIRED` 재사용 | 위 검토 §3 | 008 — 새 코드 신설 시 FE가 파서를 두 벌 관리. 헌법 24조 통보 대상 | |
| 검증 호출 fail closed · timeout 1s · 재시도 1회 · 허용 시계 오차 ±2s | 위 검토 §6 | 008 — 실측 후 조정 | |

## ③ AWS 실측 후 결정

| 항목 | 참조 문서 | 판단 기준 | 결정값 |
|---|---|---|---|
| ALB vs NLB | festa-unity/Docs/deployment-handoff.md §4 | Health Check 방식(NLB=TCP 체크로 코드수정 0 vs ALB=/healthz 추가 반나절) | |
| idle timeout 최종값 | handoff §1 | heartbeat 실측 (초기 권장 180s) | |
| pgvector를 서비스 DB와 공유/분리 | docs/07 §14, docs/15 §25 | 비용·부하 (권장: 같은 RDS + 별도 스키마). **AI 문서 처리 Job 테이블 위치가 이 결정에 종속 — 이걸 먼저 정해야 한다** (2026-08-20) | |
| SQS(문서 처리 Queue) 도입 시점 | docs/15 §6 | 문서 처리 부하 실측 | |
| Autoscaling 기준 / Channel 자동 분배 시점 | docs/12 §26, docs/15 §20 | 부하 테스트 (P2) | |
| CloudFront ↔ Unity Web 압축 설정 정합 | handoff §5 | 배포 검증 (현재 로컬은 Compression Disabled) | |

---

## 결정 기록 로그

| 날짜 | 항목 | 결정 | 반영 문서 |
|---|---|---|---|
| 2026-08-08 | Transport WebSocket 사용 (①-1의 절반) | 로컬 실측으로 사실상 확정 — wss는 AWS 검증 후 최종 | poc-status.md |
| 2026-08-08 | 아바타 구현 방식 | **자체 경량 아바타** — Genies(WebGL 미지원)·Ready Player Me(2026-01 서비스 종료) 조사 후 외부 SDK 탈락. 상세는 spec 013에서 설계 | 24_작업일지 |
| 2026-08-12 | ①번 테이블 1~14 전체 | 위 표 결정값 참조 — SDD 착수 가능 상태 도달 | 26 (본 문서), sdd/constitution.md |
| 2026-08-12 | 캐릭터 커스터마이징 | **P0 필수 승격** — 파츠 커스텀 + 실시간 동기화. 구현 방식은 이후 **모듈러 프리팹**으로 전환 (아래 항목) | 27_SDD_기능분할안, 01 |
| 2026-08-12 | 부스 노트북 → 홈페이지 (신규 P0) | 부스 내 노트북 오브젝트 클릭 → 임대자가 등록한 홈페이지를 React 오버레이로 열어 웹서핑. Unity는 트리거만, 표시·iframe 제약 처리(X-Frame-Options)는 React | 27, 02, 05/06 spec |
| 2026-08-12 | 거리 기반 음성채팅 (신규 P1/2차) | 거리별 볼륨 감쇠 proximity voice — WebRTC SFU 계열로 spec 017에서 설계 | 27 |
| 2026-08-12 | 층 구조 (신규 P1/2차) | 1층(축제 부스·포토존) + 11층. 엘리베이터 진입 → 로딩 연출 → 층 이동. **1차 MVP는 11층 단일** | 27 |
| 2026-08-12 | 마피아 미니게임 (확장/P2) | 접속 플레이어에게 역할 부여하는 게임 요소 — P2 후보로만 기록 (spec 미작성 원칙 유지, 014 비고 참조) | 27 |
| 2026-08-12 | 개인 기록·지라 규칙 | 작업 시마다 **본인 작업일지(날짜별)** + 트러블 발생 시 **본인 트러블슈팅(날짜 포함)** 업데이트, **매일 종료 시 지라 업데이트 필수** | sdd/constitution.md, CLAUDE.md/AGENTS.md |
| 2026-08-12 | 브랜치/CI-CD 전략 | `ai`/`back`/`front`/`game` 파트 브랜치 각각 CI/CD + 개발환경 개별 배포. `develop`은 실사용 환경 기준 CI/CD — 완료분만 develop으로 병합 | sdd/constitution.md, 17 |
| 2026-08-12 | 아바타 에셋 교체 | **런타임 조립 방식 → 모듈러 프리팹 방식**으로 전환. 외형 표현이 긴 문자열에서 **ID 집합**으로 바뀌어 네트워크 payload가 수십 바이트로 감소 (T-24 원인 소멸). 구 에셋·코드는 신규 동작 확인 후 제거 | 헌법 2·5·23조, spec 013, docs/29 |
| 2026-08-12 | ⚠️ 아바타 용량 리스크 | 신규 에셋 소스 약 141MB(텍스처 약 80MB) vs 현재 Web 빌드 약 87MB. **설계 전 용량 실측이 선행 조건** — 실측 결과로 카테고리·항목 수를 역산 | spec 013 C-03 |
| 2026-08-12 | **①번 공통 결정 5건 — 전부 권장안 채택** | Layout 좌표 규칙 / 아바타 길이 / 토큰 검증 / 임대 정책 D01~D11 / SSE 스키마 | 헌법 v1.1, specs/ 전체 |
| 2026-08-12 | 로그인 방식 | **소셜 전용 — Google + Kakao.** 자체 이메일 가입 없음. **게스트 로그인**은 둘러보기 전용(비영속, 종료 시 삭제, 임대·결제 불가) | 헌법 11·12조, spec 001 |
| 2026-08-12 | 접속 토큰 검증 | **서명 자체 검증 + 사용된 토큰 식별자 기록으로 재사용 차단.** 게임 서버가 매 접속마다 Spring에 조회하지 않는다 | 헌법 14조, spec 001·002 |
| 2026-08-12 | Layout 좌표 규칙 | **미터 단위 / 원점=부스 바닥 중앙 / +Z=부스 정면 / rotationY 0=+Z, 위에서 볼 때 시계방향 +.** React가 변환해서 보낸다. ~~왕복 검증 1회 필수~~ → ✅ 2026-08-20 실측 통과(아래 참조) | 헌법 21조, spec 005·006 |
| 2026-08-12 | 부스 오브젝트 상한 | **12개** (요청 10~12 중 상한 채택, 성능 실측 후 하향 가능) | 헌법 22조, spec 005 |
| 2026-08-12 | 아바타 외형 표현 | **ID 집합으로 표현.** 네트워크는 고정 크기 struct, 저장 컬럼은 **TEXT**. 기존 계약서의 29~32자는 **무효** *(모듈러 프리팹 전환으로 개정)* | 헌법 23조, spec 013 |
| 2026-08-12 | 미니게임 종류 | **타이머 정지 게임** — 5~10초 목표 시간 무작위 제시 → 정확히 맞춰 정지. **단독 플레이**. 목표 시간은 서버가 발급 | spec 014 |
| 2026-08-12 | world-sessions 층 파라미터 | **1차 MVP부터 포함** | 헌법 9조, spec 002 |
| 2026-08-12 | 층 구조 설계 | **층 = 별도 씬 + 별도 세션.** 엘리베이터가 전환을 가리는 로딩. Web 다운로드 용량·메모리가 결정 근거 | 헌법 9조, spec 018 |
| 2026-08-12 | 1층 범위 | **뼈대만** (부스 슬롯 자리 + 포토존 위치). 상세 콘텐츠는 추후 기획 | spec 018 |
| 2026-08-12 | 임대 정책 D01~D11 | **권장안 전부 채택** — 활성 임대 1개 / 결제 후 24h / 만료 시 신규 차단+유예 / 연장 없음 / 변심 환불 없음 / 재임대는 Draft 시작 | spec 004 |
| 2026-08-12 | AWS 자원 | **EC2 등 충분히 제공받음** — 층별 서버 인스턴스·SFU 등 수량 제약 없음 | 헌법 7조, INFRA |
| 2026-08-12 | 내부 설문 결과 제공 범위 | 응답 수·선택지별 수/비율·별점 평균/분포·주관식 목록 제공, 고급 분석 MVP 제외 (업로드 문서 기준) | 02, 06, 08, 10, 19 |
| 2026-08-14 | 부스 외부/내부 공간 모델 | **고정 외부 슬롯 + 동일 World Scene 내부 슬롯 풀** 채택. 외부는 제한형 Facade, 내부는 Published Layout 자유 배치, 일반 방문 공유형. 임대 만료 시 슬롯 연결을 해제하고 기존 콘텐츠는 Draft로 보존 | 01~12, 19, 20, specs 005/006 |
| 2026-08-14 | Access/Refresh Token 저장 위치 | **AT는 JS 메모리, RT는 쿠키** (FE·BE 구두 협의). ⚠️ 쿠키의 `httpOnly`·`Secure`·`SameSite` 속성과 재발급 endpoint·AT 수명은 미확인 — 확정 후 spec 001에 반영 | spec 001 C-02 |
| 2026-08-16 | 아바타 정식 UI 소유권 | 상당 수준 완성·검증된 Unity `CharacterLobby`를 정식 커스터마이징 화면으로 확정. React 오버레이 교체와 별도 웹 파츠 카탈로그 요구 폐기 | spec 013, avatar-customization-contract, sdd |
| 2026-08-18 | 부스 오브젝트 크기 조절 (spec 005 C-03) | **고정 크기 확정.** `scale` 필드를 Layout 계약에 추가하지 않는다 — 3파트 재합의 불요, FE MVP 제외 의견과 일치 | spec 005 C-03 |
| 2026-08-18 | 부스 외부 설정(Facade) 저장 | **spec 005 범위에서 제외.** 저장 계약(endpoint·Draft/Publish 여부·themeCode·logoUrl·간판 텍스트)은 FE 외부 설정 화면 착수 전 별도 확정 | docs/08 §3, docs/09 §7 |
| 2026-08-18 | Unity→React 상호작용 이벤트 계약 | **`BOOTH_LAPTOP_INTERACT { boothId: number, objectId: string, url?: string }`** 확정 (FE·Unity 합의, 헌법 24조). 전달은 기존 `window.FestaUnity.onBoothInteract(json)` 콜백 패턴. `boothId`·`objectId`는 필수 식별자, `url`은 선택 — spec 016 FR-009(주소 미등록 노트북도 상호작용은 발생, 오류 아닌 안내 표시). 잘못된 단일 이벤트가 다른 부스에 영향을 주지 않도록 JSON 파싱 실패는 격리. FE 구현 완료(`festa-frontend/src/unity/bridge/events.ts`), Unity 송신부도 `origin/game 2ec6a9d`로 구현 완료(2026-08-18). 실제 브라우저 왕복은 WebGL 빌드에서만 발생하므로 배포 후 별도 검증 필요 | spec 006 FR-009·FR-010, spec 016, avatar-bridge 계약 |
| 2026-08-19 | 코인 지급 정책 | 신규 회원 200코인, 일일 50코인. KST 날짜당 회원의 첫 인증 접속에서 자동 지급하며 게스트는 지갑·코인을 제공하지 않는다. 플랫폼 수수료는 spec 012로 이관 | spec 003 |
| 2026-08-19 | **현금 충전 영구 제외** | 코인은 **게임 내 가상 재화로만** 순환한다. MVP 한정이 아니라 영구 정책이며 결제(PG) 연동·현금 환불·충전 API를 만들지 않는다. 코인 유입 경로는 **지급·보상·환불·관리자 조정** 네 가지뿐이다 | spec 003 · 012 |
| 2026-08-20 | Layout Draft 동시 편집 (spec 005 C-05) | **낙관적 잠금 채택.** `version` 기반 낙관적 잠금 + 충돌 시 409. BE(황덕) 회신([#5](https://github.com/kanghyunsoon/ssafesta/issues/5))으로 FE 제안 그대로 확정. 요청 스키마의 `version` 위치·409 응답 body 형식은 BE 착수 시 확정 | spec 005 C-05, docs/08 §4 |
| 2026-08-20 | spec 문서 서명 공간 (2차) | **"서명만 담은 작은 PR을 develop에 직접"로 확정** — 당일 오전 결정("문서 소유 브랜치로 전환해 서명")을 오후 팀 협의로 대체. `spec.md`를 `front`→`develop`으로 sync하는 PR [#12](https://github.com/kanghyunsoon/ssafesta/pull/12) 병합(리뷰어 스킵, `festa-unity/` 0줄). BE 검토칸 서명도 `develop` 기준 새 브랜치+PR로 진행 — 직접 push는 `docs/17` §2 위반이라 제외. **황덕이 PR [#13](https://github.com/kanghyunsoon/ssafesta/pull/13)으로 이 방식대로 서명 완료**, `develop`·`front`의 `spec.md`가 다시 완전 동일 확인 | docs/17 §2, spec 005 리뷰칸 |
| 2026-08-20 | spec 005 BE 검토·리뷰 완결 | **황덕이 [Issue #5](https://github.com/kanghyunsoon/ssafesta/issues/5) 서명 가이드대로 BE 검토칸 기입** — `BE 검토: 황덕 / 검토일: 2026-08-20`. C-05 잔여였던 409 응답 body 형식도 함께 확정: **`{code, message, requestId}`**(`docs/08` §1.3, `D-BE-01` — 클라이언트는 `code`로만 분기), 이미 계약 테스트로 구현·고정된 상태. **spec 005 리뷰 4칸(①②③④) + BE 검토칸 전부 완료** — 남은 잔여는 요청 스키마 `version` 위치(BE 착수 시)와 SC-002(같은 날 아래 항목에서 해소) | spec 005 C-05, docs/08 §1.3 |
| 2026-08-20 | Layout 좌표 규칙 실측 검증 (C-02) | **왕복 검증 1회 통과 — 좌표 규칙 확정.** FE 작성 JSON(`SURVEY_KIOSK`·`CONSULTATION_DESK`, x/z 양음·rotationY 90/180)을 Unity가 실측: world 좌표·forward 벡터가 기대값과 소수점까지 일치, 부호 오류 0건. 앵커 `BoothSlot_7 (5,0,5)` 전제도 확인. 같은 실행으로 Unity 계약 정합 3건(`objectId` 정식화·엔드포인트 복수형·`assetCode` 경로) 반영 확인 — `origin/game 8852861`. 근거: [#6](https://github.com/kanghyunsoon/ssafesta/issues/6), `docs/LJH/verify/block1-roundtrip.md` | spec 005 C-02·SC-004, spec 006 T005·T006 |
| 2026-08-20 | `AI_CHAT` 오버레이 payload | **`{ boothId: number, agentId: number }` 확정** (AI 파트 김가현, [#2](https://github.com/kanghyunsoon/ssafesta/issues/2)) | spec 008, `shared/types/overlay.ts` |
| 2026-08-20 | `AI_AGENT_INTERACT` 이벤트 계약 | **`{ boothId: number, objectId: string, configId: number }` 확정** (FE·Unity·AI 3파트, [#2](https://github.com/kanghyunsoon/ssafesta/issues/2)). **Unity는 `configId`로 송신** — Layout 계약·Unity DTO 필드명과 일치, `agentId`라는 이름은 Unity에서 쓰지 않는다. **`configId` → `agentId` 매핑은 FE가 `AI_CHAT` payload 생성 시 처리.** `configId`는 이미 `BoothObjectDto`·`BoothRuntimeObject.ConfigId`에 있어 계약 변경·BE 작업 불요. 구현 잔여 — FE `events.ts` 타입 추가, Unity `BoothInteractBridge` 일반화·`AiNpcInteractable` 교체 | spec 008, `unity/bridge/events.ts`, ADR 결정 4 |
| 2026-08-20 | `assetCode` 조회·오류 처리 | **검증 통과** ([#6](https://github.com/kanghyunsoon/ssafesta/issues/6)) — DTO→런타임 전달, 대소문자 무시(`OrdinalIgnoreCase`), 미등록 코드는 타입 기본 fallback, 조회 키가 `type:assetCode` 복합키라 타입 경계를 넘는 오매칭 없음. **잘못된 `assetCode`는 Unity가 `Debug.LogWarning`으로 알린다**(미지정은 정상 경로라 무로그, 구현·검증 완료 — 커밋 `6e03998`, 4케이스 테스트로 확인). fallback 동작 유지 — 오브젝트가 사라지는 것보다 기본 자산으로라도 그려지는 편이 안전. FE 편집기 저장 시점 검증은 미추가. **미검증: 서로 다른 자산이 실제로 선택되는지** — 현재 타입당 프리팹 1개라 원리적으로 구분 불가, 카탈로그 확장 후 재검증(FE가 쓸 코드 목록 제공이 선행). Issue #6은 mock 경로 이동(`bad201b`)까지 반영 후 2026-08-20 CLOSED | spec 005, docs/09 §10 |
| 2026-08-20 | spec 005 SC-002 해소 | **"시간 목표"를 버리고 진입 시점 관찰 기준으로 재작성.** 006 C-01(부스 구역 진입 시에만 조회, 주기 폴링·서버 푸시 없음) 구조에서는 "몇 초 이내"가 시스템 성능이 아니라 방문자 행동에 좌우돼 성립하지 않는다고 판정. 새 기준: "Publish 이후 그 부스 구역 진입(재진입·외부 진입·첫 방문 모두) 시 이전 공개본이 보인 사례 0건". 서버 반영 방식(동기/캐시)은 규정하지 않되 결과로 제약 — **캐시를 쓰더라도 이 기준은 만족해야 함**(`origin/back` pom.xml에 Redis 의존성 확인, BE에 PR 코멘트로 통지). 커밋 전 Codex 독립 검증 5항목 전부 CONFIRMED. 부수: Edge Cases의 stale C-05 태그 제거, 리뷰 제목을 `FE 검토 완료 (C-04 기획 승인 대기)`로 정정 — C-04만 진짜 미결로 남음. `front 7c5f9ad` → `develop` PR [#15](https://github.com/kanghyunsoon/ssafesta/pull/15)(`242b48b`) | spec 005 SC-002, spec 006 C-01 |
| 2026-08-21 | 아바타 저장 필드명·컬럼 | **필드명 `avatarCode` 확정** ([#24](https://github.com/kanghyunsoon/ssafesta/issues/24) 통보) — DB `avatar_code`·JPA `avatarCode`·Unity `AvatarCode`와 한 이름, 와이어만 다를 이유 없음. 저장 컬럼 `VARCHAR(3800)` → **TEXT** (V10, 헌법 23조 정합). fa 인코딩 실측 최대 414자(Unity 파트 측정) | spec 013 contract, BE.md, `V10__avatar_code_text.sql` |
| 2026-08-21 | 전역 오류 봉투 실동작 통보 | `{code, message, requestId, errors, warnings}` 봉투가 005부터 **전 endpoint 실동작** — Breaking 아님(docs/08 §1.3 정합 회복). `errors`·`warnings`는 항상 존재(없으면 빈 배열), 401/403도 같은 형태, 본문에 스택트레이스 금지. [#17](https://github.com/kanghyunsoon/ssafesta/issues/17)·[#36](https://github.com/kanghyunsoon/ssafesta/issues/36) 통보 | `ErrorCode` enum, docs/08 §1.3 |
| 2026-08-21 | spec 번호 019 충돌 해소 | **erd-schema를 `020-erd-schema`로 개명** — 019는 game-studio가 사용([#21](https://github.com/kanghyunsoon/ssafesta/issues/21), develop 선착순 경주 대신 선양보) | `specs/020-erd-schema/` |
| 2026-08-21 | spec 005 FE 산출물 착수 — C-04 PROVISIONAL | C-04(기획 승인) 미결 상태에서 서버 구현 기본값(`warning`)과 같은 방향으로 잠정 확정하고 `/speckit-plan` 진행(사용자 결정) — `speckit-plan` 규칙상 unresolved clarification은 ERROR 대상이라 PROVISIONAL 명시로 우회. **FE는 서버의 `errors`/`warnings`를 렌더링만 하므로 기획 결정이 뒤집혀도 코드 변경이 없다**(FR-016). `plan`·`research`(R-01~R-11)·`data-model`·`quickstart` 4종 작성. 착수 후 `origin/develop`이 앞서 있음을 확인해 2회 재정렬 — BE 검토 상세(FR-014~018)와 `contracts/layout-api.md`(BE 작성, 계약 정본) 반영 | specs/005-booth-studio-layout/ |
| 2026-08-21 | spec 산출물 파트 충돌 발견 | 다중 파트 spec에서 `plan`·`research`·`data-model`·`quickstart`·`tasks` **파일명이 하나뿐이라 먼저 쓴 파트가 자리를 차지**한다. 001·004·005는 Java 관점, 013은 C#/Unity 관점 산출물만 존재 — **다중 파트 spec 4개 전부 단일 파트 것**이다. 원인은 `docs/00` §2의 "plan.md — 각 파트가 생성"인데 파일이 1개인 구조. **제안: `spec.md`·`contracts/`만 공동 정본, 실행 산출물은 `FE/`·`BE/` 디렉터리로 분리.** `.specify/scripts/bash/common.sh:223-230`이 모든 경로를 `$feature_dir`에서 파생하고 `feature.json`이 gitignore라 **git 충돌 0건으로 적용 가능**함을 확인. 파일명 접미사 방식은 `IMPL_PLAN`이 `plan.md` 하드코딩이라 매 실행 재충돌 — 권하지 않음 | [#43](https://github.com/kanghyunsoon/ssafesta/issues/43), docs/00 §2, specs/README.md |
| 2026-08-21 | spec 005 C-04·C-06 기획 승인 + FE 산출물 일괄 정정 | [#45](https://github.com/kanghyunsoon/ssafesta/issues/45)로 리드가 두 항목을 최종 승인. C-04는 선택지 1(경고 허용), C-06은 1종 확정. 승인과 함께 Unity `AiNpcInteractable` configId=0 가드(`9917d4d`)가 전제로 들어옴. 이 승인을 반영하며 FE `/speckit-plan` 산출물(2026-08-21 오전 작성분)이 그새 닫힌 이슈(#17·#19)의 확정값과 5곳 어긋나 있는 것을 발견해 함께 정정 — 부스 높이 6→2.72m, 스냅 0.5→0.25m, template 2종→1종, 409 conflict에서 revision을 구조적으로 못 꺼내는 것(실제로는 한글 메시지 안에만 있음), FE가 §10 통행 판정을 서버 전용이 아니라 직접 구현해야 하는 것(리드가 FE 몫으로 명시 배분). 부수로 `plan.md`의 `contracts/` 자기모순(작성 안 한다면서 인용은 함)과 백로그 다이어트 때 유실된 `assetCode`·Game Studio 소켓 항목을 복원 | specs/005-booth-studio-layout/{spec,plan,research,data-model,quickstart}.md, docs/LJH/backlog.md |
