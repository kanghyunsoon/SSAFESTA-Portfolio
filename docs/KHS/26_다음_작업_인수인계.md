# KHS 다음 작업 인수인계

> 최종 갱신: 2026-08-31
>
> 기준 브랜치: `develop` — 2026-08-26 개정으로 **완료 경로는 develop 하나**다
> (이전 판의 `game` 기준은 폐기).
>
> 기준선: `v0.0.1-poc` — Multiplayer + Booth Runtime 기준선 동결 준수
>
> 목적: 다른 PC에서 저장소를 받아 현재 상태를 다시 조사하지 않고 다음 작업부터 이어가기
>
> ⚠️ **잔여 작업의 정본은 이 문서가 아니라 Jira 보드다.** 이 문서는 2026-08-25 시점의
> 조사 결과이고, 그 뒤 진행분이 전부 반영돼 있지는 않다. 아래 체크박스는 **당시 판단의
> 근거·맥락**으로 읽고, "지금 무엇이 남았는가" 는 Jira 에서 확인해라.
> 2026-08-30 대조 결과 미완료 27건 — 그중 상당수는 타 파트 대기이거나 별도 장비가 필요하다.

---

### 컨설턴트 피드백 반영 — 게임 파트 우선순위 재편 (2026-08-24)

프로젝트 피드백(WebGL 30~40인 최적화 최우선 · 템플릿/자동 생성 구조 · Rule+LLM 부스 생성)을
분석해 GitLab 이슈 7건(#89~#95)으로 파트별 분배했다. **게임 파트 다음 작업 순서:**

1. **#89 부하 측정 인프라** — 헤드리스 봇 30기 + 인빌드 FPS/힙/GC/트래픽 HUD.
   **모든 후속 최적화보다 선행한다.** 근거: 에디터 렉 조사 때 렌더 2.9ms/프레임 99ms —
   범인은 추측(렌더)이 아니라 실측(메모리)이 찾았다. 30인 병목도 실측으로 순위를 정한다.
2. **#90 원격 아바타 비용** — Animator CullUpdateTransforms, 파츠 스킨메시 결합,
   거리 기반 갱신 완화. 맵 최적화는 이미 상당 부분 완료라 남은 큰 덩어리는 아바타 30~40기다.
3. **#91 NT 트래픽 완화** — PositionThreshold 0.001(1mm, 과민) → 2~5cm 실측 튜닝,
   HalfFloat/쿼터니언 압축, 외형 FixedString 동기화 시점 점검.
4. **#92 Three.js 대비 근거 문서** — 발표 방어용. 겪은 사례(물리 정합 점프·리타게팅·
   오클루전 베이크·NGO)가 그대로 근거.

**결정 사항 (반영 시 주의):**
- **인게임 3D 에디터 통합은 채택하지 않는다** — Unity WebGL 한글 IME 제약이 근본 사유.
  역할 분리(웹=텍스트·이미지·정보 입력 / Unity=3D 반영·미리보기) 현행 유지. #93 에 명시함.
- **GLB 자유 업로드는 POC 범위 밖** — 폴리곤 폭탄·검증 부담이 최적화 최우선 방침과 충돌.
  받더라도 "검증된 에셋 카탈로그 선택"으로 좁힌다.
- **GPU Driven Rendering 은 WebGL2 타깃에서 비실용** — WebGPU 전환 전에는 검토하지 않는다.
- per-frame 할당 제로 정책: WebGL 은 단일 스레드라 GC 스파이크가 치명 —
  OnGUI/Update 에서 new 금지 (PortalInteractor 사례 참고).
- 범용화(피드백 1번)는 코드 재작성이 아니라 **하드코딩 데이터화**로 대응 (부스 수 12·존
  이름·SSAFY 표현 → config). 필요 시점에 이슈로 승격.
- BE 계측(#94)이 붙으면 게임 클라이언트에서 부스 입장/퇴장 이벤트 발신 배선 필요
  (BoothPortal/PortalInteractor 텔레포트 지점이 훅).

**근거 문서 (컨퍼런스 폴더 08~11, 2026-08-25 추가):**
질문받을 항목의 답을 미리 정리해 뒀다. 이슈 착수·발표 준비 때 여기부터 본다.

| 문서 | 답하는 질문 | 연결 이슈 |
|---|---|---|
| [8장 WebGL 렌더링 아키텍처 선택](conference/KHS_기술_컨퍼런스.md#8-webgl-렌더링-아키텍처-선택--왜-복제-구조이고-왜-unity인가) | 왜 복제 구조인가 / 왜 Three.js가 아닌가 | #92 |
| [9장 동시접속 규모와 성능 예산](conference/KHS_기술_컨퍼런스.md#9-동시접속-규모와-성능-예산--3040명은-가능한가) | 30~40명이 현실적인가 | #89·#90·#91 |
| [10장 부스 자동 생성과 외부 확장](conference/KHS_기술_컨퍼런스.md#10-부스-자동-생성과-외부-확장--어디까지-자동화되는가) | 자동화 범위 / 외부 고객 오브젝트 | #93·#95 |
| [11장 서비스 포지셔닝과 출시 경로](conference/KHS_기술_컨퍼런스.md#11-서비스-포지셔닝과-출시-경로--vrchat과-무엇이-다른가) | VRChat과 차이 / 스팀 출시 | — |

⚠ 08~11의 `판단` 표기 항목은 **작성자 분석이지 팀 확정이 아니다.** 성능 예산 수치(09 5절),
3계층 에셋 전략(10 4절), 출시 경로(11 4절)는 팀·기획 합의를 거쳐야 확정된다.

---

### ASC — 로컬 작업 통제 체계 도입 (2026-08-21)

**`.asc/` 는 Git 에 올리지 않는다** (`.gitignore`). 세션 상태는 팀 산출물이 아니다.
기존 `Spec → 구현 → 검증 → Issue/PR` 흐름을 **바꾸지 않고**, 그 위에서 **세션 간 인수인계**와
**GitHub 요청 감지**만 따로 관리한다.

| 경로 | 용도 |
|---|---|
| `.asc/README.md` | 체계 설명·원칙. 여기부터 읽는다 |
| `.asc/controller/queue.md` | 다음 작업 큐. **우선순위는 사람이 정한다** |
| `.asc/execution/sessions/<id>/` | 세션별 `goal.md`·`scope.md`·`handoff.md` |
| `.asc/monitoring/inbox/` | 승인 대기 초안 + `.seen.json` |
| `.asc/verify/` | 검증 세션 결과 (구현 세션과 분리) |

**원칙 — AI 에는 조사·구현·검증·초안을 맡기고, 무엇을 할지 정하는 권한과 팀에 발언·반영하는 권한은 사람이 갖는다.**
Comment·Review·Issue/PR 변경·Merge 는 승인 후에만 한다.

#### Monitoring Plane — 스케줄 작업 `github-issue-watch`

- 위치: `C:\Users\SSAFY\.claude\scheduled-tasks\github-issue-watch\SKILL.md`
- 주기: `7,37 * * * *` (30분). **jitter 383초가 붙어 목록에 "13분 지점" 으로 보이는데 30분 주기 맞다** — `cronExpression` 을 직접 읽어 확인했다.
- ⚠️ **앱이 열려 있어야 돈다.** 닫혀 있으면 다음 실행 시 밀려서 돈다.
- 감지: `gh api notifications` 의 `reason`(mention·assign·author·comment·review_requested)을 쓴다. `.seen.json` 의 `id → updated_at` 과 대조해 중복 초안을 막는다.
- **범위는 현재 전부.** `kanghyunsoon` 계정을 Game Studio 세션과 공유하므로 그쪽 이슈도 걸린다. **그 세션에 작업을 넘기는 시점에 제외 필터를 넣는다.**

#### 초안 깊이 — 조사와 초안을 일부러 분리했다

| Monitor 가 한다 | 승인 후에 한다 |
|---|---|
| 3줄 요약 | 코드·문서 실측 |
| **영향 분석** — `queue.md` 대조 (조사가 아니라 대조라서 싸다) | 브랜치 대조·`git log -S` 시간선 |
| 답해야 할 질문 목록 | 완성된 회신 작성 |
| 확인할 항목 **목록** | 게시 |

**완성된 회신을 미리 쓰지 않는다.** 하루치 작업에서 틀린 것들은 정보가 늦어서가 아니라 **확인을 덜 해서** 생겼다 — 시간선을 안 본 #51, develop 반영을 안 본 #31, "무엇으로 닫혔는지" 를 안 본 spec 007 C-07. **길고 그럴듯한 초안은 그 실수를 자동화한다.**

프롬프트에 못박은 것 하나 — 남의 문서와 어긋난 것을 발견하면 **누가 틀렸다고 쓰지 않는다.** 어느 쪽이 먼저인지는 `git log -S`·날짜로 확인해야 알 수 있고 그건 승인 후 작업이라, 초안에는 "어긋남 발견, 시간선 확인 필요" 로만 적는다. #51 에서 실제로 이 실수를 했다.

#### GitHub 이벤트는 밀어넣을 수 없다

웹훅이 세션에 닿는 경로가 없다. **감지는 폴링이고 주기가 곧 지연이다.** 실시간이 아니다.
그리고 GitHub 에는 **이슈 코멘트 초안이 없다**(PR 리뷰만 draft) — 그래서 초안이 파일로 온다.

#### 정리 이력

처음에 `docs/KHS/inbox/` 로 만들었다가 ASC 설계에 맞춰 `.asc/monitoring/inbox/` 로 옮겼다.
`docs/KHS/inbox/`(untracked README·`.seen.json`)는 삭제했고 `.gitignore` 항목도 `.asc/` 로 교체했다.

---

## 1. 지금 확정된 상태

- 정식 캐릭터 커스터마이징 화면은 **Unity `CharacterLobby`**다. React 오버레이로 교체하지 않는다.
- AI 채팅·상담·설문처럼 장문 한글 입력이 필요한 기능은 기존대로 React 오버레이를 사용한다. 이 결정은 아바타 화면에만 해당한다.
- Rukha93 모듈형 `fa` 외형의 파츠 선택, 아이템별 유효 색상, 자연스러운 무작위, 최초 추천/저장 외형 복원, 썸네일·팝업·레이아웃 개선은 Unity에서 완료됐다.
- 크롭 자켓의 몸통 누락과 하의 피부 관통을 포함한 대표 조합을 수정했고, 로비와 월드는 같은 `AvatarConfig`를 사용한다.
- 과거 `sk`/`rt` 외형은 읽기 호환만 유지한다. 신규 생성·저장 기준은 `fa|g=...|i=...|p=...|q=...|w=...`, 최대 **3800자**다.
- Unity UI/런타임 완료 커밋: `16c26d6`; 계약·QA 문서 동기화 커밋: `148f6e7`. 이 문서가 포함된 최신 `origin/game`을 최종 기준으로 사용한다.

### 2026-08-24 현재 상태 (최신) — 스케일 규격 변경·이관 대기

- **⚠ 플레이어 스케일 규격이 바뀌었다 — 후속 작업은 이 기준으로.** 11층 방 모델이
  1 m = 12.5 unit 로 임포트돼 있어(실측 인체 Niraj 22.4 unit) 아바타를 **×1.25** 올렸다:
  목표 높이 22.375 unit(=1.79 m 상당), CC 16.875/2.75, walk 45 / run 65,
  점프 v 56 / g 149.33(체공 0.75초·높이 1.05 m), 카메라 거리 11.25~45.
  값은 `PlayerAvatar.prefab` 직렬화 + 스크립트 기본값에 **양쪽 다** 박혀 있다 — 한쪽만
  바꾸면 어긋난다. 피벗 오프셋 12.5 는 `PlayerAvatarVisual.FitVisualToWorld` 하드코딩.
  **부스 규격 계약(레이아웃 로컬 미터·앵커 40·boothId)은 불변** — 건드리지 않았다.
- **이동 중 점프는 발구르기 생략·즉시 도약** (제자리 점프만 0.18초 발구르기).
  애니메이터 상태 speed 와 코드 상수가 쌍이다: Launch 1.667 ↔ JumpAnticipation 0.18,
  Air 0.844 ↔ 체공 0.75초.
- **복도 입구 확정**: 진홍 벨벳 커튼(사용자 픽, 양면 렌더)+목재 인방(남쪽 개구부),
  전실 저고도 페스툰. 야시장 잡동사니 = 통 테이블 4석(음식) + 부스 옆 진열 4세트,
  재질별 병합 렌더러 2개(`Festival_Clutter_Combined`, 원본 비활성 보존).
  Crates/Food 팩은 실사용 18파일만 `_Project/Art/World` 병합, 벤더 삭제 완료.
- **GitLab 이관 대기**: 문서·워킹트리 정리 완료(이 커밋). 팀원 정리가 끝나면 사용자가
  이관을 요청한다. 병합 아님 — `game` 브랜치 그대로 이동. 백업 브랜치
  `backup/game-before-history-purge-20260822` 는 이관 확인 후 삭제 대상.

### 2026-08-22 현재 상태 — 페스티벌 존

- **월드 구조가 2막이 됐다**: 엘리베이터 게이트 → 11층 방 → **전환 복도** → **야외 밤 축제(야시장)**.
  11층 방은 부스 구역이 아니다. 외부 부스 12개(카니발 킷)가 입구 역할이고, 상호작용하면
  **내부 부스 공간으로 이동**시키는 게 다음 핵심 플로우다 (미착수).
- **복도** (`@World_11F/Corridor_Festival`): 방 개구부 실측(X -125.4~-60.3)에 벽 정합.
  3단 전환 — 흰 벽/타일 → **목재 문지방(Z -10)에서 벽·바닥·빛 동시 전환** → 하늘 열림.
  서쪽 Z 80~210 개구부(13 m)로 꺾으면 축제가 정면. 목재 리브가 점점 촘촘해지는 침식 연출,
  줄전구 5줄 31개. **손으로 만지지 말 것** — 전부 스크립트 배치라 수치가 문서화돼 있다.
- **야시장** (`@Festival`): 바닥 70×35 m (싸피맵 5.3배), 부스는 `FestivalSlot_01~12` 빈
  오브젝트의 자식 — **판매·교체 = 슬롯 자식만 교체**. 북줄 게임 6(조합 프리팹) / 남줄 먹거리 6.
  가로등 7, 화로 3 + 조리불 2(VFX_TorchLight/VFX_Fire), 불꽃놀이 5발(루프·시차),
  담장 4면(동쪽만 6.5 m + 가림 나무) + 가문비 23그루.
- **달**: 스카이박스 `FS000_Night_01` 과 `_Moonless` 를 **픽셀 디핑**해 작가가 그린 달을
  후광째 추출(`MoonSprite.png`) → 가산 빌보드 `@MoonSprite`. 스카이박스는 Moonless 사용.
  위치 기준: **복도 끝(-92, 205)에서 서쪽으로 돌아선 시선의 오른쪽 위** (고도 23도, 북 19도).
- **조명 체계 변경 (주의)**: `@Moonlight` 디렉셔널 0.85 신설(소프트 그림자),
  **URP shadowDistance 50 → 900** (5 m 그대로면 달빛이 방 천장을 뚫는다),
  **카메라 far 1000 → 5000** (하늘 오브젝트), 앰비언트 0.215 → 0.33. 씬 조명 127개.
  ⚠ WebGL 은 카메라당 가시 광원 제한이 있다 — **배치 확정 후 라이트맵 베이크 전환**이 남은 큰 일.
- **에셋 정리 (T-185 방식 반복)**: 6팩 798 MB 임포트 → 실사용 37파일 19 MB 만
  `_Project/Art/{World,VFX}` 병합, 벤더 폴더 전부 삭제. 커밋 전 삭제라 히스토리 유입 0.
  Basic Fireworks 는 빌트인 셰이더 → URP 가산 재질로 33곳 교체 후 사용.
- **콜라이더 22개** + 통행 검증: 방→축제 서쪽 끝 8지점 레이캐스트 바닥 OK, 5방향 벽 차단 OK,
  개구부 열림 OK. 문지방 단차 12 cm (stepOffset 3.0 이내).
- **발광 재질 함정 (T-187)**: 스크립트로 만든 재질의 `_EMISSION` 키워드가 후속 패스에서 풀려
  전구·가로등이 전부 안 빛나고 있었다. 값이 아니라 `IsKeywordEnabled` 로 검증한다.
- 시도 후 폐기: 커튼 glb(+glTFast 설치→제거), 반딧불, 바닥 침식 조각.
  ⚠ glTFast 가 **URP 를 17.0.4→17.5.0 으로 몰래 승격**시켰던 것을 되돌렸다 — 패키지
  설치 후에는 `git diff Packages/manifest.json` 을 확인한다.
- **성능 현황 (같은 날 후속)**: 그림자 캐스터 969→220, 오클루전 베이크(73 KB, 커밋 필수 산출물
  `Scenes/main/OcclusionCullingData.asset`), 전구 병합(렌더러 977→888), lodBias 2→1,
  카메라 far 2800. 빛샘 2건 해결(T-188 보이지 않는 벽 / T-189 단면 벽 섀도맵).
- **에디터 성능 수칙 (실측 근거)**: 게임 뷰 렌더는 2.9~5.4 ms 로 문제없다. 에디터 버벅임의
  주범은 메모리다 — ① MCP 로 장시간 작업한 세션은 동적 어셈블리가 관리 힙을 ~1 GB 까지
  키우므로 **작업 후 에디터 재시작**, ② 게임 뷰 Scale 은 1x 로(2x = 4배 픽셀),
  ③ 새 텍스처 에셋은 임포트 직후 maxSize·압축 확인 (비압축 TGA 4장이 163 MB 였다).
- 기준 커밋: `d10cacb` (feat: build the festival zone) + 같은 날 후속 fix 커밋들.

### 2026-08-21 현재 상태

- **11층 월드 모델은 `11th-0821-complete textures (without elevator).skp` 다.**
  `@World_11F` 아래 `localPosition (-5.125, 1.45, 94.0375)` / `rotation (0,180,0)` / `scale (12.5)`.
  구 모델(`11th-0820-group 2 walls, floor, ceiling.skp`)과 전용 텍스처 21장은 삭제했다
  (35 MB 회수, 766개 에셋 역참조 0건 확인 후). **정렬은 방 셸 로컬 bounds 대조로 전 축 Δ0.0000**
  — 전체 bounds 는 모델마다 딴 게 섞여 못 믿는다. 교체 시 이 방법을 쓴다.
- **엘리베이터는 별도 지역으로 분리한다 (2026-08-21 방향 변경, 미착수).** 이 모델에는 엘리베이터 칸이
  없고(`wall-elevatorside` 벽만 있다) **없어도 된다** — 엘리베이터를 11층 안에 두지 않고 **자체 지역으로
  따로 만들어 관리**하고, 외부 부스에서 상호작용하면 그 지역으로 **이동**시키는 방식으로 바뀌었다.
  모델은 확보돼 있다(`elevator with door, switch.skp`, 미임포트).
  ⚠ **#31 에 기록된 게이트 방식과 다르다** — 거기에는 "클라이언트 로컬 대기 / 엘리베이터 안쪽 스폰 →
  문 열리면 걸어 나오면 11층" 으로 적혀 있다. 착수 시점에 #31 과 spec 002(FR-006·FR-011 이관분)를
  새 방식으로 정정해야 한다. **지금은 착수하지 않는다** — 로딩 은폐라는 목적에 필요한 부분
  ("월드 준비 완료" 집계 신호 + 타임아웃)은 **엘리베이터와 무관하게** 만들 수 있고, 그게 실제 값이다.
- **모델 정리는 임포트 후처리기가 자동으로 한다** — `WorldModelNaming` + `WorldModelPostprocessor`
  (`Scripts/Editor/WorldModelNaming.cs`). 새 `.skp` 를 넣으면 **메뉴를 누르지 않아도** 아래가 적용된다.
  대상 판별은 파일명이 아니라 `ssafy-11th-room` 자식 유무로 한다.

  | 임포트 시 자동 처리 | 내용 |
  |---|---|
  | 계층 이름 정리 | `Group N` → `wall-rear-*`/`wall-front-*`·`sofa-01~05`·`lounge-zone`·`ceiling-backdrop`. **번호가 아니라 지오메트리·머티리얼로 식별**한다 (번호는 export 마다 바뀐다) |
  | 서브트리 병합 | 오브젝트 **2043 → 156**, 렌더러 **1425 → 138**. 사물함 벽 전체가 드로우 5회 |
  | 콜라이더 | **Mesh 20 / Capsule 3** — 애셋에 내장되므로 교체해도 사라지지 않는다 |
  | GPU 인스턴싱 | 머티리얼 42/42 |
  | 임포터 설정 | 탄젠트 제거(노멀맵 0개 실측, 정점버퍼 41→37.2 MB) · 메시압축 Medium · 텍스처 crunch 25장 |
  | 스케일 마커 | `mock-floor`·`mock-wall` 비활성 (방 밖에 렌더링되던 것) |

  진단용 킬스위치: `EditorPrefs` 의 `Festa.WorldModelPostprocessor.Disabled`.
- ⚠ **모델을 재임포트하면 천장 머티리얼 슬롯이 1칸 빠진다**(109→108). 재임포트 후
  `Festa/World/조명만 다시 적용 (콜라이더 제외)` 을 한 번 누른다. 콜라이더는 애셋 내장이라 무관하다.
- **콜리전은 "틈새 없음" 을 flood-fill 로 증명했다** — 스폰에서 도달 가능 8,207칸 / **방 경계 밖 0칸**.
  바닥 커버리지 1,806/1,806. 봉쇄 3개(엘리베이터 개구부)는 여전히 씬 오브젝트
  (`@WorldCollisionSeals`, `천장 조명 셋업 다시 적용` 이 재생성).
  **검증 교훈: 캡슐 캐스트 시작점이 지오메트리 안이면 PhysX 가 초기 겹침을 무시해 결과가 무효다.**
  탈출 판정은 반드시 도달 가능성과 함께 본다.
- **스폰은 `ConnectionManager` 의 슬롯 풀이다.** 중심 `(-85, 0, -234)`, 8×5, 간격 5.0,
  y 는 **레이캐스트 접지**(현재 0.1837). 슬롯을 `clientId` 로 계산하면 재접속마다 자리가 바뀐다 —
  비어 있는 최저 슬롯을 배정하고 해제 시 반납한다 (T-175).
  간격 5.0 은 아바타 지름 4.4 를 넘겨야 해서다(이전 2.25 는 절반이라 파묻혀 있었다).
  중심을 1 m 옮긴 이유는 넓힌 격자가 `SSAFY-center` 를 물었기 때문이다.
- ⚠ **허공 스폰 수정은 실기 검증 대기다 (T-177).** 이동 권위가 Owner 라 서버 배정 위치가
  경합에서 밀려 원점(방 밖)에서 낙하하는 문제였다. **Owner 가 `ServerSpawnPosition`
  NetworkVariable 로 스스로 텔레포트**하게 고쳤고, 위치를 받기 전에는 이동·중력을 멈춘다.
  **서버·클라이언트를 반드시 같은 빌드로** 실행한다(NetworkVariable 추가 = 직렬화 변경).
  실행 시 `스폰 경합 감지` 경고가 찍히면 원인 가설이 확정된다.
- **천장은 원본에서 벽 위 공중에 있다** (T-157). 정렬은 `Festa/World/천장을 벽 상단에 맞춤`.
  정렬 함수의 스케일 버그도 고쳤다 — `localPosition` 이동량은 **부모** lossyScale 로 나눈다
  (자기 lossyScale 로 나누면 25% 과다 이동. 오차가 ¼씩 줄어 수렴해 오래 안 드러났다, T-172).
- **조명은 실내 전용이다.** `Directional Light` 제거(`RenderSettings.sun = null`), 환경광
  `AmbientMode.Flat`. 라인조명·다운라이트는 `URP/Unlit`(발광 Lit 은 애셋에서 키워드가 죽는다, T-159),
  실광원은 Spot 90 + Fill 9. **1 m = 10 unit 이라 세기가 100배 필요하다** — Spot 420 / Fill 260 (T-160).
- **플레이어 이동은 `CharacterController` 기반이다.** 중력 98.1 unit/s²(스케일 보정),
  `stepOffset 3.0`, `slopeLimit 45`. **원격 플레이어는 CC 를 끈다**(NetworkTransform 대입과 싸운다).
  `Player` 레이어(8)로 **Player↔Player 충돌은 껐다**(client-authoritative 떨림·통행 방해 방지).
- **클릭·호버는 `BoothInteractionInput` 중앙 디스패처가 전담한다.** 프로젝트에 `OnMouse*` 가
  **0개**다 — `OnMouseXXX` 는 Unity 6 WebGL 에서 발생하지 않아 배포 환경에서 죽고(T-166),
  존재만으로 레거시 마우스 디스패처가 매 프레임 돌며 경고를 뿜는다(T-176).
  새 상호작용은 이 디스패처에 추가한다.
- **부스**: 파츠 10종 Stand Expo Pack 재조립, `BoothShell.prefab`(6.45 × 2.72 × 6.17 m),
  패널 그래픽 인서트, Facade `primaryColor` → 셸 적용. **앵커 스케일 10**(Layout 은 미터, 월드는
  1 m = 10 unit, T-153). 씬에 구워져 있던 `Booth7_*` 12개는 제거했다 — 부스 오브젝트는
  `BoothRuntime` 이 런타임에 Local Spawn 한다(구워두면 Play 에서 이중 스폰된다).
  ⚠ **`BoothSlot_7` 이 방 밖 z=-405 로 옮겨져 있다**(사용자 조작, 미커밋). 방 z 범위는 −333~−120 이라
  런타임 부스가 월드 밖에 생긴다. 시연 전에 방 안으로 되돌릴지 결정해야 한다.
- **인프라 전제가 바뀌었다** (#30). ALB 폐기 — **Cloudflare Edge + EC2 Nginx 이중 종료**,
  Cloudflare DNS, `demo`/`api`/`ai`/`world` 서브도메인. 브라우저는 `wss://world.<domain>`(443) 만 본다.
  WebGL 파일명 해시(`nameFilesAsHashes`)를 켰다 — 고정 URL 에 `immutable` 캐시를 걸면
  새 릴리스가 캐시에 묶이기 때문이다 (`0641192`).
- **`Assets/` 는 약 356 MB다.** 최상위는 `_Project/` 와 `Plugins/` 둘뿐이다.
- **미사용 판정은 GUID 검색으로 하지 않는다.** `Resources/` 경로 로드, `Shader.Find`,
  ProjectSettings 참조, 모델 임베드 머티리얼→텍스처는 GUID 로 안 보인다.
  `AssetDatabase.GetDependencies` 로 판정한다 (T-150).
- `Assets/Plugins/WebGL/FestaUnityBridge.jslib` 는 Unity 규약 폴더라 옮기지 않는다.
- **spec 위치는 루트 `specs/`다** (develop 규약). **spec 018 은 폐기**됐고 FR-006·FR-011 이
  spec 002 로 이관됐다 (#31, PR #54).
- **spec 005 좌표 왕복 검증 통과** (FE 이정헌 + BE 황덕 서명), `assetCode` 종단 검증 통과.
- **spec 016 브라우저 왕복 검증 통과** — WebGL 빌드에서 실제 클릭으로 확인.
- **모델 포맷은 `.skp` 유지 최종 확정** — FBX/glTF/GLB/DAE 4종 실측. FBX·DAE 모두 이름이나
  계층이 깨져 파이프라인 재작업이 필요하고, Blender 5.2 는 Collada 를 제거해 우회로도 없다.
  원본 편집(중복 메시 이름 `함께가요 미래로!` ×2 개명, 정점 감소)은 **SketchUp 안에서만** 가능하다.

## 2. 다음 작업 우선순위

### P0-0a. 페스티벌 존 후속 (2026-08-22 신규)

- [x] **외부 부스 상호작용 → 내부 부스 공간 이동 — 구현 완료 (2026-08-23).** `@BoothInteriors`
  12실(밀폐, 서로 불가시) + `BoothPortal` 24개 + F키 `PortalInteractor`(프리팹 부착).
  번호 규약: **boothId 1~12 = FestivalSlot_XX = Interior_XX** — 백엔드 연동 식별자.
  실기 검증(프롬프트·왕복·원격 동기화)만 남음. 재생성 메뉴: `Festa/World/내부 부스 공간 재생성`.
  **v2 (2026-08-23)**: 방 32 m, 방마다 `BoothSlot_{id}` 앵커 + ExpoKit 셸(트러스 제외) +
  `BoothRuntime` 실통신 배선. **규격 계약: 레이아웃 좌표 = 부스 로컬 미터 (불변),
  앵커 스케일 40 = 부스 로컬 1 m → 월드 4 m 표현 배율 (v5, 2026-08-23). FE·BE 데이터 계약 변경 없음** —
  이 문장을 백엔드·프론트에 그대로 전달하면 된다. 야외 구 BoothSlot_7 은 제거(이중 런타임 방지).
- [ ] ~~(구) 외부 부스 상호작용~~ — `FestivalSlot_XX` 에
  상호작용 컴포넌트를 붙이고(`BoothInteractionInput` 디스패처 재사용, T-166), 내부 부스
  공간(별도 지역)을 만들어 텔레포트로 잇는다. 내부 공간 설계는 사용자와 정해야 한다.
- [x] ❌ **라이트맵 베이크 전환 — 철회** (T-222, 2026-08-29). 밝기는 기준선을 회복했으나 로비 전체에 각진 얼룩이 남았고 가설 4개를 전부 실측으로 기각하고도 원인을 못 찾았다. 성능은 오클루전 컬링만으로 확보했다. 원문: — 배치 확정 후. 씬 실시간 조명 127개는 WebGL 카메라당 가시
  광원 제한을 넘는다. 가로등·전구·화로는 굽고 실시간은 플레이어 주변 몇 개만 남긴다.
  베이크 전에 스카이박스가 확정돼야 한다 (앰비언트가 베이크에 들어간다).
- [x] ✅ **복도 프리팹화 — 부분 완료** (2026-08-31, `S15P21A604-161`). `Corridor_Festival` 만 프리팹으로 뺐다 — `main.unity` 순 6,331줄 감소, 시각은 전/후 렌더 SHA-256 일치. **`@Festival` 은 의도적으로 남겼다**: 서브트리 밖 참조가 13건(`BoothPortal.destination` 12개 → `@BoothInteriors/…/SpawnPoint`)이라 프리팹 에셋에서 끊긴다(격리 실험으로 확인). `BoothPortal` 이 목적지를 런타임에 찾도록 바꾸는 것이 선행이며 `S15P21A604-330` 으로 분리했다. 원문: — 지금은 씬에 직접 배치돼 있다. `Corridor_Festival` 과 `@Festival` 을 프리팹으로 묶어 맵 교체·씬 재구성에 견디게 한다.
- [ ] **불꽃놀이 발사 연출 검토** — 현재 상공에서 루프 폭발만 한다. 지상 발사 궤적이 필요하면
  `Roman candle firework` 프리팹(미사용, 삭제됨 — 필요 시 재임포트)을 검토한다.

### P0-0. 지금 바로 착수 가능 (2026-08-21 기준)

- [x] ✅ **허공 스폰 수정 실기 검증 (T-177)** — 2026-08-31 완료 (`S15P21A604-78`). 봇 빌드를 `-server` 로 띄운 전용 서버에 에디터를 붙여 10회 반복: **허공 스폰 0회 / 실패 0회**, 10회 모두 좌표가 서버 배정값 `(-29.50, 0.18, -248.00)` 과 일치. 서버 `Approved client=N slot=0 pos=…` 확인. '스폰 경합 감지' 는 10회 중 0회여서 **경합을 주입해 로그 경로를 따로 태워 확인**했다(이탈 120유닛 → 경고 + 강제 이동). 한계: 순차 접속이라 항상 `slot=0` — 동시 접속 슬롯 분배는 `-206` 의 몫. 도구 `SpawnRepeatProbe`(F11). 원문: — 서버·클라이언트를 같은 빌드로 띄워 접속한다. 볼 것 3가지: 서버 `Approved…` / Owner 쪽 `스폰 경합 감지…` / 이후 항상 맵 안 스폰.
- [x] ✅ **`BoothSlot_7` 위치 결정 — 이미 해소돼 있었다** (2026-08-31 확인, `S15P21A604-78`). 실측: `@BoothInteriors/Interior_07/BoothSlot_7` `pos=(2100, 0, 700)`, 12개 인테리어가 이루는 **700 간격 4×3 격자**의 제자리이고 `main.unity` 도 커밋 상태다. 씬을 건드릴 필요가 없었다. 원문: — 방 밖 `z=-405` 로 옮겨져 있고 **미커밋**이다. 그대로면 런타임 부스가 월드 밖에 생겨 노트북·AI 상호작용에 접근할 수 없다.
- [ ] **#52 wss 종단 실측** (infra 공동) — #30 에서 TLS 가 확정돼 블로킹이 풀렸다. `world.<domain>` 이 서면 붙인다. `ConnectionManager` 의 scheme 분기는 이미 있다. Nginx 쪽 요청 2건: WebSocket Upgrade 헤더 통과, `proxy_read_timeout` 180초 이상(기본 60초면 NGO 연결이 끊긴다).
- [x] ✅ **`AI_AGENT_INTERACT` 송신부** — 2026-08-30 완료 (`S15P21A604-303`·`-304`·`-77`). 아래 계획대로 브리지를 일반화하고 Mock 직접 호출을 걷어냈다. **FE 잔여: `events.ts` 타입 추가·`configId → agentId` 매핑.** 원문: — 필드 `{ boothId, objectId, configId }`로 확정됐다 (Issue #2, ghkim1632). `configId` → `agentId` 매핑은 FE가 한다. `BoothInteractBridge`가 LAPTOP 전용이라 `type`을 인자로 받도록 일반화하고, `AiNpcInteractable`의 Unity 내부 Mock AI 직접 호출을 제거해 브리지 송신으로 교체한다. `configId`는 이미 `BoothObjectDto`·`BoothRuntimeObject.ConfigId`에 있어 계약 변경이 필요 없다. **클릭 감지는 `BoothInteractionInput` 을 재사용하면 된다** — 중앙 디스패처가 레이캐스트로 대상을 찾으므로 `LaptopInteractable` 처럼 컴포넌트만 붙이면 동작한다 (T-166).
- [x] ✅ **T-148 카탈로그 기본 자산 정책** — 2026-08-30 완료 (`S15P21A604-104`). `assetCode` 가 빈 엔트리만 타입 기본으로 인정하는 3단 해석으로 좁혔고, 기본이 배열 순서에 좌우되면 경고를 남긴다. 데이터 쪽도 `AssetIntegrityTests` 가 모호한 타입 기본을 CI 에서 잡는다. 원문: — 한 타입에 프리팹이 2개 이상 되는 순간 `BuildMap()` 의 last-wins 때문에 어떤 자산이 나오는지가 Registry 배열 순서에 달린다.
- [ ] **`docs/27_SDD_기능분할안.md` develop 반영** — Issue #24. game·back 이 같은 013a 행을 각자 다시 써서 담당 파트·저장 메서드가 충돌한다. **back 이 먼저 올린 뒤 그 위에 game 잔여분을 얹는 순서로 합의 요청해 뒀다.** back 이 올라오면 착수한다.

### 다른 파트 회신 대기

- [ ] **FE가 쓸 `assetCode` 목록** — 받으면 프리팹 카탈로그를 그 값으로 확장하고 종단 재검증한다. 현재는 타입당 프리팹이 1개라 hit와 fallback이 같은 자산이어서 구분 검증이 성립하지 않는다.
- [ ] **013 UI 소유권 — develop 헤더 정정 알림** — `develop`의 `specs/013` 헤더에 아직 `주 담당: Unity + Frontend(창)`이 남아 있다. 정작 develop 자신의 `docs/29_아바타_커스터마이징_작업지시.md`가 "대상: Unity 파트"로 CharacterLobby UI를 Unity에 지시하고 있어 문서끼리 어긋난 상태다. 이정헌 님에게 "Unity CharacterLobby 확정, docs/29대로 구현 완료" 한 줄만 알리면 된다. 답을 기다릴 필요는 없다.
- [x] **spec 016 브라우저 왕복 — 완료 (2026-08-20).** WebGL 빌드에서 실제 클릭으로 검증했다. 노트북 클릭 1회당 이벤트 1건, 빈 공간·다른 파츠는 0건, `url` 없음·정상·따옴표·백슬래시·한글 5종 모두 보낸 값과 일치. **남은 것은 React 오버레이 연결뿐이다** (FE 몫). `OnMouseDown` 은 Unity 6 WebGL 에서 발생하지 않아 Input System 레이캐스트로 교체했다 (T-166). 검증 하네스는 `Assets/_Project/Scenes/Verify_LaptopBridge.unity`.

### 환경 미해결

- [x] **GitHub MCP 토큰 — 해결 (2026-08-21).** `GITHUB_PERSONAL_ACCESS_TOKEN` 설정 후 MCP 연결됨. OAuth 는 GitHub 이 동적 클라이언트 등록(DCR)을 지원하지 않아 불가능하므로 PAT 헤더 방식을 유지한다 (T-147).
- [ ] **`game` → `develop` PR** — 구현 확정 후에 올린다. 문서성 변경은 그때그때 별도 PR 로 올려 왔다(#28·#54 머지 완료). Unity 충돌은 game 채택으로 해결되는 것을 시험 병합으로 확인했고, `docs/24`·`docs/26`만 append 충돌이다.

### P0-1. Spring 외형 영구 저장과 재접속 복원

가장 먼저 해야 한다. 현재 `useMockApi=false`여도 User API는 `MockUserApiClient`로 대체되므로 실제 계정 저장이 아니다.

- [ ] Spring User에 `avatar_code VARCHAR(3800)` 이상 또는 `TEXT` 추가. 신규 사용자는 `NULL` 허용
- [ ] `GET /api/v1/users/me` 응답에 `avatarCode` 포함
- [x] **아바타 저장 엔드포인트 — 정리 완료 (2026-08-21, Issue #24).** spec 계약서(`specs/013-avatar-customization/contracts/avatar-profile-api.md`)가 권위이고 **`PUT /api/v1/users/me/avatar` + 필드 `avatar`** 다. `BE.md` 도 같다. **메서드는 `PUT`(spec 기준, 내 문서의 `PATCH` 가 오기), 필드명은 `avatarCode`(#24 BE 통보 — DB·JPA·Unity 세 층과 같은 이름).** 컬럼은 BE 가 `TEXT` 마이그레이션 진행. Unity 클라이언트 구현 시 `PUT` + `avatarCode` 그대로 쓰면 된다. develop 반영은 PR #28.
  **길이 상한 — 500자로 충분하다.** `Encode()` 의 `fa` 분기를 그대로 재현해 재 봤다: 기본값 138자, 색 전부 지정 + tint 406자, item ID 가 4자리여도 414자다. `fa` 는 필드 수가 고정(`g` 1 + `i` 8 + `p` 9 + `q` 9 + `w` 36)이라 길이가 거의 안 변한다. **여유는 86자(17%)뿐이라는 점만 알고 있으면 된다** — 의상 색 슬롯(`w`, 현재 36개)이 늘면 슬롯당 7자씩 먹으므로 12개만 추가돼도 넘는다. `MaxEncodedLength = 3800` 은 `fa` 때문이 아니라 **구 `rt` 모드**(Sidekick 파츠 이름 ~25자 × 16종) 때문에 잡은 값이고, `rt` 는 읽기 호환만 유지한다. 그래서 컬럼은 `TEXT`(또는 `VARCHAR(3800)`) 를 권하지만 **500 은 차단 요인이 아니다.**
- [x] ✅ 2026-08-30 확인 (`S15P21A604-301`·`-302`) — Unity `HttpUserApiClient` 구현 후 `ApiServices.Init(false, ...)`의 `MockUserApiClient`를 교체
- [x] ✅ 2026-08-30 확인 (`S15P21A604-300`) — WebGL 호스트의 Access Token을 Unity `IAccessTokenProvider`에 안전하게 전달. Refresh Token은 Unity에 전달 금지
- [ ] 신규 사용자: 추천 무작위 생성 → 저장 → 브라우저 완전 종료 → 같은 외형 복원 확인
- [ ] 저장 실패: 현재 세션 외형 유지 + 사용자에게 실패 사유 표시 확인

완료 기준: 서로 다른 계정 두 개에서 저장값이 섞이지 않고, 재접속 10회 모두 동일한 `fa` 외형이 복원된다.

### P0-2. 실제 멀티클라이언트 외형 E2E

- [ ] Editor/브라우저 또는 브라우저 2개로 A·B 접속
- [ ] A의 파츠·얼굴색·의상색을 바꿨을 때 B 화면이 1초 이내 같은 외형으로 갱신되는지 확인
- [ ] 크롭 자켓·서스펜더 반바지·한벌옷·모자·안경 조합에서 몸통 누락, 피부 관통, 중복 얼굴, 목 경계 재검사
- [ ] A 재접속 뒤 B 화면에서도 저장 외형이 일치하는지 확인
- [ ] 30~40명 부하 전에는 최소 4클라이언트 서로 다른 `fa` 외형 생성/해제 시 프레임·메모리 추이 기록

### P0-3. 이동 애니메이션과 상태 동기화 마감

- [ ] 모든 생성 파츠의 Humanoid Avatar/Animator 연결 확인
- [ ] Idle/Walk 전환과 원격 클라이언트 애니메이션 상태 확인
- [ ] 파츠 재조립 뒤 Animator 참조가 끊기지 않는지 확인
- [ ] `festa-unity/Docs/avatar-setup-checklist.md`의 미완료 항목 갱신

### 다음 핵심 기능

아바타 P0-1~3이 끝나면 아래 순서로 진행한다.

1. ~~spec 005 Layout 좌표·스키마 3파트 계약 확정~~ — 2026-08-20 완료 (왕복 검증 통과, FE·BE 서명)
2. spec 006 외부 슬롯/내부 앵커, Lease 검증, 입장·퇴장·만료 E2E
3. spec 016 부스 노트북 홈페이지 트리거와 React 오버레이 연동
4. Linux Dedicated Server Docker → AWS `wss://` 60초 이상 연결 유지 검증

전체 초기 계획은 `docs/22_다음_할일.md`, 현재 우선순위 판단은 이 문서를 따른다.

## 3. 다른 PC에서 시작하는 절차

```powershell
git clone <원격 저장소 URL> ssafesta
Set-Location ssafesta
git switch game
git pull --ff-only origin game
git status --short
```

1. Unity Hub에서 `festa-unity`를 **Unity 6000.0.78f1**로 연다.
2. `Assets/_Project/Scenes/CharacterLobby.unity`를 열고 컴파일 완료를 기다린다.
3. Unity MCP를 해당 Editor 인스턴스에 연결한다.
4. Play Mode에서 최초 추천 무작위, 저장된 `fa` 복원, 기본 의상 탭 `상의`, 색상 팝업 드래그를 확인한다.
5. Console error/warning 0건과 16:9 Game View 레이아웃을 확인한 뒤 다음 구현을 시작한다.

새 PC에서는 깨끗한 clone을 권장한다. 현재 작업 PC에만 남아 있는 vendor `.unitypackage.meta` 삭제와 `ProjectSettings` 변경은 아바타 작업 커밋에 포함되지 않았으므로 그대로 따라 커밋하지 않는다.

## 4. 핵심 파일

- 정식 UI: `festa-unity/Assets/_Project/Scripts/World/Avatar/Lobby/CharacterLobbyController.cs`
- 모듈 조립: `festa-unity/Assets/_Project/Scripts/World/Avatar/Modular/AvatarAssembler.cs`
- 외형 포맷: `festa-unity/Assets/_Project/Scripts/World/Avatar/AvatarAppearance.cs`
- 네트워크/저장 요청: `festa-unity/Assets/_Project/Scripts/World/Avatar/PlayerAppearanceController.cs`
- API 조립 지점: `festa-unity/Assets/_Project/Scripts/Integration/ApiServices.cs`
- User 계약: `festa-unity/Assets/_Project/Scripts/Integration/Contracts/IUserApiClient.cs`
- Mock 구현: `festa-unity/Assets/_Project/Scripts/Integration/Mock/MockUserApiClient.cs`
- 기능 spec: `docs/specs/013-avatar-customization/spec.md`
- 파트 간 계약: `festa-unity/Docs/avatar-customization-contract.md`
- Unity 구조: `festa-unity/Docs/architecture.md`
- 작업 기록: `docs/KHS/24_작업일지.md`, `docs/KHS/25_트러블슈팅.md`

## 5. 작업 종료 규칙

- 기준선 코드를 재구현·대규모 리팩터링하지 않는다.
- `reference/`는 읽기 전용이다.
- 완료한 작업은 즉시 `docs/KHS/24_작업일지.md`에 기록한다.
- 문제가 한 번이라도 발생하면 `docs/KHS/25_트러블슈팅.md`에 T-번호로 증상/원인/해결/예방을 남긴다.
- Unity 코드와 문서는 가능하면 논리적으로 분리해 커밋하고, `git diff --check` 및 Console 검증 후 `game`에 push한다.

## 11층 모델 재export 요청 — SSAFY 글자 분리 (2026-08-20)

### 요청 내용

11층 SketchUp 모델을 다시 내보낼 때 **바닥에 서 있는 SSAFY 3D 글자를 별도 그룹**으로
분리해 달라고 요청한다. `walls, floor, ceiling` 을 그룹으로 나눠준 것과 같은 방식이다.

### 왜 필요한가

현재 글자는 독립 오브젝트가 아니라 `ssafy-11th-room` 노드의 **자체 병합 메시**
(8,052 verts, 서브메시 3개) 안에 있다. 두 단계로 확인했다.

1. 그 렌더러만 끄면 글자만 사라지고 벽·바닥·부스·현판은 그대로다.
2. 서브메시 2(`ssafy_blue`)만 다른 색으로 칠하면 글자가 칠해진다.

그런데 **서브메시 2 에는 글자만 있는 게 아니다.** 파란 안내 데스크
("함께가요 미래로!", x≈-140 / z≈-190)도 같은 슬롯이다. 같은 브랜드 색이라
하나로 병합됐다. 그래서 오브젝트 단위도, 재질 단위도 글자만 분리할 수 없다.

Unity 에서 메시를 잘라내는 것도 가능하지만 `Read/Write Enabled` 활성화 =
재임포트가 필요하고, T-157 의 한글 이름 메시 중복(`Mesh 함께가요 미래로!`) 때문에
참조 재연결이 보장되지 않는다. 게다가 모델을 다시 받을 때마다 반복해야 한다.

### 같이 요청하면 좋은 것

- **메시 이름을 영문으로** — 현재 `함께가요 미래로!` 등 한글 이름이 중복돼
  임포트마다 `Identifier uniqueness violation` 경고가 난다 (T-157).
- 천장 높이 — 원본에서 천장이 벽 상단보다 66단위 위에 떠 있다. Unity 에서
  정렬해 쓰고 있지만 원본이 맞으면 그 단계가 없어진다.

### 새 모델을 받은 뒤 할 일

**`Festa/World/천장 조명 셋업 다시 적용`** 메뉴 한 번 실행하면 끝난다
(`Assets/_Project/Scripts/Editor/WorldCeilingSetup.cs`).

재적용되는 것 — 모델 인스턴스에 걸린 오버라이드라 교체하면 날아간다:

| 항목 | 수량 |
|---|---|
| 머티리얼 슬롯 (라인조명·다운라이트·천장배경) | 109 |
| ~~콜라이더~~ → **임포트 시 애셋 내장으로 이관 (2026-08-21)** — Mesh 20 / Capsule 3, `WorldModelNaming.AddColliders`. 재적용 불필요 | 23 |
| 벽 개구부 봉쇄 — 엘리베이터 문 (좌표를 박지 않고 훑어서 탐지) | 3 |
| 천장 광원 재배치 (기구 bounds 기준) | Spot 90 + Point 9 |

재임포트와 무관해서 **다시 만들지 않는 것**:

- `WorldCeilingLights` 아래 광원 — 모델과 별개인 일반 씬 오브젝트
- `WorldCeiling{LightStrip,Downlight,Black}.mat` — 프로젝트 소유 애셋
- `RenderSettings`(환경광·sun 제거), `Mobile_RPAsset` 광원 한도
- `BoothSlot_*` 아래 부스 오브젝트
- `@WorldCollisionSeals` 아래 봉쇄 박스 — 모델 밖 독립 오브젝트 (다만 도구가 매번 재생성한다)

주의 — **천장 정렬은 재적용에서 분리해 뒀다** (`Festa/World/천장을 벽 상단에 맞춤`).
층고를 의도적으로 조정해 둔 경우 재적용이 덮어쓰면 안 되기 때문이다.

교체 후에는 `Festa/World/셋업 검증 (읽기 전용)` 으로 스폰 40/40 과 콜라이더 수를 확인한다.
모델 구조(노드 이름)가 바뀌면 이 도구의 이름 상수를 먼저 맞춰야 한다.

### 조명 색·세기를 바꿀 때

값은 **`WorldCeilingSetup.cs` 상단 상수**에 모여 있다. 씬에서 직접 고치면
도구를 다음에 실행할 때 되돌아가므로, **상수와 씬을 같이 바꾼다** (또는 상수만
고치고 도구를 실행한다 — 이쪽이 안전하다).

| 상수 | 현재 값 | 의미 |
|---|---|---|
| `SpotColor` | `(1, 0.945, 0.855)` | 다운라이트 색. 살짝 웜 |
| `FillColor` | `(1, 0.955, 0.885)` | 타일 채움광 색 |
| `AmbientColor` | `(0.215, 0.208, 0.198)` | 환경광. **광원과 같은 색 방향이어야 한다** |
| `SpotIntensity` / `FillIntensity` | 420 / 260 | 1 m = 10 unit 스케일 보정값 (T-160) |
| `SpotRange` / `FillRange` | 120 / 160 | 천장~바닥 49.5 unit 을 덮는 값 |

주의 두 가지:

1. **환경광 색 방향을 광원과 맞춰라.** 쿨한 환경광(B > R)은 웜톤 광원을 상쇄한다.
   실제로 처음 웜톤 작업 때 환경광이 `(0.20, 0.21, 0.24)` 로 쿨이라 효과가 반감됐다.
2. **조명면 머티리얼도 같이 바꿔라** — `WorldCeiling{LightStrip,Downlight}.mat` 의
   base color. 기구 자체가 웜 화이트로 보여야 바닥에 떨어지는 빛과 일관된다.
   이 둘은 애셋이라 도구가 건드리지 않는다.

---

## 2026-08-31 갱신 — 실 백엔드 종단 연결과 세계 스케일 통일

**이 절이 이 문서에서 가장 최신이다.** 위쪽 절들은 2026-08-25~30 시점 조사라 아래와 어긋나면
이 절을 따르라.

### 지금 무엇이 되는가 (전부 실측으로 확인)

- **임대 → Studio 발행 → Unity 방문** 전 구간이 실물로 돈다. 목 데이터가 아니라 실 DB 다.
- 브라우저(WebGL) → 목 서버 프록시(:8000) → 실 Spring(:8080) → 게임 서버(:7777) 접속 성립.
- 부스 상호작용(F): 근접 자동 조준 → `BOOTH_LAPTOP_INTERACT` / `AI_AGENT_INTERACT` payload 송신.

### 로컬 풀스택 기동 방법 (이 순서를 지켜라)

1. `cd backend && docker compose up -d` — Postgres·Redis
2. Spring — **JDK 21 이 없으면 컨테이너로 띄운다** (자세한 사정은 S15P21A604-342):
   `docker run --rm -v "$(pwd -W)":/app -w /app -v ssafesta-m2:/root/.m2 --env-file <env> -p 8080:8080 maven:3.9-eclipse-temurin-21 mvn -B spring-boot:run`
   env 에 `POSTGRES_HOST=host.docker.internal`·`REDIS_HOST=host.docker.internal`·
   `SPRING_PROFILES_ACTIVE=local` 을 더해야 한다. **`backend/.env` 는 compose 만 읽는다 —
   Spring 은 그 파일을 읽지 않는다** (dotenv 의존성 없음).
3. 게임 서버 — `Builds/bot/festa-bot.exe -server -port 7777 -batchmode -nographics`,
   환경변수 `CONNECTION_TOKEN_SECRET` 을 **Spring 과 같은 값**으로 주입.
4. 목 서버 프록시 — `node Tools/mock-api/serve.mjs --root ../../Builds/web-verify --port 8000
   --spring http://localhost:8080`. 프록시 모드에서는 시크릿이 필요 없다.
5. 브라우저에서 `http://localhost:8000`.

> **왜 프록시인가**: WebGL 페이지가 Spring 을 직접 부르면 CORS 에 막힌다(Spring 은
> `FRONTEND_BASE_URL` 하나만 허용하며 그 정책이 옳다). 같은 origin 으로 받아 서버 사이에서
> 넘기면 CORS 자체가 발생하지 않는다.

> **도커가 재시작되면 컨테이너가 전부 내려간다** — `Exited(255)` 를 보면 위 1~4 를 다시 하라.
> 개발 머신 메모리 압박 때 실제로 겪었다.

### 세계 스케일 — 규약이 바뀌었다 (S15P21A604-350, T-235)

**기준은 싸피맵 내장 인물("Niraj", 22.4 unit = 2.24 m)이다.**

- 아바타 시각 높이 = 22.375 (`PlayerAvatarVisual._targetVisualHeight`)
- 부스·아바타 영역의 거리 상수는 **1 m ≈ 13.26 unit** 로 읽어라
  (상호작용 사거리 `40f` = 3 m). 코드 곳곳의 "1 m = 10 unit" 주석은 이 영역에서는 낡았다.
- 부스 실내 `Interior_NN` = 0.53, 축제 슬롯·프롭·램프·장식은 ×1.326 적용됨.
- 부스 오브젝트 프리팹 루트: 소형물 ×1.30, 키오스크 ×1.57, NPC ×1.5(사람 키), 스크린 ×1.15.
  **이 값들은 에디터 플레이 반복으로 사람이 눈으로 확정한 것**이라, 바꿀 때도 같은 방식으로 하라
  (빌드 20분 루프 금지 — 에디터는 회당 30초다).

### 씬을 만졌으면 서버도 다시 빌드하라

게임 서버는 충돌 지형의 권위다. 씬·프리팹을 바꾸고 서버를 그대로 두면 **벽이 어긋난다.**
`Festa/부하테스트/봇 클라이언트 빌드 (Windows)` 로 재빌드 후 재기동.

### 다음에 손댈 것

- `-341`(FE env.example) · `-342`(BE 컨테이너 경로) · `-344`(AI 로컬 기동) — **타 파트 몫**으로
  넘긴 이슈다. 게임 파트가 고치지 마라.
- Spring 에 아직 AI 호출 코드가 없다 (`INTERNAL_SPRING_TO_AI_TOKENS` 미사용) —
  Sprint 4 Document 파이프라인에서 배선될 자리.
- 축제존 `Festival_Clutter_Combined` 는 단일 피벗 결합 메시라 스케일 대상에서 제외했다.
  나중에 손대려면 결합을 풀어야 한다.
