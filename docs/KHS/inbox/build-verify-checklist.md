# 빌드 검증 체크리스트 (게임 파트)

> 빌드는 몰아서 한 번. 에디터에서 확인할 수 있는 것은 여기 적지 않는다 — 에디터 Play 로 바로 검증한다.
> 여기에는 **빌드(WebGL·서버 이미지)로만 확인 가능한 항목**을 그때그때 한 줄씩 쌓고, 빌드 한 번에 위에서부터 전부 확인한 뒤 결과를 적는다.
> 확인 환경: 데스크톱 앱 Browser 패널(보이는 탭 — `document.visibilityState === "visible"` 먼저 확인, T-115) 또는 사용자 Chrome. 숨은 탭에서 잰 수치는 무효.

## 다음 Development 빌드에서 확인할 것

> **빌드 준비됨 — 2026-09-06 05:43, `Builds/web`, develop `7f964d28`(!303·!304 포함, !305 `_MainTex` 수정은 미포함), 301 MB, 10.4분.** 프록시 `:8000` 이 그대로 서빙한다.
> 실측은 **창이 보일 때** 진행 — 06:50 시점 데스크톱 앱 Browser 패널과 사용자 Chrome 탭이 모두 `visibilityState === "hidden"` 이라(화면 잠김/최소화 추정) 보류. 숨은 상태에서 잰 진입 55.1s 는 무효.
> 걸어가지 않고 보는 법: `http://localhost:8000/probe.html`(FE 없이 빌드를 열어 `window.unityInstance` 노출, 빌드 폴더라 git 무시) →
> `unityInstance.SendMessage('Character Lobby Flow','EnterWorld')` 로 입장 → `SendMessage('Arcade_01_Slot_Machine_01','Interact')`·`SendMessage('Arcade_03_Cabinet_01','Interact')` 로 #1·#3·#4·#5·#6·#7(자기 아바타 숨김) 확인, Esc 는 `canvas.focus()` 뒤 키 이벤트. #2 는 게임기 토스트(같은 IMGUI 9-slice 경로)로 갈음, #8 은 에디터에서 확인된 씬 데이터라 배치 자체는 동일.

| # | 항목 | 어떻게 | 기대 | 근거 |
|---|---|---|---|---|
| 1 | UI 킷 스프라이트·TMP 외곽선 글자 | 슬롯머신 F → HUD / 타이밍 스톱 HUD / 로비 | 파란 패널·주황 버튼·아이콘이 깨지지 않고 보인다(런타임 Sprite.Create 9-slice, 아틀라스 2048). 한글 외곽선 정상 | !TBD (-439·-440 UI 킷) |
| 2 | 상호작용 프롬프트 IMGUI 3×3 조각 | 데스크·슬롯머신 조준 | 남색 알약 + 주황 F 키캡, 모서리 늘어남 없음 | 같은 MR |
| 3 | 초점 카메라 + 잠금 + Esc | 슬롯머신·게임기 F → Esc | 카메라 복귀·입력 복구. 브라우저에서 Esc 가 캔버스에 전달되는지(captureAllKeyboardInput=false) | -439·-440 |
| 4 | 게스트 지갑 403 → 팝업 문구 | 게스트로 슬롯머신 돌리기 | "게스트는 코인을 쓸 수 없어요 — 로그인하면 코인을 받아요" 팝업 | -439 |
| 5 | 게임기 `WORLD_ARCADE_INTERACT` 송신 | 게임기 F | 콘솔 `onBoothInteract → {"type":"WORLD_ARCADE_INTERACT","machineId":"plaza-arcade-01"}` (FE 수신부 없어 오버레이는 안 뜸) | -440 |
| 6 | UI v3(밝은 카드) — 슬롯·타이밍 스톱·프롬프트 (로비는 원래 UI 유지) | 각 화면 열기 | 크림 카드·코랄 버튼·어두운 글자, TMP 한글 정상. 킷 스프라이트는 코인 아이콘만 | -439 UI v3 |
| 7 | 초점 중 자기 아바타 숨김·이름표 오클루전 | 슬롯머신 F / 벽 뒤 다른 플레이어 | 줌인 중 내 캐릭터 안 보임, 벽 너머 상대 이름표 안 보임(오클루전 컬링은 빌드에서만 정확) | 사용자 지적 09-06 |
| 9 | 새 글꼴(주아·Noto Sans KR Bold Dynamic SDF) + 게스트 팝업(#4 재확인) | 슬롯머신 F → HUD·게스트 스핀 → 팝업 / 관리 데스크 프롬프트 / 이름표 | 제목·버튼은 주아, 본문·이름표는 Noto Bold, 한글 누락 글리프 없음(Dynamic SDF 가 WebGL 에서 채워짐 — Malgun 과 같은 경로). 팝업 자리에 회색 사각형 없음 | -443 글꼴 · -442 팝업 그림자 |
| 10 | 부스 내부 v3 — **뒤돌아본 문틀·EXIT 사인·"나가기" 프롬프트·노트북 하이라이트만 남음**(카메라 회전은 주입 불가 → 월요일 손 확인) | 부스 안에서 우클릭 드래그로 뒤돌기 / 노트북 조준 F | 문틀·EXIT 사인(글자 정방향)·매트, "나가기" 프롬프트는 문 앞에서만, 노트북 F 하이라이트가 노트북에만 | -445 (스폰 시점은 09:40 확인 ✅) |
| 11 | 월드 우상단 "Customize (C)" 데모 HUD 부재 | 월드 진입 직후 우상단 / C 키 | 버튼 없음, C 키 무반응(에디터 전용으로 내림 — `Application.isEditor`). 이름표가 가려지지 않음 | -447 |
| 12 | 조작 안내 카드(-451·-456) | **FE 임베드**: Unity 카드·알약이 없고 FE WorldHud 카드만 / probe.html: `window.FestaUnity` 스텁이 있어 역시 숨김(단독 실행에서만 보임) | FE 임베드에서 조작 안내가 한 장만 보인다. Unity 카드는 `HostProvidesUi` 로 숨김 | -456 |
| 13 | 길찾기 표지판 4개(-452) | 스폰에서 북서쪽 / 복도 북단 / 축제 입구 / 부스 거리 가운데 | 나무 기둥 + 화살 판, TMP 3D 글자(주아) 양면이 읽힘, 판이 목적지 방향을 가리킴, 기둥에 걷기 충돌 | -452 |
| 14 | **회원 접속(-457, T-125)** | FE 회원 로그인(Google/Kakao) → 외형 저장한 계정으로 월드 입장 / 로컬은 `mint_member.js` 토큰 | `[NetworkBootstrap] 비분할 메시지 상한 4096 B` 로그 뒤 승인·스폰. `OverflowException` 없음. 서버 이미지도 재빌드본이어야 함(클라만으로도 옛 이미지에 붙는 건 확인) | -457 |
| 8 | 티켓 부스 NPC·축제 아케이드 배치 | 축제 입구 티켓 부스 / 아케이드 줄 | 창구 NPC 근접 프롬프트·대화 카메라, 타이밍 스톱 캐비닛·슬롯머신 2·게임기 2 가 한 줄 | 09-06 배치 |

## 다음 릴리스 빌드에서 확인할 것

| # | 항목 | 어떻게 | 기대 | 근거 |
|---|---|---|---|---|
| — | (없음 — 2026-09-06 00:00 클린 트리 빌드에서 -438 태그 확인 예정) | | | |

## 확인 완료 기록

| 날짜 | 빌드 | 항목 | 결과 |
|---|---|---|---|
| 2026-09-06 09:40 | dev (develop 6466422d, 304 MB 13.0분, `Builds/web`, probe.html) | #10 부스 내부 v3 스폰 시점 / -444 게임기 이동 / 포털 F 입장(WebGL) / 진입 시간 | ✅ `TeleportNear Portal_Ext_01` → F → `pos=(700.0, 0.2, 51.7) yaw=180` — WebGL 에서도 포털 입장·목적지·방향 일치. 스폰 시점 화면: 6 m 셸(파란 벽) 이 방을 채우고 밝음, 왼쪽 테이블+노트북·정면 상담 데스크·**AI 도우미 캐릭터 + 이름표** 셸 안에 겹침 없이, 내 이름표 Noto Bold. 진입 **3.3s**(2회차, 1회차 6.0s 는 캐시 전 main_loaded 2.8s). ⚠ 뒤돌아본 문/EXIT 사인·노트북 하이라이트는 카메라 회전 주입이 안 돼 미확인 → #10 잔여로 남김(에디터 확인분으로 갈음) |
| 2026-09-06 19:00 | **릴리스** (develop 834ea911, `Builds/web-release` 143 MB, Brotli+Fallback) | 조명/렌더 파이프라인 · 진입 시간 · 프레임 · 길찾기 표지판(#13) · 호스트 API 주입(-459) · 서버 이미지 | ✅ 조명·그림자 정상(빌더 검사 오탐이었다 — 압축 전 데이터에 `Mobile_RPAsset` 존재, -461 로 수정). probe 진입 **3.2s / 1280×720 80 fps**, 프런트 임베드 진입 **5.8s**. 표지판 글자 가독 ✅. `[ApiServices] 호스트 주입 apiBaseUrl 사용 — 'https://api.ssafesta.world' → 'http://localhost:8000'` ✅. 서버 이미지 `festa-world:834ea911` 기동 시 `비분할 메시지 상한 4096 B`(T-125) ✅. ⚠ #10 뒤돌아본 문·EXIT, #12 조작 카드, #14 회원 로그인 경로는 월요일 손 확인 |
| 2026-09-06 07:35 | dev (develop fd024fff, `Builds/web`, probe.html manifest 기반) | #9 글꼴 / #4 팝업 그림자 / -441 콘솔 / 진입 시간 | ✅ 이름표 Noto Bold·HUD 주아·토스트 주아 렌더, `_MainTex` 에러 0(화면 콘솔 사라짐), 팝업 자리 회색 사각형 없음, **진입 3.1s**(보이는 탭). ⚠ 게스트 팝업 클릭은 패널·JS 주입 모두 uGUI 에 안 닿아 미확인(에디터 확인으로 갈음). ❌ 게임기 01 앞 풍선 겹침 → -444 z=161 이동(에디터 확인, 다음 빌드에서 시각 확인) |
| 2026-09-06 06:10 | dev (develop 7f964d28, `Builds/web`, probe.html + SendMessage) | #1 UI v3 카드·TMP / #3 초점+Esc / #5 `WORLD_ARCADE_INTERACT` / #6 v3 / #7 자기 아바타 숨김 / #2 프롬프트 알약 | ✅ 전부 렌더·동작. Esc 는 `canvas.focus()` 뒤 `KeyboardEvent` 로 전달됨. ❌ **팝업 자리에 회색 반투명 사각형 상주**(비활성 팝업의 형제 그림자, -442) / ❌ 재접속 재스폰 시 초점 잔류(-442) → 둘 다 수정, 다음 빌드에서 #4(게스트 팝업 문구·그림자 없음) 재확인. #8 배치는 에디터와 동일 씬 데이터. 진입 시간은 패널 stall 로 무효 — 월요일 사용자 Chrome 에서 잰다 |
| 2026-09-06 01:10 | dev #6 + 에디터 클라 (Docker `festa-world:cf83e396`) | 게스트 즉시 입장 / 2클라 외형 동기화(-76) | ✅ `guest=True` → `[CharacterLobby] 게스트 — 커스터마이징 생략, 월드 입장` → 총 7.9s. 2클라 상호 표시·외형 변경 전파 확인 |
| 2026-09-06 00:50 | 에디터 Play(클라이언트 → Docker `festa-world:cf83e396`) | F2 토글(!294) | ✅ `s_panelVisible` False → F2 주입(InputSystem QueueEvent) → True. 빌드 없이 에디터로 확인 — 새 규칙 첫 적용. 릴리스 이미지 서버에 승인·스폰(총 7.0s) |
| 2026-09-06 00:00 | dev #6 (develop 1353abe3) | 접속 패널·PerfHud 기본 숨김 / F3 / GC/1s / 외곽선·프롬프트 / `[WorldLoadTimeline]` | ✅ 숨김 확인, F3 동작, GC/1s 0~1, NPC 외곽선·`F · 내 부스 관리` 정상, 총 3.0s (main_loaded 1.2s). ❌ F2 안 켜짐 → !294 |
