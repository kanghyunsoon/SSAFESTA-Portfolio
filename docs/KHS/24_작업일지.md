# 작업일지

## 2026-08-18

### 월드 카메라 다중 사용자 충돌·우클릭 공전 개선 ✅

- 🤖 기존 카메라가 모든 레이어를 충돌 검사하면서 자기 자신만 제외해, 원격 플레이어의 Collider까지 벽으로 인식하고 카메라가 앞으로 튀는 원인을 코드에서 확인했다.
- 🤖 로컬·원격 `NetworkPlayer` Collider는 카메라 장애물에서 제외하고 건축물·월드 지형만 충돌 거리 계산에 반영하도록 변경했다.
- 🤖 매 프레임 배열을 생성하던 `SphereCastAll` 대신 64개 고정 버퍼의 `SphereCastNonAlloc`을 적용해 30~40명 접속 환경에서 카메라 충돌 검사의 GC 할당을 제거했다.
- 🤖 충돌 시 카메라는 즉시 앞으로 당기고 장애물이 사라질 때만 부드럽게 원래 거리로 복귀시켜 건축물 모서리의 떨림을 줄였다.
- 🤖 마우스 우클릭 드래그로 캐릭터의 시점 축을 기준으로 수평·수직 공전하도록 추가하고, 수직 각도를 `-20°~65°`로 제한했다.
- 🤖 빌드는 실행하지 않았으며 변경 파일 `git diff --check` 통과 및 최신 Unity Editor 로그에서 관련 C# 컴파일 오류가 없음을 확인했다. 실제 다중 클라이언트 카메라 체감 검증은 플레이 테스트로 남아 있다.
- 관련 파일: `PlayerCameraFollow.cs`, `PlayerAvatar.prefab`

### 월드 플레이어 이동·애니메이션·카메라 조작 마감 1차 ✅

- 🤖 기존 코드를 확인해 월드 이동을 카메라 기준 WASD 방향으로 변경하고, 캐릭터가 이동 방향을 향하도록 회전을 유지했다.
- 🤖 Shift 달리기와 `Idle / Walk / Run` 네트워크 상태를 추가해 Owner가 기록한 상태를 원격 Client도 동일하게 재생하도록 확장했다.
- 🤖 별도 Run 원본 애니메이션이 없는 현재 에셋 구성에서는 Walk 클립 재생 속도를 높여 Run 상태를 표현하고, 외형 재조립 후 새 Animator에도 현재 동기화 상태를 다시 적용하도록 기존 경로를 유지했다.
- 🤖 Owner 전용 카메라에 휠 거리 조절(3~8m), 캐릭터 자신을 제외한 SphereCast 벽 충돌 보정, 부드러운 추적을 적용했다.
- 🤖 전체 빌드는 실행하지 않았고 변경 파일 `git diff --check`를 통과했다. Unity Editor Console 및 2-Client 실제 동작 검증은 에디터 연결 후 남아 있다.
- 관련 파일: `NetworkPlayer.cs`, `PlayerMovement.cs`, `PlayerAvatarVisual.cs`, `PlayerCameraFollow.cs`, `PlayerAvatar.prefab`

> **운영 규칙 (필수)**
> 1. 하나의 작업이 끝나면 **그날 날짜 섹션에 즉시 기록**한다 (최신 날짜가 위).
> 2. 기록 단위: 무엇을 / 왜 / 결과 / 관련 파일·커밋·태그.
> 3. 작업 중 문제가 발생하면 해결 여부와 무관하게 **같은 폴더의 `25_트러블슈팅.md`에 반드시 등록**하고, 일지에는 트러블 번호(T-xx)로 링크만 남긴다.
> 4. AI 세션에게 작업을 시켰다면 그 결과도 동일하게 기록한다 (누가 했는지 구분: 👤 사람 / 🤖 AI).

---

## 2026-08-16

### 성별 전환 시 의상 썸네일·크롭 몸통 표시 통일 ✅

- 🤖 남성 의상 정의에 남아 있던 구형 캐릭터 렌더 썸네일과 영문 이름 때문에 성별 전환 후 의상 카드가 이전 모습으로 돌아가는 원인을 확인
- 🤖 상의·하의·한벌옷은 성별별 실제 메시와 아이템 ID를 그대로 선택하되, 같은 순번의 여성 카탈로그에 등록된 한국어 이름과 일러스트 썸네일을 공통 표시 정보로 사용하도록 변경
- 🤖 남성 `Top.07-A`가 여성과 다른 하체 인접 몸통 파츠까지 숨겨 허리가 비던 문제를 확인하고 남성 Spine01/Spine02를 강제 표시하도록 에셋과 재생성 빌더 규칙을 함께 수정
- 🤖 Unity 에셋 리프레시·스크립트 컴파일·어셈블리 재로딩 완료 후 최근 Editor 로그에서 C# 컴파일 오류가 없음을 확인
- 🤖 현재 UI에서 참조하지 않는 구형 헤어 시트 5장, 교체 전 의상 탭 아이콘 4장, 미참조 자동 생성 썸네일 11장을 제거해 프로젝트 UI 자산을 정리
- 📎 관련 파일: `CharacterLobbyController.cs`, `CharacterLobbyBuilder.cs`, `M_Top.07-A.asset`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`, `docs/KHS/conference/05_아바타_커스터마이징과_동기화.md`
- 📎 트러블슈팅: T-130

---

### Unity 정식 커스터마이징 UI 확정 및 다음 작업 인수인계 ✅

- 🤖 완성된 Unity `CharacterLobby`를 정식 아바타 커스터마이징 화면으로 확정하고 React 오버레이 교체 요구를 폐기
- 🤖 spec 013, 아바타 계약서, Unity 아키텍처, SDD 분할안·FE 가이드·파트별 할 일·팀 결정 문서의 UI 소유권과 `fa` 최대 3800자 저장 계약을 현재 구현 기준으로 동기화
- 🤖 다른 PC에서 바로 이어갈 수 있도록 완료 상태, Spring 저장/재접속 복원·멀티클라이언트 E2E·애니메이션 우선순위, 시작 절차와 핵심 파일을 `docs/KHS/26_다음_작업_인수인계.md` 한 파일에 정리
- 🤖 `docs/22_다음_할일.md`은 2026-08-08 초기 계획임을 표시하고 최신 KHS 인수인계 문서를 우선하도록 안내
- 🤖 프론트 브랜치는 다른 담당자의 작업과 충돌하지 않도록 Unity 코드·KHS 개인 기록·삭제된 spec을 되살리지 않고, FE가 필요한 UI 소유권 결정만 별도 신규 문서로 반영하도록 분리
- 📎 관련 파일: `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`, `festa-unity/Docs/architecture.md`, `docs/sdd/`, `docs/26_팀_결정_필요사항.md`, `docs/27_SDD_기능분할안.md`, `docs/KHS/26_다음_작업_인수인계.md`
- 📎 트러블슈팅: T-128, T-129

---

### 아바타 커스터마이징 UI 가독성·레이아웃 전면 개선 ✅

- 🤖 959×539 실제 Game View를 기준으로 좌·우 패널 폭, 상단 패딩, 제목·탭·섹션·목록 기준선을 통일하고 양쪽 탭을 120px 높이로 확대; 아이콘과 설명을 분리해 큰 아이콘 위·설명 아래 구조로 변경
- 🤖 모든 파츠 선택 카드를 2열 대형 카드로 바꾸고 썸네일 비율을 보존했으며, 얼굴·의상·모자·안경은 같은 중성 회색 배경, 어두운 헤어는 밝은 대비 배경을 사용하도록 정리
- 🤖 색상 영역을 카테고리별 필요한 높이만 패널 하단에 고정해 얼굴은 2행, 헤어는 약 3행의 아이템을 동시에 확인할 수 있도록 탐색 영역을 확장
- 🤖 범용 `주 원단/보조 원단/디테일` 문구를 제거하고 서스펜더 반바지의 `상의 원단/반바지 원단/멜빵·단추`, 사각 안경의 `안경테/브리지·다리`처럼 현재 아이템별 실제 부위명으로 교체
- 🤖 흰 외곽선이 겹쳐 깨져 보이던 얼굴 색상 아이콘을 단색 마커로 단순화하고 색상칩 크기·테두리를 통일; 텍스트 Rect 이중 높이로 행 밖에 벗어나던 문제도 수정
- 🤖 차가운 사각형 위주의 스타일을 따뜻한 중성 팔레트·둥근 카드·버튼·팝업·선택 강조로 교체하고, 색상 팝업은 실제 부위명·색상환·HEX·취소/완료만 남긴 컴팩트 모달로 축소
- 🤖 색상 팝업의 전체 화면 스크림을 제거하고 팝업 Rect에서만 캐릭터·카메라 입력을 막도록 변경했으며, 제목 영역 드래그로 창을 중앙 프레임 안에서 이동 가능하게 구현
- 🤖 의상 탭 네 아이콘은 내장된 어두운 카드·금색 장식을 ImageGen 투명 누끼로 분리해 별도 `*_cutout.png` Sprite로 적용하고, 카드 간격 11px·아이콘 내부 여백·라벨 하단 간격을 맞춰 오른쪽 외형 탭과 같은 호흡으로 정리
- 🤖 고정 여성 기본값으로 시작하던 초기화를 `현재 로컬 외형 → 저장된 fa 프로필 → 최초 추천 무작위` 순으로 교체하고, 최초 추천은 상의·하의 조합과 `상의` 탭으로 시작하도록 변경
- 🤖 무작위는 자연스러운 피부·머리·홍채·입술 팔레트와 조화된 의상 테마를 사용하고 모자·안경은 낮은 확률로 하나만 선택하도록 제한; Unity MCP 24회 반복에서 구조 오류 0건·동시 액세서리 0건·모듈 저장 왕복 성공 확인
- 🤖 실제 기능 없이 안내 문자열만 출력하던 `미리보기 설정`과 개발용 내부 ID를 출력하던 `현재 외형 정보` 버튼을 사용자 UI에서 삭제
- 🤖 spec 013, 런타임 계약서, 발표용 아바타 문서, 레거시 Sidekick 세팅 체크리스트를 전수 대조해 부위당 단일 색상 UI·아이템별 유효 영역·최초 외형 복원/추천·팝업/탭 규격을 현재 구현과 일치하도록 동기화
- 🤖 Unity MCP로 얼굴·헤어·안경·의상 색상 팝업을 959×539 Game View에서 시각 검증하고, 스크립트 오류 0건·Unity Console 오류/경고 0건을 확인
- 📎 관련 파일: `CharacterLobbyController.cs`, `CategoryIcons/category_*_cutout.png`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`, `docs/KHS/conference/05_아바타_커스터마이징과_동기화.md`, `festa-unity/Docs/avatar-setup-checklist.md`
- 📎 검증 화면: `avatar-ui-final-tabs.png`, `avatar-ui-final-hair.png`, `avatar-ui-final-accessories.png`, `avatar-ui-final-popup.png`
- 📎 트러블슈팅: T-113, T-114, T-115, T-116, T-117, T-118, T-119, T-120, T-121, T-122, T-123, T-124, T-125, T-126, T-127

---

### 전 착용 파츠 아이템별 유효 색상 영역 정리 ✅

- 🤖 Unity MCP에서 여성·남성 상의·하의·한벌옷과 공용 신발·모자·안경을 하나씩 장착하고, 각 A/B/C 영역을 검정↔흰색으로 바꾼 전·후면 렌더의 픽셀 변화량을 측정
- 🤖 다른 장비가 검사 대상을 가리지 않도록 모자·헤어·하의를 분리한 재검사를 거쳐, 가장 잘 보이는 영역 대비 변화가 5% 미만인 영역을 아이템별 UI에서 제거
- 🤖 `AvatarItemDefinition.garmentColorAreaMask`를 추가하고 빌더 재생성 규칙에도 동일한 마스크를 반영하여 같은 카테고리라도 아이템에 따라 색상 컨트롤이 1~3개만 표시되도록 변경
- 🤖 여성·남성에서 실제 노출되는 70개 아이템 선택을 Unity MCP로 전수 검사하여 예상 행 개수와 실제 UI 행 개수가 모두 일치함(`tested=70`, `failed=0`)을 확인
- 📎 관련 파일: `AvatarItemDefinition.cs`, `CharacterLobbyBuilder.cs`, `CharacterLobbyController.cs`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`
- 📎 검증 화면: `festa-unity/Assets/Screenshots/avatar-item-specific-color-areas-1.png`
- 📎 트러블슈팅: T-110, T-111, T-112

---

### 전 파츠 색상 UI 부위별 단일 컨트롤 적용 ✅

- 🤖 상의·하의·한벌옷·신발·모자·안경에서 같은 부위를 조절하던 A1/A2·B1/B2·C1/C2를 사용자에게 숨기고, 실제 부위마다 한 행과 한 색상 버튼만 표시하도록 단순화
- 🤖 A/B/C 기호 대신 상의 `몸통/소매·안감/배색·깃/단추/테두리`, 하의 `바지/치마·허리선/벨트/포켓·스티치/장식`, 신발 `갑피·밑창·끈/장식`, 모자 `본체·챙/밴드/바이저·로고/장식`, 안경 `프레임·브리지/다리/렌즈·포인트 장식` 등 실제 변경 부위를 표시
- 🤖 사용자가 고른 한 색을 해당 부위의 내부 슬롯 쌍에 동일하게 기록하고 무작위 색상도 부위당 한 색만 생성하도록 변경하여 기존 `w=` 36색 네트워크 포맷과의 호환 유지
- 🤖 Unity MCP로 상의·하의·한벌옷·신발·모자·안경 6종 × 3부위의 내부 슬롯 쌍이 모두 같은 색으로 저장됨을 검증하고, Game View에서 3개 단일 행의 가독성을 시각 확인
- 📎 관련 파일: `CharacterLobbyController.cs`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`, `festa-unity/Assets/Screenshots/avatar-color-one-control-per-area.png`
- 📎 트러블슈팅: T-106, T-107, T-108, T-109

---

### 크롭형 상의 착용 시 허리 몸통 누락 수정 ✅

- 🤖 Unity MCP로 하의의 Hips 숨김이 크롭형 상의에서도 유지돼 아랫배가 비는 조합 원인을 확인
- 🤖 최초 첨부 의상을 `Top.02`로 잘못 식별한 뒤 사용자 재확인 화면의 선택 카드와 카탈로그를 대조해 실제 대상이 `Top.07-A`(크롭 재킷, ID 1013383372)임을 교정
- 🤖 의상 정의에 `forcedVisibleBodyParts`를 추가하고 `Top.07-A`가 허리 Spine01(1)만 다시 표시하도록 여성·남성 에셋과 카탈로그 빌더를 갱신; 추정으로 추가했던 `Top.02` 골반 강제 표시는 제거하고 기존 Spine01 예외만 유지
- 🤖 실제 여성 `Top.07-A`와 하의 6종 전부에서 hips 비활성·waist 활성·적용 오류 없음으로 몸통 공백과 바지 관통이 함께 사라짐을 확인
- 🤖 Unity MCP 스크립트 검증 오류·경고 0건, 에셋 리프레시·도메인 리로드 후 Console 오류·경고 0건 및 Game View 시각 검증 완료
- 📎 관련 파일: `AvatarItemDefinition.cs`, `AvatarAssembler.cs`, `CharacterLobbyBuilder.cs`, `F_Top.07-A.asset`, `M_Top.07-A.asset`, `festa-unity/Assets/Screenshots/crop-jacket-pants-clipping-fixed-close.png`
- 📎 트러블슈팅: T-101, T-102, T-103, T-104, T-105

---

## 2026-08-15

### 아바타 로비 WebGL 반응형 레이아웃·얼굴/안경 썸네일 정리 ✅

- 🤖 좌·중앙·우 패널을 1600×900 기준의 단일 16:9 프레임 아래로 묶고, 화면 비율에 따라 Canvas 폭/높이 매칭 기준을 전환해 창 크기 변경 시 카드·패딩이 서로 다른 속도로 움직이지 않도록 수정
- 🤖 16:9 웹 캔버스에서는 화면을 가득 채우고, 다른 비율에서는 UI를 찌그러뜨리지 않는 안전 여백을 두도록 구성
- 🤖 카메라 입력 판정을 하드코딩 화면 비율이 아니라 실제 중앙 미리보기 RectTransform 경계에 연결
- 🤖 전달받은 투명 PNG로 사각/라운드 안경 썸네일을 교체하고, 얼굴형 4종 이미지를 기존 2×2 atlas 순서에 맞춰 교체하면서 기존 `.meta` GUID 연결을 유지
- 🤖 Unity 6000.0.78f1에서 재임포트·컴파일 Error 0건, 세로형 Game View에서도 내부 프레임 1600×900 및 좌 328 / 중앙 848 / 우 424 고정 폭을 확인
- 📎 관련 파일: `CharacterLobbyController.cs`, `AvatarThumbnails/2107176923.png`, `AvatarThumbnails/218425618.png`, `FaceThumbnails/face_shapes.png`
- 📎 트러블슈팅: T-100

### 모자 카탈로그 이름·썸네일 교체 ✅

- 🤖 모자 패밀리 1001·1002·1003의 표시 이름을 각각 `볼캡`, `비니`, `헬멧`으로 통일
- 🤖 볼캡과 비니는 전달받은 투명 PNG를 Sprite로 가져와 해당 패밀리의 모든 헤어 변형이 같은 카탈로그 썸네일을 사용하도록 연결
- 🤖 헬멧으로 전달된 세 번째 PNG가 비니 PNG와 완전히 동일한 파일임을 확인해 잘못 덮어쓰지 않고 기존 실제 헬멧 썸네일을 유지
- 📎 관련 파일: `festa-unity/Assets/_Project/Resources/Avatar/UI/HatThumbnails/`, `festa-unity/Assets/_Project/ScriptableObjects/Avatar/Shared_Hat.*.asset`
- 📎 트러블슈팅: T-97

### 헤어 없음 상태의 모자 장착 복구 ✅

- 🤖 모자 카탈로그가 현재 헤어 그룹과 정확히 일치하는 프리팹만 찾던 탓에 `헤어 없음`에서는 캡·비니 변형을 찾지 못해 모자까지 사라지는 문제를 수정
- 🤖 정확한 헤어 그룹 → 범용 `None` 변형 → 같은 모자 패밀리의 첫 변형 순으로 선택하도록 폴백을 추가
- 🤖 폴백 프리팹에 포함된 맞춤 헤어 렌더러만 비활성화하고 모자 본체는 유지하도록 분리 처리
- 🤖 플레이 모드에서 `hairId=0`과 캡·비니·헬멧을 조합해 캡/비니 본체 ON·내장 헤어 OFF, 헬멧/바이저 ON, 적용 오류 0건을 확인
- 📎 관련 파일: `AvatarCatalog.cs`, `AvatarAssembler.cs`
- 📎 트러블슈팅: T-96

### 기본 Body 속옷 검정 고정·피부색 격리 ✅

- 🤖 원본 여성·남성 Body `_BaseMap`이 피부/속옷 RGB 영역 마스크인데 Runtime `SkinTint` 변환에서 버려져 전신이 피부색으로 출력되던 원인을 확인
- 🤖 `AvatarSkinTint`에서 마스크 녹색 채널만 속옷 영역으로 사용하고 고정 검정색을 적용했으며, `AvatarAssembler`가 Body BaseMap과 UV Scale/Offset을 보존하도록 수정
- 🤖 속옷 색은 `AvatarConfig`와 네트워크 외형 문자열에 추가하지 않아 피부색·멀티플레이 외형 데이터와 독립적으로 유지
- 🤖 Unity 컴파일 Error 0건 확인 후 여성의 브라·하의와 남성 하의를 의상 없음 상태에서 검증하고, 청록색 피부 격리 테스트에서도 속옷이 검정으로 유지됨을 확인
- 📎 관련 파일: `AvatarSkinTint.shader`, `AvatarAssembler.cs`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`
- 📎 트러블슈팅: T-84 재발 기록, T-95

### KHS 개인 문서 이동·개인 기술 컨퍼런스 작성 ✅

- 🤖 개인 기록인 작업일지와 트러블슈팅을 `docs/KHS/`로 이동하고 저장소 운영 규칙·문서 링크를 새 경로로 갱신
- 🤖 `docs/KHS/conference/`를 팀 전체 자료실이 아닌 KHS 개인 기술 공유 공간으로 명시
- 🤖 KHS가 직접 작업한 NGO 멀티플레이, Unity Booth Runtime, Linux Dedicated Server·Docker, Unity 연동 계약, 아바타 커스터마이징·동기화, Unity/WebGL 문제 해결·검증 자료 6편과 인덱스를 작성
- 🤖 React·Spring·FastAPI의 타 담당자 내부 구현은 제외하고 KHS Unity 작업에 필요한 외부 입출력 계약만 기록
- 📎 관련 문서: `docs/KHS/README.md`, `docs/KHS/conference/00_컨퍼런스_인덱스.md`~`06_Unity_문제_해결과_검증.md`

### Booth 구조 공유안 기반 스펙·기존 문서 정합화 ✅

- 🤖 Booth Studio→Spring→Unity 구조와 `고정 외부 슬롯 + 동일 Scene 내부 앵커` 모델을 spec 005/006의 기준 계약으로 재확인
- 🤖 외부 Facade와 내부 Layout 분리, Dedicated Server 권한 입장·퇴장·임대 만료 이동, Client Published Layout 로컬 생성 책임을 문서 간 동일하게 정리
- 🤖 Booth당 홈페이지 URL 1개와 `LAPTOP` 1개를 확정하고, URL은 Spring 설정에 저장하며 Layout은 `configId`만 참조하도록 spec 005/016·Backend API·DB·Frontend 계약을 갱신
- 🤖 퇴장 설문은 선택형·퇴장 비차단 UX로, 모집 게시판은 `WANTED` 상태로 정의
- 🤖 AI 문서 20개·200MB와 콘텐츠 삭제 후 14일 유예는 확정값이 아닌 제안으로 분리하고, Lease 만료는 삭제 이벤트가 아님을 명시
- 🤖 AI 원본 파일=S3, 메타데이터=PostgreSQL, 임베딩=pgvector, 설문 영구 데이터=PostgreSQL, Redis=세션·락·재생성 가능한 캐시로 저장 책임을 정리
- 🤖 World Channel 30~40명 목표와 다중 채널 배정 원칙을 spec 002에 반영하고, 최초 warm server 수는 AWS 실측 후 결정하도록 남김
- 📎 관련 문서: `docs/specs/002-world-session/spec.md`, `docs/specs/005-booth-studio-layout/spec.md`, `docs/specs/006-booth-runtime/spec.md`, `docs/specs/016-booth-laptop-homepage/spec.md`, `docs/02`, `docs/03`, `docs/05`~`11`, `docs/26`, `docs/sdd/parts/BE.md`, `docs/sdd/parts/FE.md`

---

## 2026-08-14

### 전 브랜치 문서 전용 원격 동기화 ✅

- 🤖 `docs/` 변경만 별도 커밋해 `game`, `main`, `develop`, `ai`, `back`, `front` 6개 원격 브랜치에 반영
- 🤖 `game`의 `docs/specs`와 다른 브랜치의 루트 `specs` 구조 차이를 존중해 Booth spec 005/006을 기존 위치에 병합하고, 브랜치별 작업일지·T-38/T-39 기록을 보존
- 🤖 Unity 코드·에셋·씬·ProjectSettings 및 `festa-unity/` 아래 미완성 작업은 스테이징·커밋·푸시 대상에서 제외하고 `game` 작업 트리에 그대로 유지
- 📎 1차 문서 커밋: `game b1da903`, `main 0f3d675`, `develop e87a3a5`, `ai c4a0dce`, `back e25cd12`, `front f5286d3`
- 📎 트러블슈팅: T-93, T-94

### Booth 계약 정합·외부/내부 슬롯 MVP 구조 확정 ✅

- 🤖 Backend 문서를 기준으로 Unity 공개 Layout 경로를 `/api/v1/booths/{boothId}/layouts/published`, 오브젝트 식별자를 `objectId`로 통일하고 `assetCode`를 DTO에 추가
- 🤖 `SURVEY_KIOSK`, `CONSULTATION_DESK`, `LAPTOP`을 포함한 canonical Object Type 10종을 Unity에 반영하고, 동결 POC 데이터의 `SURVEY`, `CONSULT_DESK`는 읽기 호환만 유지
- 🤖 1차 MVP 공간 모델을 `고정 외부 슬롯 + 동일 World Scene 내부 슬롯 풀`로 확정: 외부는 제한형 Facade, 내부는 자유 Layout, 일반 방문은 공유형, 독점 예약은 상담·행사 세션에만 적용
- 🤖 임대 만료 시 신규 입장 차단 → 내부 방문자 외부 이동 → Client Runtime Clear → Slot/Booth 연결 해제 → 빈 외부 상태 복원 순서를 확정하고, 기존 소유자의 Layout·AI·문서 데이터는 Draft로 보존하도록 문서화
- 🤖 기획·정책·User Flow·UI/UX·Wireframe·Architecture·Backend API·DB·Frontend·Unity Client/Server·QA·Demo·팀 결정·spec 005/006·다음 할 일·POC 상태 문서를 같은 계약으로 갱신
- 🤖 Unity 스크립트 6개 진단 오류·경고 0건, canonical JSON 파싱 PASS, Console Error 0건을 확인하고 플레이 모드를 종료
- 📎 관련 코드: `BoothLayoutDto.cs`, `BoothObjectType.cs`, `BoothObjectFactory.cs`, `BoothRuntimeObject.cs`, `MockBoothApiClient.cs`, `HttpBoothApiClient.cs`
- 📎 트러블슈팅: T-84, T-91, T-92, T-93

### Booth Studio → Spring → Unity 구조 점검 ✅

- 🤖 전체 아키텍처·Frontend·Backend API·DB·Unity Client 설계와 현재 `game` 브랜치의 Booth Runtime 구현을 대조해 부스 생성·저장·공개·렌더링 흐름을 점검
- 🤖 React는 새 3D 모델을 제작하는 도구가 아니라 Unity가 제공하는 오브젝트 종류를 선택해 위치·회전·콘텐츠 연결값을 편집하고 Layout JSON을 만드는 제한형 UGC 편집기임을 확인
- 🤖 Spring은 임대·권한·Draft/Published 버전·Layout JSONB의 Source of Truth이고, Unity Client는 Published Layout만 조회해 Registry의 프리팹을 각 클라이언트에서 로컬 생성하는 구조임을 확인
- 🤖 현재 구현은 Unity의 Mock Layout 파싱·Registry/Factory·Local Spawn·미지원 타입 격리·HTTP 조회 경계까지이며, `main.unity`와 `ApiConfig.asset`은 아직 `useMockApi=1`; React/Spring 실행 코드는 현재 브랜치와 front/back 브랜치에 없음
- 🤖 통합 전 정리가 필요했던 Published API의 `layouts/published` 대 `layout/published`, 오브젝트 식별자의 `objectId` 대 `id` 차이를 확인했으며 후속 작업에서 Backend 기준으로 해소
- 📎 관련 문서: `docs/07_전체_시스템_아키텍처.md`, `docs/08_Backend_API_명세서.md`, `docs/09_DB_ERD_DB_설계서.md`, `docs/10_Frontend_설계서.md`, `docs/11_Unity_Client_설계서.md`, `docs/specs/005-booth-studio-layout/spec.md`, `docs/specs/006-booth-runtime/spec.md`
- 📎 관련 코드: `BoothRuntime.cs`, `BoothLayoutDto.cs`, `BoothObjectRegistry.cs`, `BoothObjectFactory.cs`, `HttpBoothApiClient.cs`, `MockBoothApiClient.cs`, `ApiServices.cs`
- 📎 트러블슈팅: T-91, T-92

### 의상 RGB 영역 혼색 보정 ✅

- 🤖 의상 마스크의 RGB 값을 공통 임계값으로 단순 분류하면서 노랑·흰색 픽셀이 여러 영역의 색을 평균 내던 원인을 확인
- 🤖 Rukha93 원본 `SubGraph_ColorCustomization`의 계산식을 분석해, 각 머티리얼의 `_MaskRemap`·`_Mask_Factor`를 런타임 의상 머티리얼로 그대로 전달하도록 수정
- 🤖 영역 A를 적용한 뒤 B, C를 순차 선택하는 원본 우선순위 계산을 `Festa/Avatar/GarmentTint`에 복원하여 서로 다른 원단 영역이 넓게 혼색되지 않도록 보정
- 🤖 UI 제목을 `원단 영역 A/B/C · 영역별 명암 2색`으로 바꿔 A1/A2가 별도 옷 조각이 아니라 같은 영역의 명암 그라데이션임을 명확히 표시
- 🤖 상의와 한벌옷에 A=빨강, B=초록, C=파랑의 강한 분리색을 적용해 몸통·테두리 영역이 명확히 나뉨을 Game View에서 확인하고, 플레이 모드를 종료해 테스트 색을 복원
- 🤖 `AvatarAssembler.cs`·`CharacterLobbyController.cs` 오류 0건, `GarmentTint` 셰이더 메시지 0건, 관련 Console Error 0건 확인
- 📎 관련 파일: `AvatarGarmentTint.shader`, `AvatarAssembler.cs`, `CharacterLobbyController.cs`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`
- 📎 검증 화면: `festa-unity/Assets/Screenshots/character-lobby-garment-mask-top-zones.png`, `festa-unity/Assets/Screenshots/character-lobby-garment-mask-outfit-zones.png`
- 📎 트러블슈팅: T-84, T-89, T-90

### 왼쪽 의상 탭·RGB 마스크 6색 커스터마이징 ✅
- 🤖 왼쪽에 상의·하의·한벌옷·신발을 모두 펼치던 목록을 유지 위치는 그대로 둔 채, 오른쪽 파츠 패널과 같은 이미지 탭·선택 카드 구조로 변경
- 🤖 Rukha93 의상 에셋이 통짜가 아니라 RGB 마스크 A/B/C 세 영역과 각 영역의 어두운색·밝은색, 총 6개 색상 채널을 제공함을 실제 머티리얼에서 확인
- 🤖 원본 RGB 마스크와 노멀맵을 유지하는 `Festa/Avatar/GarmentTint` 셰이더를 추가하고 상의·하의·한벌옷·신발의 6색을 각각 독립 조절하도록 구현
- 🤖 의상 24색(4종×6채널)을 `avatarCode`의 `w=` 세그먼트로 저장·복원해 멀티플레이 외형 동기화에도 포함하고, 기존 7~9색 코드는 전체색 방식으로 하위 호환
- 🤖 모자 6색과 안경 6색을 추가해 `w=`를 총 36색으로 확장하고, 기존 24색·30색 외형 문자열은 그대로 읽도록 하위 호환
- 🤖 안경 프레임의 A/B/C 여섯 영역을 각각 조절하고 별도 투명 렌즈는 B2 색으로 변경되도록 구현
- 🤖 새로 전달된 실제 헬멧 이미지를 `hat_helmet` Sprite로 교체하고 `헬멧` 항목에 연결
- 🤖 안경 2종 썸네일을 새 이미지로 교체하고 표시 이름을 `사각 안경`, `라운드 안경`으로 변경. 원본의 흰 배경을 투명 처리하고 실제 안경 영역을 기준으로 512×512 카드 중앙에 정렬
- 🤖 색상 드래그 중에는 메시 전체를 재조립하지 않고 머티리얼 색상만 갱신해 중복 메시와 입력 끊김을 방지
- 🤖 24색 인코딩 왕복 불일치 0건, 관련 스크립트 오류 0건, GarmentTint 셰이더 메시 0건, 관련 Console Error 0건 확인 후 플레이 모드 종료
- 🔗 관련 파일: `AvatarConfig.cs`, `AvatarAppearance.cs`, `AvatarAssembler.cs`, `CharacterLobbyController.cs`, `AvatarGarmentTint.shader`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`, `design-qa.md`
- 🔗 검증 화면: `festa-unity/Assets/Screenshots/character-lobby-left-wardrobe-top-tab-final.png`, `festa-unity/Assets/Screenshots/character-lobby-garment-color-picker.png`
- 🔗 트러블슈팅: T-05, T-87, T-88

### 얼굴·목·전신 피부색 동기화 ✅
- 🤖 피부색 값은 얼굴과 몸에 이미 같이 전달되고 있었지만 얼굴은 전용 셰이더, 몸은 URP Lit을 사용해 동일한 RGB도 서로 다른 밝기로 보이는 원인을 확인
- 🤖 전신 피부용 `Festa/Avatar/SkinTint` 셰이더를 추가하고 얼굴 셰이더와 같은 기본 조명 계산을 사용하도록 통일
- 🤖 `AvatarAssembler`가 몸통·목·팔·손·다리·발의 모든 신체 머티리얼에 같은 `_SkinColor`를 전달하도록 변경
- 🤖 피부 노출이 많은 상의·하의 조합과 강한 시험 피부색으로 얼굴·목·팔·손이 한 색으로 이어지는지 Game View에서 검증
- 🤖 `AvatarAssembler.cs` 오류 0건, `SkinTint`·`FaceTint` 셰이더 컴파일 메시 0건, 관련 Console Error 0건 확인
- 🔗 관련 파일: `AvatarSkinTint.shader`, `AvatarAssembler.cs`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`, `design-qa.md`
- 🔗 검증 화면: `festa-unity/Assets/Screenshots/character-lobby-skin-face-body-synced-separate-clothes.png`
- 🔗 트러블슈팅: T-73, T-86

### 자연스러운 입술 색조 복구·하순 번짐 제거 ✅
- 🤖 입술색 기능을 다시 제공하되 선택색을 단색으로 덮지 않고 피부색과 혼합해 원본 얼굴 명암과 입술 윤곽이 유지되도록 변경
- 🤖 위·아래 입술의 원본 텍스처 디테일을 경계로 사용하고 색조 강도를 낮춰 기본색과 정밀 색상 모두 자연스럽게 반영
- 🤖 아랫입술 마스크가 별도의 턱 음영 UV까지 포함하던 범위를 잘라내 하순 아래로 색이 번지는 현상을 제거
- 🤖 밝기 임계값 기반 경계가 입술 주변 피부까지 흐리게 포함하던 방식을 폐기하고, 측정한 위·아래 입술 폭 변화로 테이퍼드 실루엣을 구성해 입술 모양 안쪽에만 색상이 남도록 최종 보정
- 🤖 얼굴 색상 목록에 `입술` 행을 다시 추가하고 6개 행이 패널 안에 맞도록 높이를 조정
- 🤖 원본 ShaderGraph 직접 복원은 현재 런타임 조립 색상과 호환되지 않음을 확인하고, 재조립 없는 색상 갱신 경로로 기본 상태와 근접 화면을 검증
- 🔗 관련 파일: `AvatarFaceTint.shader`, `AvatarAssembler.cs`, `CharacterLobbyController.cs`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`, `design-qa.md`
- 🔗 검증 화면: `festa-unity/Assets/Screenshots/character-lobby-lips-natural-tint-default.png`, `festa-unity/Assets/Screenshots/character-lobby-lips-natural-tint-clean-bottom_1.png`, `festa-unity/Assets/Screenshots/character-lobby-lips-exact-mask-close_1.png`
- 🔗 트러블슈팅: T-79, T-80, T-81, T-82, T-83, T-84, T-85

### 입술색 사용자 조절 제거·원본 얼굴 표현 복구 ✅
- 🤖 입술 전체를 절차 마스크로 채우는 방식이 원본보다 진한 립스틱처럼 보이는 문제를 확인하고 사용자 입술색 기능을 제거
- 🤖 얼굴 셰이더와 머티리얼 색상 적용 경로에서 입술색 덮어쓰기를 제거해 원본 얼굴 디테일과 내부 입 재질만 보이도록 복구
- 🤖 얼굴 색상 목록에서 입술 항목을 제거하고 남은 `피부 / 흰자위 / 홍채 / 동공 / 눈썹` 다섯 행의 높이와 간격을 재정렬
- 🤖 기존 9색 외형 문자열의 입술 슬롯은 하위 호환용 예약 필드로 유지하되 렌더링과 UI에는 사용하지 않도록 계약·스펙을 갱신
- 🤖 근접 얼굴과 얼굴 색상 패널을 다시 확인하고 스크립트 오류 0건, FaceTint 셰이더 메시 0건, 관련 Console Error 0건 확인
- 🔗 관련 파일: `AvatarFaceTint.shader`, `AvatarAssembler.cs`, `CharacterLobbyController.cs`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`, `design-qa.md`
- 🔗 검증 화면: `festa-unity/Assets/Screenshots/character-lobby-lips-original-restored-close_1.png`, `festa-unity/Assets/Screenshots/character-lobby-face-colors-without-lips.png`
- 🔗 트러블슈팅: T-77, T-78

### 입술 전체 영역 색상 마스크 보정 ✅
- 🤖 입술색이 중앙의 얇은 선에만 적용되던 화면을 기준으로 여성·남성 얼굴 RGB 마스크의 실제 입술 UV 범위를 측정
- 🤖 위·아래 입술 마스크를 각각 전체 UV 섬으로 확장하고 원본 얼굴 디테일을 경계 마스크로 사용해 피부로 번지지 않으면서 입술 전체가 채색되도록 수정
- 🤖 기본 입술색과 강한 시험색 모두 위·아래 입술 전체 윤곽에 적용되는지 Game View에서 확인하고 `AvatarFaceTint` 셰이더 메시 0건 확인
- 🔗 관련 파일: `festa-unity/Assets/_Project/Shaders/AvatarFaceTint.shader`, `design-qa.md`
- 🔗 검증 화면: `festa-unity/Assets/Screenshots/character-lobby-lips-full-default.png`, `festa-unity/Assets/Screenshots/character-lobby-lips-full-red.png`
- 🔗 트러블슈팅: T-75, T-76

### 눈 3영역·입술 색상·포인터 중심 줌 보정 ✅
- 🤖 눈을 `흰자위 / 홍채 / 동공` 세 영역의 독립 색상으로 분리하고 외형 네트워크 문자열을 9색으로 확장하면서 기존 7·8색 문자열의 하위 호환을 유지
- 🤖 공유 눈 UV의 중심을 바깥쪽으로 보정해 사시처럼 보이던 시선을 평행하게 정렬하고, 고정 하이라이트를 숨긴 뒤 동공을 따라가는 캐치라이트를 셰이더에서 생성
- 🤖 실제 보이는 입술이 내부 입 재질이 아니라 얼굴 표면 마스크에 포함된 구조를 반영해 피부·입술·눈썹을 독립 색상으로 처리
- 🤖 마우스 휠 줌이 화면 중앙 고정이 아니라 포인터가 가리킨 초점 평면을 향하도록 카메라 초점을 보정하고, UI 위에서는 기존 입력 잠금을 유지
- 🤖 9색 외형 문자열 왕복, 좌우 포인터 줌 초점 이동, 얼굴 근접 렌더링을 확인하고 스크립트·셰이더 컴파일 오류 0건 확인
- 🔗 관련 파일: `AvatarConfig.cs`, `AvatarAppearance.cs`, `AvatarAssembler.cs`, `CharacterLobbyController.cs`, `AvatarIrisTint.shader`, `AvatarFaceTint.shader`, `AvatarMouthTint.shader`, `docs/specs/013-avatar-customization/spec.md`, `festa-unity/Docs/avatar-customization-contract.md`, `design-qa.md`
- 🔗 검증 화면: `face-eyes-lips-render-final.png`
- 🔗 트러블슈팅: T-65, T-66, T-67, T-68, T-69, T-70, T-71, T-72, T-73, T-74

### 얼굴형 통합 색상 목록 및 우측 레이아웃 복구 ✅
- 🤖 얼굴 메시 안에 눈·입술 형태가 포함된 현재 에셋 구조에 맞춰 `얼굴형 / 눈 / 입술` 하위 탭을 제거
- 🤖 얼굴형 카드 바로 아래에 `피부 / 흰자위 / 홍채 / 눈썹 / 입술`을 이름·현재 색상 스와치가 함께 보이는 세로 항목으로 배치
- 🤖 색상 항목 선택 시 팝업 제목도 `피부 색상 변경`, `홍채 색상 변경`처럼 대상 부위를 명시하고 선택 항목을 금색 테두리로 표시
- 🤖 고정 높이 아이템 스크롤 때문에 발생한 큰 빈 공간을 줄이고 얼굴형 목록·색상 목록·하단 버튼의 수직 간격을 다시 맞춰 겹침과 밀림을 제거
- 🤖 다섯 색상 항목 모두 대응하는 색상 팝업이 열리는지 확인하고 스크립트 오류 및 CharacterLobby 관련 Console Error 0건 확인 후 플레이 모드 종료
- 🔗 관련 파일: `CharacterLobbyController.cs`, `docs/specs/013-avatar-customization/spec.md`, `design-qa.md`
- 🔗 검증 화면: `face-colors-final.png`, `festa-unity/Assets/Screenshots/design-qa-face-color-layout-fixed.png`
- 🔗 트러블슈팅: T-62, T-63, T-64

### CharacterLobby 의상 레일·얼굴 세부 탭 재구성 ✅
- 🤖 좌측 중복 얼굴형·헤어·추천 스타일 영역을 제거하고 상의·하의·신발·한벌 의상 전용 가로 스크롤 레일로 교체
- 🤖 우측 최상위 탭은 얼굴·헤어·모자·안경만 남겨 의상과 얼굴 세부 설정이 같은 패널에 뒤섞이지 않도록 정보 구조를 분리
- 🤖 현 Catalog에는 눈·입술 독립 메시가 없고 얼굴형 메시 안에 포함된 것을 확인해 얼굴 탭을 `얼굴형 / 눈 / 입술` 하위 탭으로 구성
- 🤖 얼굴형에는 얼굴 선택과 피부색, 눈에는 흰자위·홍채·눈썹색, 입술에는 입술색만 노출하고 눈·입술 모양이 얼굴형에 연동된다는 안내를 표시
- 🤖 좌측 상의를 고르면 한벌옷을 해제하고 비어 있던 하의를 기본값으로 보완하며, 한벌 의상을 고르면 상·하의를 해제하는 실제 조립 규칙을 플레이 모드에서 검증
- 🤖 레퍼런스와 `얼굴 > 눈` 구현 화면을 나란히 비교해 P0/P1/P2 시각 결함이 없음을 확인하고 플레이 모드 종료
- 🔗 관련 파일: `festa-unity/Assets/_Project/Scripts/World/Avatar/Lobby/CharacterLobbyController.cs`, `design-qa.md`
- 🔗 검증 화면: `festa-unity/Assets/Screenshots/character-lobby-wardrobe-face-shape-ui.png`, `character-lobby-wardrobe-face-eyes-ui.png`, `design-qa-wardrobe-face-tabs.png`
- 🔗 트러블슈팅: T-46, T-54, T-60, T-61

### 얼굴 5영역 독립 색상 및 UI 입력 잠금 ✅
- 🤖 얼굴 색상 슬롯을 흰자위·눈썹·홍채·입술·피부 다섯 영역으로 분리하고 얼굴 탭에 각각의 실제 색상 스와치를 표시
- 🤖 눈 전용 셰이더에 흰자위 색상 마스크와 `_ScleraColor`를 추가해 기존 `_IrisColor`와 독립적으로 적용
- 🤖 `AvatarConfig`와 `fa` 네트워크 문자열의 팔레트/정밀 RGB를 8색으로 확장하고 기존 7색 문자열은 흰자위 흰색으로 복원하는 하위 호환 유지
- 🤖 색상 팝업·확정 버튼 등 중앙 미리보기 위 UI를 직접 Raycast해, UI를 조작하는 동안 좌클릭 캐릭터 회전·우클릭 카메라 회전·휠 줌을 모두 차단
- 🤖 다섯 얼굴 슬롯 표시, 홍채/흰자위의 서로 다른 머티리얼 값, 8색 RGB 인코딩 왕복 및 7색 구버전 복원을 검증
- 🔗 관련 파일: `AvatarConfig.cs`, `AvatarAppearance.cs`, `AvatarAssembler.cs`, `CharacterLobbyController.cs`, `AvatarIrisTint.shader`
- 🔗 트러블슈팅: T-59

### 의상 사이 피부 노출 수정 ✅
- 🤖 기본 한벌옷의 가슴 부위로 신체 피부가 뚫고 나오는 현상을 Game View 확대 화면에서 재현
- 🤖 벤더 `BodyPartType`과 프로젝트 신체 메시 이름의 매핑을 대조해, 번호 없는 torso가 `Spine02(2)`이고 `torso.002`가 목 브리지 `Spine03(5)`임을 확인
- 🤖 잘못 뒤바뀐 torso 매핑을 수정해 의상 데이터가 지정한 가슴 몸체만 숨기고 목 브리지는 유지하도록 변경
- 🤖 여성·남성 기본 한벌옷 모두 `body_torso=False`, `body_torso.002=True`를 확인하고 여성 확대 화면에서 피부 노출 제거를 시각 검증
- 🤖 스크립트 재컴파일 및 최종 Console Error 0건 확인 후 플레이 모드 종료
- 🔗 관련 파일: `festa-unity/Assets/_Project/Scripts/World/Avatar/Assembly/AvatarAssembler.cs`
- 🔗 검증 화면: `festa-unity/Assets/Screenshots/character-lobby-clothing-fixed-close-snap.png`
- 🔗 트러블슈팅: T-58

### CharacterLobby 마우스 카메라 조작 개선 ✅
- 🤖 중앙 캐릭터 미리보기 영역에서 보조 키 없이 마우스 휠로 줌인·줌아웃하도록 변경
- 🤖 좌클릭 드래그는 기존처럼 캐릭터만 좌우 회전하고, 우클릭 드래그는 캐릭터를 건드리지 않고 카메라만 수평·수직 공전하도록 입력 상태를 분리. 카메라 수직 각도는 `-30°~55°`로 제한
- 🤖 파츠별 카메라 프리셋을 선택하면 회전 각도가 정면으로 초기화되도록 하고, 화면 조작 안내 문구도 새 입력 방식에 맞게 수정
- 🤖 Unity Editor가 종료된 상태여서 플레이 입력 검증은 보류했으나 `Assembly-CSharp` 빌드로 신규 코드 컴파일 오류 0건을 확인
- 🔗 관련 파일: `festa-unity/Assets/_Project/Scripts/World/Avatar/Lobby/CharacterLobbyController.cs`
- 🔗 트러블슈팅: T-55, T-56, T-57

### CharacterLobby 레퍼런스 기반 디자인 마감 및 시각 QA ✅
- 🤖 제공된 MMORPG 캐릭터 생성 화면을 기준으로 좌측 프리셋 레일·중앙 상반신 캐릭터·우측 이미지 탭/단일 편집 패널의 비율과 정보 밀도를 다시 조정
- 🤖 기본 카메라를 얼굴과 상체가 크게 보이는 초상 구도로 고정하고, 중앙 SSAFY 사진 위에 반투명 음영을 더해 캐릭터와 UI 가독성을 개선
- 🤖 파츠 탭 선택 시 해당 부위 카메라·아이템 목록·색상 슬롯만 전환되도록 정리하고, 선택된 파츠와 현재 색상을 금색 테두리/실제 색상 스와치로 표시
- 🤖 색상환에 현재 선택 지점, 밝기 바에 현재 값 표시를 추가하고 색상환·밝기 드래그가 실제 외형 색에 반영되는지 확인
- 🤖 성별 전환 시 좌측 추천 프리셋 썸네일과 선택 상태가 새 성별 기준으로 즉시 재구성되도록 보강
- 🤖 레퍼런스와 구현 화면을 동일한 `512×304` 상태(여성·얼굴 탭·피부 색상 팝업 열림)로 나란히 비교해 주요 레이아웃과 상호작용의 P0/P1/P2 차이가 없음을 확인
- 🤖 Unity 플레이 모드를 종료하고 스크립트 재컴파일 및 Console Error 0건을 확인
- 🔗 디자인 QA: `design-qa.md`, `festa-unity/Assets/Screenshots/design-qa-comparison-final.png`
- 🔗 트러블슈팅: T-54

## 2026-08-13

### CharacterLobby 품질 개선·정밀 색상·멀티플레이 외형 동기화 🚧
- 🤖 목 분리 원인이던 기본 HeadSlot/교체 머리 중복을 제거하고, 대체 메시 생성 성공 뒤에만 신체 부위를 숨기도록 보강
- 🤖 한벌옷과 상·하의를 상호 배타 처리하고 기본 외형을 완성형 한벌옷으로 변경해 겹침·피부 경계 문제를 완화
- 🤖 눈 원본 텍스처와 노멀을 보존하는 URP Lit 호환 머티리얼 경로로 홍채 표시와 마젠타 셰이더 문제를 해결
- 🤖 Hue 바 + 채도/명도 2차원 영역의 마우스 드래그 정밀 색상 선택기를 구현하고 7개 슬롯의 정확한 RGB를 네트워크 문자열에 직렬화
- 🤖 `fa` 모듈 외형을 기존 서버 권한 `NetworkVariable` 경로에 연결해 로비 설정과 월드 아바타가 같은 Catalog 조립 결과를 사용하도록 구현
- 🤖 한국어 UI의 버튼 범위·2열 스크롤·호버/눌림 피드백·금색 테두리·상체 중심 기본 카메라를 개선하고, 사용자 제공 SSAFY 공간 사진을 로비 배경으로 적용
- 🤖 최종 레이아웃을 레퍼런스와 같은 좌측 프리셋 레일·중앙 대형 캐릭터·우측 단일 세부 패널·중앙 색상 팝업 구조로 재작성
- 🤖 얼굴·헤어·모자·안경·상의·하의·한벌옷·신발 탭과 105개 선택 항목을 실제 조립 캐릭터 이미지 썸네일로 교체하고, 탭 선택 시 관련 목록과 색상만 표시
- 🤖 156개 파츠/성별 조합, 정밀 RGB 왕복, 월드 Provider 생성 검증을 통과했으며 Console Error 0건 확인
- 🤖 사용자 요청으로 플레이 모드를 종료하고 작업을 중단했으며, 최종 카메라·반응형 레이아웃·성별 전환 프리셋·실제 2클라이언트 동기화 검증은 다음 세션으로 이월
- 🔗 트러블슈팅: T-40, T-41, T-42, T-43, T-44, T-45, T-46, T-47, T-48, T-49, T-50, T-51, T-52, T-53

### CharacterLobby 로컬 아바타 커스터마이징 화면 구현 ✅
- 🤖 벤더 아바타 에셋을 조사하고 성별별 머리 4·상의 13·하의 6·한벌옷 4, 공용 헤어 31·모자 변형 11·안경 2·신발 7 구성을 Catalog로 변환
- 🤖 GUID 기반 안정 ID, 성별·부위·색상 설정, 상하의/한벌옷 배타 규칙, 헤어-모자 호환 규칙과 신체 클리핑을 구현
- 🤖 남녀 신체 프리팹과 105개 ItemDefinition, AvatarCatalog, `CharacterLobby.unity`을 생성하고 조립기·회전·줌·카메라 프리셋·초기화·무작위 기능을 연결
- 🤖 동양풍 소재에 한정하지 않고 상용 MMORPG 캐릭터 생성 화면 느낌의 어두운 남색·금색 uGUI로 디자인했으며 모든 사용자 노출 문구를 한국어로 구성
- 🤖 한국어 폰트를 연결하고 Unity 플레이 검증에서 성별, 8개 부위, 7개 색상, 3개 카메라, 무작위 10회를 확인. 최종 Console Error 0건
- 🤖 기준선 네트워크/월드/저장/백엔드/WebGL 코드는 변경하지 않음
- 🤖 완성된 CharacterLobby 작업물을 파트 운영 규칙에 맞춰 `game` 브랜치로 이전하고 커밋 준비
- 🔗 트러블슈팅: T-28, T-29, T-30, T-31, T-32, T-33, T-34, T-35, T-36, T-37

### AI 에이전트 매뉴얼 재작성 — AGENTS.md를 규칙 단일 출처로 ✅
- 👤 결정: 리드는 **Codex로 개발**한다. 단 **팀원 중 Claude Code를 쓰는 사람이 있을 수 있으므로 두 도구를 동등 지원**한다
- 👤 확인 필요 사항: **팀원들은 spec-kit이 설치돼 있지 않을 수 있다**
- 🤖 확인 결과: 설치할 필요가 없다. `.specify/`(21) + `.agents/skills/speckit-*`(10) + `.claude/skills/speckit-*`(10)이
  **모두 커밋되어 있어** `git pull`만으로 `$speckit-*` 명령이 동작한다. 필요한 것은 **저장소 루트에서 실행 + bash(Windows는 Git Bash/WSL)** 뿐
- 🤖 `AGENTS.md` **전면 재작성** — 이 파일만 읽어도 작업을 시작할 수 있는 단독 매뉴얼로 승격. **Codex/Claude Code 공통 규칙**
  - 상단에 도구 대응표(자동으로 읽는 파일 / 명령 접두사 `$` vs `/` / 명령 정의 위치) 명시
  - §1 세팅(설치 금지 경고 포함) / §2 SDD 흐름 + `feature.json` / §3 헌법 하드 게이트 20개 조항 표
  - §4 금지 사항(git force·reset·clean, Secret, 수정 금지 파일 목록) / §5 기록 의무 / §6 함정 표 T-05·07·08·15·18·19·21·22·23·24·25·26·27
  - §7 검증된 docker 명령 / §8 파트 간 계약 / §9 프로젝트 지도 / §10 막힐 때
- 🤖 `CLAUDE.md` — **Claude Code 진입점**으로 재작성. 맨 위에 "AGENTS.md를 전부 읽어라" 지시를 두되,
  CLAUDE.md만 로드된 세션에서도 사고가 나지 않도록 **금지 사항·기록 의무·자주 터지는 것**을 요약으로 함께 담았다
  (규칙 중복은 최소화 — 상세는 전부 AGENTS.md 참조)
- 🤖 `docs/00_SDD_가이드.md` §1-1 신설 — "spec-kit은 설치하지 않는다" + Windows bash 요구사항 + `specify init` 금지 경고.
  §5-2를 **Codex/Claude Code 2열 대응표**로 변경하고 **스킬 미인식 시 SKILL.md 직접 실행** 우회법 추가. §9 실수 표 2행 추가
- 🤖 `specs/README.md` §3 동일 반영 + `feature.json`이 gitignore 대상임을 명시
- 📌 팀원은 **어느 도구를 쓰든 상관없다** — 결과물이 같은 `specs/`에 쌓이므로 도구를 통일할 필요가 없다
- ⚠️ **재설치 금지 이유**: `specify init`은 `.specify/memory/constitution.md`(헌법 v1.2)를 빈 템플릿으로 덮어쓴다

---

## 2026-08-12

### 팀 결정 확정 + SDD 문서 세트 작성 ✅
- 👤 팀 결정: docs/26 ①번 1~14 전체 확정 — GMS 키 지급 예정, repo 파트 자율, 도메인 자체 구매, AWS 서버 제공, **MVP 컷 유지 + 캐릭터 커스텀 P0 승격**
- 👤 신규 기능 결정: **016 부스 노트북→홈페이지(P0)**, 017 거리 기반 음성채팅(P1), 018 층 구조 1층+11층·엘리베이터(P1, 1차는 11층만), 마피아 게임(P2 후보)
- 👤 프로세스 결정: 개인별 작업일지(날짜별)·트러블슈팅(날짜 포함)·**매일 지라 업데이트**, 브랜치(ai/back/front/game)별 CI/CD + develop 실사용 기준 CI/CD
- 🤖 문서 산출 (Claude/Codex 공용):
  - `docs/26` 결정값·결정 로그 갱신, `docs/27` v1.1 (013a/016/017/018 반영)
  - `docs/sdd/constitution.md` — 헌법 v1.0 (28 초안 빈칸 채움 + 프로세스/CI-CD 조항 22개)
  - `docs/sdd/README.md` — spec-kit 설치(claude+codex 동시)·진행 순서 가이드
  - `docs/sdd/parts/BE·FE·AI·INFRA.md` — 파트별 spec 권장 브리프 (specify 입력 초안 + 예상 clarify). **권장안 — 각 파트 검토 후 사용**
- Unity 파트는 브리프 없이 기존 방식 유지 (002/006/013a 소급 spec은 리드 직접 작성)
- 🤖 원본 문서 소급 반영: `docs/01` (P0/P1/P2·Cut Line·층 구조), `docs/02` (WORLD-04 P0 승격, RUNTIME-06/PROJECT-05/WORLD-09/WORLD-10/GAME-05 신설, SURVEY-08 범위 확정, LAPTOP 오브젝트, 완료 판정 11~12단계), `docs/17` (§2-1 파트 브랜치 CI/CD 전략, §9 Squash 확정, §15 `_camelCase` 확정, §1 repo 자율)

### spec.md 초안 6종 작성 — 파트 배포용 ✅
- 👤 방식 결정: **리드가 spec 초안을 만들어 배포 → 각 파트가 점검**. 단 `plan.md`부터는 각 파트가 직접 실행 (오너십·도메인 정확도)
- 👤 작성 범위 기준: **파트 경계를 넘거나 계약이 걸린 spec만** 리드가 작성. 한 파트에서 닫히는 것(003/004/007/008/009)은 담당이 직접
- 🤖 `docs/specs/` 신설 — 001 auth / 002 world-session / 005 studio-layout / 006 booth-runtime / 013 avatar-customization / 016 laptop-homepage + README
- 원칙 준수:
  - clarify 항목은 **대신 답하지 않고** `[NEEDS CLARIFICATION]` + C-xx 표로 남김 (헌법 22조)
  - 소급 spec(002/006/013)은 `[구현됨]`/`[신규]` 표시로 현재 구현을 기록하고 증분만 정의 (헌법 18조)
  - 각 spec 말미에 **리뷰 3칸**(clarify 답변/틀린 요구사항 지적/누락 추가) — "좋아요"로 끝나는 형식적 검토 방지
- 발견: 기존 `avatar-customization-contract.md`의 **avatarCode 29~32자 제한이 무효**임을 문서화 (파츠 조립 시 600자+, T-24)
- 발견: 노트북 홈페이지(016)의 **iframe 삽입 거부**가 예외가 아니라 기본 경로일 수 있음 → 새 창 열기를 1급 기능으로 명세
- 🤖 `docs/sdd/파트별_할일.md` 작성 — 팀원이 자기 이름 칸만 보면 되는 실행 지시서 (BE/FE 이정헌/AI 김가현/Infra/Unity + 이번 주 합동 결정 안건 5건)

### Sidekick Runtime API 채택 — 런타임 캐릭터 크리에이터 구현 ✅
- 🤖 설치된 패키지 실조사로 Runtime API 존재 확인: `SidekickRuntime.cs`(44KB), `Combiner.cs`(메시 병합), `FacialController.cs`(블렌드셰이프), SQLite 파츠 DB, 런타임 데모 씬 3종
- 👤 에디터에서 `Sidekick_RuntimePartsDemo` 동작 확인 → **WebGL 빌드에서도 로드 성공** (Synty 패키지를 프로젝트에 둔 채로)
- 🤖 우리 시스템에 통합:
  - `SidekickRuntimeService` — DB/파츠 라이브러리 1회 초기화 후 공유, `CreateCharacter(combineMesh:true)`로 드로우콜 1 유지, 좌우 파츠 자동 동기화
  - `AvatarAppearance` 확장 — 프리셋 모드(`sk_01`)와 런타임 조립 모드(`rt|10=Torso_A|17=Hips_B|c=E85D5D`) 공존, 미지원 세그먼트 무시(forward compatible)
  - `PlayerAppearanceController` — 동기화 필드를 **FixedString512Bytes로 신설** (파츠 데이터가 32바이트 초과). NetworkPlayer는 여전히 무수정
  - `AvatarCustomizationHud` — 파츠별 `< 3/12 >` 화살표(로스트아크식) + Randomize All + 색상 + Custom/Preset 토글
- 판정 정정: 이전 "Sidekick 런타임은 WebGL 불가" 결론은 **오판**이었음 (T-20 참조)
- ⏳ 검증 대기: Custom 모드 파츠 교체 → 상대 화면 실시간 반영

### Custom 버튼 무반응 수정 (T-24) ✅
- 👤 증상 보고: Docker 서버 재빌드 후에도 Custom 클릭 무반응 + Catalog is empty 지속
- 🤖 원인: 인코딩 500자 초과 시 `Encode()`가 조용히 `sk_01`로 되돌림 → 같은 값 전송 → 무반응. 상세·해결은 **T-24**
- 🤖 수정: FixedString4096Bytes 확대, 거부 사유 HUD 표시, 미러 파츠 인덱스 폴백
- ⏳ **Web + Linux Server 둘 다 재빌드 필요** (NetworkVariable 타입 변경). Catalog empty는 별건 — AvatarCatalog Inspector에 프리셋 재입력 필요 (T-21 데이터는 복구 불가)
- 🤖 후속: 클라 로그에 긴 인코딩이 생성됐는데도 `runtime=False`가 지속 → **서버만 옛 빌드**로 판정. 서버 RPC의 재인코딩 제거 + 2초 미반영 시 HUD 경고 추가 (T-24 후속 항목)
### 런타임 아바타 T포즈 수정 (T-27) ✅
- 👤 Custom 모드 파츠 조립 성공 확인 (Head/Hair/Torso 등 16종 화살표 동작, 파츠 교체 반영됨) — **T-24/T-25 해결 검증 완료**
- 👤 후속 증상: 파츠 조립 아바타가 T포즈로 굳음
- 🤖 Synty 원본 코드 확인으로 원인 규명: `CreateCharacter`는 Animator와 avatar는 붙여주지만 **Controller는 생성자 인자가 있을 때만** 설정 → 우리는 null로 생성했으므로 비어 있음
- 🤖 수정: 인스펙터 슬롯 → 루트 Animator 폴백 → 실패 시 원인 명시 로그 (T-27)

- 👤🤖 서버가 갱신되지 않은 진짜 이유 규명: **이름이 다른 옛 컨테이너(`festa-world-01`)가 7777을 계속 점유** (T-25) + 빌드 컨텍스트 오지정 (T-26). 컨테이너 이름은 `festa-world-01`로 통일

### Web 빌드 파이프라인 정비
- 🤖 `Tools/serve.py` — wasm MIME + 캐시 차단 로컬 서버 (python -m http.server의 두 문제 해결)
- 트러블: T-18(MPPM 창 차단), T-19(압축/Content-Encoding), T-20(fd_sync LinkError), T-21(필드명 변경 데이터 소실), T-22(WebGL 한글 미표시), T-23(스테일 빌드)

---

## 2026-08-08

### 착수 전 기술 결정 정리
- 🤖 `docs/21_착수전_기술결정_ADR.md` 작성 — SDD 착수 전 확정 필요한 6개 결정(Transport/Embedding/LLM/SSE·UI/Token/EC2·ECS)의 선택지·트레이드오프·권장안
- 결정 근거: 되돌리기 비용 기준 분류. Transport(wss)와 Embedding 차원만 비가역

### Unity 프로젝트 초기 구축
- 👤 Unity 6000.0.78f1 + URP로 festa-unity 프로젝트 생성
- 🤖 초기 아키텍처 스크립트 26종 작성 — Core(Bootstrap) / Network(NGO 2.4.3, WebSocket 강제, Connection Approval, Owner 권위 이동) / Booth(DTO·Registry·Factory·Runtime) / Integration(인터페이스 3종 + Mock 3종 + Http 스켈레톤)
- 🤖 BossRoom 레퍼런스에서 검증된 패키지 조합 채택 (NGO 2.4.3 / Transport 2.5.1 / Playmode 1.5.0) — `festa-unity/Docs/reference-audit.md`
- 관련: `festa-unity/Docs/architecture.md`, `editor-setup-checklist.md`

### POC A/B/C/D — 에디터 검증
- 👤 씬 구성, Player Prefab, Multiplayer Play Mode로 검증
- 결과: 2클라 상호 스폰/이동 동기화/Despawn ✅, Booth 4종 생성 ✅, AI Mock 스트리밍 ✅
- 트러블: T-01(Scale 0), T-02(TextMesh 폰트), T-03(라벨 왜곡), T-04(피벗 묻힘), T-05(재컴파일), T-14(카메라)

### Web(WebGL) 빌드 검증
- 👤 WebGL/Linux Server 모듈 설치, Web 빌드
- 결과: 브라우저 2탭 ↔ 에디터 서버 ws://127.0.0.1:7777 멀티플레이 ✅
- 트러블: T-06(빈 씬 빌드), T-07(Task.Delay), T-08(마젠타 머티리얼), T-09(OnGUI 미표시), T-10(포트 혼동), T-13(서빙 방식)

### Docker Dedicated Server 검증
- 👤🤖 Linux Server 빌드 → `festa-unity/Docker/Dockerfile` → `festa-world:dev` 이미지 → 컨테이너 실행
- 결과: 브라우저 3탭 ↔ Docker 컨테이너 서버 멀티플레이 ✅ — **배포와 동일한 체인 로컬 완성**
- 트러블: T-11(Docker daemon), T-12(Linux 빌드 대화상자)

### 문서/워크플로 정비
- 🤖 `deployment-handoff.md` 작성 + 3건 교정(NLB TLS 종료 가능, idle timeout 권장값화, endpoint 구조화 계약)
- 🤖 Mock JSON 보강 (5종 전부 + graceful skip 검증용 HOLOGRAM + version 필드)
- 🤖 `.gitignore` 2종(루트/festa-unity), `docs/22_다음_할일.md`, `docs/23_기준선_동결_워크플로.md`
### 아바타 커스터마이징 실시간 동기화 구현 ✅
- 🤖 커스터마이징 창(Unity 임시 HUD) + 즉시 반영 + 전원 전파까지 구현
  - `AvatarAppearance` — avatarCode 인코딩(`sk_01|c=E85D5D`), forward-compatible 파서
  - `PlayerAppearanceController` — Owner 요청 → ServerRpc → 서버 기록 → 전파 (NetworkPlayer 무수정)
  - `AvatarCustomizationHud` — 프리셋/색상 선택 창 (C키), 클릭 즉시 반영
  - `AvatarBridge` — React 이관용 경계 (`SendMessage('AvatarBridge', 'ApplyAppearance', ...)`)
  - `IUserApiClient.UpdateMyAvatarAsync` — 프로필 저장 경로 (현재 Mock, Spring 연동 시 구현만 교체)
- 🤖 **계약 문서 작성**: `festa-unity/Docs/avatar-customization-contract.md`
  — Spring에 요청할 API 2개, React가 만들 창 사양, 프리셋 추가 절차, 미결 사항 5건
- 당시 정식 커스터마이징 창은 **React 담당**으로 기록했으나, 2026-08-16 Unity `CharacterLobby` 완성도를 기준으로 결정을 변경해 Unity 정식 UI로 확정하고 React 교체 요구를 폐기했다.

### Synty Sidekick 아바타 검증 — **채택 확정** ✅
- 👤 FREE Starter Pack(v1.0.4, 무료, URP) 임포트 → `Assets/Synty/` 격리
- 👤 Unity 6에서 Character Creator Tool 정상 실행 → 캐릭터 3종 제작 → FBX Export (`_Project/Experimental/Sidekick/`)
- 검증 결과:
  - **Rig: Humanoid** — Mixamo 등 표준 애니메이션 사용 가능
  - **머티리얼 1개/캐릭터** — 드로우콜 1, 30~40명 월드에 유리 (Sidekick 최대 강점)
  - **WebGL 빌드 성공**, 브라우저에서 캐릭터 3종 동시 표시 확인
  - **용량: 64.7MB → 86.9MB (+22.2MB)** — 기준(+50MB) 대비 여유
- 🤖 Adapter 구조 작성 (기준선 무수정): `IAvatarVisualProvider` / `AvatarCatalog`(SO) / `CatalogAvatarVisualProvider` / `PlayerAvatarVisual`
  - NetworkPlayer는 Sidekick을 모름 — 외형은 로컬 생성, avatarCode만 동기화 (Booth Runtime과 동일 원칙)
- 트러블: T-17(http.server 상대경로 404)

### 아바타 SDK 조사 — 외부 SDK 전원 탈락(Genies/RPM), Sidekick으로 대체
- 🤖 **Genies Avatar SDK**: 설치 전 문서 조사로 탈락 — Unity SDK는 Windows/iOS/Android만 지원, **Unity Web 미지원** (Planned Features 명시). 별도의 "Web SDK"(Early Access)가 존재하나 **Three.js 전용 npm 패키지**(`@geniesinc/genies-naf-webgl`)라 Unity WebGL 빌드와 통합 불가 — 두 SDK는 별개 제품. (Web SDK는 React 측 장식 기능 P2 아이디어로만 메모)
- 🤖 **Ready Player Me**: Netflix 인수 후 **2026-01-31 서비스 종료** (공식 포럼 공지 확인) — 채택 불가
- 결론: **자체 경량 아바타** (avatarCode 동기화 구조 기존재, spec 013에서 설계)
- 교훈: **외부 SDK는 설치 전에 플랫폼 지원·서비스 존속 여부를 문서로 확인** — 반나절 실험 대신 30분 조사로 판정

### HTTP Vertical Slice — 완료 ✅
- 검증 결과: [A] Mock 경로 정상, [B] HTTP(localhost:8000 정적 JSON) → Booth 5개 생성, [C] boothId 99 → 404 경고 후 graceful skip (클라 중단 없음)
- [D] WebGL 검증은 다음 Web 재빌드 때 수행
- 커밋: `feat(unity): add HTTP booth layout vertical slice` → origin/main push 완료

### HTTP Vertical Slice 구현 (검증 대기)
- 🤖 기준선 위 델타 4개만 구현 (재작성 없음):
  - `ApiConfig`(SO) — LOCAL/DEV/PROD 환경별 Base URL, useMock 플래그 통합
  - `IAccessTokenProvider` + Empty/Mock 구현 — Auth 붙기 전 토큰 주입 경계
  - `HttpBoothApiClient` 강화 — timeout 10s, Bearer 헤더, 404/5xx/네트워크 에러 분기, 빈 layout 경고, 실패 시 null(클라 전체 미중단)
  - `Tools/mock-api/` — 정적 JSON endpoint (같은 오리진 서빙으로 CORS 회피)
- ⏳ 검증 대기: 에디터 Mock/HTTP 각 1회 + 404 + WebGL (체크리스트는 보고 참조)

### Git 기준선 수립 (v0.0.1-poc)
- 👤🤖 로컬 스냅샷 커밋 → 브랜치 main 확정 → 원격(github.com/kanghyunsoon/ssafesta) fetch → unrelated-histories merge → push
- 충돌 1건: `docs/24_작업일지.md` (add/add) → 로컬 우선으로 해결. 원격에서 `docs/README.md` 유입
- 커밋: `d1c2007` (POC 기준선) / `4b1b675` (merge, **tag: v0.0.1-poc**)
- ⏳ 대기: 회귀 검증 3건(CONSULT_DESK/HOLOGRAM skip/멀티 유지)은 에디터에서 미실시 — 다음 에디터 작업 시 확인

---

<!-- 새 날짜는 이 위에 추가 -->
