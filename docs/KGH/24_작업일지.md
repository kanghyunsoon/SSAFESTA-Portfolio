# 작업일지

## 2026-09-03

### boothId+agentId 스코프 벡터 검색 서비스 구현 (S15P21A604-128) ✅

- 🤖 Jira 전체 검색에서 기존 `S15P21A604-128`이 FastAPI RAG 검색 연결 범위와 일치함을 확인해 새 이슈를 만들지 않고, 최신 `gitlab/develop`에서 `feat/S15P21A604-128-scoped-vector-search` 브랜치를 분기했다.
- 🤖 `ChunkRepository.search_ready_chunks()`를 유일한 검색 진입점으로 만들고, 호출자가 생략할 수 없는 `ChunkScope`를 통해 SQL에 `booth_id + agent_id + searchable=true` 조건을 강제했다.
- 🤖 검색 결과에도 Booth·Agent 식별자를 보존하고 반환 직전 전건을 재검증해, DB·매핑 경계에서 Scope 불일치가 한 건이라도 생기면 `RAG_SCOPE_MISMATCH`로 fail closed하도록 했다.
- 🤖 `VectorSearchService`가 질문을 기존 Embedding Provider로 변환한 뒤 같은 Scope로 repository를 호출하도록 연결했다. 빈 질문·1536차원 불일치·NaN/무한대는 DB 검색 전에 거부한다.
- ✅ 단위 테스트 전체 **124건 통과**. 별도 PostgreSQL 16+pgvector 컨테이너에서 같은 벡터를 가진 타 Booth·타 Agent·`searchable=false` Chunk가 모두 제외되고 허용 Chunk 1건만 반환되는 통합 격리 테스트를 통과했다.
- 📎 대화 Context·LLM·SSE 연결은 각각 기존 Jira `S15P21A604-129`, `S15P21A604-140` 범위이며 이번 브랜치에 섞지 않았다. Chunk 생성·READY 활성화는 선행 `S15P21A604-124` 범위다.
- 📎 트러블슈팅: [T-13](./25_트러블슈팅.md)

## 2026-09-02

### Worker sweeper·재시도 backoff·DEAD 전환 구현 (S15P21A604-183) ✅

- 🤖 MR !203을 squash로 `develop`에 병합해 `S15P21A604-122` 선행 작업을 완료하고, 뒤집혀 있던 Jira 링크를 `183 blocks 147` 방향으로 바로잡음
- 🤖 최신 `gitlab/develop`에서 `feat/S15P21A604-183-worker-sweeper-retry-dead` 격리 worktree를 생성
- 🤖 만료된 `RUNNING` Job을 `FOR UPDATE SKIP LOCKED`로 회수해 실행 횟수에 따라 1·5·15분 `RETRY_WAIT`로 보내고, 최초 1회+재시도 3회를 모두 소진하면 `DEAD`와 `PROCESSING_INTERRUPTED` 사유를 기록하도록 repository 구현
- 🤖 sweeper가 기동 즉시 실행된 뒤 설정 주기로 반복하고, 한 주기에서 관측한 만료 Job을 모두 배출하도록 `JobRecoveryService` 구현
- 🤖 실제 PostgreSQL 17 + pgvector에서 Worker 상실 후 다른 Worker 재획득, 정상 lease 보존, backoff 전 순서, 4번째 실패의 DEAD 전환, 다중 sweeper 단일 회수를 검증하고 전체 테스트 133건 통과(외부 MinIO 선택 테스트 5건 skip)
- 📎 GitLab MR: !205 (`develop` 대상, squash, source branch 삭제, `Closes S15P21A604-183`)
- 📎 관련 Jira: S15P21A604-122, S15P21A604-183, S15P21A604-147
- 📎 트러블슈팅: T-12

### Worker pickup·lease·heartbeat 구현 (S15P21A604-122) ✅

- 🤖 Jira `S15P21A604-147`의 DEAD 비율·경고 검증 선행 조건으로 `S15P21A604-183` 의존 관계를 추가하고, 실제 구현 순서를 122 → 183 → 147로 정리
- 🤖 최신 `gitlab/develop`에서 `feat/S15P21A604-122-worker-lease-heartbeat` 격리 worktree를 생성해 기존 371번 미커밋 변경을 보존
- 🤖 `QUEUED` 및 실행 시각이 도래한 `RETRY_WAIT` Job을 `FOR UPDATE SKIP LOCKED`로 획득하고 같은 트랜잭션에서 `RUNNING`·worker 소유권·attempt·90초 lease를 기록하도록 repository 구현
- 🤖 별도 짧은 트랜잭션에서 `(jobId, workerId, RUNNING)`을 조건으로 heartbeat lease를 갱신하고, 소유권 상실 시 처리 결과 publish를 호출하지 않는 Worker loop 구현
- 🤖 실제 PostgreSQL 17 + pgvector에서 다중 Worker 중복 pickup 0건, 단일 Job 경쟁, retry 시각, heartbeat 소유권, heartbeat 중단 후 만료 Job 판정을 검증하고 전체 테스트 127건 통과(외부 MinIO 선택 테스트 5건 skip)
- 🤖 develop행 MR의 squash 커밋에도 Jira 완료 자동화 조건이 남도록 커밋 본문과 MR 설명에 `Closes S15P21A604-122`를 기록
- 📎 GitLab MR: !203 (`develop` 대상, squash, source branch 삭제)
- 📎 관련 Jira: S15P21A604-122, S15P21A604-147, S15P21A604-183
- 📎 트러블슈팅: T-03, T-11

## 2026-09-01

### 구조·의미·Parent-Child 청킹 전략 비교 Jira 생성 ✅

- 👤 고정 토큰 청킹 외에 표 구조 인식, 줄글 의미 기반, 작은 Child 검색→큰 Parent 생성 방식을 비교하기로 결정
- 🤖 Jira `S15P21A604-380`을 AI Agent Epic 아래 생성하고 Baseline·구조 인식·의미 기반·Parent-Child·혼합 전략의 범위와 Gold Label 평가 지표·완료 조건을 명시
- 🤖 `S15P21A604-370 → S15P21A604-380 → S15P21A604-371` Blocks 의존성을 연결해 청킹 전략 확정 후 Reranker 비교가 진행되도록 구성
- 📎 관련 Jira: https://ssafy.atlassian.net/browse/S15P21A604-380

### 청킹·Overlap·Top-K 조합 최적화 (S15P21A604-370) ✅

- 🤖 최신 `gitlab/develop`에서 `feat/S15P21A604-370-chunk-topk-tuning` 격리 worktree를 생성하고 92번의 승인 Gold Label·Embedding 후보를 기준선으로 사용
- 🤖 `text-embedding-3-small/large × Chunk 300/600/900 × Overlap 10/15/20% × Top-K 3/5/8/10`을 PinLog·SSAFESTA·수달 35개 정답 질문에 실제 GMS + pgvector로 평가해 216개 결과 행 생성
- 🤖 검색 P50/P95, 평균·P95 LLM Context token, 실패 질문 ID를 지표에 추가하고 세 문서 가중 Recall·MRR 및 모델별 최적 조합을 자동 집계하는 CLI·테스트 구현
- 🤖 품질 게이트 통과 9개 조합 중 `text-embedding-3-large + 900 tokens + 20% overlap + Top-K 10`을 기본값으로 확정 — 전체 Recall 1.0000, MRR 0.6900, 문서별 최저 Recall 1.0000, 평균 Context 5,023.5 tokens, 최대 검색 P95 48.47ms, 격리 위반 0건
- 🤖 비용 기준선 small은 같은 900/20%/10에서 Recall 1.0000·MRR 0.5667이며, 전체 실험 추정 Credit은 small 0.030 / large 0.300으로 기록
- 🤖 재현용 `tuning-grid.json`·`retrieval-defaults.json`, 123·128·163번 후속 영향 문서와 결과 JSON/Markdown을 추가하고 단위 테스트 16건·compileall·diff check 통과
- 📎 관련 Jira: S15P21A604-370, S15P21A604-123, S15P21A604-128, S15P21A604-163
- 📎 트러블슈팅: T-10

### R2/MinIO object storage 어댑터 구현 (S15P21A604-120) ✅

- 🤖 `festa-ai/app/providers/storage.py`(T018 마지막 조각 — document_parser·embedding protocol은 이미 있었음) `ObjectStorage` protocol과 `festa-ai/app/providers/s3_compatible_storage.py`(T045) boto3 기반 구현체 신규 작성. R2·MinIO 둘 다 같은 클래스, endpoint만 다르게 주입해 설정만으로 전환 가능하게 함
- 🤖 `verify_and_download()` — HEAD로 크기 먼저 확인(빠른 실패), 다운로드 후 SHA-256 재계산해 대조(`SOURCE_HASH_MISMATCH`). `ClientError`는 Provider 원문·Secret 노출 없이 `SOURCE_NOT_FOUND`/`STORAGE_PROVIDER_ERROR`로 정규화
- 🤖 `app/providers/factory.py`에 `create_object_storage()` 추가 — Job의 `storage_provider`로 매 요청 R2/MinIO 어댑터 선택(FastAPI는 활성 쓰기 Provider를 선택하지 않는다, plan.md §2-2)
- 🤖 Jira 완료조건("로컬 MinIO 대상 통합 테스트")대로 실제 Docker MinIO 컨테이너 띄워 put→get 왕복·metadata 불일치 거부 검증(통합 5개) + moto 유닛 12개, 총 113개 GREEN
- 🤖 착수 전 확인 — 147번(구조화 로깅) 먼저 시도했으나 Jira에 122번(Worker) 선행 의존 명시돼 있어 DEAD 비율 지표 검증 불가함을 확인하고 보류, 120번으로 전환
- 📎 관련 Jira: https://ssafy.atlassian.net/browse/S15P21A604-120

### GitLab MR !189 병합 조건 확인·자동 Merge 요청 진행

- 👤 develop 대상 MR !189 병합 진행 요청
- 🤖 GitLab connector·CLI 부재와 브라우저 런타임 시작 실패를 확인하고, Git 자격증명을 노출하지 않는 REST API 상태 조회를 시도했으나 API 인증 범위가 없어 중단
- 🤖 필수 파이프라인·승인·충돌 조건을 우회하지 않는 GitLab push option `merge_request.merge_when_pipeline_succeeds` 적용 경로로 전환
- 📎 관련: GitLab MR !189, Jira S15P21A604-92
- 📎 트러블슈팅: T-09

### S15P21A604-92 전용 브랜치·develop MR 생성 ✅

- 👤 92번 전용 브랜치에서 GitLab develop 반입 요청
- 🤖 최신 `gitlab/develop`(`00175c6f`)에서 `feat/S15P21A604-92-embedding-benchmark` 격리 worktree 생성
- 🤖 스파이크 코드·테스트·질문·Gold Label·평가 결과와 KGH 개인 기록만 50개 파일로 선별하고 로컬 `.env`·원본 PDF·Python 캐시 제외
- 🤖 단위 테스트 13건·compileall·staged diff·Secret 파일 검사를 통과한 커밋 `e58010df` 생성 (`Closes S15P21A604-92` 포함)
- 🤖 GitLab 원격 브랜치 push 및 develop 대상 Squash MR `!189` 생성
- 📎 관련: GitLab MR !189, Jira S15P21A604-92
- 📎 트러블슈팅: T-08

### 승인 수달 Gold Label 기반 Embedding 3종 평가 ✅

- 👤 수달 정답 청크 제안 문서를 수정·승인하고 세 Embedding 모델 평가 진행 요청
- 🤖 사용자가 수정한 수달 q12 제안 답변·판정 이유를 평가용 JSONL에 동기화하고 `approval.json` 승인 대상에 수달 데이터셋 추가
- 🤖 `수달.pdf` 16페이지·26청크에서 정답 있는 14문항을 대상으로 600-token·15% overlap·Top-K 1/3/5 조건의 GMS 실제 모델 평가 수행 (`NO_ANSWER` 1문항 제외)
- 🤖 수달에서 `text-embedding-3-large`가 Recall@5 0.7143·MRR@5 0.3810으로 1위, small 0.5000·0.2071, Gemini 0.4286·0.2464 기록
- 🤖 세 프로젝트 35문항 가중 합산에서 large가 Recall@5 0.8571·MRR@5 0.5338로 1위임을 확인했으나 0.95 품질 기준은 미충족하여 청킹·Top-K 튜닝 필요성을 기록
- 🤖 세 모델 모두 1536차원, 격리 위반 0건, 단위 테스트 13건 통과
- 📎 관련 파일: `spikes/ai-rag/data/gold/approval.json`, `spikes/ai-rag/data/evaluation/sudal-gold-metrics.json`, `spikes/ai-rag/data/evaluation/gold-metrics-summary.md`
- 📎 트러블슈팅: T-07

### 확정 질문 45건 반영·수달 Gold Label 후보 작성 ✅

- 👤 PinLog·SSAFESTA·수달 각 15개의 최종 평가 질문을 확정
- 🤖 PinLog·SSAFESTA 질문을 기존 승인 Gold Label과 대조하고, SSAFESTA q13의 사용자 원문 표기를 query·JSONL·Markdown에 동기화
- 🤖 PinLog Markdown의 PARTIAL 질문에서 정답 청크가 `NO_ANSWER`로 잘못 표시된 문서 불일치를 JSONL 기준으로 수정
- 🤖 `수달.pdf` 16페이지를 600-token·15% overlap 기준 26개 청크로 분할해 15개 질문의 답변 상태·제안 답변·판정 이유·`relevant_contains`를 작성
- 🤖 수달 Gold Label 분포 `ANSWERABLE 11 / PARTIAL 3 / NO_ANSWER 1`, 질문 ID·문구 일치 및 모든 정답 문자열의 실제 청크 포함을 검증
- 🤖 스파이크 단위 테스트 13건 통과
- 📎 관련 파일: `spikes/ai-rag/data/queries/`, `spikes/ai-rag/data/gold/`
- 📎 트러블슈팅: T-06

### RAG 모델·검색 파라미터 결정 Jira 티켓 재구성 ✅

- 👤 Embedding 모델, 청킹·Top-K, Reranker, LLM 모델을 각각 측정하고 최종 조합을 결정할 수 있도록 기존 스파이크와 후속 티켓 분리를 요청
- 🤖 Jira `S15P21A604-92`를 Embedding 3종의 Recall@K·MRR·지연·비용 비교 및 후보 선정 범위로 수정하고 현재 산출물·수달 문서 잔여 작업·후속 범위 분리를 명시
- 🤖 `S15P21A604-370` 청킹·Overlap·Top-K 최적화, `S15P21A604-371` Reranker 방식 비교, `S15P21A604-372` RAG 답변 생성 LLM 비교, `S15P21A604-373` 최종 모델·검색 파라미터 ADR 티켓 생성
- 🤖 `-92 → -370 → -371 → -372 → -373` 의존 흐름과 `-371 → -373`, `-373 → -163/-148` Blocks 링크를 연결하고, 모든 신규 티켓이 `AI Agent(S15P21A604-8)` Epic 아래 생성됐는지 재검증
- 📎 관련 Jira: S15P21A604-92, S15P21A604-148, S15P21A604-163, S15P21A604-370, S15P21A604-371, S15P21A604-372, S15P21A604-373

### 임베딩 골드 라벨 승인·수달 프로젝트 질문셋 생성 ✅

- 👤 PinLog·SSAFY FESTA의 Sol 골드 라벨 30건 최종 승인
- 🤖 승인 상태를 `data/gold/approval.json`에 기록
- 🤖 `수달.pdf` 원문을 열람하지 않고 신규 프로젝트의 서비스 이해·기술 검증용 query-only 질문 15건을 생성
- 📎 관련 파일: `spikes/ai-rag/data/gold/approval.json`, `spikes/ai-rag/data/queries/sudal.jsonl`

### spec 011 상담 정책 C-01~C-03 확정 반영 (S15P21A604-354) ✅

- 👤 GitLab work_items#118(상담 정책 C-01~C-03 확정) 검토 후 확정안(C-01 만료 10분, C-02 선택안 A, C-03 데이터별 보존 분리) 결정, 이슈 댓글로 등록
- 🤖 `specs/011-staff-consultation/spec.md` Clarifications 표에 C-01~C-04 결정값 반영 (C-04는 헌법 12조·spec 001 기존 확정 재확인)
- 🤖 문서 반영 작업을 Jira Task S15P21A604-354로 생성 — docs/08·09·16 갱신 범위 명시
- 🤖 `docs/08_Backend_API_명세서.md` §12 Consultation — 요청 생성 응답에 `expiresAt` 추가, `GET`/`accept` EXPIRED 처리 명시, History Endpoint·보존 정책 보류 상태 반영
- 🤖 `docs/09_DB_ERD_DB_설계서.md` §21 `consultations` — `expires_at` 컬럼 추가, C-02 비동기 문의 컬럼 미포함 사유 명시, §22 `consultation_messages` P2 보류 명시, §27에 Consultation 보존 정책(프로젝트 종료 시 일괄 삭제) 서브섹션 추가
- 🤖 `docs/16_Realtime_통신_명세서.md` §11 `CONSULTATION_EXPIRED` 이벤트 추가, §12 원문 저장 보류 명시, §13 상태 머신에 10분 타이머 표기
- 📎 관련 파일: `specs/011-staff-consultation/spec.md`, `docs/08_Backend_API_명세서.md`, `docs/09_DB_ERD_DB_설계서.md`, `docs/16_Realtime_통신_명세서.md`
- 📎 관련: GitLab work_items#118, Jira S15P21A604-354

## 2026-08-31

- 🤖 GitLab Work Item #84의 SHA-256 후속 요청을 확인해 FastAPI가 저장소 원본 바이트를 재검증하고 불일치 시 Chunk 없이 `SOURCE_HASH_MISMATCH`로 실패하는 계약·설계·검증·구현 작업을 spec 007에 반영 (T-05)

### PDF→Embedding→검색 모델·청킹·Top-K 비교 스파이크 구현 ✅

- 🤖 `text-embedding-3-small`, `text-embedding-3-large`, `gemini-embedding-2`를 동일한 1536차원 계약으로 비교하는 독립 실행형 Python 스파이크를 구현
- 🤖 페이지 보존 토큰 청킹, overlap 조합, Top-K별 Recall@K·MRR·검색 P95·격리 위반·처리 시간·요청 횟수·추정 Credit을 JSON으로 기록하도록 구성
- 🤖 pgvector 임시 테이블과 메모리 대체 저장소 모두에서 `boothId + agentId + modelId` 필터를 강제하고, 차원 불일치와 API 오류를 명시적으로 실패 처리
- 🤖 Miniforge로 `ssafesta-rag-spike` 환경(Python 3.12.14)을 생성하고 전체 의존성 설치, 단위 테스트 7건, Python 컴파일 및 `rag-spike` CLI 검증 통과
- 🤖 GMS 실제 계약에 맞춰 OpenAI Bearer `/v1/embeddings` 배치 호출과 Gemini `x-goog-api-key` `:embedContent` 단건 호출을 provider별로 분리하고, 각 응답의 `data[].embedding`·`embedding.values` 파싱 및 요청 횟수 산정을 검증
- 🤖 임베딩 검색 평가셋을 처음 서비스를 접한 사용자·심사자·개발자가 실제로 궁금해할 질문 50건으로 전면 교체(PinLog 20건/기술 10건, FESTA 30건/기술 15건)하고, 미확정 기술은 임의 답변 대신 현재 기획상 미정으로 응답하도록 구성
- 🤖 사용자 제공 질문 30건을 GMS의 임베딩 3종으로 실제 호출해 PinLog 3청크·FESTA 10청크의 Top-K 3 결과, 모델별 시간·요청 수·추정 Credit, 질문별 `relevant_contains` 후보와 모델별 Top-1을 검토 JSON/JSONL로 저장
- 🤖 JSON 검토 난도를 낮추기 위해 질문별 모델 Top-1 비교표, 후보 체크박스, 최종 판정란, Top-3 문맥 발췌를 포함한 PinLog·FESTA Markdown 검토 문서를 추가
- 🤖 `gpt-5.6-sol`이 검색 결과와 독립적으로 원문을 대조해 질문 30건의 골드 정답 청크 후보를 `ANSWERABLE / PARTIAL / NO_ANSWER`로 제안하고, 질문 ID 일치·JSON 파싱·원문 근거 문자열 존재를 검증한 JSONL 및 Markdown을 저장
- 🤖 Sol 골드 후보 중 정답이 있는 21문항으로 세 임베딩 모델의 Recall@1/3/5·MRR을 재계산하고, 전체 가중 평가에서 `text-embedding-3-large`가 Recall@5 0.9524·MRR@5 0.6357로 1위임을 확인
- 📎 관련 파일: `spikes/ai-rag/`, `.gitignore`
- 📎 트러블슈팅: T-01, T-02, T-03, T-04, T-05

## 2026-08-28

- 🤖 Jira `S15P21A604-121`의 `POST /ai/v1/documents/process`를 구현. Spring이 Business DB 검증 후 전달한 불변 snapshot만 사용하며 FastAPI의 Business DB 조회 경로는 추가하지 않음
- 🤖 AI DB의 활성 Job(`QUEUED/RUNNING/RETRY_WAIT`)을 조회해 snapshot 10개 필드가 모두 같으면 기존 Job과 `existing:true`를 202로 반환하고, 하나라도 다르면 `DOCUMENT_SNAPSHOT_CONFLICT` 409를 반환하며, 활성 Job이 없으면 `QUEUED` Job과 `existing:false`를 생성하도록 구현
- 🤖 PostgreSQL partial unique index 경합 시 rollback 후 승자 Job을 다시 조회·비교하도록 구현해 동시 동일 요청도 활성 Job 1건으로 수렴
- 🤖 AI DB 전용 async session factory를 앱에 연결하고, `document-processing-api.yaml` 기반 문서 snapshot 요청·Job 접수 응답 DTO를 camelCase·추가 필드 금지 Pydantic 모델로 구현
- 🤖 Spring→FastAPI Bearer Token을 `INTERNAL_SPRING_TO_AI_TOKENS` 전체와 `secrets.compare_digest`로 비교하고, 누락·형식 오류·오류 토큰·반대 방향 토큰을 동일한 정제된 401 응답으로 거부. 방향별 Token 설정 중복도 부팅 시 차단
- 🤖 공통 `ApiError`와 정제된 `{code, message}` 401·409·422 응답 경계를 등록하고 실제 PostgreSQL 17 + pgvector를 포함한 전체 테스트 88건 통과
- 🤖 실제 FastAPI HTTP endpoint에서 AI DB까지 연결한 E2E 테스트를 추가해 인증 401, 0 byte 422, 신규 202/`existing:false`, 동일 요청 202/`existing:true`, SHA-256 변경 409, AI DB 활성 Job 1건 및 동시 HTTP 요청 단일 Job을 한 흐름으로 검증
- 🤖 생성 OpenAPI의 operationId·요약·설명·Service Token 보안 스키마·401/409/422 오류 응답·요청/응답 필드와 제약을 `document-processing-api.yaml` 정본과 자동 비교하는 계약 테스트를 추가하고, `int64` 형식과 오류 응답 문서화를 정본에 맞춤
- 🤖 접수 API는 파일 바이트를 받거나 저장소에서 내려받는 단계가 아니므로 테스트 문서는 `guide.pdf` snapshot 메타데이터로 구성. SHA-256은 형식·비교 검증용 64자리 `a`/`b` 값, 20MB 제한은 `20,971,520/20,971,521` 숫자 경계값으로 검증
- 🤖 파일 크기 경계 검증을 보강해 `0` byte 거부, 정확히 `20,971,520` byte 허용, `20,971,521` byte 거부를 각각 독립 테스트로 확인
- 🤖 spec 007 `tasks.md`의 T012·T015·T019 완료와 T016·T050의 `S15P21A604-121` 부분 완료 범위를 반영
- 📎 관련 파일: `festa-ai/app/api/`, `festa-ai/app/db/session.py`, `festa-ai/app/repositories/`, `festa-ai/app/services/`, `festa-ai/tests/`, `specs/007-ai-agent-document/tasks.md`
- 📎 트러블슈팅: T-03, T-04

- 🤖 `S15P21A604-94`(LLM·Embedding Provider 어댑터 인터페이스·Mock Provider)가 MR [!79](https://lab.ssafy.com/s15-metaverse-game-sub1/S15P21A604/-/merge_requests/79)의 스쿼시 커밋 `678e499`로 `develop`에 반영됐음을 확인 (`git log gitlab/develop`으로 검증)
- 🤖 해당 커밋 메시지에 `Closes S15P21A604-94`가 없어 Jira 자동전환(Webhook→Jira Automation)이 트리거되지 않고 "In Progress"로 남아 있던 문제 발견 — 코드는 이미 develop에 있으나 상태만 어긋난 상태
- 🤖 rule 5 규정대로 임의 수동 전환 대신, `Closes S15P21A604-94`를 담은 이 후속 문서 커밋으로 develop 반입 → Jira 자동전환 트리거를 유도

## 2026-08-27

- 🤖 Jira `S15P21A604-94` 작업을 위해 GitLab 최신 `develop` 기반 `feat/S15P21A604-94-provider-adapters` 격리 worktree를 생성해 기존 `ai` 작업 트리의 미커밋 변경을 보존
- 🤖 Embedding batch 결과·모델 식별자와 비동기 LLM token stream을 Provider 독립적으로 소비하는 `EmbeddingProvider`·`LLMProvider` 계약과 불변 값 객체를 정의
- 🤖 동일 입력에 항상 같은 1536차원 단위 벡터를 생성하는 `MockEmbeddingProvider`와 마지막 사용자 질문을 고정 응답으로 streaming하는 `MockLLMProvider`를 구현
- 🤖 `EMBEDDING_PROVIDER=mock|gms` 선택과 GMS 필수 설정의 fail-fast 검증, Provider factory 조립 경계를 추가
- 🤖 Managed Embedding adapter에서 응답 index가 있으면 전체 항목의 고유·연속 범위를 검증해 정렬하고, index가 전부 없으면 응답 순서를 임시 보존하도록 구현. GMS 모델·API 확정 후 순서 계약 재검증 필요사항을 `docs/26_팀_결정_필요사항.md`에 등록하고, 개수·숫자 유한성·1536차원 및 안전 오류 정규화를 적용
- 🤖 Provider·Mock·Managed adapter 테스트와 기존 설정·부팅 회귀 테스트를 포함한 단위 테스트 44건 통과. Windows 제한 환경에서는 worktree 내부 cache·basetemp를 지정해 실행 ([T-03](25_트러블슈팅.md)), spec 007의 T018a·T047 진행 상태 반영
- 🤖 중앙 기록 문서를 수정한 경로 오판을 바로잡고 개인 기록을 `docs/KGH/`로 이동 ([T-03](25_트러블슈팅.md))
- 📎 관련 파일: `festa-ai/app/core/config.py`, `festa-ai/app/providers/`, `festa-ai/tests/unit/`, `specs/007-ai-agent-document/tasks.md`

## 2026-08-26

- 🤖 Jira `S15P21A604-93`의 FastAPI 스캐폴드·설정 fail-fast·health 구현과 유닛 테스트 20건 통과분(`ec7108e` → `996198c` → `10340b4` → `2c66e98` → `5241393`)이 코드 안전 반입 MR [!22](https://lab.ssafy.com/s15-metaverse-game-sub1/S15P21A604/-/merge_requests/22)의 `5def4c6`으로 `develop`에 반영됐음을 확인. 기존 `ai → develop` MR [!17](https://lab.ssafy.com/s15-metaverse-game-sub1/S15P21A604/-/merge_requests/17)은 파트 브랜치 전체 병합 시 타 파트 파일 삭제 위험이 있어 머지 없이 닫고, `festa-ai/` 22개 파일만 이식한 !22를 완료 근거로 확정
- 🤖 spec 007의 GitLab Work Item #100 후속 미확정 2건을 `docs/26_팀_결정_필요사항.md`에 등록하고, P0 저장소 장애 대응을 probe evidence 수집 + 운영자 수동 `UPLOAD_BLOCKED` 적용으로 통일
- 🤖 GitLab Work Item #106의 callback 404 원인별 정책을 spec·plan·data-model·OpenAPI·quickstart·tasks에 반영 — `JOB_NOT_REGISTERED`만 1초·3초·10초 최대 3회 재시도, 영구 404는 정상 전달과 분리해 종료 기록
- 🤖 spec 007의 세 OpenAPI 스키마 검증과 storage reconciliation Backend 동작 테스트, callback 종료 필드 migration 작업, `develop` 기준 Flyway 표현을 보완하고 tasks 식별자를 `T001~T079` 연속 번호로 정리 (T-001)

## 2026-08-25

- 🤖 spec 007의 Spring callback `404`가 문서 없음·`jobId` 미등록·문서 불일치를 구분하지 않아 영구 재시도될 수 있음을 확인하고, 오류 코드별 재시도·종료 정책과 기록 방식을 Backend와 확정하기 위한 GitLab Work Item #106 생성
- 🤖 spec 007 자체 결정 가능 정합성 항목을 반영 — `docs/14_AI_Server_API_명세서.md`를 OpenAPI 정본과 동기화하고, 중앙 Jenkins component adapter 경로·V13 Flyway·AI 장애 격리·내부 API 네트워크 경계·수동 Provider 전환 검증 작업을 `tasks.md`에 추가해 기존 72개에서 75개로 보완
- 🤖 GitLab Work Item #100의 Infra 추가 답변을 확인해 자동 failover·이중 쓰기·자동 원복 금지, 운영자 승인 수동 MinIO 전환, 문서별 `storageProvider + bucket + objectKey` 읽기와 유한 재시도를 spec 007에 반영하고, `R2_RECONCILING` 중 업로드·장애 판정 수치·reconcile 기록·quota 오류 코드는 미확정으로 분리
- 🤖 GitLab Work Item #102의 방향별 Service Token·상수 시간 검증·callback `jobId` 404/멱등/`sourceHash` 409·단계적 무중단 회전·mTLS P2 결정을 spec 007 요구사항·계획·연구·OpenAPI·검증·작업에 반영하고, `tasks.md` 확정 전 잔여 계약과 작업 커버리지를 점검
- 🤖 GitLab Work Item #100의 Backend 답변을 검토해 수동 전환·행 단위 `storage_provider`·provider 변경 시 새 문서 생성 방향에 동의하고, 활성 쓰기 provider는 Spring 설정이 소유하며 FastAPI는 행의 provider를 따르는 역할 분리와 과거 저장소 읽기 adapter 병존 필요성을 확인
- 🤖 Spring ↔ FastAPI 내부 API 인증의 P0 서비스별 토큰·SG 제한, P1 토큰 운영 강화, P2 조건부 mTLS 적용 방안을 검토하도록 Backend·Infra 담당자를 태그한 GitLab Work Item #102 생성
- 🤖 GitLab Work Item #84의 업로드 미완료 정리 계약을 spec 007 산출물에 반영 — URL 15분, 생성 후 1시간 `EXPIRED`, 24시간 복구 유예, Spring R2 삭제·재시도 책임과 bucket/prefix 제한 `DeleteObject` 최소 권한을 명시
- 🤖 R2 장애 대응을 P0 자동 failover 없이 운영자 검증 후 단일 MinIO 저장소로 수동 전환하고, 복구 후 reconcile·원복하는 제안으로 정리해 GitLab Work Item #100을 생성. Infra `@powerful7305`, Backend `@ejraks1548`를 태그하고 파트별 확인 사항과 완료 조건을 명시

## 2026-08-24

- 🤖 Jira 보드 15273의 AI 범위 데일리 플래닝을 수행해 Sprint 2 미완료 이월 0건을 확인하고, 활성 이슈 SP·설명·Blocks 링크를 실제 의존성에 맞춰 보완한 뒤 오늘 작업을 `S15P21A604-96(1SP) → S15P21A604-92(2SP)`로 확정
- 🤖 spec 007 구현 전 분석에서 AI·FastAPI 단독 결정 항목을 권장안으로 확정 — 완료 범위를 `READY + boothId/agentId 격리 검색 가능`으로 한정하고 실제 LLM 답변·SSE는 spec 008로 분리, FastAPI가 처리 시작·성공 commit 직전 `DISABLED`를 재확인해 `CANCELLED` 처리하도록 작업 계약과 검증 기준을 구체화
- 🤖 spec 007의 확정된 spec·plan·data model·OpenAPI 계약·검증 시나리오를 기준으로 Spring·FastAPI·Frontend 역할과 의존성을 구분한 구현 작업 72개를 `tasks.md`로 생성하고, P0 AI 직원 설정·문서 처리와 P1 문서 관리가 각각 독립 검증되도록 단계·테스트·병렬 실행 순서를 정리
- 🤖 Jira `S15P21A604-96 → S15P21A604-92` 순서에 맞춰 spec 007 `tasks.md`에 Jira 대응표와 구현 전 게이트를 추가하고, 미니 스파이크의 청킹·Top-K·비용 실측값을 T047 설정과 T066 성능·품질 검증에 후속 반영하도록 추적 관계를 명시
- 🤖 데일리 랩업으로 Jira `S15P21A604-96`·`S15P21A604-92`에 오늘 진행/남은 작업/Blocker 코멘트를 남기고, 누락된 `S15P21A604-92` 상태를 In Progress로 보정

## 2026-08-21

- 👤 AI 상담은 코인을 차감하지 않는 무료 이용으로 확정
- 🤖 AI 상담 과금·결제·환불·수익배분 관련 요구사항과 미확정 논의를 spec 008·012·003 및 기획/API/QA/ADR 문서에서 제거하고, spec 012를 장식 구매·인벤토리 중심으로 정리

## 2026-08-20

- 🤖 spec 008 리뷰 ② FR-003 격리 테스트 방법 구체화 — 실제 PostgreSQL+pgvector 고정 Fixture와 유일 검증 코드를 사용해 검색 결과, Conversation Scope 위조, Prompt Injection, 조립된 LLM 입력, SSE source/답변, 50개 혼합 동시 요청을 자동 검증하고 1건 유출 시 CI 배포 차단하는 프로토콜 확정
- 🤖 spec 008 Edge Case 정책 구체화 — 대화 원문은 휘발성 저장소에서 세션 종료/30분 유휴 시 삭제, 질문 2,000자·입력 8,000토큰·최근 왕복 6회, 공유 LLM 풀의 사용자/Agent/전체 동시성·대기열 한도, 문서 미준비 시 LLM 미호출 안내를 확정 반영
- 🤖 Prompt Injection 방어를 불변 플랫폼 정책, 비신뢰 입력·문서 구분, 서버 강제 `boothId + agentId + READY` 범위, 시스템 프롬프트·비밀정보 비노출과 공격 테스트 기준으로 구체화
- 🤖 spec 004 D03의 임대 만료 시 “짧은 유예”를 spec 008 C-09·Issue #14의 확정 계약과 동기화 — 이미 진행 중인 AI 응답 1건만 기존 전체 응답 제한 안에서 최대 60초까지 완료하고 이후 신규 질문 차단
- 🤖 GitHub Issue #14 Backend 답변을 spec 008 C-09와 AI API 명세에 반영 — Conversation 생성 시 Spring 1회 검증·응답 본문 `leaseEndsAt` 전달, 이후 FastAPI UTC 판정, 검증 timeout 1초·1회 재시도·최종 Fail Closed, 진행 중 응답 1건만 기존 최대 60초 완료, 이후 `BOOTH_LEASE_EXPIRED` 차단으로 확정
- 🤖 Backend `origin/back` 구현을 확인해 임대 만료는 조회 시 논리 판정하고 진행 중 AI SSE 유예 계약은 미구현임을 확인. Backend–FastAPI 협의를 위한 GitHub Issue #14 생성 후 spec 008 C-09에 연결
- 🤖 spec 007에 Issue #11 재시작 복구 계약을 반영하고 `plan.md`, `research.md`, `data-model.md`, OpenAPI 계약 2종, `quickstart.md`, 요구사항 체크리스트를 생성. Worker lease 만료(`RETRY_WAIT`)와 부스 임대 만료(`CANCELLED → DISABLED`)를 분리하고, callback 재전송·청크 원자 교체·최소 DB 권한까지 구현 설계 확정
- 🤖 GitHub Issue #11의 Backend 재시작 복구 권고안을 검토하고 동일 RDS `ai` 스키마, Spring/FastAPI 상태 소유권 분리, lease·heartbeat·재시도, 내부 상태 콜백, FK·트랜잭션 정책을 확정 회신. 기존 임대 만료 계약을 위해 `JobStatus.CANCELLED`를 유지하고 `DocumentStatus.DISABLED`로 매핑
- 🤖 spec 007 Clarifications C-01~C-07 확정 반영 — C-03은 동일 Agent SHA-256 중복 재처리 방지·기존 `documentId` 지정 교체로 변경하고, 나머지는 권장안을 확정값으로 구체화. 서버 재시작 복구 저장 방식은 Issue #11 후속 협의로 유지
- 🤖 서버 재시작·heartbeat 저장소와 관련하여 논의 필요. Backend의 `ai_documents.processing_status`, `ai_document_chunks`, pgvector 스키마를 확인한 뒤, FastAPI 재시작 복구 계약 협의를 위한 GitHub Issue #11 생성
- 🤖 spec 007 Edge Case 3건 검토 — 임대 만료 시 원본·메타데이터는 보존하되 처리 중단 및 검색 비활성화, 서버 재시작 시 stale `PROCESSING` 작업 재큐잉·재시도 상한 후 `FAILED`, 다량 문서는 `boothId + agentId + READY` 선필터·문서/총량 상한·최대 허용량 성능시험으로 결정 권장. 확정된 임대 만료·다량 문서 정책과 `DocumentStatus`/`JobStatus` 상태 모델을 반영
- 🤖 spec 007 C-03 동일 문서 재업로드 정책 검토 — 파일명이 아닌 파일 바이트의 SHA-256 해시를 AI Agent 범위에서 비교해 완전 중복을 판정하는 방안을 권장
- 🤖 완전 중복은 재처리하지 않고 기존 문서를 안내하며, 내용이 달라진 개정본 교체는 자동 중복 판정이 아니라 기존 `documentId`를 지정하는 명시적 교체 흐름으로 분리해야 함을 확인
- 🤖 FE 공통 계약 `festa-frontend/src/shared/types/overlay.ts`, `festa-frontend/src/unity/bridge/events.ts`를 spec 006/008 및 AI API 명세와 대조 검토
- 🤖 Issue #2 `AI_AGENT_INTERACT { boothId, objectId, configId }` 송신을 최종 확정함. 잔여 회신은 AI 파트 동의와 FE의 `configId → agentId` 매핑·`events.ts` 반영 요청으로 축소
