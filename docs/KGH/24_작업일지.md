# 작업일지

## 2026-08-31

- 🤖 `S15P21A604-123` [AI] PDF 파싱·청킹 구현 — TDD로 `festa-ai/app/providers/pdf_parser.py`(T045)와 `festa-ai/app/services/text_chunker.py`(T047) 신규 작성. 스캔 PDF는 `ScannedDocumentError`로 구분해 실패시키고, chunk_size·overlap은 `S15P21A604-92` 스파이크 실측값이 `research.md`에 반영되기 전까지 코드에 기본값을 고정하지 않고 호출자 전달을 강제
- 🤖 착수 전 확인 — `festa-ai`는 git 추적 파일이 0개였고(pycache 잔재만 존재), 진행 중인 다른 worktree/branch(`feat/S15P21A604-121-document-process-api`, `feat/S15P21A604-95-ai-db-migration` 등)에도 PDF 파싱·청킹 구현은 없었음을 확인. `spikes/ai-rag`(S15P21A604-92)의 스파이크 코드는 참고만 하고 그대로 재사용하지 않음(Spring 소유권·SHA-256 dedupe 등 spec 007 요구사항 미충족)
- 🤖 `worktree-festa-ai-scaffold-config`를 새 worktree(`.claude/worktrees/S15P21A604-123-pdf-chunking`, branch `worktree-S15P21A604-123-pdf-chunking`)에 병합해 FastAPI 골격(T001~T007) 위에서 작업. Docker `python:3.12-slim` 컨테이너로 pytest 실행(로컬에 conda/python 미설치)
- 🤖 업로드 API·Job/DB 연동·S3 저장은 범위에서 제외 — tasks.md 후속 항목(T048 등)으로 남김
- 📎 관련 Jira: https://ssafy.atlassian.net/browse/S15P21A604-123

## 2026-08-21

- 🤖 현재 문서 변경을 spec 008 대화 계약, spec 007 R2 저장소 계약, KGH 작업 기록 세 그룹으로 분리해 `ai` 브랜치에 반영하고 브랜드·비주얼 변경은 제외
- 🤖 GitHub Issue #51의 `Cloudflare R2 1차 / S3-compatible fallback` 결정을 확인하고 FastAPI는 기존 S3 호환 SDK를 유지한 채 endpoint·bucket·자격증명만 교체할 수 있어 기술적 이견이 없음을 검토
- 🤖 Issue #51의 R2 전환 계약을 spec 007의 FR-010·Document 필드·C-07과 계획·데이터 모델·OpenAPI 계약·검증 문서에 반영하고, `s3Key/s3_key`를 `objectKey/object_key`로 중립화. AI API·시스템 설계·공통 정책·Backend API·DB·AI SDD의 동일 계약도 동기화
- 🤖 GitHub Issue #51에 AI 측 동의, FastAPI의 R2 endpoint 전환 가능, 저장소 중립 필드명과 Spring 소유권 유지 방침을 회신
- 📎 관련 이슈: https://github.com/kanghyunsoon/ssafesta/issues/51
- 👤 AI 상담은 코인을 차감하지 않는 무료 이용으로 확정
- 🤖 AI 상담 과금·결제·환불·수익배분 관련 요구사항과 미확정 논의를 spec 008·012·003 및 기획/API/QA/ADR 문서에서 제거하고, spec 012를 장식 구매·인벤토리 중심으로 정리
- 📎 트러블슈팅: T-96
- 🤖 GitHub Issue #32에 C-08 최종 확정 답변 게시 — `sourceUrl` P0 생략, 오류별 `retryable`·`timeoutPhase`, 동일 Conversation 재시도, `data.type`, `handoffRecommended` P0 무동작, `sequence` 처리 계약을 Frontend에 회신
- 🤖 GitHub Issue #32 Frontend 답변 확인 — 공통 envelope·sequence 무결성 검증·재시도 UX·TypeScript union 계약에 동의하고 FE 서명 완료, 추가 요청 5건을 검토
- 🤖 C-08 최종 확정 — `data.type` 채택, P0 `sourceUrl`·handoff UI 생략, `LLM_TIMEOUT.timeoutPhase`와 오류별 `retryable` 확정, 같은 Conversation에서 새 요청·메시지 ID로 재시도하며 실패 turn은 이력에 확정 저장하지 않는 계약을 spec 008·AI Server API 명세에 반영
- 📎 트러블슈팅: T-95
- 👤 spec 008 C-06 확정 — 게스트는 AI 상담 이용 불가
- 🤖 게스트 요청은 로그인 안내 후 Conversation 생성·검색·LLM 호출을 모두 차단하도록 User Story·Edge Case·FR-019·FR-027·SC-011·Clarifications에 반영
- 👤 spec 008의 나머지 AI 파트 단독 결정 항목은 권장안으로 확정
- 🤖 컨텍스트 8,000토큰 조립 우선순위, 사용자·Agent 즉시 429와 전체 한도 FIFO 대기열 경계, 명시적 세션 종료+30분 TTL, 첫 token 15초 이내 95%·근거 source 100% 목표를 spec 008과 AI Server API 명세에 반영. 기획·BE·FE 합의 대상인 C-07 과금과 C-08 SSE payload는 미확정 유지
- 🤖 spec 008 확정 전 잔여 항목을 점검해 C-08 SSE payload가 AI와 Frontend Main의 합의가 필요한 핵심 계약임을 확인하고, 메인 FE 담당자와 논의할 공통 envelope·이벤트별 필드·순서·오류 처리 제안안을 작성
- 👤 `[front] spec 008 C-08 SSE 이벤트 payload 계약 확정 요청` GitHub Issue #32 생성
- 🤖 Issue #32의 FE 답변 전까지 C-08은 미확정 상태로 유지하며, 합의 후 spec 008·AI Server API 명세·FE 공통 SSE 타입·계약 테스트를 함께 갱신하기로 정리
- 📎 관련 이슈: https://github.com/kanghyunsoon/ssafesta/issues/14, https://github.com/kanghyunsoon/ssafesta/issues/32

## 2026-08-20

- 🤖 spec 008 리뷰 ② FR-003 격리 테스트 방법 구체화 — 실제 PostgreSQL+pgvector 고정 Fixture와 유일 검증 코드를 사용해 검색 결과, Conversation Scope 위조, Prompt Injection, 조립된 LLM 입력, SSE source/답변, 50개 혼합 동시 요청을 자동 검증하고 1건 유출 시 CI 배포 차단하는 프로토콜 확정
- 🤖 spec 008 Edge Case 정책 구체화 — 대화 원문은 휘발성 저장소에서 세션 종료/30분 유휴 시 삭제, 질문 2,000자·입력 8,000토큰·최근 왕복 6회, 공유 LLM 풀의 사용자/Agent/전체 동시성·대기열 한도, 문서 미준비 시 LLM 미호출 안내를 확정 반영
- 🤖 Prompt Injection 방어를 불변 플랫폼 정책, 비신뢰 입력·문서 구분, 서버 강제 `boothId + agentId + READY` 범위, 시스템 프롬프트·비밀정보 비노출과 공격 테스트 기준으로 구체화
- 🤖 GitHub Issue #14 Backend 답변을 spec 008 C-09와 AI API 명세에 반영 — Conversation 생성 시 Spring 1회 검증·응답 본문 `leaseEndsAt` 전달, 이후 FastAPI UTC 판정, 검증 timeout 1초·1회 재시도·최종 Fail Closed, 진행 중 응답 1건만 기존 최대 60초 완료, 이후 `BOOTH_LEASE_EXPIRED` 차단으로 확정
- 🤖 Backend `origin/back` 구현을 확인해 임대 만료는 조회 시 논리 판정하고 진행 중 AI SSE 유예 계약은 미구현임을 확인. Backend–FastAPI 협의를 위한 GitHub Issue #14 생성 후 spec 008 C-09에 연결
- 🤖 spec 007에 Issue #11 재시작 복구 계약을 반영하고 `plan.md`, `research.md`, `data-model.md`, OpenAPI 계약 2종, `quickstart.md`, 요구사항 체크리스트를 생성. Worker lease 만료(`RETRY_WAIT`)와 부스 임대 만료(`CANCELLED → DISABLED`)를 분리하고, callback 재전송·청크 원자 교체·최소 DB 권한까지 구현 설계 확정
- 🤖 GitHub Issue #11의 Backend 재시작 복구 권고안을 검토하고 동일 RDS `ai` 스키마, Spring/FastAPI 상태 소유권 분리, lease·heartbeat·재시도, 내부 상태 콜백, FK·트랜잭션 정책을 확정 회신. 기존 임대 만료 계약을 위해 `JobStatus.CANCELLED`를 유지하고 `DocumentStatus.DISABLED`로 매핑
- 🤖 spec 007 Clarifications C-01~C-07 확정 반영 — C-03은 동일 Agent SHA-256 중복 재처리 방지·기존 `documentId` 지정 교체로 변경하고, 나머지는 권장안을 확정값으로 구체화. 서버 재시작 복구 저장 방식은 Issue #11 후속 협의로 유지
- 🤖 서버 재시작·heartbeat 저장소와 관련하여 논의 필요. Backend의 `ai_documents.processing_status`, `ai_document_chunks`, pgvector 스키마를 확인한 뒤, FastAPI 재시작 복구 계약 협의를 위한 GitHub Issue #11 생성
- 🤖 spec 007 Edge Case 3건 검토 — 임대 만료 시 원본·메타데이터는 보존하되 처리 중단 및 검색 비활성화, 서버 재시작 시 stale `PROCESSING` 작업 재큐잉·재시도 상한 후 `FAILED`, 다량 문서는 `boothId + agentId + READY` 선필터·문서/총량 상한·최대 허용량 성능시험으로 결정 권장. 확정된 임대 만료·다량 문서 정책과 `DocumentStatus`/`JobStatus` 상태 모델을 반영
- 🤖 spec 007 C-03 동일 문서 재업로드 정책 검토 — 파일명이 아닌 파일 바이트의 SHA-256 해시를 AI Agent 범위에서 비교해 완전 중복을 판정하는 방안을 권장
- 🤖 완전 중복은 재처리하지 않고 기존 문서를 안내하며, 내용이 달라진 개정본 교체는 자동 중복 판정이 아니라 기존 `documentId`를 지정하는 명시적 교체 흐름으로 분리해야 함을 확인
- 🤖 FE 공통 계약 `festa-frontend/src/shared/types/overlay.ts`, `festa-frontend/src/unity/bridge/events.ts`를 spec 006/008 및 AI API 명세와 대조 검토
- 🤖 Issue #2 `AI_AGENT_INTERACT { boothId, objectId, configId }` 송신을 최종 확정함. 잔여 회신은 AI 파트 동의와 FE의 `configId → agentId` 매핑·`events.ts` 반영 요청으로 축소
