# 트러블슈팅 모음

> **운영 규칙 (필수)**
> 1. 작업 중 문제가 발생하면 **해결 여부와 무관하게 즉시 여기 등록**한다. "나중에 정리"는 없다 — 그 자리에서 T-번호를 따고 한 줄이라도 적는다.
> 2. 형식: 증상 / 원인 / 해결 / 예방. 원인 미상이면 원인란에 "조사 중"으로 두고 해결되면 갱신한다.
> 3. `24_작업일지.md`에서 T-번호로 참조한다.
> 4. 같은 증상이 재발하면 새 항목을 만들지 말고 기존 항목에 재발 기록을 추가한다.

---

## Unity 에디터 / 씬

### T-01. 캡슐·오브젝트가 납작한 원반/종잇장으로 보임
- **증상**: 플레이어 캡슐이 파란 원반처럼 찌부러져 렌더링
- **원인**: "NetworkTransform의 Scale 동기화 체크 해제" 지시를 **Transform 컴포넌트의 Scale 값에 0 입력**으로 오해 → Scale (0,1,0)
- **해결**: 프리팹/부모 오브젝트 Transform Scale을 (1,1,1)로 복원
- **예방**: Scale "체크 해제"는 NetworkTransform의 Syncing 체크박스를 뜻함. Transform 숫자값 아님. 부모 오브젝트 Scale도 자식 전체를 찌그러뜨리므로 함께 확인

### T-02. 알록달록한 소용돌이/얼룩이 떠다님 (글자 깨짐)
- **증상**: 이름표·라벨 위치에 글리치 텍스처 얼룩
- **원인**: **Unity 6는 런타임 생성 TextMesh에 기본 폰트를 자동 지정하지 않음**
- **해결**: `Resources.GetBuiltinResource<Font>("LegacyRuntime.ttf")` 지정 + MeshRenderer material에 font.material 할당
- **예방**: 코드로 TextMesh를 만들면 폰트 지정 필수 (NetworkPlayer, BoothObjectFactory에 반영됨)

### T-03. 라벨 글자가 찌그러져 보임
- **증상**: 비디오 스크린 등 위의 라벨이 눌린 형태
- **원인**: 비균등 스케일(2.4×1.4×0.1) 오브젝트의 **자식**으로 라벨을 붙여 부모 스케일이 글자에 전파
- **해결**: 라벨 localScale에 부모 스케일의 역수 적용
- **예방**: 비균등 스케일 오브젝트의 자식에 텍스트/UI를 두지 말거나 역스케일 보정

### T-04. 오브젝트가 바닥에 절반 묻힘
- **증상**: 스폰된 캡슐/부스 도형의 하반이 지면 아래
- **원인**: Unity primitive는 **피벗이 중심** — Y=0에 스폰하면 절반이 묻힘
- **해결**: 스폰 위치에 절반 높이 가산 (Player 스폰 Y=1, BoothObjectFactory groundLift)
- **예방**: 프리팹 제작 시 피벗을 바닥 기준으로 만들거나, 코드 스폰 시 높이 보정

### T-05. 코드를 고쳤는데 실행 결과가 그대로
- **증상**: 파일은 갱신됐는데 Play 결과 불변
- **원인**: 에디터가 Play 중이거나 포커스를 안 받아 재컴파일이 안 됨 (외부에서 파일이 변경된 경우)
- **해결**: Play 정지 → Unity 창 포커스 → Ctrl+R → 우하단 스피너 종료 후 Play
- **예방**: 외부(AI 세션 등)에서 파일을 수정하면 반드시 Play 정지 상태에서 Refresh
- **2026-08-14 재확인**: 플레이 중 스크립트 컴파일 뒤 첫 캡처가 이전 의상 레일을 유지했다. `Stop → Play`로 도메인을 새로 시작한 뒤 왼쪽 탭형 UI가 반영된 화면을 다시 검증했다.

### T-14. 자기 캡슐이 화면에 안 보임
- **증상**: 접속은 되는데 캡슐이 화면 밖
- **원인**: 카메라가 고정이라 스폰 위치를 안 비춤
- **해결**: `PlayerCameraFollow` (Owner 전용 3인칭 추적) 프리팹에 추가
- **예방**: POC용 — 정식 카메라 워크는 spec 확정 후 교체

## Web(WebGL) 빌드

### T-06. 빌드했더니 빈 씬만 나옴 (HUD/부스/바닥 없음)
- **증상**: 브라우저에 스카이박스만 렌더링
- **원인**: Build Profiles **Scene List**에 Main 씬 누락/순서 문제, 또는 씬 미저장(`main*`) 상태로 빌드
- **해결**: Scene List에 Main만 남기고 맨 위 + 씬 저장 후 재빌드
- **예방**: 빌드 전 타이틀바 `*`(미저장) 확인 습관

### T-07. WebGL에서 부스/AI Mock이 영원히 로딩 안 됨
- **증상**: `[BoothRuntime] built` 로그가 안 찍힘. 에디터는 정상
- **원인**: **`Task.Delay`는 WebGL(단일 스레드)에서 완료되지 않음**
- **해결**: Mock 3종을 `Awaitable.WaitForSecondsAsync`로 교체
- **예방**: WebGL 타깃 코드에서 Task.Delay/스레드 API 금지. Unity `Awaitable` 사용
- **부수효과**: 에디터의 무해한 `TaskCanceledException`도 함께 사라짐

### T-08. 런타임 생성 오브젝트가 전부 마젠타(핑크)
- **증상**: 에디터는 정상, 빌드에서만 핑크
- **원인**: `CreatePrimitive` 기본 머티리얼의 셰이더가 빌드에 포함되지 않음
- **해결**: `Shader.Find("Universal Render Pipeline/Lit")`로 머티리얼 명시 생성 (씬 머티리얼이 이미 셰이더를 빌드에 포함시키므로 Find 가능)
- **예방**: 런타임 생성 렌더러는 항상 명시적 셰이더/머티리얼 사용

### T-09. 빌드에서 IMGUI(OnGUI) 패널이 아예 안 보임
- **증상**: 에디터에선 보이는 Dev HUD가 브라우저에서 사라짐
- **원인**: **Active Input Handling이 "Input System Package (New)"만이면 플레이어 빌드에서 OnGUI가 렌더링되지 않음**
- **해결**: Project Settings > Player > Other Settings > Configuration > Active Input Handling = **Both** (에디터 재시작)
- **예방**: DevConnectionHud를 정식 uGUI로 교체하면 원복 검토

### T-10. HUD에 웹페이지 주소를 넣어 접속 실패
- **증상**: Connect 무반응
- **원인**: 웹페이지 http 포트(예: 62385/8000)와 **게임 서버 포트(7777)** 혼동
- **해결**: 브라우저 주소창 = 웹페이지(localhost:8000), HUD Addr/Port = 게임 서버(127.0.0.1:7777)
- **예방**: 정식 구현에서는 world-sessions API가 endpoint를 내려주므로 수동 입력 자체가 사라짐

### T-13. Build And Run 포트가 매번 바뀌고 플랫폼에 묶임
- **증상**: 테스트 URL 고정 불가, Linux Server로 스위치하면 웹 서빙 불가
- **원인**: Build And Run은 에디터 임시 서버 (랜덤 포트, 에디터 종속)
- **해결**: Web Publishing Settings **Compression Format = Disabled** (개발용) 후 빌드 → `python -m http.server 8000 --directory Builds/web` → localhost:8000 고정
- **예방**: 빌드 산출물은 에디터와 무관하게 서빙 가능. 실배포는 Brotli + CloudFront 헤더로 복원 (deployment-handoff.md)

### T-17. `python -m http.server --directory`로 띄웠는데 404
- **증상**: 빌드 폴더에 index.html이 있는데도 브라우저가 404
- **원인**: PowerShell의 현재 위치가 달라 상대경로 `--directory Builds/web-...`가 엉뚱한 폴더를 가리킴
- **해결**: 대상 폴더로 `cd` 후 `python -m http.server <port>` (옵션 없이) 또는 절대경로 지정
- **예방**: 서버 실행 후 브라우저 접속 시 PowerShell에 요청 로그가 찍히는지로 정상 서빙 확인

### T-18. Sidekick 툴 등 에디터 창이 안 열림 (`Attempted to open unsupported window in cloned player systems`)
- **증상**: Synty 툴 창이 열리지 않고 `NullReferenceException` + 이어서 `Failed to load tool data`
- **원인**: **Multiplayer Play Mode의 가상 플레이어(Player 2/3)가 활성 상태**이면 MPPM이 복제 에디터에서 에디터 창 열기를 차단한다
- **해결**: Play 정지 → Window > Multiplayer > Multiplayer Play Mode → Player 2/3 **체크 해제** → **Unity 재시작**(부트스트랩이 이미 꼬였으면 필수)
- **예방**: 에디터 툴(Sidekick 등) 작업 시엔 가상 플레이어를 끈다. 멀티 테스트할 때만 켠다

### T-19. `WebAssembly streaming compilation failed` / Content-Encoding 에러
- **증상**: 브라우저에서 빌드 로드 실패
- **원인**: 빌드가 압축(Brotli/Gzip)돼 있는데 로컬 서버가 `Content-Encoding` 헤더를 안 보냄 (또는 반대)
- **해결**: Build Profiles > Web > Player Settings > Publishing Settings > **Compression Format = Disabled** 후 재빌드 (개발용). 실배포는 Brotli + CloudFront 헤더 설정
- **확인법**: `Builds/web/Build/`에 `.br`/`.gz` 확장자가 있으면 압축된 것
- **부가**: `Tools/serve.py` 사용 시 wasm MIME·캐시 문제도 함께 예방됨

### T-20. `LinkError: wasi_snapshot_preview1 / fd_sync` (Sidekick SQLite)
- **증상**: WebGL 빌드가 아예 로드되지 않음
- **원인**: Sidekick의 SQLite 플러그인이 요구하는 WASI 호출을 런타임이 제공하지 못함. **재빌드 후 정상 동작** — 정확한 트리거는 미확정 (스테일 빌드/캐시 가능성)
- **해결**: 재빌드 + 캐시 무시 새로고침으로 해소됨
- ⚠️ **판정 정정**: 이 에러를 근거로 "Sidekick 런타임은 WebGL 불가, Synty 폴더 삭제 필요"라고 결론 냈으나 **오판**이었다. 근거:
  - Synty 체인지로그 v1.0.2 *"Updated SQLite build targets to allow Web build targets"*
  - `sqlite3.c.meta`에 `WebGL: enabled: 1`
  - `DatabaseManager`는 플레이어 빌드에서 파일 I/O를 쓰지 않음 (TextAsset → `:memory:` Deserialize)
  - 실제로 Synty를 둔 채 WebGL 빌드 성공
- **교훈**: 에러 하나로 "플랫폼 미지원"을 단정하지 말 것. 벤더 문서·플러그인 메타·실제 재현을 교차 확인한다

### T-23. 코드를 고쳤는데 브라우저 결과가 그대로 / 옛 외형이 나옴
- **증상**: 에디터에선 새 기능이 되는데 브라우저는 이전 모습
- **원인**: **Web 빌드를 다시 하지 않음.** 브라우저는 빌드 산출물만 보므로 에디터 변경이 자동 반영되지 않는다
- **해결**: Build Profiles → Scene List에 Main 씬만 체크 → Build → `Builds/web` 덮어쓰기 → Ctrl+Shift+R
- **예방**: 빌드 전 씬 저장(`Ctrl+S`, 타이틀 `*` 없음) 확인. `Tools/serve.py`는 캐시를 차단하므로 새로고침만으로 반영됨

## 코드/에셋 규칙

### T-21. SerializeField 이름을 바꿨더니 Inspector 값이 전부 사라짐
- **증상**: ScriptableObject/프리팹에 입력해둔 값이 빈 값으로 초기화됨 (AvatarCatalog의 preset 코드 소실)
- **원인**: Unity 직렬화는 **필드 이름 기준**. `avatarCode` → `presetCode`로 리팩터링하면 기존 asset 데이터와 매칭이 끊긴다
- **해결**: `[FormerlySerializedAs("이전이름")]` 부착 (`using UnityEngine.Serialization;`)
- **예방**: 직렬화 필드 이름 변경 시 **항상** FormerlySerializedAs를 함께 붙인다. 이미 값이 날아갔다면 붙인 뒤 재입력

### T-24. 커스터마이징 Custom 버튼이 무반응 — 인코딩 길이 초과가 조용히 삼켜짐 (2026-08-12)
- **증상**: HUD의 Custom 버튼을 눌러도 아무 변화 없음. 에러도 안 보임. 서버(Docker)를 재빌드해도 동일
- **원인**: `AvatarAppearance.Encode()`가 500자 초과 시 **조용히 `sk_01`을 반환**하도록 되어 있었음. Sidekick 파츠 이름이 길어(파츠당 ~25자 × 16종) 런타임 조립 인코딩이 500자를 넘으면, 서버에 "현재와 같은 값"이 전송되어 OnValueChanged가 발화하지 않음 → 완전한 무반응
- **해결**:
  1. 동기화 필드를 `FixedString512Bytes` → **`FixedString4096Bytes`**로 확대, `MaxEncodedLength` 500 → 3800
  2. Encode의 몰래 되돌리기 제거 — 초과 판단은 호출자(RequestChange)가 하고 거부 사유를 `LastRequestError`로 노출
  3. HUD가 `LastRequestError`를 **화면에 표시** (콘솔을 안 봐도 원인이 보이게)
  4. `SyncMirrorParts`의 이름 치환 실패 시 인덱스 매칭 폴백 (오른팔/오른다리 소실 방지)
- **주의**: NetworkVariable **타입이 바뀌었으므로 Web 빌드와 Linux Server(Docker) 빌드를 반드시 둘 다 다시** 해야 한다 (한쪽만 바꾸면 직렬화 불일치)
- **교훈**: 실패를 조용히 기본값으로 대체하는 코드는 "무반응 버그"가 된다. 거부/실패는 반드시 사용자 또는 로그에 드러낸다

#### T-24 후속 — "클라만 새 빌드, 서버는 옛 빌드"의 함정
- **증상**: 클라 로그에는 `[MockUserApi] 아바타 저장(모의): rt|1=SK_HUMN...`처럼 **긴 인코딩이 정상 생성**되는데, `[AvatarVisual] 적용`은 계속 `runtime=False`
- **판독법**: 이 조합은 "클라는 새 코드 / 서버는 옛 코드"의 지문이다
  - 클라(새 코드)는 600자 인코딩을 만들어 RPC 전송 ✅
  - 서버(옛 코드)의 `RequestChangeServerRpc`가 `Decode(...).Encode()`로 재인코딩 → 옛 500자 제한에 걸려 **`sk_01` 반환** → 현재 값과 동일 → `OnValueChanged` 미발화 → 화면 무변화
- **해결**: Linux Server 빌드 → 이미지 재빌드 → 컨테이너 교체 (Web 빌드와 **항상 쌍으로**)
- **재발 방지 (코드에 반영)**:
  1. 서버 RPC에서 **재인코딩 제거** — 길이 검증 후 받은 값을 그대로 기록
  2. 클라가 요청 후 2초 내 반영이 없으면 HUD에 `server did not apply the change — 서버(Docker) 빌드가 최신인지 확인` 표시
- **서버 로그 확인법**: 브라우저 콘솔에는 서버 로그가 안 나온다. `docker logs -f festa-world`로 `[Appearance] 서버 반영: client=.. len=..`이 찍히는지 본다 (안 찍히면 서버가 옛 빌드)

### T-25. 서버를 새로 빌드했는데 계속 옛 동작 — 이름 다른 옛 컨테이너가 포트를 점유 (2026-08-12)
- **증상**: Linux Server 재빌드·이미지 재생성을 반복해도 서버 동작이 그대로. `docker run` 시 `Bind for 0.0.0.0:7777 failed: port is already allocated`, `docker rm -f festa-world` 는 `No such container`
- **원인**: 실제로 돌고 있는 컨테이너 이름이 **`festa-world-01`**(README 표기)인데 `festa-world`로 지우려 해서 제거 실패. 옛 컨테이너가 계속 7777을 점유한 채 **옛 코드로 응답**하고 있었다
- **해결**:
  ```powershell
  docker ps                                   # 실제 이름 확인
  docker rm -f festa-world-01 festa-world     # 전부 제거
  ```
- **예방**: 서버 갱신은 항상 `docker ps` → `rm -f` → `build` → `run` 순서. 컨테이너 이름을 **`festa-world-01` 하나로 통일**한다 (Docker/README.md 기준)

### T-26. Docker 빌드가 `chmod: cannot access '/app/festa-unity.x86_64'`로 실패 (2026-08-12)
- **증상**: `[6/6] RUN chmod +x /app/festa-unity.x86_64` 단계에서 no such file. 로그의 `transferring context: 3.57kB`가 단서
- **원인**: **빌드 컨텍스트를 `Docker/` 폴더로 지정**했다. 이 Dockerfile은 `COPY . /app`으로 컨텍스트 전체를 복사하므로 컨텍스트가 **Linux Server 빌드 출력 폴더**여야 한다
- **해결**: `docker build -t festa-world:dev -f Docker/Dockerfile Builds/linux-server` (festa-unity 폴더에서 실행)
- **판별법**: `transferring context`가 수 KB면 실행 파일이 안 들어간 것 (정상은 수십~수백 MB)

### T-27. 파츠를 바꾸면 캐릭터가 T포즈로 굳는다 (2026-08-12)
- **증상**: Custom 모드로 파츠를 조립하면 외형은 정상인데 애니메이션이 전혀 재생되지 않고 T포즈. 프리셋 모드에서는 정상
- **원인**: Sidekick `SidekickRuntime.CreateCharacter()`는 생성한 캐릭터에 Animator를 붙이고 **avatar(휴머노이드 정의)까지 복사**하지만, `runtimeAnimatorController`는 **생성자에 넘긴 컨트롤러가 있을 때만** 설정한다.
  ```csharp
  // Synty 원본 (SidekickRuntime.cs)
  Animator newModelAnimator = newSpawn.AddComponent<Animator>();
  newModelAnimator.avatar = baseModelAnimator.avatar;
  newModelAnimator.Rebind();
  if (_currentAnimationController != null)          // ← 우리는 null로 생성했다
      newModelAnimator.runtimeAnimatorController = _currentAnimationController;
  ```
  우리는 `new SidekickRuntime(baseModel, material, null, _db)`로 만들었으므로 **Controller가 비어 있는 Animator**가 생성된다 → 재생할 상태 머신이 없어 바인드 포즈(T포즈) 그대로 멈춘다
- **해결**:
  1. `PlayerAvatarVisual`의 인스펙터 슬롯 **Animator Controller에 `AvatarAnimator` 할당** (근본 해결)
  2. 코드: 슬롯이 비어 있으면 **Player 루트 Animator의 Controller를 폴백**으로 사용하고, 그것도 없으면 콘솔에 원인을 명시
- **확인 사항**: 애니메이션 FBX(Idle/Walking)의 **Rig가 Humanoid**여야 리타게팅된다. Generic이면 다른 스켈레톤에 적용되지 않는다. Animator 인스펙터의 `Clip Count: 0`은 컨트롤러에 클립이 없다는 신호
- **교훈**: 외부 SDK가 컴포넌트를 "붙여준다"고 해서 **설정까지 채워주는 건 아니다**. 생성 직후 상태를 확인하고 부족분을 우리 코드가 메운다

### T-22. WebGL 빌드에서 IMGUI 한글이 안 보임
- **증상**: 에디터에선 보이는 HUD 한글 라벨이 브라우저에서 공백
- **원인**: IMGUI 기본 폰트에 한글 글리프가 없음 (WebGL 빌드에서 시스템 폰트 폴백 불가)
- **해결(임시)**: POC HUD는 **영문 라벨** 사용
- **정식 해결**: uGUI/TMP + 한글 폰트 에셋, 또는 계획대로 React 오버레이로 이관 (ADR 결정 4)

## Docker / 빌드

### T-11. `docker build` 시 "failed to connect to the docker API"
- **증상**: npipe 연결 실패 에러
- **원인**: Docker CLI만 있고 **Docker Desktop(데몬)이 실행 중이 아님**
- **해결**: Docker Desktop 실행 → "Engine running" 확인 후 재시도
- **예방**: Settings > General > 로그인 시 자동 시작 켜기

### T-12. Linux 빌드 대화상자에서 폴더 선택이 안 됨
- **증상**: 폴더를 고르는 UI가 아님
- **원인**: Linux 빌드는 폴더가 아니라 **실행 파일 이름**(*.x86_64)을 입력받음
- **해결**: 파일 이름에 `festa-unity` 입력 → 저장

## 코드 규칙 (사전 예방으로 잡은 것)

### T-15. NGO NetworkVariable은 필드로만 선언
- 프로퍼티(`{ get; }`)로 선언하면 ILPP(코드 생성)가 인식하지 못할 수 있음 → `public readonly` 필드 사용

### T-16. (관찰) `[EmoteId] Write permissions` 에러
- 1회 관측 후 재현 안 됨. 재발 시 전체 스택 캡처해서 이 항목 갱신 — **원인 조사 중**

---

<!-- 새 트러블은 해당 카테고리에 T-번호 이어서 추가 -->

### T-38. 브랜치 전환을 막는 `.specify` 폴더 삭제 요청이 안전 정책상 거부됨 (2026-08-13)
- **증상**: `game`에서 `ai`로 전환하기 전에 untracked `.specify`를 재귀 삭제하려던 작업이 거부됨
- **원인**: `.specify`는 프로젝트 운영 파일이며, Unity 제거 요청은 해당 디렉터리 삭제 권한을 포함하지 않음
- **해결**: 파일을 삭제하지 않고 `work/specify-branch-switch-backup`으로 이동해 보존한 뒤 브랜치 전환
- **예방**: 브랜치 전환을 막는 추적 파일은 삭제 대신 명시적인 임시 백업 위치로 이동한다

### T-39. Unity 에디터가 사용 중인 잔여 폴더를 이동하지 못함 (2026-08-13)
- **증상**: 추적 파일 제거 후 남은 Unity 캐시를 `work`로 이동하려 했으나 다른 프로세스가 파일을 사용 중이라 실패
- **원인**: 열린 Unity 에디터가 프로젝트의 캐시 또는 패키지 파일 핸들을 보유함
- **해결**: 잔여 파일은 삭제하지 않고 작업 트리에 그대로 보존했으며, Git 브랜치에서는 추적 파일이 0개인지 별도로 검증
- **예방**: Unity 프로젝트 폴더 자체를 이동해야 할 때는 에디터를 먼저 종료하고, 브랜치 정리는 `git ls-tree` 기준으로 검증한다

### T-40. 제거된 Sidekick 패키지 참조로 Unity 컴파일 실패 (2026-08-13)
- **증상**: Sidekick 패키지가 없는 `game` 브랜치에서 `Synty` 네임스페이스 관련 컴파일 오류 14건 발생
- **원인**: 과거 런타임 서비스와 HUD가 제거된 외부 패키지 타입을 계속 직접 참조
- **해결**: 신규 생성 경로를 Rukha93 Catalog/Assembler로 통일하고 Sidekick 런타임 서비스와 직접 호출을 제거. 과거 `rt` 문자열은 읽기 호환만 유지
- **예방**: 외부 패키지를 제거할 때 어셈블리 참조와 런타임 진입점을 함께 검색하고, 계약 문서의 활성 구현도 같은 커밋에서 갱신한다

### T-41. Shader Graph 지원 판정과 실제 렌더 결과 불일치 (2026-08-13)
- **증상**: `Shader.isSupported`가 true인 벤더 Shader Graph 머티리얼이 Game View에서 마젠타로 출력
- **원인**: API의 지원 판정만으로 현재 URP 렌더 패스의 실제 호환성을 보장할 수 없음
- **해결**: URP Lit 런타임 머티리얼로 변환하고 눈·입·눈썹 등 필요한 알베도와 노멀만 선택적으로 전달
- **예방**: 셰이더 호환성은 플래그뿐 아니라 실제 Game View 캡처로 확인한다

### T-42. 기본 머리 중복과 의상 혼합으로 목·피부 경계가 깨짐 (2026-08-13)
- **증상**: 교체 머리와 기본 HeadSlot이 겹쳐 목이 분리되어 보이고, 상하의와 한벌옷을 함께 켜면 피부/의상이 찢어진 듯 겹침
- **원인**: 기본 신체 머리를 유지한 채 교체 메시를 추가했고 서로 배타적인 의상 계열을 동시에 조립
- **해결**: 교체 머리 생성 성공 후에만 기본 HeadSlot을 숨기고, 한벌옷과 상·하의를 상호 배타 처리. 기본 외형은 완성형 한벌옷으로 시작
- **예방**: 신체 숨김은 대체 메시 생성 성공 여부를 기준으로 적용하고 의상 계열 호환 규칙을 조립 전에 정규화한다

### T-43. 없는 unitypackage의 고아 meta가 새로고침 때 반복 삭제됨 (2026-08-13)
- **증상**: 복원한 `.unitypackage.meta` 파일이 Unity 새로고침마다 다시 삭제됨
- **원인**: 대응하는 에셋 파일이 저장소에 없어 Unity가 고아 meta로 정리
- **해결**: 반복 복원을 중단하고 고아 meta 삭제를 정상 정리로 반영
- **예방**: meta 복원 전 대응 원본 에셋의 존재를 먼저 확인한다

### T-44. Unity 즉석 검증에서 Roslyn 백엔드 사용 불가 (2026-08-13)
- **증상**: 자동 검증 코드 실행 시 Roslyn 컴파일러를 사용할 수 없음
- **원인**: 현재 Editor AppDomain에 Microsoft.CodeAnalysis가 로드되지 않음
- **해결**: 동일한 읽기/검증 코드를 CodeDom 백엔드로 실행
- **예방**: 단순 검증 코드는 C# 6 호환으로 작성하고 Roslyn 부재 시 CodeDom으로 즉시 전환한다

### T-45. UI 가로 레이아웃이 정밀 색상 선택기 폭을 강제로 확장 (2026-08-13)
- **증상**: 채도·명도 면과 Hue 바가 지정 폭보다 커져 버튼 및 레이아웃이 어긋남
- **원인**: `HorizontalLayoutGroup`의 자식 폭 제어/강제 확장 기본값
- **해결**: 자식 폭·높이 강제 확장을 끄고 각 컨트롤의 명시적 LayoutElement 크기를 사용
- **예방**: 런타임 UI 레이아웃은 고정 크기 입력 컨트롤을 추가할 때 LayoutGroup 확장 옵션을 함께 검증한다

### T-46. Unity 카메라 캡처 액션 이름 오사용 (2026-08-13)
- **증상**: 최종 화면 캡처에서 `capture_screenshot` 액션이 지원되지 않는다는 오류 발생
- **원인**: 현재 Unity MCP 카메라 도구의 실제 액션명은 `screenshot`
- **해결**: 제공된 액션 목록을 확인하고 `screenshot`으로 재실행
- **예방**: 동적 MCP 도구는 호출 전 현재 스키마/액션 목록을 확인한다

### T-47. PowerShell에서 rg 경로에 Unix식 와일드카드 사용 실패 (2026-08-13)
- **증상**: `.../Avatar/*.asset` 경로 검색이 Windows 경로 구문 오류로 실패
- **원인**: 해당 호출에서 와일드카드가 파일 열거로 확장되지 않고 리터럴 경로로 전달됨
- **해결**: 디렉터리를 검색 대상으로 넘기고 ripgrep의 재귀 검색을 사용
- **예방**: Windows에서는 파일 glob보다 검색 루트 디렉터리와 필터 옵션을 사용한다

### T-48. CharacterLobby 재생성 때 정리 전 원본 프리팹이 Missing Script를 기록 (2026-08-13)
- **증상**: 최종 화면은 정상이나 CharacterLobby 생성 직후 Console에 `The referenced script (Unknown)` 1건 기록
- **원인**: 이미 정리된 프로젝트용 신체 프리팹이 있는데도 생성기가 벤더 원본을 먼저 인스턴스화하여, 누락 컴포넌트를 제거하기 전에 Unity가 오류를 기록
- **해결**: 프로젝트용 신체 프리팹이 존재하면 재사용하고 최초 생성 때만 원본을 정리해 복제
- **예방**: 벤더 변환 산출물은 멱등적으로 재사용하고, 매 빌드마다 문제가 있는 원본을 인스턴스화하지 않는다

### T-49. 고해상도 Game View 캡처 중 Unity MCP 세션 일시 단절 (2026-08-13)
- **증상**: `screenshot_super_size=2` 캡처 대기 중 Unity 플러그인 세션 연결이 끊김
- **원인**: 고해상도 캡처 처리 중 MCP 응답 세션이 종료됨
- **해결**: 일반 해상도 캡처로 즉시 재시도해 성공
- **예방**: UI 반복 검증은 기본 해상도로 수행하고 최종 산출물에만 고해상도 캡처를 사용한다

### T-50. Product Design 컨텍스트 확인 스크립트에서 python 명령 미인식 (2026-08-13)
- **증상**: 디자인 컨텍스트 preflight 실행 시 PowerShell이 `python` 명령을 찾지 못함
- **원인**: 시스템 PATH에 Python 실행 파일이 등록되지 않음
- **해결**: Codex 워크스페이스 의존성 정보를 조회하고 번들 Python의 절대 경로로 재실행
- **예방**: 문서·이미지 도구가 필요한 작업은 워크스페이스 번들 런타임 경로부터 확인한다

### T-51. Unity AssetPreview 썸네일이 마젠타 실루엣으로 생성됨 (2026-08-13)
- **증상**: 얼굴·헤어·의상 이미지 탭을 위해 만든 에디터 미리보기가 파츠 형태만 보이는 마젠타 이미지로 출력
- **원인**: AssetPreview가 벤더 Shader Graph를 그대로 사용하여 실제 Game View와 같은 셰이더 비호환 발생
- **해결**: `AvatarAssembler`의 검증된 URP 호환 머티리얼로 캐릭터를 임시 조립하고 전용 Camera/Light로 105개 파츠를 직접 렌더링
- **예방**: 외부 에셋 썸네일도 AssetPreview 결과를 그대로 신뢰하지 않고 실제 런타임 렌더 경로로 생성한다

### T-52. LayoutGroup이 카드 지정 크기를 무시해 거대한 네모 버튼으로 출력 (2026-08-13)
- **증상**: 38~76px로 지정한 이미지 탭과 카드가 기본 RectTransform 크기로 늘어나 패널을 가득 채움
- **원인**: `childControlWidth/Height=false`라 LayoutElement의 preferred 크기를 LayoutGroup이 적용하지 않음
- **해결**: 자식 크기 제어는 활성화하고 강제 확장만 비활성화하여 이미지 카드의 명시 크기를 적용
- **예방**: LayoutElement 기반 UI는 `childControl=true`, `childForceExpand=false` 조합을 기본으로 검증한다

### T-53. 잔여 마우스 휠 입력으로 캐릭터 기본 카메라 구도가 바뀜 (2026-08-13)
- **증상**: 얼굴 중심으로 맞춘 CharacterLobby 카메라가 플레이 직후 전신 또는 과도한 확대 구도로 바뀌어 최종 레이아웃 검증 결과가 일정하지 않았다.
- **원인**: 포인터가 UI 위에 있지 않으면 일반 마우스 휠 입력을 항상 카메라 줌으로 받아, 화면 조작 중 남은 스크롤 입력도 기본 구도에 반영됐다.
- **해결**: 카메라 줌은 `LeftShift + 마우스 휠` 조합에서만 동작하도록 제한하고 카테고리별 명시적 프리셋을 사용하도록 변경했다.
- **예방**: 커스터마이징 화면의 카메라 조작에는 수정 키 또는 전용 포인터 포커스를 요구하고, 최종 QA 전 기본 진입 구도를 별도로 재검증한다.

### T-54. 플레이 중 Game View 강제 갱신으로 PlayerLoop 재귀 오류 발생 (2026-08-14)
- **증상**: 최종 화면 캡처를 강제로 갱신하는 과정에서 `PlayerLoop internal function has been called recursively` 오류가 기록되고 Unity MCP 연결이 일시적으로 끊겼다.
- **원인**: 플레이 모드에서 `EditorApplication.QueuePlayerLoopUpdate()`와 `InternalEditorUtility.RepaintAllViews()`를 함께 호출해 실행 중인 PlayerLoop 안에서 추가 PlayerLoop 갱신을 요청했다.
- **해결**: Unity 연결을 복구한 뒤 강제 갱신 호출을 제거하고, Game View 창에 포커스만 준 다음 한 프레임 이상 기다려 일반 스크린샷을 촬영했다. 최종 재컴파일 후 Console Error 0건을 확인했다.
- **예방**: 플레이 모드 캡처에서는 PlayerLoop/Repaint 강제 호출을 사용하지 않고 `GameView.Focus()` → 대기 → 캡처 순서만 사용한다.

### T-55. 카메라 입력 변경 검증 중 Unity MCP 세션 미연결 (2026-08-14)
- **증상**: 스크립트 수정 후 Unity 새로고침과 플레이 검증을 요청했으나 활성 Unity MCP 인스턴스가 0개로 조회됐다.
- **원인**: Unity Editor가 종료되어 있었고 남아 있던 Unity 프로세스도 확인 도중 종료됐다.
- **해결**: 프로젝트 어셈블리를 별도로 빌드해 컴파일 오류가 없음을 확인했다. 실제 휠·우클릭 드래그 입력 검증은 Editor를 다시 연 뒤 수행해야 한다.
- **예방**: Unity 입력 기능 수정 전 Editor와 MCP 세션 연결 상태를 먼저 확인하고, 미연결 시 코드 수정 후 컴파일 검증과 플레이 검증을 구분해 기록한다.

### T-56. 샌드박스에서 Unity 생성 csproj의 최초 dotnet 빌드 실패 (2026-08-14)
- **증상**: `Assembly-CSharp.csproj` 빌드가 사용자 `.dotnet` sentinel과 Microsoft SDK 경로 접근 제한, 이후 누락된 `project.assets.json` 때문에 순차적으로 실패했다.
- **원인**: 샌드박스가 사용자 프로필의 .NET 최초 실행 경로를 허용하지 않았고 `--no-restore` 빌드에 필요한 Unity 임시 복원 산출물도 없었다.
- **해결**: 작업공간 내부의 임시 `DOTNET_CLI_HOME`을 사용하고 승인된 환경에서 로컬 복원을 포함한 빌드를 실행해 오류 0건으로 완료한 뒤 임시 홈을 삭제했다.
- **예방**: Unity Editor가 닫혀 csproj 빌드로 대체할 때는 쓰기 가능한 전용 CLI 홈과 필요한 복원 단계를 처음부터 사용한다.

### T-57. 카메라 회전 입력 변경 때 기존 좌클릭 캐릭터 회전을 제거함 (2026-08-14)
- **증상**: 우클릭 카메라 회전을 추가하면서 기존 좌클릭 드래그의 캐릭터 좌우 회전 동작이 사라졌다.
- **원인**: 새 입력을 기존 회전 동작의 대체로 해석했으나, 요구사항은 두 동작을 각각 유지하는 것이었다.
- **해결**: 좌클릭과 우클릭의 드래그 상태·직전 포인터 위치를 분리해 좌클릭은 캐릭터 Y축 회전, 우클릭은 카메라 공전으로 동시에 지원했다.
- **예방**: 기존 입력에 새 입력을 추가할 때는 대체 여부를 명시적으로 확인하고 입력 매핑 표를 기준으로 회귀 검증한다.

### T-58. 의상 아래 가슴 몸체가 남아 피부가 옷을 뚫고 보임 (2026-08-14)
- **증상**: 기본 한벌옷을 입은 캐릭터의 가슴에 피부 조각이 옷 밖으로 튀어나온 것처럼 보였다.
- **원인**: 벤더 `BodyPartType`의 `Torso_Spine02(2)`와 `Torso_Spine03(5)`를 프로젝트 메시 이름에 반대로 대응했다. 그 결과 의상이 숨기도록 지정한 번호 없는 `body_torso` 가슴 메시가 계속 활성화됐다.
- **해결**: 번호 없는 `M/F_body_torso`를 2, 상단 목 브리지인 `body_torso.002`를 5로 매핑했다. 여성·남성 기본 의상에서 가슴 메시 비활성화와 목 브리지 유지를 확인했다.
- **예방**: 외부 에셋의 정수 파츠 코드는 이름 추측으로 연결하지 않고 원본 enum과 샘플 런타임의 활성화 결과를 함께 대조한다.

### T-59. 중앙 UI 조작이 캐릭터·카메라 입력으로 함께 처리됨 (2026-08-14)
- **증상**: 색상 팝업이나 중앙 확정 버튼에서 클릭·드래그·휠을 사용할 때 캐릭터 회전 또는 카메라 회전·줌이 함께 발생할 수 있었다.
- **원인**: 입력 허용 여부를 중앙 미리보기의 화면 좌표 범위만으로 판정해, 그 영역 위에 겹쳐진 uGUI 요소를 구분하지 못했다.
- **해결**: 현재 포인터 위치로 `EventSystem.RaycastAll`을 수행하고 UI Raycast 결과가 없는 중앙 배경에서만 캐릭터/카메라 입력을 허용했다.
- **예방**: 3D 미리보기 위에 UI가 겹치는 화면은 좌표 범위 검사와 UI Raycast 검사를 함께 사용한다.

### T-60. 파츠 enum을 그대로 노출해 의상·얼굴 설정이 중복되고 과밀해짐 (2026-08-14)
- **증상**: 헤어와 의상이 좌우 패널에 중복되고, 얼굴 탭 하나에 얼굴형과 피부·흰자위·홍채·눈썹·입술 색상이 한꺼번에 표시되어 원하는 항목을 찾기 어려웠다.
- **원인**: 실제 사용 흐름과 파츠의 독립 교체 가능 여부를 구분하지 않고 `AvatarPartCategory` 전체를 우측 탭으로 그대로 생성했으며, 좌측 프리셋도 얼굴·헤어·의상을 별도로 중복 노출했다.
- **해결**: 좌측을 상의·하의·신발·한벌 의상 전용 레일로 바꾸고 우측은 얼굴·헤어·모자·안경만 유지했다. 눈·입술 모양은 얼굴 메시 안에 고정된 에셋 제약에 맞춰 얼굴형·눈·입술 하위 탭을 만들고 각 탭에 관련 색상만 분리했다.
- **예방**: 커스터마이징 정보 구조는 enum 항목 수가 아니라 사용자가 수행하는 작업과 에셋의 실제 독립 교체 단위를 먼저 표로 정리한 뒤 구성한다.

### T-61. 스크린샷 에셋 갱신 중 Unity MCP 연결이 일시 재시도됨 (2026-08-14)
- **증상**: 최종 QA 스크린샷을 Assets에 반영하는 새로고침 중 Unity MCP 연결이 잠시 끊겼다가 자동 재시도됐다.
- **원인**: 여러 고용량 PNG를 한 번에 임포트하는 동안 에디터 응답이 늦어져 최초 요청 연결이 종료됐다.
- **해결**: Unity MCP가 동일 요청을 재시도해 에디터 준비 상태로 복구됐고, 이후 스크립트 검증 오류 0건과 CharacterLobby 관련 Console Error 0건을 다시 확인했다.
- **예방**: QA 이미지는 필요한 최종본만 Assets에 넣고, 대용량 이미지 임포트 직후에는 에디터 준비 상태를 확인한 뒤 후속 검증을 실행한다.

### T-62. 얼굴형에 포함된 눈·입술을 별도 탭으로 분리해 색상 대상이 불명확해짐 (2026-08-14)
- **증상**: 얼굴형·눈·입술 탭이 따로 보이지만 실제로는 눈·입술 모양을 독립 선택할 수 없고, 아래 색상 스와치에는 부위명이 작거나 보이지 않아 어떤 색이 바뀌는지 알기 어려웠다.
- **원인**: 에셋 제약을 안내하기 위해 하위 탭을 만들었지만 독립 파츠처럼 보이는 잘못된 조작 모델이 되었고, 가로 스와치 카드의 텍스트 영역도 지나치게 작았다.
- **해결**: 하위 탭을 제거하고 얼굴형 카드 바로 아래에 피부·흰자위·홍채·눈썹·입술을 이름과 현재 색상이 함께 보이는 세로 행으로 통합했다. 색상 팝업 제목에도 대상 부위를 표시했다.
- **예방**: 독립 선택할 수 없는 속성은 탭으로 분리하지 않고 실제 소유 파츠 아래의 명시적인 속성 행으로 표시한다.

### T-63. 플레이 모드 유지 상태에서 수정 전 UI를 검증함 (2026-08-14)
- **증상**: 스크립트 수정 뒤 첫 플레이 검증에서 제거한 눈·입술 하위 탭과 이전 안내 문구가 계속 조회됐다.
- **원인**: Unity가 이미 플레이 중이어서 수정된 스크립트의 도메인 리로드와 런타임 UI 재생성이 적용되지 않은 상태였다.
- **해결**: 플레이 모드를 완전히 종료한 뒤 스크립트 새로고침·컴파일을 수행하고 다시 플레이해 변경된 UI를 검증했다.
- **예방**: 런타임 생성 UI를 수정한 뒤에는 현재 플레이 상태를 먼저 확인하고 `Stop → Compile → Play` 순서를 고정한다.

### T-64. 포커스 없는 Game View에서 ScreenCapture 파일 생성이 지연됨 (2026-08-14)
- **증상**: `ScreenCapture.CaptureScreenshot` 호출은 성공했지만 지정한 PNG가 생성되지 않거나 이전 프레임이 저장됐다.
- **원인**: Game View가 포커스를 잃어 최신 UI 프레임이 그려지지 않은 상태에서 프레임 종료 캡처가 예약됐다.
- **해결**: Game View 창에 포커스를 주고 해당 창만 Repaint한 뒤 충분히 대기해 최신 얼굴 색상 화면을 캡처했다.
- **예방**: 플레이 화면 캡처는 `GameView.Focus/Repaint → 프레임 대기 → ScreenCapture` 순서로 수행하고, 파일 존재와 수정 시간을 확인한 뒤 QA에 사용한다.

### T-65. 눈 텍스처 전체가 한 색으로 변해 흰자위·홍채가 분리되지 않음 (2026-08-14)
- **증상**: 홍채 색상을 바꾸면 눈 전체가 함께 변하고, 눈동자가 흰자위처럼 표시됐다.
- **원인**: 벤더 눈 텍스처의 RGB를 일반 알베도처럼 틴트했고 속눈썹·하이라이트 재질까지 눈 재질로 오분류했다.
- **해결**: 눈 전용 셰이더에서 흰자위·홍채·동공 영역을 분리해 각각 `_ScleraColor`, `_IrisColor`, `_PupilColor`를 적용하고 재질 이름 분류에서 눈썹·속눈썹·하이라이트를 제외했다.
- **예방**: 외부 캐릭터 에셋의 얼굴 텍스처는 채널 의미와 실제 재질 슬롯을 확인한 뒤 색상 슬롯을 설계한다.

### T-66. 공유 눈 UV 중심 보정 후 양쪽 눈이 사시처럼 보임 (2026-08-14)
- **증상**: 양쪽 홍채와 동공이 코 쪽으로 몰려 정면 시선이 서로 교차했다.
- **원인**: 좌우 눈이 공유하는 미러 UV의 실제 중심이 텍스처 정중앙과 달랐는데 같은 중앙 좌표로 절차 마스크를 생성했다.
- **해결**: 공유 UV의 실측 중심과 가로·세로 반경을 반영하고 좌우 눈의 미러 방향을 고려해 홍채·동공 중심을 바깥쪽으로 이동했다.
- **예방**: 공유·미러 UV에는 화면상 좌우 방향까지 확인하는 정면 렌더 테스트를 포함한다.

### T-67. 홍채 이동 뒤 고정 하이라이트 점이 동공 밖에 남음 (2026-08-14)
- **증상**: 눈의 빛 반사점이 홍채·동공과 무관한 위치에 떠 보였다.
- **원인**: 벤더 하이라이트가 별도 고정 메시·재질이라 절차적으로 이동한 홍채 중심을 따라가지 않았다.
- **해결**: 기존 하이라이트 재질을 투명 처리하고 눈 셰이더에서 동공 중심을 기준으로 좌우 눈 각각의 캐치라이트를 생성했다.
- **예방**: 눈 중심을 변경할 때 홍채·동공뿐 아니라 하이라이트와 시선 방향을 하나의 검증 항목으로 본다.

### T-68. 입술 색상 값은 바뀌지만 보이는 입술에는 적용되지 않음 (2026-08-14)
- **증상**: 입술 색상 선택과 외형 값은 변경되지만 캐릭터 얼굴의 입술 색은 그대로였다.
- **원인**: `mat_base_mouth`는 입 안쪽 재질이며 화면에 보이는 입술 표면은 얼굴 텍스처·마스크에 포함되어 있었다.
- **해결**: 얼굴 전용 셰이더가 입술 UV 섬과 디테일 마스크를 사용해 `_LipColor`를 실제 얼굴 표면에 혼합하도록 변경하고 피부·눈썹 색상도 같은 재질에서 독립 처리했다.
- **예방**: 색상 기능은 재질 이름만으로 대상을 추정하지 않고 강한 시험색을 적용한 근접 렌더로 실제 표면을 확인한다.

### T-69. 마우스 휠 줌이 항상 화면 중앙을 기준으로 동작함 (2026-08-14)
- **증상**: 포인터를 얼굴이나 화면 가장자리에 두어도 줌이 중앙으로만 확대·축소됐다.
- **원인**: 카메라 거리만 변경하고 포인터가 가리킨 월드 공간 지점을 초점에 반영하지 않았다.
- **해결**: 포인터 레이를 카메라 초점 평면과 교차시키고 줌 비율만큼 `_cameraFocus`를 이동해 포인터 위치를 고정점으로 유지했다.
- **예방**: 줌 검증은 중앙뿐 아니라 화면 좌·우·상·하 포인터에서 초점 좌표가 해당 방향으로 이동하는지 확인한다.

### T-70. 외형 전체 재조립 직후 QA 렌더에 머리가 중복 표시됨 (2026-08-14)
- **증상**: 테스트 색상을 적용한 즉시 렌더했을 때 머리와 얼굴이 겹쳐 보였다.
- **원인**: `Destroy`는 프레임 종료에 지연 실행되는데 같은 프레임에서 전체 `Apply` 재조립과 수동 렌더를 연속 호출했다.
- **해결**: 색상 검증은 메시 재조립 없이 `ApplyColors`만 호출하고, 전체 재조립 검증은 최소 한 프레임 뒤 렌더하도록 분리했다.
- **예방**: 런타임 오브젝트 교체 QA에서는 지연 삭제 완료 시점을 기다리거나 비구조 변경 경로를 사용한다.

### T-71. 대상 텍스처 없는 수동 Camera.Render가 URP Console Error를 남김 (2026-08-14)
- **증상**: Game View를 수동 렌더한 뒤 attachment dimension mismatch와 RenderPass 관련 오류 3건이 Console에 기록됐다.
- **원인**: URP가 Game View 백버퍼를 관리하는 중 `Camera.Render()`를 대상 텍스처 없이 직접 호출해 렌더 패스 상태가 충돌했다.
- **해결**: 이후 근접 검증은 크기가 명시된 `RenderTexture`로 렌더했으며 프로젝트 코드·셰이더 컴파일 오류가 0건임을 별도로 확인했다.
- **예방**: MCP 시각 QA에서 대상 없는 `Camera.Render()`를 호출하지 않고 일반 Game View 캡처 또는 명시적 `RenderTexture`를 사용한다.

### T-72. Windows 임시 녹화 파일을 샌드박스에서 직접 읽지 못함 (2026-08-14)
- **증상**: ScreenSketch `TempState/Recordings` 경로의 사용자 제공 MP4를 직접 열 수 없었다.
- **원인**: 해당 임시 폴더가 작업공간 쓰기·읽기 허용 범위 밖에 있었다.
- **해결**: 사용자 승인 범위에서 원본을 프로젝트 작업공간으로 복사한 뒤 프레임을 분석했다.
- **예방**: 외부 임시 첨부 파일은 원본을 변경하지 않고 승인된 작업공간 사본으로 검사한다.

### T-73. Unity MCP 임시 검증 코드에서 Roslyn·CodeDom 컴파일 경로가 제한됨 (2026-08-14)
- **증상**: 런타임 검증용 C# 조각에서 Roslyn 참조를 찾지 못하고 CodeDom은 `using` 선언을 포함한 입력을 처리하지 못했다.
- **원인**: Unity MCP 실행 환경에 일반 데스크톱 .NET과 동일한 동적 컴파일 참조 집합이 제공되지 않았다.
- **해결**: 동적 컴파일 의존을 제거하고 로드된 타입의 리플렉션 호출과 Unity MCP의 표준 스크립트 검증 기능으로 대체했다.
- **예방**: Unity MCP QA 코드는 프로젝트 어셈블리와 Unity API만 사용하고 별도 Roslyn·CodeDom 컴파일러에 의존하지 않는다.
- **추가 확인**: 단순 Unity API 조회는 CodeDom 입력을 메서드 본문으로 취급해 `using` 선언을 빼고 타입명을 완전히 적으면 실행할 수 있다. 이 방식으로 `ShaderUtil.GetShaderMessages`를 조회해 피부 셰이더 메시 0건을 확인했다.

### T-74. AvatarAppearance 스크립트 검증 경로를 잘못 지정함 (2026-08-14)
- **증상**: Unity MCP 표준 스크립트 검증에서 `AvatarAppearance.cs` 파일을 찾지 못했다.
- **원인**: 실제 파일은 `World/Avatar/` 바로 아래에 있는데 다른 Core 타입과 같은 `World/Avatar/Core/` 경로로 추정했다.
- **해결**: 프로젝트 파일 검색으로 실제 위치를 확인한 뒤 올바른 경로로 다시 검증해 오류 0건을 확인했다.
- **예방**: 검증 대상 파일 경로는 타입의 네임스페이스나 주변 파일 위치로 추정하지 않고 `rg --files` 또는 타입 선언 검색으로 확정한다.

### T-75. 얼굴 마스크 이미지 치수 확인에 ImageMagick을 사용할 수 없음 (2026-08-14)
- **증상**: 입술 UV를 측정하기 위해 `magick identify`를 호출했지만 실행 파일이 설치되어 있지 않았다.
- **원인**: 현재 Windows 작업 환경에 ImageMagick 명령줄 도구가 제공되지 않았다.
- **해결**: 추가 설치 없이 .NET `System.Drawing.Bitmap`으로 원본 PNG의 크기와 픽셀 채널을 읽어 입술 범위를 측정했다.
- **예방**: 로컬 이미지 분석은 먼저 사용 가능한 기본 런타임을 확인하고, 단순 치수·픽셀 검사는 설치 의존성이 없는 API를 우선 사용한다.

### T-76. 입술 색상 마스크가 중앙의 얇은 부분만 덮음 (2026-08-14)
- **증상**: 입술색을 변경해도 위·아래 입술 전체가 아니라 입 중앙의 가느다란 부분만 어둡게 변했다.
- **원인**: 여성·남성 얼굴 텍스처의 실제 입술 UV는 세로 약 `0.238~0.293`인데, 기존 위·아래 타원 마스크의 세로 반경이 각각 `0.0048`, `0.0043`에 불과했고 디테일 임계값도 지나치게 높아 가장 어두운 중심부만 통과했다.
- **해결**: 위·아래 입술의 전체 UV 섬을 덮는 범위로 중심과 반경을 재측정하고, 얼굴 RGB 디테일의 낮은 대비까지 포함하도록 임계값을 조정해 실제 입술 윤곽 전체를 채색했다.
- **예방**: 절차 마스크는 화면 추측으로 반경을 정하지 않고 원본 텍스처 픽셀 범위와 강한 시험색·기본색 렌더를 함께 검증한다.

### T-77. 입술 전체 절차 마스크가 원본보다 진한 립스틱처럼 보임 (2026-08-14)
- **증상**: 입술 전체 UV를 덮도록 확장한 뒤에는 색상 경계는 맞았지만 캐릭터의 위·아래 입술이 뭉친 진한 립스틱처럼 보여 원본 얼굴 인상과 달라졌다.
- **원인**: 원본 셰이더의 복합 얼굴 색상 처리를 단일 색상 절차 마스크로 대체하면서 입술의 미세 명암과 피부색 혼합을 잃었다.
- **해결**: 사용자 요구에 따라 입술색 조절을 UI와 렌더 경로에서 제거하고, 얼굴 디테일 텍스처와 내부 입 재질의 기본 표현만 유지했다. 문자열 슬롯은 이전 데이터 파싱 호환을 위해 예약 상태로 남겼다.
- **예방**: 얼굴처럼 인상 변화가 큰 영역은 색상 슬롯 추가 전에 원본 셰이더가 제공하는 마스크·명암 구조를 완전히 재현할 수 있는지 확인하고, 불가능하면 원본 표현을 우선한다.

### T-78. 근접 입술 QA에서 카메라 거리 가정이 모델 스케일과 맞지 않음 (2026-08-14)
- **증상**: 얼굴 프리셋 거리와 첫 위치 지정 캡처가 기대보다 멀어 입술 상태를 판독하기 어려웠다.
- **원인**: 조립 캐릭터의 실제 월드 스케일보다 큰 거리 값을 사용했고 런타임 카메라의 보간 목표와 직접 지정 위치가 연속으로 바뀌었다.
- **해결**: 미리보기 카메라의 실제 FOV와 현재 위치를 조회하고 더 가까운 명시적 위치에서 UI 제외 근접 렌더를 촬영한 뒤, 일반 Game View에서 최종 패널 상태를 별도로 확인했다.
- **예방**: 얼굴 디테일 QA는 모델 바운드·카메라 FOV를 먼저 확인하고 근접 렌더와 전체 UI 검증을 각각 독립 캡처한다.

### T-79. 강한 입술 시험색 검증에서 전체 재조립으로 얼굴이 중복됨 (2026-08-14)
- **증상**: 입술 시험색을 적용한 근접 캡처에 서로 다른 위치의 얼굴 두 개가 겹쳐 보였다.
- **원인**: 기존 T-70에서 확인한 지연 `Destroy` 문제를 잊고 공개 검증 메서드의 전체 `Apply` 경로를 다시 사용했다.
- **해결**: 플레이 모드를 재시작해 중복 오브젝트를 제거하고, 이후에는 컨트롤러와 어셈블러 설정만 맞춘 뒤 비공개 `ApplyColors`를 호출해 메시 재조립 없이 색상만 갱신했다.
- **예방**: 색상 셰이더 QA에서는 `Apply`·`VerifyColorIsolation`을 사용하지 않고 색상 전용 갱신 경로를 고정한다.

### T-80. 벤더 원본 얼굴 ShaderGraph 직접 복원 시 얼굴색이 어둡게 깨짐 (2026-08-14)
- **증상**: 원본 얼굴 머티리얼을 런타임 렌더러에 그대로 복제하자 얼굴 전체가 짙은 갈색으로 출력되고 기존 조립 색상과 맞지 않았다.
- **원인**: 원본 ShaderGraph는 여러 색상 쌍과 마스크 리맵 값을 함께 사용하는데, 현재 `AvatarConfig`의 단일 피부·눈썹·입술 슬롯과 속성 계약이 달랐다.
- **해결**: 원본 셰이더 직접 복원은 폐기하고 현재 URP 호환 FaceTint에서 원본 얼굴 디테일 텍스처를 경계로 사용하며 피부색과 입술색을 혼합했다.
- **예방**: 외부 ShaderGraph를 복원할 때는 셰이더 지원 여부뿐 아니라 모든 노출 속성과 런타임 색상 계약을 먼저 대조한다.

### T-81. 직접 지정한 근접 QA 카메라가 무작위 파츠 캐릭터의 측후면을 촬영함 (2026-08-14)
- **증상**: 근접 캡처가 얼굴 정면 대신 모자·안경이 포함된 캐릭터의 측면 또는 후면을 보여 입술 비교가 어려웠다.
- **원인**: 런타임에 남아 있던 파츠 구성과 모델의 실제 전방 축을 고려하지 않고 월드 원점·고정 회전을 정면으로 가정했다.
- **해결**: 하순 경계는 측면 근접 화면과 원본 UV 범위로 확인하고, 플레이 종료로 테스트 회전과 색상 상태를 복구했다.
- **예방**: 근접 렌더는 현재 어셈블러의 머리 바운드와 `transform.forward`를 기준으로 카메라 위치를 계산한다.

### T-82. 하순 색조 마스크가 턱 음영 UV까지 포함해 아래로 번짐 (2026-08-14)
- **증상**: 자연스러운 입술색 혼합 후에도 아랫입술 아래쪽에 옅은 색 띠가 번진 것처럼 보였다.
- **원인**: 하순 마스크의 최저 UV가 `0.237` 부근까지 내려가 실제 하순과 분리된 턱 음영 섬을 함께 선택했다.
- **해결**: 하순 중심을 위로 이동하고 세로 반경을 줄여 최저 경계를 약 `0.248`로 제한하면서 윗입술과 하순 본체는 유지했다.
- **예방**: 입술 마스크는 위·아래 입술뿐 아니라 인접한 턱 주름·콧밑 음영 섬이 분리되는 경계까지 확인한다.

### T-83. 얼굴 명암 임계값 기반 입술 경계가 주변 피부에 흐리게 번짐 (2026-08-14)
- **증상**: 턱 음영을 제외한 뒤에도 입술 둘레에 옅은 색이 퍼져 입술 외곽이 흐릿하게 보였다.
- **원인**: 얼굴 RGB 마스크의 어두운 정도로 입술을 선택하면서 안티앨리어싱과 주변 피부 명암 픽셀까지 반투명 경계에 포함됐다.
- **해결**: 명암 임계값을 경계 판단에서 제거하고, 원본 텍스처에서 측정한 Y 위치별 입술 반폭을 위·아래 각각 테이퍼드 실루엣으로 구성해 실루엣 안쪽에만 색조를 적용했다.
- **예방**: 경계가 명확해야 하는 화장 영역은 명암 임계값만 사용하지 않고 UV 실루엣 또는 전용 마스크 텍스처로 범위를 고정한다.

### T-84. 셰이더 새로고침 뒤 플레이 상태 조회와 실제 Game View 상태가 어긋남 (2026-08-14)
- **증상**: 도구는 플레이 중이라고 응답했지만 Game View에는 캐릭터가 사라지고 배경과 받침대만 표시됐다.
- **원인**: 플레이 중 에셋 강제 새로고침과 도메인 상태 변경 사이에 MCP의 직전 상태 응답과 실제 에디터 상태가 일시적으로 달랐다.
- **해결**: 플레이 모드를 명시적으로 종료·재진입한 뒤 일반 Game View에서 캐릭터가 정상 생성되는 것을 확인하고 근접 캡처를 다시 수행했다. 후속 의상 셰이더 갱신과 Booth HTTP 스크립트 진단 중 발생한 일시적 MCP 연결 종료도 자동 재연결 뒤 에디터 준비 상태를 확인하고 같은 작업을 재시도해 복구했다.
- **예방**: 플레이 중 셰이더 강제 새로고침 후에는 에디터 상태 조회뿐 아니라 Game View의 런타임 오브젝트 존재를 확인하고, 불일치하면 `Stop → Play`로 초기화한다.

### T-85. 최종 변경 검사 도구 스크립트의 문자열 따옴표 오류 (2026-08-14)
- **증상**: `git diff --check`와 문서 검색을 묶은 검증 호출이 JavaScript 구문 오류로 실행되지 않았다.
- **원인**: 결과 출력 함수의 문자열 시작 부분에 불필요한 이스케이프 문자를 넣었다.
- **해결**: 올바른 문자열 리터럴로 즉시 다시 실행해 diff 검사 종료 코드 0과 문서·셰이더 반영 위치를 확인했다.
- **예방**: 여러 검증 결과를 합치는 도구 스크립트는 단순한 따옴표와 문자열 연결만 사용하고 실행 전 시작·종료 구분자를 확인한다.

### T-86. 얼굴과 몸에 같은 피부색 값이 적용돼도 목 경계가 보임 (2026-08-14)
- **증상**: 얼굴 피부색을 바꾸면 목·손·다리에도 같은 RGB 값이 전달되지만 얼굴과 신체의 밝기·채도가 달라 목 경계선이 보였다.
- **원인**: 얼굴은 `Festa/Avatar/FaceTint`, 신체는 `Universal Render Pipeline/Lit`을 사용해 서로 다른 조명 계산을 수행했다.
- **해결**: 얼굴과 같은 기본 조명 계산을 사용하는 `Festa/Avatar/SkinTint`를 추가하고 모든 `body` 머티리얼을 이 셰이더로 변환한 뒤, 같은 `_SkinColor`를 얼굴·목·몸통·팔·손·다리·발에 적용했다.
- **예방**: 하나의 색상 슬롯으로 이어져 보여야 하는 메시들은 색 값뿐 아니라 셰이더의 조명 모델도 통일하고, 피부가 노출되는 의상 조합으로 경계를 검증한다.

### T-87. 원본 Rukha93 의상 ShaderGraph를 런타임에서 그대로 쓰면 분홍색으로 표시됨 (2026-08-14)
- **증상**: 의상의 6색 속성과 RGB 마스크를 보존하려고 원본 머티리얼을 복제하자 상의·하의·한벌옷이 분홍색 오류 머티리얼로 렌더링됐다.
- **원인**: 벤더 ShaderGraph가 현재 프로젝트의 런타임 렌더 경로와 호환되지 않았고, 기존 단색 URP 변환은 오류를 피하는 대신 RGB 마스크 정보를 버리고 있었다.
- **해결**: 원본 `_BaseMap` RGB 마스크, `_BumpMap`, `_Color_A_1`부터 `_Color_C_2`까지의 속성을 받는 URP 전용 `Festa/Avatar/GarmentTint`를 추가해 세 영역과 원본 명암을 유지했다.
- **예방**: 외부 ShaderGraph는 속성 존재만 보고 재사용하지 않고 실제 플레이 렌더를 확인하며, 비호환이면 원본 텍스처·속성 계약을 유지하는 프로젝트 전용 셰이더를 사용한다.

### T-88. 의상 셰이더에서 `_BaseMap` 재정의 컴파일 오류 (2026-08-14)
- **증상**: 첫 `AvatarGarmentTint` 컴파일에서 `redefinition of '_BaseMap'` 오류가 1건 발생했다.
- **원인**: 셰이더가 `_BaseMap`을 직접 선언한 상태에서 `SurfaceInput.hlsl`도 같은 텍스처를 선언했다.
- **해결**: 노멀 언팩에 필요한 공통 함수만 제공하는 `Packing.hlsl`로 include를 좁혀 중복 선언을 제거했고 셰이더 메시 0건을 확인했다.
- **예방**: URP 보조 include를 추가할 때 해당 파일이 선언하는 표준 머티리얼 속성을 먼저 확인하고, 필요한 최소 헤더만 포함한다.

### T-89. 벤더 셰이더 JSON 조사 도구와 PowerShell 인라인 명령 호환 문제 (2026-08-14)
- **증상**: 벤더 ShaderGraph 내부 계산식을 조사하는 과정에서 `jq`와 기본 `python` 실행 파일을 찾을 수 없었고, Node 인라인 코드와 PowerShell 와일드카드 경로는 따옴표·경로 해석 오류로 여러 번 중단됐다.
- **원인**: 현재 작업 환경의 기본 PATH에 JSON 분석 도구가 없었으며 PowerShell, `cmd`, Node가 각각 따옴표와 와일드카드를 다르게 해석했다.
- **해결**: Codex 번들 런타임에서 Node 경로를 확인하고 PowerShell의 stop-parsing 방식으로 JSON을 읽었으며, 파일 검색은 디렉터리를 명시한 `rg` 호출로 한정했다.
- **예방**: 대형 ShaderGraph JSON을 조사하기 전 사용할 런타임과 명령 해석기를 먼저 고정하고, Windows에서는 셸 사이에 와일드카드나 중첩 따옴표를 넘기지 않는다.

### T-90. 의상 셰이더 Range 속성 기본값 괄호로 인한 ShaderLab 파싱 오류 (2026-08-14)
- **증상**: 영역 분리 속성을 추가한 직후 `Shader.Find("Festa/Avatar/GarmentTint")`가 실패하고 `unexpected '(' expecting TVAL_NUMBER` 파싱 오류가 발생했다.
- **원인**: `_Mask_Factor`의 `Range` 속성 기본값을 `(.65)`처럼 괄호로 감싸 ShaderLab 숫자 문법과 맞지 않았다.
- **해결**: 기본값을 `.65`로 수정한 뒤 에셋을 새로고침했고 `Shader.Find` 성공, 셰이더 메시 0건을 확인했다.
- **예방**: ShaderLab Properties의 숫자·Range 기본값은 괄호 없는 숫자 리터럴을 사용하고, 속성 추가 직후 `ShaderUtil.GetShaderMessages`로 파싱 상태를 확인한다.

### T-91. 아키텍처 점검 중 문서 경로와 검색 정규식 가정 오류 (2026-08-14)
- **증상**: Frontend·Unity 설계 문서를 읽는 첫 호출이 실제 파일명의 `설계서` 접미사를 누락해 실패했고, 여러 검색어를 한 정규식으로 묶은 명령은 PowerShell 따옴표 해석 때문에 정규식이 닫히지 않았다. 후속 Markdown 검사에서는 Git이 한글 경로를 따옴표와 8진 escape로 출력한 값을 실제 경로로 넘겨 `Test-Path`가 실패했다.
- **원인**: 파일명을 먼저 열거하지 않고 문서 인덱스의 표시명을 경로로 추정했으며, 셸 명령 문자열 안에 중첩 따옴표를 사용했다. 또한 `git diff --name-only`의 기본 `core.quotePath` 출력을 PowerShell 경로로 바로 재사용했다.
- **해결**: `docs` 파일 목록에서 실제 경로를 확인한 뒤 읽었고, 검색어는 `rg -e` 옵션 또는 단순 alternation 문자열로 전달해 셸 따옴표 중첩을 제거했다. Markdown 검사는 Git 출력 대신 `Get-ChildItem`의 실제 `FullName`을 사용해 다시 수행했다.
- **예방**: 문서 경로는 `rg --files` 또는 디렉터리 목록으로 확인한 뒤 사용하고, PowerShell에서 복수 검색어는 복잡한 그룹 정규식보다 여러 `-e` 옵션을 사용한다. Git 경로 목록을 재사용할 때는 `-c core.quotePath=false`를 지정하거나 셸의 파일 객체를 직접 사용한다.

### T-92. Booth Layout의 문서·최신 스펙·Unity HTTP 계약 불일치 (2026-08-14, 해결)
- **증상**: Backend API 문서의 공개 Layout 경로는 `/booths/{boothId}/layouts/published`지만 Unity `HttpBoothApiClient`는 `/booths/{boothId}/layout/published`를 호출한다. 기존 Backend·Frontend·DB 문서는 오브젝트 식별자를 `objectId`로 정의하지만 최신 spec 005와 실제 Unity DTO는 `id`를 사용한다.
- **원인**: 초기 설계 문서 이후 Unity POC와 소급 spec이 갱신되었으나 3파트 합동 계약 확정과 기존 문서·코드의 일괄 동기화가 아직 수행되지 않았다.
- **해결**: Backend 계약을 기준으로 공개 경로를 `/api/v1/booths/{boothId}/layouts/published`, 식별자를 `objectId`로 통일하고 Unity DTO에 `assetCode`를 추가했다. canonical Type 10종과 구 POC 별칭 읽기 호환을 적용했으며, spec 005/006·Frontend Type·Backend API·DB·Unity 문서를 함께 갱신하고 Backend 형태 JSON 파싱을 검증했다.
- **예방**: Layout 계약 변경은 spec 005의 3파트 서명 뒤 공통 JSON 예제·Backend API·Frontend Type·Unity DTO·계약 테스트를 한 작업으로 함께 갱신한다.

### T-93. 문서 전용 커밋 스테이징이 stale Git index lock으로 중단됨 (2026-08-14, 해결)
- **증상**: `git add -- docs`가 `.git/index.lock`이 이미 존재한다는 오류로 실행되지 않았다.
- **원인**: 실행 중인 Git 프로세스는 없었지만 전날 생성된 0바이트 `index.lock`이 남아 새 인덱스 작업을 막고 있었다.
- **해결**: Git 관련 프로세스가 없고 잠금 파일의 생성 시각·크기·대상 경로가 안전한지 확인한 뒤 해당 `index.lock` 파일만 제거했다.
- **예방**: 인덱스 잠금 오류가 발생하면 Git 프로세스 존재 여부와 잠금 파일 시각을 먼저 확인하고, 활성 작업이 없다고 확정된 경우에만 정확한 `.git/index.lock` 단일 파일을 제거한다.

### T-94. game 문서 커밋을 다른 브랜치에 직접 cherry-pick할 때 문서 구조·기록 충돌 (2026-08-14, 해결)
- **증상**: `game`의 문서 커밋을 `main`에 적용하자 작업일지, Booth Runtime spec, Avatar spec 경로에서 충돌했고 `ai` 계열 브랜치에서는 고유 T-38/T-39 기록도 충돌했다.
- **원인**: `game`은 spec이 `docs/specs/`에 남아 있지만 다른 브랜치는 정식 `specs/` 루트로 이동됐고, 각 브랜치의 작업일지·트러블슈팅에 브랜치 정리 이력이 별도로 존재했다.
- **해결**: 임시 Git worktree에서 브랜치별 기존 문서 위치를 유지하고 Booth spec 005/006만 현재 위치에 병합했다. 작업일지와 T-38/T-39는 양쪽 기록을 모두 보존했으며, Unity 코드·에셋은 커밋에 포함하지 않은 채 6개 원격 브랜치를 각각 푸시했다.
- **예방**: 브랜치 간 문서 동기화는 전체 브랜치 포인터를 이동하지 말고 docs 전용 커밋만 적용한다. spec 디렉터리 이동 여부와 브랜치별 기록 차이를 먼저 비교하고, 충돌 시 최신 내용으로 덮어쓰기보다 양쪽 이력을 병합한다.

### T-95. Windows 기본 bash·Python 환경으로 spec/OpenAPI 검증을 바로 실행할 수 없음 (2026-08-27, 부분 해결)
- **증상**: `bash .specify/scripts/bash/check-prerequisites.sh`는 WSL `E_ACCESSDENIED`로 실패했고, 시스템 `py` 런처와 Codex 번들 Python의 PyYAML 모듈이 없어 OpenAPI YAML 파싱 검사를 실행하지 못했다.
- **원인**: PowerShell의 기본 `bash`가 Git Bash가 아니라 권한이 제한된 WSL 실행 파일을 가리켰고, 사용 가능한 Python 런타임에는 PyYAML이 포함되지 않았다.
- **해결**: `C:\Program Files\Git\bin\bash.exe --login -c`로 prerequisite 스크립트를 실행해 feature 경로와 tasks 존재를 검증했다. OpenAPI는 전체 diff·들여쓰기·필수 schema/reference를 수동 점검했으나 전용 YAML parser 검증은 미수행으로 남겼다.
- **예방**: Windows spec-kit 검증은 Git Bash login shell을 명시하고, OpenAPI 검증 환경에는 저장소가 고정한 YAML parser 또는 Spectral 실행 경로를 사전에 제공한다.

### T-96. 테스트에서 `booths.published_layout_version`을 가짜로 세울 수 없음 — 복합 FK (2026-08-28, 해결)
- **증상**: spec 016 홈페이지 노출 게이트 테스트에서 부스를 "공개 상태"로 만들려고 `jdbc.update("UPDATE booths SET published_layout_version = 1 WHERE id = ?")`를 픽스처로 썼더니 `DataIntegrityViolationException` — `fk_booths_published_layout_version` 위반, `Key (id, published_layout_version)=(6, 1) is not present in table "booth_layout_published_versions"`.
- **원인**: `booths.published_layout_version`은 단순 정수 칼럼이 아니라 `(id, published_layout_version)` **복합 FK**로 `booth_layout_published_versions`를 가리킨다(V8). "공개 회차 포인터"는 실재하는 공개본 없이 존재할 수 없고, 게이트가 이 칼럼을 신뢰할 수 있는 근거가 바로 그 FK다.
- **해결**: 픽스처를 실제 경로로 교체 — `PUT /booths/{id}/layouts/draft` + `POST /booths/{id}/layouts/publish`(`BoothLayoutTestSupport.saveRequest(0)` 재사용).
- **예방**: `booths`의 상태 칼럼을 픽스처에서 직접 쓰기 전에 해당 마이그레이션의 제약을 먼저 본다 — `current_slot_id`(UNIQUE)·`published_layout_version`(복합 FK)이 이미 그렇다. 기존 헬퍼(`grantLease`가 슬롯을 조회해 INSERT하는 형태)가 왜 그렇게 생겼는지가 곧 제약이 있다는 신호다.
- **관련**: S15P21A604-109, `BoothHomepageApiIntegrationTest.publishLayout`. 개인 기록은 `docs/HDD/트러블슈팅.md` T-128(같은 사건, 별도 번호 체계)

### T-97. `jsonPath(...).doesNotExist()`가 명시적 `null`도 통과시켜 계약을 못 지킴 (2026-08-28, 해결)
- **증상**: spec 016 계약은 "`homepageUrl` 키는 항상 응답에 있고 값이 `null`로 의미를 전달한다"(FE가 `null` 하나로 미등록/미공개를 분기)인데, 검증을 `jsonPath("$.homepageUrl").doesNotExist()`로 써서 **키가 아예 없어도 통과**하는 상태였다. 나중에 Jackson null 제외 설정이 들어오면 계약이 깨져도 테스트는 계속 green.
- **원인**: Spring의 `doesNotExist()`는 JsonPath 평가 결과가 `null`이면 통과한다 — 필드 부재와 명시적 `null`을 구분하지 못한다. 실제로 같은 응답에서 `doesNotExist()`와 `has() && isNull()`이 **둘 다** 통과하는 것으로 확인했다(현재는 null이 직렬화되고 있음, 전역 `default-property-inclusion` 미설정).
- **해결**: 응답 본문을 `JsonMapper.readTree`로 파싱해 **`has(field)`와 `get(field).isNull()`을 따로 단언**하는 헬퍼로 5곳 전부 교체(등록 해제 응답·`/mine`·미공개 부스 조회·미등록 공개 부스 조회 + 게이트 입력 `publishedLayoutVersion`).
- **예방**: "필드가 있고 값이 null"이 계약이면 `doesNotExist()`·`exists()`를 쓰지 않는다 — `exists()`는 명시적 null에서 실패하고 `doesNotExist()`는 부재에서도 통과하므로 **둘 다 그 계약을 표현할 수 없다.** 본문 파싱으로 두 조건을 명시한다.
- **⚠️ Jackson 3**: 이 저장소는 `tools.jackson`(Jackson 3)이다. `com.fasterxml.jackson.databind.ObjectMapper` 빈은 **존재하지 않는다** — `tools.jackson.databind.json.JsonMapper`를 주입한다(annotation 패키지만 `com.fasterxml.jackson.annotation`으로 남아 혼동하기 쉽다).
- **관련**: S15P21A604-109

### T-98. `java.net.URI` 를 URL 신뢰 경계로 쓰다 6 건이 연속으로 샜다 (2026-08-28, 해결)
- **증상**: spec 009 URL 검증기(`common/HttpUrlValidator`)가 리뷰 4 라운드에 걸쳐 결함 6 건을 받았다. 전부 "파싱은 되는데 목적지로는 쓸 수 없는 주소"이거나 "검증한 구조와 저장되는 문자열이 갈리는" 종류였다.

  | # | 입력 | 무슨 일이 났나 |
  |:--:|---|---|
  | 1 | `https://x.com:99999` | RFC 3986 은 포트를 `*DIGIT` 로만 정의한다. host·port 가 멀쩡히 파싱되지만 연결 불가 |
  | 2 | `https://한글도메인.com` | `URI` 가 authority 를 RFC 2396 으로 읽어 `getHost()` 가 `null`. **한국 서비스가 한국 도메인을 거부**하고 있었다 |
  | 3 | `https://user@한글.com` | 고치려고 `IDN.toASCII` 에 authority 를 통째로 넘겼더니 `xn--user@-lt1tk15s.com` — `@` 를 라벨 안으로 삼켜 **다른 이름**이 됐다 |
  | 4 | `https://한글도메인.com@evil.com` | userinfo 거부를 IDN 경로에만 걸어 둬서, host 가 ASCII 면 조기 반환에 걸려 검사가 아예 안 돌았다 |
  | 5 | `https://한글.com＠evil.com` | 전각 `＠`(U+FF20)는 raw 검사 시점엔 `@` 가 **아니다**. IDN 매핑 후 `@` 가 되어 `evil.com` 으로 해석됐다 |
  | 6 | `https://한글.com：8080/x` | 전각 `：` 도 같은 방식으로 **원본에 없던 포트**를 만들었다. 값이 정상 범위라 포트 규칙에도 안 걸렸다 |

- **원인**: `java.net.URI` 는 **문법 파서**이지 도달 가능성·구조 무결성 검사기가 아니다. 파싱 성공을 "쓸 수 있는 주소"로 읽은 것이 1·2 의 원인이고, 3~6 은 그것을 고치려고 **정규화 함수의 출력을 재해석**하면서 생겼다. `IDN.toASCII` 는 인자를 도메인 이름 하나로 보므로 구분자를 이름의 일부로 삼키거나 반대로 구조 문자를 만들어 낸다.
- **해결**: 문자를 하나씩 쫓는 것을 그만두고 규칙을 뒤집었다 — **변환 결과가 이름의 모양인지 묻는다.**
  - `isHostLabels` — IDN 이 내놓은 host 는 `[A-Za-z0-9.-]` 만 담아야 한다. 이 한 줄이 `＠`·`：`·`／`·`？`·`＃` 를 한꺼번에 막는다. 5·6 을 각각 잡느라 두 라운드를 썼는데 이 규칙 하나면 됐다
  - `isPlainAuthority` — 재조립한 URI 에 path·query·fragment 가 생기면 거부
  - userinfo 는 **raw authority 와 해석된 URI 양쪽**에서 본다 (4·5 가 서로 다른 쪽에서만 잡힌다)
  - 포트는 **해석된** URI 로 판정한다. 원본은 registry-based 라 `getPort()` 가 항상 `-1` 이다
  - 검증은 punycode 로 하되 **저장은 원문 그대로** — 왕복 무손실(불변식 I-3)은 그대로 지킨다
- **예방**: URL 을 신뢰 경계에서 받을 때 **파싱 성공을 통과 근거로 쓰지 않는다.** ⑴ 도달 가능성(포트 범위)과 ⑵ 구조 무결성(변환 전후로 authority 가 같은 것을 가리키는가)을 따로 묻는다. 정규화 함수의 출력을 다시 파싱해 쓸 때는 **출력이 입력과 같은 종류의 것인지** 반드시 검사한다. 새 결함이 나오면 문자를 추가하지 말고 **허용 집합을 좁히는 쪽**을 먼저 본다.
- **⚠️ 016 도 같이 엄격해졌다**: `BoothHomepageService` 가 같은 검증기를 쓴다. 포트·userinfo·전각 규칙이 홈페이지 URL 에도 걸린다 — `specs/016/contracts/homepage-api.md` 와 `docs/08` §4 에 시점과 함께 적었다. 기존 통합 테스트 19 개는 문구를 바이트 단위로 지킨다.
- **관련**: S15P21A604-110, `common/HttpUrlValidator`, `ProjectValidationApiIntegrationTest`. 회귀 47 클래스 / 459 테스트, 실패 0

### T-99. 잠금을 잡는다고 **주석에만** 적고 코드에는 없었다 (2026-08-30, 해결)
- **증상**: spec 007 AI 직원 삭제(`AiAgentService.delete`)에 "publish 와 같은 부스 행 잠금을 먼저 잡는다"는 javadoc 과 인라인 주석이 둘 다 있었는데, **`booths.findWithLockById` 호출이 없었다.** 반대편(`BoothLayoutService.publish`)에는 잠금이 실제로 들어가 있어, 겉보기에는 양쪽이 갖춰진 것처럼 보였다.
- **원인**: 설계 문서를 근거로 주석을 먼저 쓰고 구현을 이어 붙이는 순서로 작성했다. 주석은 **의도의 기록**이지 코드가 아니므로 컴파일러도 테스트도 검증하지 않는다. 여기에 더해 배치의 직원 참조는 layout JSON 안에 있어 **외래키가 없다** — 잠금이 유일한 방어라 한쪽만 잡으면 방어가 0 이다. 문서·상담 참조는 외래키가 최후에 잡아 주지만 배치 참조는 아무도 안 잡는다.
- **결과였다면**: 공개(publish)가 직원 검증을 마친 직후 삭제가 커밋되면 **공개된 배치가 존재하지 않는 직원을 가리킨다**(불변식 A-3 위반). 방문자에게는 클릭해도 아무 일도 안 일어나는 오브젝트로 나타나고, 서버 로그에는 아무것도 남지 않는다.
- **해결**: `AiAgentService` 에 `BoothRepository` 를 주입하고 `requireUnreferenced` **앞에서** `findWithLockById` 를 잡는다. 참조 검사가 배치를 읽으므로 읽기 전에 잡아야 의미가 있다 — 검사 뒤에 잡으면 검사와 잠금 사이가 그대로 창이다.
- **예방**: 잠금·트랜잭션 경계처럼 **없어도 정상 경로가 초록인 것**은 주석으로 주장하지 말고 테스트로 박는다. 이번에 추가한 `AiAgentConcurrencyIntegrationTest` 는 한쪽 트랜잭션을 `TransactionTemplate` 로 열어 둔 채 상대를 부른다 — 잠금을 빼면 이 테스트가 깨진다. 두 경로가 같은 잠금을 잡아야 하는 설계는 **양쪽을 한 커밋에서** 넣고, 한쪽만 넣은 상태로 커밋하지 않는다.
- **한계**: 이 테스트가 고정하는 것은 인터리빙이 아니라 **결과**다. 상대 스레드가 잠금 앞에 도달했는지 관측할 신호가 없어, 어느 순서로 갈리든 "공개된 배치가 사라진 직원을 가리키는 상태는 없다"만 단언한다. 테스트 주석에 같은 내용을 적었다.
- **관련**: S15P21A604-105, spec 007 C-14 · data-model A-3, `AiAgentService` · `BoothLayoutService` · `BoothRepository.findWithLockById`. 회귀 51 클래스 / 501 테스트, 실패 0

### T-100. 게시 게이트 테스트에서 `published_layout_version` 을 손으로 세우려다 7 건이 한꺼번에 실패했다 (2026-08-31, 해결)
- **증상**: `ProjectVisitorApiIntegrationTest` 의 게시 픽스처를 `UPDATE booths SET published_layout_version = 1 WHERE id = ?` 로 만들었더니 **7 개 테스트가 같은 예외로 죽었다** — `ERROR: insert or update on table "booths" violates foreign key constraint "fk_booths_published_layout_version"`. 게이트를 안 거치는 두 테스트(부스 없음·미게시)만 초록이라, 처음에는 구현이 아니라 픽스처 문제인 것이 안 보였다.
- **원인**: 그 컬럼은 **복합 외래키**다 — `(id, published_layout_version)` 이 `booth_layout_published_versions` 의 행을 가리켜야 한다. 즉 "게시됨"은 포인터를 세우는 것이 아니라 **버전 행이 실제로 존재하는 상태**다. 이 제약이 있어서 게이트가 컬럼 하나만 읽고도 신뢰할 수 있다 — 픽스처가 우회할 수 있으면 게이트도 우회될 수 있다는 뜻이다.
- **해결**: 실제 발행 두 번(`PUT /layouts/draft` → `POST /layouts/publish`)을 태운다. **016 홈페이지 테스트가 같은 벽을 이미 만나 같은 해법을 갖고 있었다** — private helper 로 숨어 있어서 재사용되지 않았다. `BoothLayoutTestSupport.publishLayout(mockMvc, boothId, bearer)` 로 올리고 016 은 위임하게 했다.
- **예방**: **스키마가 막는 픽스처는 스키마가 옳다.** 통합 테스트에서 상태를 SQL 로 심을 때, 그 컬럼에 걸린 제약을 먼저 본다 — 제약이 있으면 그 상태는 "값"이 아니라 "결과"이므로 그 결과를 만드는 경로를 태워야 한다. 그리고 **같은 게이트를 두 spec 이 읽으면 픽스처를 공유한다**: 각자 심으면 "게시됨"의 정의가 테스트마다 갈린다.
- **관련**: S15P21A604-177, `BoothLayoutTestSupport.publishLayout` · `V8__booth_published_layout_version.sql` · 016 `BoothHomepageApiIntegrationTest`. 회귀 3 클래스 / 44 테스트, 실패 0

### T-101. `infra` 프로파일로 띄우면 설정 96줄이 사라져 기동 자체가 안 됐다 (2026-08-31, 해결)
- **증상**: 배포 프로파일을 `application-infra.yml`(5줄)로 확정하고 파일까지 들어왔는데, `SPRING_PROFILES_ACTIVE=infra` 로 띄우면 `spring.datasource.url` 이 없어 JPA·Flyway 자동설정이 실패한다. 환경변수를 전부 주입해도 결과가 같다.
- **원인**: Spring 의 `application-{profile}.yml` 은 프로파일 간에 누적되지 않는다. `datasource`·`jpa`·`flyway`·`data.redis`·`security.oauth2` 와 `app.*` 전체가 `application-local.yml` 96줄 안에만 있어서 `local` 을 끄는 순간 통째로 사라졌다.
- **해결**: 프로파일 중립 설정을 `application.yml` 로 승격. `application-local.yml` 에는 `.env` import 와 로컬 기본값 3개만 남긴다.
- **예방**: 배포에서 로컬 값으로 조용히 뜨면 안 되는 키는 공용 위치에 **기본값을 두지 않는다** — `POSTGRES_PASSWORD`·`FRONTEND_BASE_URL`·`AUTH_COOKIE_SECURE`. 특히 `cookie-secure:false` 가 기본값이면 HTTPS 배포에서 Refresh 쿠키가 Secure 없이 나가고 화면에는 아무 이상이 안 보인다. `SharedConfigProfileTest` 가 `infra` 프로파일 해석을 고정한다 — 컨테이너 없이 1초, 설정이 다시 프로파일 파일로 새면 깨진다.
- **관련**: S15P21A604-347, GitLab #117, `a51f88a0`(`-170`). 회귀 549 테스트, 실패 1 (T-102 와 무관한 기존 red)

### T-102. `target/classes` 에 남은 죽은 마이그레이션이 테스트 2건을 깨고 내 변경을 범인으로 만들었다 (2026-08-31, 해결)
- **증상**: 설정만 옮겼는데 `AiAgentApiIntegrationTest` 2건이 `null value in column "content_sha256" ... violates not-null constraint` 로 죽었다. 같은 테스트가 clean develop 워크트리에서는 통과했다.
- **원인**: `content_sha256` 은 `src/main/resources/db/migration/` 에 **없다.** `target/classes/db/migration/V16__ai_documents_upload.sql` 만 남아 있었다 — 다른 브랜치에서 빌드한 산출물이다. Flyway 는 클래스패스를 보므로 `target` 의 죽은 V16 을 적용했고, V16 이전 스키마를 전제로 쓴 테스트의 raw INSERT 가 깨졌다.
- **해결**: `mvn clean`. 그 뒤 549 테스트 중 실패는 develop 에도 있는 1건뿐이다.
- **예방**: 브랜치를 옮겨 다니며 테스트할 때 **`target/` 을 신뢰하지 않는다.** 특히 Flyway·리소스처럼 클래스패스에서 읽는 것은 소스에서 삭제해도 산출물에 남는다. 회귀를 내 변경 탓으로 판정하기 전에 clean 한 번, 그리고 기준선은 clean 워크트리에서 뜬다 (T-23·T-25 와 같은 부류).
- **관련**: S15P21A604-347, `V16__ai_documents_upload.sql`(어느 브랜치 소유인지 미확인)

### T-103. 전체 스위트에서만 `nickname` 이 `VARCHAR(30)` 을 넘어 1건 실패했다 (2026-08-31, 해결)
- **증상**: `BoothLayoutConfigLinkIntegrationTest.aLaptopCarryingAConfigIdIsReportedAsUnverified` 가 `leasedOwner:216` 에서 `value too long for type character varying(30)` 로 죽는다. **그 클래스만 단독 실행하면 10건 전부 통과**한다. 러너가 없어 develop 이 이 1건으로 red 인 것을 아무도 몰랐다.
- **원인**: `users.nickname` 은 `VARCHAR(30) NOT NULL UNIQUE`(V1:6)인데 헬퍼가 `prefix + SEQUENCE + "_" + System.nanoTime()` 으로 만들었다. **`nanoTime` 하나가 16~19자리를 먹는다.** 실패한 호출부 prefix 는 `"노트북configId"` 11자다 — 단독 실행이면 `11 + 1 + 1 + 17 = 30` 으로 상한에 딱 걸려 통과하고, 전체 스위트에서는 앞선 클래스들이 이미 수백 번 호출해 순번이 3자리가 되어 `11 + 3 + 1 + 17 = 32` 로 넘친다. 즉 **`SEQUENCE` 가 몇 자리인지가 통과와 실패를 갈랐다.**
- **해결**: `nanoTime` 을 뺀다. `SEQUENCE` 는 JVM 당 유일하고 Testcontainers 컨테이너는 `withReuse` 를 안 써서 실행마다 새로 뜨므로 **실행 간 충돌이 애초에 불가능**했다 — `nanoTime` 은 없는 위험을 막느라 예산 17자를 쓰고 있었다. 다만 헬퍼마다 `SEQUENCE` 가 따로여서 그냥 빼면 서로 같은 값을 낼 수 있다(`nickname` 은 UNIQUE 다). 그래서 헬퍼별 태그 한 글자를 넣는다 — `b`(Booth)·`w`(Wallet)·`g`(Game)·`a`(Avatar)·`v`(World). 다섯 곳 전부 같은 방식으로 고쳤다. 전체 549 테스트 실패 0.
- **예방**: 테스트 픽스처가 만드는 문자열은 **컬럼 폭 안에 여유를 두고** 만든다. 유일성 재료를 둘(순번 + 타임스탬프) 겹치면 폭을 넘기기 쉽고, 넘긴 뒤에도 실행 순서에 따라 통과해 버려 회귀로 잡히지 않는다. 그리고 **`nanoTime` 을 뺄 때는 그것이 무엇의 유일성을 지키고 있었는지 먼저 본다** — 여기서는 실행 간이 아니라 헬퍼 간이었다.
- **관련**: S15P21A604-352, `BoothTestSupport`·`WalletTestSupport`·`GameTestSupport`·`MyAccountAvatarApiIntegrationTest`·`WorldSessionApiIntegrationTest` · `V1__initial_schema.sql:6`. S15P21A604-347 검증 중 발견했고 그 변경과는 무관하다

### T-104. 구매 API 잔액 테스트에 일일 지급 50코인이 끼어 기대값이 어긋났다 (2026-09-01, 해결)
- **증상**: 아바타 파츠 100코인 구매 테스트에서 요청 전후 잔액 차이를 단언했는데, 기대한 `-100` 대신 `-50`이 관측됐다. 구매 원장과 인벤토리는 정상이라 처음에는 차감 로직 또는 잠금 순서를 의심했다.
- **원인**: 첫 인증 요청에 `DailyCoinGrantInterceptor`가 오늘의 50코인을 먼저 지급한다. 테스트가 구매 요청을 그 사용자의 첫 요청으로 만들면서 `+50`과 `-100`을 한 구간에서 함께 측정했다.
- **해결**: 잔액 스냅샷 전에 해당 사용자의 일일 지급 경로를 한 번 완료하는 테스트 헬퍼를 두고, 그 이후 구매 전후 차액과 `PURCHASE` 원장을 검증했다.
- **예방**: 인증 API의 코인 변화를 검증할 때는 일일 지급을 픽스처 단계에서 명시적으로 정착시킨다. 최종 잔액만 보지 말고 구매 원장의 `amountDelta`·`reason`·`idempotencyKey`와 인벤토리 행도 함께 단언한다.
- **관련**: S15P21A604-378, `AvatarItemOwnershipApiIntegrationTest`

### T-105. 같은 `target`에서 전체 테스트 두 개가 겹쳐 클래스 파일이 실행 중 사라졌다 (2026-09-01, 해결)
- **증상**: `mvn clean test`는 `backend/target` 삭제 실패로 중단됐고, 이어 실행한 `mvn test`는 컴파일에 성공했지만 555건 중 428건이 `ClassNotFoundException` 또는 “test class cannot be opened because it does not exist”로 연쇄 실패했다.
- **원인**: 별도의 Maven 프로세스가 같은 저장소에서 `-o clean test`를 동시에 수행하고 있었다. 한 프로세스의 clean/compile이 다른 Surefire 프로세스가 읽는 `target/classes`와 `target/test-classes`를 교체하면서 테스트 클래스패스가 실행 도중 달라졌다. 애플리케이션 구현이나 개별 테스트 실패가 아니다.
- **해결**: 기존 Maven/Surefire 프로세스를 강제 종료하지 않고 완료될 때까지 기다린 뒤, 단일 프로세스만 남은 상태에서 전체 테스트를 다시 실행한다.
- **예방**: 동일 worktree의 Maven 회귀는 하나만 실행한다. IDE 테스트·`spring-boot:run`과 구분해 프로세스 명령행을 확인하고, `clean` 삭제 실패 직후에는 곧바로 두 번째 테스트를 겹쳐 실행하지 않는다.
- **관련**: S15P21A604-378, `backend/target`

### T-106. 새로 만든 worktree에서 테스트 66건이 전부 컨텍스트 로딩 실패했다 (2026-09-02, 해결)
- **증상**: `git worktree add` 로 새 작업 트리를 만든 직후 `./mvnw -o test -Dtest=...` 를 돌리자 6개 클래스 66건이 모두 `IllegalState Failed to load ApplicationContext` 로 실패했다. Docker 는 정상이었고(`docker ps` 에 postgres·redis 기동 중), 같은 커밋을 기존 체크아웃에서 돌리면 통과한다. Surefire 요약은 컨텍스트 설정 문자열만 길게 뱉어 실제 원인이 보이지 않는다.
- **원인**: `backend/.env` 가 `backend/.gitignore:2` 에 걸려 있어 **새 worktree 에는 존재하지 않는다.** 이 파일이 애플리케이션 기동에 필요한 환경 변수를 공급하므로 Spring 컨텍스트가 뜨지 못한다. 테스트 코드나 Testcontainers 문제가 아니다.
- **해결**: 기존 체크아웃에서 `backend/.env` 를 새 worktree 로 복사한다. gitignore 대상이라 커밋되지 않는다 — `git check-ignore -v backend/.env` 로 확인하고 진행했다.
- **예방**: worktree 를 새로 만들면 **추적되지 않는 로컬 설정부터 옮긴다.** 현재 backend 에서 그 대상은 `backend/.env` 하나다(`backend/src/test/resources/application-local.properties` 는 저장소에 있다). 컨텍스트 로딩이 클래스 단위로 전부 실패하면 코드보다 환경을 먼저 의심한다 — 개별 테스트가 실패하는 모양과 다르다.
- **관련**: S15P21A604-388, `backend/.env`, `backend/.gitignore`

### T-107. 해석되지 않은 `${FESTA_ENVIRONMENT}` 가 Redis 네임스페이스 검사를 전부 통과했다 (2026-09-02, 해결)
- **증상**: 증상이 없는 것이 증상이었다. MR !201 을 develop 과 병합하면서 `SharedConfigProfileTest` 의 `DEPLOY_ENV`(스스로 "배포가 주입해야 하는 전부" 라고 적어 둔 배열)에 `FESTA_ENVIRONMENT` 를 더했는데, 그 값을 도로 빼고 돌려도 테스트가 한 건도 붉어지지 않았다. 기본값 없이 두어 "빠뜨리면 기동 실패" 를 계약으로 삼은 설정인데 아무도 그것을 확인하고 있지 않았다.
- **원인**: Spring 의 `PropertySourcesPlaceholdersResolver` 는 해석하지 못한 placeholder 를 예외 대신 **문자열 그대로** 바인딩에 넘긴다. `RedisKeyspaceProperties` 의 검사 셋(빈 값·앞뒤 공백·`:` 포함)을 `${FESTA_ENVIRONMENT}` 가 전부 통과한다 — 비어 있지 않고, 앞뒤 공백이 없고, `:` 도 없다. 그래서 env 를 빠뜨린 배포가 멀쩡히 기동하고 dev·demo 가 그 문자열 하나를 네임스페이스로 공유한다. 티켓이 막으려던 세션·일일 지급 충돌이 격리된 척하면서 그대로 돌아오는 것이라, 접두사가 없던 이전보다 알아채기 어렵다. T-101 이 저장소 자격증명에서 낸 것과 같은 모양이다.
- **해결**: `RedisKeyspaceProperties` 가 `${...}` 형태를 따로 거절하고 빠진 변수 이름을 메시지에 담는다. 검사 순서는 빈 값 바로 다음이다 — 가장 구체적인 메시지가 먼저 나온다. 테스트 3건을 함께 두었다. 값 검증 단위 1건(`RedisKeyspacePropertiesTest`)과 바인딩 2건(`SharedConfigProfileTest` — 주입되면 `dev:` 로 묶이고, 빠뜨리면 기동이 실패하며 실패 메시지가 변수 이름을 든다). 가드를 지우고 돌려 두 건이 실제로 붉어지는 것을 확인한 뒤 되돌렸다.
- **예방**: 기본값 없는 필수 설정은 "값이 있는가" 가 아니라 **"해석됐는가"** 까지 봐야 한다. 값 검증만 하는 생성자 검사는 placeholder 를 정상 값으로 읽는다. 새 필수 env 를 만들 때는 `SharedConfigProfileTest.DEPLOY_ENV` 에 넣고 **빼면 기동이 실패하는 테스트를 같이** 둔다. `Environment` 만 들여다보는 테스트로는 이 층이 잡히지 않는다 — 바인딩을 실제로 태워야 한다(T-101 과 같은 결론).
- **관련**: S15P21A604-349, MR !201, T-101, `backend/src/main/java/com/example/ssafesta/common/RedisKeyspaceProperties.java`, `backend/src/test/java/com/example/ssafesta/SharedConfigProfileTest.java`

### T-108. 실패를 조여서 처리하는 잠금 판정이, 도착 전에 돌면서 정상 외형을 전부 지웠다 (2026-09-04, 해결)
- **증상**: 구현 중에 잡았다 — 배포되지 않았다. 아바타 파츠 잠금을 서버 `owned` 기준으로 바꾸면서 "보유 정보가 없으면 전부 잠긴 것으로 본다" 는 fail-closed 규칙을 넣었는데, 그 상태로 로비를 띄우면 캐릭터가 **알몸으로** 나오고 `[AvatarCatalog] ... 기본 제공 항목이 하나도 없다` 경고가 카테고리마다 쏟아진다.
- **원인**: fail-closed 자체는 옳고, **판정을 부르는 시점**이 틀렸다. `CharacterLobbyController.Awake` 가 `SanitizeLocked` 를 부르는데 이때는 카탈로그 조회가 시작조차 안 된 상태라 모든 항목이 미보유로 나온다. 그러면 ⑴ `SanitizeLocked` 가 정상 외형을 전부 "잠김" 으로 판정해 기본값으로 교체하고 ⑵ 그 기본값을 고르는 `Default()` 는 해제된 후보로 좁히므로 후보가 0개라 `null` 을 돌려주고 ⑶ 결과가 모든 슬롯 0, 즉 알몸이 된다. **"모른다" 를 "없다" 로 읽은 것**이 실수다 — fail-closed 는 판정을 신뢰할 수 있을 때만 판정이고, 그 전에는 판정이 아니라 무지다.
- **해결**: `AvatarOwnership.JudgementReady`(= `Loaded` 또는 `DevUnlockedAll`)를 두어 세 곳을 갈랐다. `SanitizeLocked` 는 준비되기 전에 **아무것도 걸러내지 않고 즉시 반환**한다. 대신 골라 주는 자리(기본·무작위)는 `GetUnlockedItems` 대신 `GetSelectableItems` 를 쓰는데, 준비 전에는 전체 목록에서 고른다. 그리고 `InitializeFromServerAsync` 가 보유 정보를 받은 **뒤에** `SanitizeLocked` 를 다시 부른다. 사용자에게 보이는 것은 늘 옷을 입은 아바타이고, 잠금은 판정이 도착한 시점부터 정확해진다.
- **예방**: fail-closed 를 넣을 때 **"거부" 와 "아직 모른다" 를 같은 값으로 두지 않는다.** 불린 하나(`HasServerData`)로는 이 둘이 구분되지 않아 호출자가 무지를 거부로 읽는다 — 상태를 열거형으로 두고(`NotLoaded`/`Loaded`/`Failed`/`DevUnlockedAll`), 판정을 소비하는 곳마다 "이 값을 신뢰할 수 있는가" 를 먼저 묻게 한다. 특히 **비동기로 도착하는 근거에 의존하는 판정은 `Awake`·`Start` 에서 부르지 않는다.** T-24 와 같은 뿌리(조용한 기본값 복귀)의 거울상이다 — T-24 는 실패를 개방으로 바꿨고 이쪽은 무지를 차단으로 바꿨는데, 둘 다 원인은 상태를 두 값으로만 표현한 것이다.
- **관련**: S15P21A604-412, GitLab #120 §2-1, T-24, `festa-unity/Assets/_Project/Scripts/World/Avatar/Catalog/AvatarOwnership.cs`, `festa-unity/Assets/_Project/Scripts/World/Avatar/Lobby/CharacterLobbyController.cs`

### T-109. 부스 프리팹에 실내 조명이 남아 있어, 커밋하면 부스마다 광원이 2개가 된다 (2026-09-04, 해결)
- **증상**: 커밋 전에 잡았다. 미커밋 변경을 뒤지다 CarnivalKit 부스 프리팹 7개에 `@BoothInteriorLight`(Light + `BoothInteriorLight`, guid `7296f72e…`)가 붙어 있는 것을 발견했다. 커밋했다면 부스마다 실내 광원이 두 개가 되어 직원 얼굴이 다시 하얗게 타는 S15P21A604-355 의 "달걀귀신" 증상이 돌아왔을 것이다.
- **원인**: 같은 기능이 **두 곳에** 있다. develop 의 `main.unity` 에는 `BoothLight_FestivalSlot_01`~`12` 가 씬 오브젝트로 12개 들어가 있고(현재 화면을 만드는 것이 이쪽이다), develop 의 프리팹에는 이 컴포넌트가 0개다. 프리팹 GUID 는 씬에서 27~63회 참조되므로 인스턴스화도 실제로 된다 — 프리팹에 광원을 넣으면 씬 광원 위에 하나가 더 얹힌다. `BoothInteriorLight.Fit()` 의 `LimitExposureOnStaff` 는 **자기 광원 하나**의 `intensity/거리²` 만 보므로 두 광원의 합계를 잡지 못한다. 각각은 상한을 지키는데 사람 얼굴에 닿는 총량이 두 배가 되는, 상한이 통과되면서 증상은 재발하는 모양이다.
- **해결**: 커밋하지 않고 `git stash` 로 격리했다(프리팹 7개 + 함께 발견된 PPv2 3개를 stash 하나로). 지운 것이 아니라 빼 둔 것이라 `git stash pop` 으로 돌아온다. 그대로 두면 다음 `git add -A` 에 쓸려 들어가므로 작업 트리에 남겨 두는 것 자체가 위험이었다.
- **예방**: ⑴ **광원 상한은 광원 단위가 아니라 대상 단위로 잰다.** "이 광원이 사람에게 주는 노출" 이 아니라 "사람이 받는 총 노출" 을 봐야 광원이 늘어날 때 잡힌다. 지금 코드는 전자다. ⑵ **같은 기능을 씬과 프리팹 양쪽에 두지 않는다.** `BoothInteriorLight.cs` 주석이 "프리팹에 함께 들어간다" 라고 적혀 있는데 실제 동작은 씬 배치였다 — 문서와 실제가 갈린 상태가 이 중복을 눈에 안 보이게 만들었다. **2026-09-05 씬 배치로 확정**하고 주석을 실제에 맞췄다. 지금은 "프리팹에는 절대 넣지 마라" 와 그 이유(상한이 광원 단위라 합계를 못 잡는다)까지 클래스 주석에 적혀 있다. ⑶ Unity 프로젝트에서 `git status` 의 파일 수는 변경 규모가 아니다 — 이 17건 중 4건은 줄바꿈만 달랐고 `git diff --numstat` 으로만 갈렸다.
- **관련**: S15P21A604-355, S15P21A604-412(발견 시점), `festa-unity/Assets/_Project/Scripts/World/Festival/BoothInteriorLight.cs`, `festa-unity/Assets/_Project/Art/Booth/CarnivalKit/Prefabs/`, `stash@{0}`

### T-110. 사내 리졸버가 미등록 서브도메인을 한 IP 로 답해, 배포되지 않은 서비스를 "떠 있다/503" 으로 오독했다 (2026-09-05, 해결)
- **증상**: `demo.ssafesta.world` 가 200(Nginx 자리표시 페이지), `world`·`api` 가 503 으로 응답해 "프론트만 자리표시, 게임·백엔드는 뒤에 없다" 고 보고했다. 응답 헤더 `Server: cloudflare` 까지 있어 의심하지 않았다.
- **원인**: 기본(사내) 리졸버가 `demo`·`world`·`api`·`dev` 를 전부 `210.220.163.82`(SK브로드밴드) 로 답했다. 공용 리졸버 `1.1.1.1` 로 다시 조회하니 그 넷은 **DNS 에 존재하지 않고** `ci.ssafesta.world`(AWS EC2) 만 있다. 즉 사내망이 미등록 이름을 어떤 프록시로 흡수한 것이고, 그 프록시가 Cloudflare 헤더를 흘려보내 진짜처럼 보였다. 결론("배포된 것이 없다")은 우연히 같았지만 이유("컨테이너가 없다" vs "레코드가 없다")가 달라 다음 행동이 달라진다 — 전자는 배포, 후자는 DNS 등록이 먼저다.
- **해결**: `nslookup <host> 1.1.1.1` 로 재확인하고 보고를 정정했다(GitLab #52 코멘트). 월요일 선행 항목에 "Cloudflare A 레코드 3건" 을 올렸다.
- **예방**: **외부 도달성은 공용 리졸버로 교차 확인한 뒤에만 단정한다.** 사내망에서 `nslookup` 한 번으로 "떠 있다/죽었다" 를 말하지 않는다. 특히 여러 서브도메인이 **같은 IP** 로 답하면 캐치올을 의심한다. 응답 헤더는 프록시 체인 어느 층에서든 붙을 수 있어 근거가 못 된다.
- **관련**: GitLab #52, S15P21A604-169, `infra/unity-server/nginx/world.conf.template`

### T-111. 로컬 `.git` 에서 blob 65개가 사라져 `pull` 이 전 브랜치에서 실패했고, 부분 FF 가 작업 트리를 반쯤 덮었다 (2026-09-05, 해결)
- **증상**: (환경: `/private/tmp/two-url-fresh.8A2JHF/ssafesta` — 아래 원인란의 검수용 sandbox) `git fetch --all` 이 `fatal: pack has 131 unresolved deltas` / `fatal: fetch-pack: invalid index-pack output` 으로 실패했다. 재시도해도 같았다. `git fetch origin develop`(FETCH_HEAD 만 갱신)은 되는데, ref 를 갱신하는 fetch 는 56개 중 3개 브랜치가 `did not send all necessary objects` 로 실패했다. 이 상태에서 `git merge --ff-only origin/front` 를 돌리자 `error: unable to read sha1 file of <path>` 13건을 뱉고 `Aborting` — **HEAD 는 안 움직였는데 작업 트리는 이미 수백 파일이 새 내용으로 덮여** D 253 / M 260 / ?? 316 혼합 상태가 됐다. 시작 시점의 작업 트리에도 이유 모를 삭제 242건이 있었다.
- **원인**: `git fsck --connectivity-only` 가 tree→blob broken link **65건**을 냈다. 서버는 협상에서 클라이언트가 "가졌다"고 답한 커밋을 기준으로 thin pack 을 보내는데, 그 base 가 될 blob 이 로컬에 없어 델타를 풀 수 없었다. 네트워크가 아니라 **로컬 객체 DB 손상**이다. blob 이 사라진 직접 원인은 **미확정**이다. 이 클론은 08-28 01:44 KST 에 JAM Two-URL bootstrap 검수용으로 `mktemp -d /tmp/two-url-fresh.XXXXXX` 아래 만든 **일회성 sandbox** 였는데(`e437f376` 에서 `--branch front` clone), 이후 08-30~09-05 여러 세션에서 실작업 위치로 재사용됐다. 그 사이 어느 시점에 객체가 유실됐는지는 특정하지 못했다. 본 클론 `~/projects/ssafesta` 와는 **독립**이다 — sandbox 생성 스크립트가 거기서 읽은 것은 원격 URL·브랜치명뿐이고 clone 은 원격에서 새로 받았다. 본 클론은 fsck 0 으로 정상. 작업 트리 삭제 242건도 같은 뿌리로 본다(체크아웃이 못 쓴 파일).
- **해결**: ① `git fetch --refetch origin` 으로 전 객체를 다시 받았다 → broken link 0. ② 반쯤 덮인 작업 트리는 손대지 않고 **fresh clone 으로 갈아탔다.** 그 전에 로컬 고유 내용을 전수 확인했다 — 원격에 없는 커밋 0(`git rev-list --branches --not --remotes`), stash 0, worktree 1, ignored 는 `festa-frontend/node_modules/.bin` 8개뿐, reflog 에만 남은 `9823ef84`(-195 WIP 보존 커밋)는 develop `38916ab7` 이 상위 호환(코드 동일, 주석 7줄 차이). 부분 FF 가 써 놓은 M 260·?? 316 은 전부 `origin/front` 와 바이트 동일함을 `cmp` 로 확인했다. 미추적 316 은 tar 로, WIP 는 `format-patch` 로 보관한 뒤 진행했다. 새 클론은 fsck 0, `front` = `origin/front`.
- **예방**: ⑴ `pull` 이 `unresolved deltas` 로 죽으면 네트워크 재시도가 아니라 **`git fsck --connectivity-only` 부터** 돌린다 — 로컬 문제다. ⑵ 손상 상태에서 `merge`·`checkout` 을 돌리지 않는다 — 실패해도 되돌리지 않고 작업 트리를 반쯤 덮는다. `--refetch` 로 고친 **뒤에** 한다. ⑶ 클론을 갈아타기 전 로컬 고유 내용은 커밋(`--branches --not --remotes`)·stash·ignored(`.env` 류)·reflog 순으로 전수 확인하고, 삭제·덮어쓰기 대신 **백업 + 새 clone** 을 택한다. ⑷ zsh 에서 `"$b:refs/remotes/..."` 는 `:r` 수식자로 먹혀 refspec 이 `frontefs/...` 로 깨진다 — 변수를 `${b}` 로 감싼다.
- **관련**: S15P21A604-247, `git fetch --refetch`, `git fsck --connectivity-only`, T-24(조용한 기본값 회귀 — 여기서는 조용한 부분 체크아웃)

### T-112. 배포 빌드가 ApiConfig 환경을 강제하지 않아 깨끗한 체크아웃의 WebGL 은 Mock·localhost 를 부른다 (2026-09-05, 해결 — !255, 후속 T-113)
- **증상**: develop head 배포 빌드가 도는 동안 "이 산출물이 어느 API 를 부르나" 를 확인했다. `ApiConfig.asset` 커밋본은 `useMockApi: 1`, `activeEnvironment: 0(Local)`, Prod 항목은 `https://api.example.com`/`https://ai.example.com` 자리표시. 로컬 에셋은 `useMockApi: 0` 이지만 `Local` 이라 이번 빌드도 `http://localhost:8000` 을 부른다.
- **원인**: `FestaReleaseBuilder`·`FestaWebBuilder`·`CiBuild` 어디에도 `ApiConfig` 를 읽거나 바꾸는 코드가 없다(grep 0건). 압축·타깃·development 플래그는 빌더가 캡처·강제·복원하는데 **API 환경만 빠졌다.** 그래서 "누가 어떤 에디터 상태에서 뽑았나" 가 산출물을 결정하고, CI(깨끗한 체크아웃)에서는 항상 Mock+Local 이 나간다. -418 이 "부트스트랩 없으면 Mock" 이었다면 이건 "부트스트랩이 있어도 에셋이 Mock" 이다 — 둘 다 고쳐야 실서버에 붙는다.
- **해결(진행 중)**: ① 에셋 Dev/Prod URL 채움 ② `FestaReleaseBuilder.Run` 이 시작 시 캡처 → `Prod·mock=false` 강제·`AssetDatabase.SaveAssets` → `finally` 복원·저장. `example.com`/빈 URL 이면 빌드 중단 ③ `CiBuild -festaEnv prod|dev|local`(기본 prod), Mock 항상 false ④ 로그 `[Release] 이 빌드의 API — env=… mock=… spring=…`. Jira -419.
- **예방**: **빌더가 캡처·복원하는 설정 목록에 "산출물이 부를 주소" 가 들어 있어야 한다.** 압축·타깃은 눈에 보이지만 API 주소는 빌드가 끝나도 보이지 않아 빠지기 쉽다. 빌드 로그에 env/mock/spring 을 항상 찍어 "무엇을 뽑았는지" 를 산출물 근거로 남긴다. 가정한 도메인은 인프라 확정 시 에셋 한 줄로 바꾼다(코드 무변경).
- **관련**: S15P21A604-419, -418, -417, GitLab #30·#127, `festa-unity/Assets/_Project/ScriptableObjects/ApiConfig.asset`, `Scripts/Editor/FestaReleaseBuilder.cs`, `Scripts/Editor/CiBuild.cs`

### T-113. 배포 빌드의 ApiConfig 복원이 "가짜 null" 참조로 조용히 빠져 작업본이 Prod 로 남았다 (2026-09-05, 해결)
- **증상**: 14:33 Prod 재빌드(20.4분) 로그에 `[Release] 이 빌드의 API — env=Prod …` 는 있는데 `[Release] ApiConfig 복원` 줄이 없고, `git diff ApiConfig.asset` 에 `activeEnvironment: 0 → 2(Prod)` 가 남았다. 다음 에디터 플레이·빌드가 전부 Prod 주소를 부를 상태.
- **원인**: `Run()` 이 빌드 **전에** `LoadApiConfig()` 로 잡아 둔 ScriptableObject 참조를 `finally` 에서 그대로 썼다. Linux 서버 → WebGL 타깃 전환·리임포트를 거치면 그 참조는 파괴된 오브젝트(Unity 의 "가짜 null")가 되고, `RestoreApiEnvironment` 의 `if (cfg == null) return;` 이 "에셋 없음" 으로 보고 **말없이** 빠졌다. T-112 를 고치면서 "조용히 기본값" 을 하나 더 만든 셈이다.
- **해결**: 복원 시점에 경로로 다시 읽는다(`cfg = LoadApiConfig()`). 못 읽으면 `LogError("[Release] ApiConfig 복원 실패 — … 손으로 확인해라")`. MR !257(`cedaddb6`). 이번 빌드의 잔류값은 손으로 `Local` 로 되돌렸다(커밋 안 함).
- **예방**: **빌드 파이프라인을 가로지르는 에셋 참조는 빌드 뒤에 다시 읽는다.** 타깃 전환은 도메인 리로드 없이도 에셋을 언로드한다. "복원" 코드가 `null` 을 만나면 그것은 정상 종료가 아니라 **복원 실패**다 — 로그로 드러낸다.
- **관련**: S15P21A604-419, T-112, `Scripts/Editor/FestaReleaseBuilder.cs`

### T-114. FE 가 만든 `<canvas>` 에 `id` 가 없어 Unity 6 WebGL 이 `_main` 직후 죽는다 (2026-09-05, 해결)
- **증상**: FE(5173) 임베드로 `/app/world` 진입 시 로더·데이터는 다 받는데 캔버스가 진남색으로만 남는다. 콘솔 `SyntaxError: Failed to execute 'querySelector' on 'Document': '#' is not a valid selector` at `registerKeyEventCallback ← _emscripten_set_keydown_callback_on_thread ← Module._main`.
- **원인**: Unity 6 WebGL 은 키보드 이벤트 타깃을 `"#" + canvas.id` 선택자로 찾는다. `festa-frontend/src/unity/host/UnityHost.tsx` 의 `<canvas ref=… style=… />` 에 `id` 가 없어 선택자가 `"#"` 이 됐다. Unity 기본 템플릿은 `<canvas id="unity-canvas" tabindex="-1">`. FE 는 mock 로더(`VITE_USE_MOCK=true`)로만 개발해 왔고, **실 빌드를 임베드한 것은 이날이 처음**이었다.
- **해결**: `<canvas id="unity-canvas" tabIndex={-1} …>`. MR !259 (게임 파트 작성 · FE 리뷰 승인 후 author 머지, develop). 18:20 develop head 재실측에서 `_main` 예외 없이 캐릭터 로비 렌더 확인. GitLab #128 §1, Jira S15P21A604-421.
- **예방**: 임베드 계약에 **캔버스 요구사항(id·tabindex)** 을 명시한다. "mock 로더로 통과" 는 실 빌드 로드를 검증하지 않는다 — 통합 실측을 한 번은 실 빌드로 해야 한다.
- **관련**: S15P21A604-421, GitLab #128, T-116·T-117

### T-115. 릴리스 WebGL 클라이언트가 게임 서버에 승인되지 않는다 — 에디터 클라이언트는 같은 서버에 승인·스폰된다 (2026-09-05, **해결 — 빌드 결함 아님, 백그라운드 탭 스로틀링**)
- **진짜 원인 (16:05 확정)**: 테스트한 Chrome 탭이 **`document.visibilityState === "hidden"`** 이었다(Claude-in-Chrome 이 만든 탭 그룹이 다른 창 뒤에 있었음). Chrome 은 숨은 탭의 `requestAnimationFrame` 을 **완전히 멈추고**(3초 측정 rAF 0회) Unity WebGL 의 메인 루프는 rAF 로 돈다 → NGO 가 연결 요청·하트비트를 처리하지 못한다 → 서버는 10초 승인 버퍼 타임아웃으로 `Client N disconnected`, 클라는 30초 `DisconnectTimeoutMS` 로 `Failed to connect`. 릴리스/Development 빌드, Docker/에디터 서버, 긴/짧은 토큰 전부 같은 결과였던 이유다. JS `Runtime.evaluate` 가 45초 타임아웃 난 것도 같은 현상(rAF 큐 정지).
- **검증**: 데스크톱 앱의 Browser 패널(`visible`, rAF 82fps)로 같은 Development 빌드를 열자 **에디터 서버·Docker 서버(`festa-world:dev`=60b8cd42) 모두 `Approved client=… nickname=게스트-3fea` → `[NetworkPlayer] Spawned` → `[Appearance] 서버 반영`**, 이동 정상, 1분 이상 연결 유지(disconnect 0건). 릴리스 빌드 재검증은 같은 조건에서 하면 된다(코드 차이 없음).
- **아래 "분리 검증" 기록은 원인을 찾아가던 과정으로 남긴다.**
- **예방**: ① 브라우저 자동화로 접속을 검증할 때는 **먼저 `document.visibilityState`·rAF 를 확인**한다 — 숨은 탭은 Unity 가 멈춘 것과 같다. ② **제품 관점의 후속(T-120)**: 실사용자가 탭을 30초 넘게 뒤로 보내면 같은 이유로 접속이 끊긴다 — 복귀 시 재접속 UX 또는 `DisconnectTimeoutMS` 상향·백그라운드 하트비트가 필요하다.
- **증상**: `Builds/web-verify-0905`(= 13:42 릴리스 빌드, Development OFF·Brotli) 를 FE 임베드·단독 페이지 양쪽에서 열어 월드 입장. `world-sessions` 200 → `[ConnectionManager] StartClient ws://127.0.0.1:7777 → True` 까지는 되는데, 서버(`festa-world:dev`=60b8cd42) 로그는 `[ConnectionManager] Client N disconnected, session cleaned` **한 줄만** — `Approved`/`입장 거부`/`Denied` 어느 것도 없다(승인 콜백 자체가 돌지 않음, NGO `ClientConnectionBufferTimeout` 10초). 클라이언트는 33초 뒤 `Failed to connect to server.`(UTP `DisconnectTimeoutMS` 30000) + `[Disconnect] 서버가 사유를 보내지 않았다`. 엘리베이터 게이트는 열리지 않는다.
- **분리 검증**: ① 같은 컨테이너·같은 프록시(게스트 grant)로 **에디터 클라이언트**(`DevConnectionHud.ConnectViaSessionApi` 리플렉션 호출) → `Approved client=3 … nickname=게스트-f85d`, `[NetworkPlayer] Spawned` — 서버·토큰·시크릿은 정상. ② `serve.mjs` 자체 서명(짧은 목 토큰, sub=12)으로 WebGL 재시도 → 같은 실패 — **페이로드 크기(721B, 비단편 한계 1296B 미만) 아님**. ③ 남은 변수는 **릴리스 WebGL 빌드 자체**(IL2CPP 스트리핑 `managedStrippingLevel` 기본값, `webGLExceptionSupport=1`, Development OFF, Brotli) 또는 브라우저 WebSocket 경로. Development WebGL(`Festa/부하테스트`) 빌드로 15:27 분리 검증 시작.
- **해결(진행 중)**: 개발 빌드가 접속되면 릴리스 설정을 하나씩 되돌려 범인을 찾는다(우선 후보: `link.xml` 로 `Unity.Netcode.Runtime`·`Unity.Networking.Transport` 보존). 접속 안 되면 브라우저 WebSocket 경로(UTP `WebSocketNetworkInterface.jslib`)로 좁힌다.
- **예방(잠정)**: **릴리스 설정의 WebGL 로 실제 서버 접속을 한 번은 해 봐야 배포본이라 부를 수 있다.** 지금까지의 접속 검증은 전부 Development 빌드·에디터였다.
- **관련**: S15P21A604-420, -83, -206, GitLab #52, `Scripts/Network/Connection/ConnectionManager.cs`, `Scripts/Editor/FestaReleaseBuilder.cs`

### T-116. FE 가 게스트에게 Access Token 을 전달하지 않아 게스트 Unity 는 카탈로그·world-sessions 가 전부 401 이다 (2026-09-05, 해결)
- **증상**: FE `authBridge.ts` — `session.kind === 'member' ? getAccessToken() : null`, 게스트는 `ClearAccessToken`. 콘솔 `[AuthBridge] Access Token 해제됨`. 이번 실측은 개발용 프록시(`serve.mjs --spring`)가 무토큰 요청에 게스트 토큰을 대신 붙여 통과했지만, **배포에는 그 프록시가 없다.**
- **원인**: FE 주석 "게스트는 미전달(열람 전용)" 은 월드 입장 토큰 체계(S15P21A604-85, `POST /api/v1/world-sessions` = `authenticated()`)보다 오래된 전제. 실측: 무토큰 `world-sessions`·`catalog/items` → 401, 게스트 AT → 200(role=GUEST grant, 서버 승인 확인). BE 는 게스트에게 정상적으로 입장 grant 를 준다.
- **해결**: 제안보다 한 단계 일반화했다 — kind 분기를 없애고 **세션에 Access Token 이 있으면 전달, 없으면 Clear**. 토큰과 kind 는 세션 스토어가 항상 함께 갱신하므로 결과는 같고 세션 종류가 늘어도 이 파일을 다시 안 고친다. Refresh Token 미전달 유지(헌법 13조). MR !266, Jira S15P21A604-91. 단위 테스트 5건. 18:20 재실측에서 게임 서버 `Approved … nickname=게스트-4c3d` — 프록시가 붙이는 게스트와 다른 신원이라 FE 가 넘긴 AT 로 world-session 을 받은 것이 확인됐다.
- **관련**: S15P21A604-421, -85, T-114

### T-117. FE `WORLD_GATE_TIMEOUT_MS=60s` 가 로비 체류 중 만료돼 "월드를 불러오지 못했습니다" 가 정상 동작 위에 겹친다 (2026-09-05, 해결)
- **증상**: 첫 씬이 캐릭터 로비인데 FE 는 인스턴스 생성 시점부터 60초 안에 `onWorldGateReady` 를 기다린다. 로비에서 옷을 고르는 동안 타임아웃 → `failed` 오버레이. "다시 시도" 는 인스턴스를 재생성해 로비를 처음부터 다시 한다.
- **원인**: 게이트 신호는 "월드 입장 → 서버 승인 → 스폰" 뒤에 온다(`WorldEntryGate`). 로비 도입(08-13) 전 설계가 남았다.
- **해결**: 값을 늘리지 않고 **측정 대상을 바꿨다.** 타이머는 boot attempt(생성 시작~인스턴스 resolve)에만 걸리고 진행률 콜백마다 다시 잰다 — 느린 회선의 긴 다운로드는 실패가 아니다. 인스턴스가 서면 해제되고 **로비 체류·게이트 대기에는 타임아웃이 없다.** 게이트 상한은 Unity `WorldEntryGate` 의 30초 강제 개방이 이미 갖고 있어 "월드 진입 시작" 브리지 이벤트는 신설하지 않았다(Unity 변경 0). 상수명 `WORLD_GATE_TIMEOUT_MS` → `UNITY_BOOT_STALL_TIMEOUT_MS`. MR !267, Jira S15P21A604-426. 18:20 재실측에서 로비 약 1분 + main 로드 약 50초 동안 실패 안내 미발생 확인.
- **후속**: 로비→main 50~84초 구간의 **정상 대기 안내**는 별건이다 — MR !273 (S15P21A604-429), GitLab #129.
- **관련**: S15P21A604-421, T-114

### T-118. `manifest.json` 의 상대 URL 을 FE 가 그대로 `script.src` 에 넘겨 FE 오리진 기준으로 해석된다 (2026-09-05, 해결)
- **증상**: `VITE_UNITY_BUILD_BASE=http://localhost:8000` 인데 `Build/<hash>.loader.js` 를 `http://localhost:5173/Build/…` 로 요청 → 404. 이번 실측은 manifest 를 절대 URL 로 손봐서 통과(빌드 산출물만 수정, 커밋 없음).
- **원인**: `resolver.ts` 는 값을 검증만 하고, `loader.ts` 는 그대로 `script.src`/`createUnityInstance` 에 넘긴다. #60 계약은 "키 4종" 만 정했고 **상대/절대** 를 정하지 않았다.
- **해결**: 제안대로 FE 가 base 기준으로 해석한다. 해석 함수는 Unity 전용이 아니라 `shared/assets/resolveAssetUrl.ts` 의 공통 seam 으로 뒀다(향후 Booth 에셋 manifest 가 같은 해석을 쓴다). 절대 URL 은 그대로 통과하므로 **게임 빌드는 서빙 위치를 몰라도 된다.** 함께 base 를 런타임 주입(`PUBLIC_UNITY_BUILD_BASE` → `window.__FESTA_CONFIG__.unityBuildBase`)으로 정본화해 같은 FE 이미지로 환경마다 base 만 바뀐다. MR !268, Jira S15P21A604-427. 단위 테스트 13건. 18:20 재실측에서 상대 manifest 로 로더·데이터·wasm 4종 로드 확인.
- **관련**: S15P21A604-417, -82, GitLab #60·#127·#128

### T-119. 페스티벌 스태프 NPC 2기에 벤더 데모 컨트롤러(Controller.PlayerInput/CharacterMover)가 살아 있어 매 프레임 애니메이터 경고 4건 (2026-09-05, 해결)
- **증상**: 에디터를 서버로 띄우자 콘솔이 `Parameter 'Speed'/'Hor'/'Vert'/'State' does not exist` 로 넘쳤다(Editor.log 5분에 270만 줄, MCP `read_console` 2초 타임아웃). 스택은 `Assets/ithappy/Casino_Free/Scripts/Character_Controller/PlayerInput.Update → MovementState → AnimationHandler`.
- **원인**: `@Festival/Festival_Slots/FestivalSlot_04/Staff_04/Visual_3_Actor`·`FestivalSlot_09/Staff_09/Visual_3_Actor` 가 ithappy `Ready_Characters/3_Actor` 프리팹 인스턴스인데, 이 프리팹에는 데모용 플레이어 컨트롤러가 붙어 있다. NPC 는 Animator 만 필요하다. 컨트롤러가 매 프레임 Input 을 읽고(WASD 가 NPC 를 움직일 수 있음) 자기 Animator 에 없는 파라미터를 쏜다 → 클라·서버 양쪽 매 프레임 경고, WebGL 에서는 `console.warn` 비용. 관리 데스크의 `31_Cashier`(-414)는 컨트롤러가 없어 무관(확인).
- **해결**: main.unity 의 두 인스턴스에서 `Controller.*` 컴포넌트 제거(벤더 프리팹 원본은 손대지 않음 — 벤더 에셋 수정 금지). Jira S15P21A604-425.
- **예방**: **벤더 "Ready" 프리팹은 데모 스크립트를 달고 온다** — 씬에 넣을 때 컴포넌트 목록을 한 번 보고 Animator·렌더러만 남긴다. 서버 로그가 정상일 때의 초당 줄 수를 알아 두면 이런 스팸을 바로 알아챈다.
- **관련**: S15P21A604-355(스태프 배치), -414, `Assets/ithappy/Casino_Free/Prefabs/Ready_Characters/3_Actor.prefab`

### T-120. 백그라운드 탭에서 30초가 지나면 월드 접속이 끊긴다 — 실사용자 UX 후속 (2026-09-05, 미결 — 팀 결정)
- **증상**: T-115 의 메커니즘 그대로. 사용자가 다른 탭·창으로 30초 넘게 가 있으면 Chrome 이 rAF 를 멈춰 Unity 하트비트가 끊기고, 돌아오면 `Failed to connect`/`[Disconnect] 서버가 사유를 보내지 않았다` 상태다. 서버는 10초 안에 반응 없는 클라이언트를 정리한다.
- **선택지**: ① 복귀 시 자동 재접속(world-session 재발급 → StartClient; grant 는 120초 1회용이라 새로 받아야 함) ② `UnityTransport.DisconnectTimeoutMS` 상향(현재 30000; 서버 쪽 유령 세션이 길어진다) ③ `visibilitychange` 에서 안내 오버레이. 권장 ① + ③. docs/26 ③ 에 올림.
- **관련**: T-115, S15P21A604-83·-206, `Scripts/Network/Auth/WorldDisconnectReporter.cs`, `Scripts/World/Entry/WorldEntryGate.cs`

### T-121. 빌드 첫 씬(CharacterLobby)에 GameBootstrap 이 없어 WebGL 이 Mock API 로 시작했다 — 로비만 깨져서 아무도 못 봤다 (2026-09-05, 해결)
- **증상**: -412 에디터 검증에서 `ApiConfig.useMockApi=false` 인데 로비 씬 단독 플레이 콘솔에 `[ApiServices] Init — mock=True spring=` 이 떴다. 실패 경로를 시험하려 했는데 개발 모드(`DevUnlockedAll`)가 돌았다.
- **원인**: `ApiConfig` 를 읽어 `ApiServices.Init` 을 부르는 것은 `GameBootstrap` 하나인데, 그 오브젝트는 `main.unity` 에만 있었다(씬 파일 grep: main 1 / CharacterLobby 0). 로비가 부르는 `ApiServices.EnsureInitialized()` 는 `if (Booth == null) Init(true, "", "")` — 부트스트랩이 없으면 **조용히 Mock**. 실사용자 경로에서는 로비의 프로필 조회가 Mock(sk_01)이라 저장 외형이 복원되지 않고(P0 WORLD-04), 카탈로그도 Mock 이라 유료 파츠가 전부 열린 채 월드 저장에서 409 가 난다. main 에 들어가면 `GameBootstrap` 이 실서버로 재초기화하므로 **월드는 정상 — 로비만 깨진다.** 그래서 -333 WebGL 접속 실측("목 서버 발급")도, 그 뒤 어떤 검증도 이걸 잡지 못했다. -168(Avatar 저장·복원 검증)이 계속 "해야 할 일" 로 남아 있던 이유와 같다.
- **해결**: `CharacterLobby.unity` 에 main 과 같은 `@GameBootstrap`(ApiConfig 참조) 을 루트 최상단에 넣었다. 싱글톤 + `DontDestroyOnLoad` 라 main 진입 시 중복은 스스로 파기된다(실측 1개). `EnsureInitialized()` 의 Mock 폴백에 `LogError` 를 달아 다시 조용히 떨어질 수 없게 했다(실측: `Booth` 를 비운 뒤 호출 → 에러 로그). MR !253, Jira -418 완료.
- **예방**: ⑴ **"단독 실행 안전장치" 가 곧 "배포 첫 씬의 기본 경로" 가 될 수 있다** — 첫 씬이 바뀌거나 부트스트랩 오브젝트가 한 씬에만 있으면 그렇게 된다. 빌드 첫 씬에는 부트스트랩이 반드시 있어야 하고, 폴백은 경고가 아니라 에러여야 한다(T-24 와 같은 결론). ⑵ **"에디터에서 Mock 으로 잘 되더라" 는 실서버 검증이 아니다.** 검증 전에 `ApiServices.IsMock` 을 먼저 찍는다 — 이번에도 그 한 줄이 문제를 드러냈다. ⑶ 씬을 나눠 쓰는 싱글톤 컴포넌트는 **어느 씬에 있는지를 grep 으로 확인**한다. 두 씬을 오가며 개발하면 "어딘가에는 있다" 로 착각한다.
- **관련**: S15P21A604-418, -412, -168, -333, T-24, `festa-unity/Assets/_Project/Scenes/CharacterLobby.unity`, `festa-unity/Assets/_Project/Scripts/Integration/ApiServices.cs`, `Core/Bootstrap/GameBootstrap.cs`

### T-122. Overlay 를 닫아도 focus 가 `body` 에 남아, 닫은 뒤 WASD·F 가 월드로 가지 않는 경로가 있었다 (2026-09-05, 해결)

- **증상**: React Overlay(Project·Survey·Consultation·AI·LAPTOP)를 열었다 닫으면 `document.activeElement` 가 `body` 로 남는다. Unity 6 WebGL 은 키보드 이벤트를 canvas 타깃으로 받으므로(T-114 와 같은 뿌리), 그 상태에서는 이동·상호작용 키가 월드로 가지 않는다.
- **원인**: `OverlayFrame` 이 열릴 때만 자기 루트(`tabIndex={-1}`)에 focus 를 가져가고 닫힐 때 돌려주지 않았다. 09-08 사용자 테스트의 주 동선(오브젝트 상호작용 → 오버레이 → 닫기 → 이동)이 정확히 이 경로다.
- **해결**: 언마운트 시 ⑴ 열기 직전 focus 요소가 아직 문서에 있고 `body` 가 아니면 그쪽으로 ⑵ 아니면 문서의 `canvas` 로 ⑶ canvas 도 없으면(Game Studio 등) 아무것도 하지 않는다. 월드에서 F 로 연 경우 ⑴이 곧 canvas 복구다. `#unity-canvas` id 대신 `canvas` 태그로 찾아 T-114 의 MR 순서에 묶이지 않게 했다. MR !269, Jira S15P21A604-428. 단위 테스트 4건.
- **예방**: **focus 는 가져간 쪽이 돌려준다.** 모달을 여는 코드를 쓸 때 닫기 경로의 focus 복구를 같은 커밋에서 짠다. 이 결함은 브라우저만 보면 안 보이고 "닫은 뒤 키가 안 먹는다" 로만 드러난다.
- **관련**: S15P21A604-428, T-114, GitLab #132(G-8 Input Lock — Overlay 중 월드 입력 차단은 별건, Unity 합의 대기)
