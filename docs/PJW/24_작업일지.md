# PJW 작업일지

> 박준우 — Game Studio 파트 개인 작업 기록. 공용 작업일지와 별도로 관리한다.

## 작성 지침

- 작업(티켓 단위)을 완료하면 **그 즉시** 이 파일을 갱신한다. 하루 끝에 몰아서 적지 않는다.
- 기록은 최신 날짜가 위로 오도록 `## YYYY-MM-DD` 섹션 아래, 작업 세션마다 `### 제목 (S15P21A604-N)` 소제목으로 작성한다.
- 각 항목에 담을 것: 무엇을 / 왜 / 결과, 관련 파일·MR·커밋.
- 담당 구분: 👤 사람 / 🤖 AI.
- 문제가 생기면 해결 여부와 무관하게 **`25_트러블슈팅.md`에 반드시 등록**하고, 여기엔 T-번호로 링크만 남긴다.

---

## 2026-08-27

### Jira 담당자 변경 통보 및 개인 문서 뼈대 생성 (S15P21A604-15)

- 👤🤖 Game Studio QA/고도화 → Game Studio 자체 구현 담당으로 역할 확장. 강형순님 소유 Game Studio Jira 티켓 8건(113·115·155·156·180·200·201·202) 중 백엔드 무관 범위를 확인해 GitLab 이슈로 통보 후 담당자를 박준우로 변경.
- 🤖 `115`는 전체가 아니라 game-studio 내부 로더·세션 포트 부분만 이관 — 전역 라우팅은 이정헌님 코멘트 기준 그대로 유지.
- 🤖 `docs/PJW/24_작업일지.md`·`25_트러블슈팅.md` 뼈대 생성 (에픽 `S15P21A604-15` 키 재사용, MR !49).
- 📎 관련 파일: `docs/PJW/24_작업일지.md`, `docs/PJW/25_트러블슈팅.md`
- 🔗 트러블슈팅: 신규 없음

### Authoring·Maker front 반입 스모크 테스트 (S15P21A604-113) ✅ 완료

- 🤖 완료 조건 중 "front 통합 배포 환경 smoke test"는 CI 러너 부재로 통합 배포 환경이 없어 로컬 dev 서버(`VITE_USE_MOCK=true`)로 대체 검증.
- 🤖 새 프로젝트 생성 → 시작 템플릿 6종 확인 → PLATFORMER 템플릿 진입 → 오브젝트 선택·수정 → 저장 → Preview까지 전 구간 콘솔 에러 0건, 회귀 0건.
- 🤖👤 캔버스 클릭/드래그 배치는 1차 검증 환경(Claude Browser, 스크린샷 비활성)에서 좌표 클릭이 안 돼 한계로 남겼으나, Claude in Chrome(실 화면)으로 재검증하여 클릭·드래그 배치 둘 다 정상 확인 — 한계 항목 해소.
- 📎 관련 파일: `docs/PJW/verify/113-authoring-maker-smoke.md`, MR [!50](https://lab.ssafy.com/s15-metaverse-game-sub1/S15P21A604/-/merge_requests/50)
- 🔗 트러블슈팅: 신규 없음
- ✅ Jira 자동 전환 확인 — `develop` 도달 즉시 '완료' 전환됨

### Game Studio 성능 실측 (S15P21A604-156) ✅ 완료 + 후속 이슈 분리

- 🤖 편집 조작 p95 100ms: `editorPerformance.test.ts`(500-object 이동) 3회 반복 실행, 매번 통과.
- 🤖👤 활성 탭 55fps: `?fixture=max`(500 Object/10,000 Tile) 로드 후 `/play?source=local&perf=1`에서 Claude in Chrome으로 3회 실측(80/75/77 fps) — 탭 최전면 필요(`document.visibilityState==='visible'`)해서 사람이 직접 확인.
- 🤖 완료조건은 아니지만 p95 frame time·느린 frame 비율이 오버레이 자체 설계 목표(18.2ms·5%)를 3회 모두 2.3~2.7배 초과 — 재현되는 병목으로 판단해 별도 Bug [S15P21A604-263](https://ssafy.atlassian.net/browse/S15P21A604-263)로 분리.
- 📎 관련 파일: `docs/PJW/verify/156-perf-measure.md`, MR [!51](https://lab.ssafy.com/s15-metaverse-game-sub1/S15P21A604/-/merge_requests/51)
- 🔗 트러블슈팅: 신규 없음
- ✅ Jira 자동 전환 확인 — '완료'

### Game Studio 접근성 — 키보드 전용 조작 검증 (S15P21A604-201) ✅ 완료 + 버그 수정

- 🤖 `TopDownCanvas.tsx`의 오브젝트 배치가 캔버스 클릭/드롭 전용이라, 배치 모드 진입까지는 키보드로 가능해도(팔레트가 `<button>`) 실제 배치 확정은 키보드로 불가능한 완전 차단 상태였음을 발견.
- 🤖 `GameStudioShell.tsx`에 배치 모드에서 Enter/Space로 Scene 중앙에 배치 확정하는 핸들러 추가. 1차 구현은 기존 `keyboardCanvasContext`(포커스가 캔버스/body여야 함) 게이트 안에 넣어 팔레트 버튼에 포커스가 남는 실사용 흐름에서 영구히 발동 못 하는 상태였다가, 재현 검증 중 발견해 게이트 밖으로 이동.
- 🤖 `placeObject`를 `useCallback`으로 감싸 TDZ 버그(선언 전 참조) 방지 + `react-hooks(exhaustive-deps)` lint 경고 해소.
- 🤖👤 Claude in Chrome으로 배치(Enter/Space)·선택(Tab+Enter, 기존 정상)·이동(방향키, 기존 정상)·저장(Ctrl+S, 기존 정상) 전 플로우 키보드 전용 재현 확인. `tsc -b`·`lint`·`vitest run src/game-studio`(117 tests) 전부 통과.
- 🤖 검증 중 발견한 원인 미상 별건 현상(Scene 이름 입력창에 우연히 문자 입력됨) T-1로 등록, 재현 안 돼 별도 수정 없이 보류.
- 📎 관련 파일: `docs/PJW/verify/201-keyboard-a11y.md`, `festa-frontend/src/game-studio/studio/ui/GameStudioShell.tsx`
- 🔗 트러블슈팅: [T-1](25_트러블슈팅.md)
- ✅ Jira 자동 전환 확인 — '완료' (MR [!54](https://lab.ssafy.com/s15-metaverse-game-sub1/S15P21A604/-/merge_requests/54) 머지 직후)

### Game Studio 최대 fixture 성능 병목 원인 규명·수정 (S15P21A604-263) ✅ 완료

- 🤖 원인 규명: `ReferenceGamePlayer.tsx`가 타일마다(레이어당 최대 10,000회) `resolveTilesetVisual` tileset lookup을 반복 실행 — 레이어당 1회면 충분한 계산을 10,000배 낭비하고 있었고, 메모이제이션도 없어 런타임 상태(월드 tick·이동)가 바뀔 때마다 정적 타일맵까지 매번 재계산됐음.
- 🤖 수정: 타일 렌더링을 `TileLayers`라는 `memo` 컴포넌트로 분리 + tileset lookup을 타일 루프 밖(레이어 루프 안)으로 이동.
- 🤖 신규 회귀 테스트(`referenceGamePlayerTileMemo.test.tsx`)로 `resolveTilesetVisual` 호출 횟수 직접 계측 — 수정 전 10,000회 → 수정 후 1회(이동 3회 리렌더 후에도 그대로) 확인. DOM 노드 개수/참조만 보는 1차 테스트는 React의 key 재사용 때문에 memo 유무를 구분 못 해 무의미했던 걸 발견하고 지표를 호출 횟수로 바꿈.
- 🤖 `tsc -b`·`lint`·`vitest run src/game-studio`(118 tests, 신규 1건 포함) 전부 통과. Claude in Chrome으로 `?fixture=max` 시각 확인 — 렌더링 정상, 콘솔 에러 0건.
- 👤 완료 조건 실측 재검증: 탭 최전면 두고 3회 측정 — FPS 119/119/119, p95 8.6/8.6/8.5ms, 끊김 0%/0%/0%. 목표(55fps·18.2ms·5%) 전량 여유 있게 통과, 156 실측치(75~80fps·41.7~50.1ms·11~12%) 대비 p95 5~6배 감소·끊김 완전 해소 — 원인 규명이 실측으로 확정됨.
- 📎 관련 파일: `docs/PJW/verify/263-perf-fix.md`, `festa-frontend/src/game-studio/runtime/reference/ReferenceGamePlayer.tsx`, `festa-frontend/src/game-studio/__tests__/unit/referenceGamePlayerTileMemo.test.tsx` (MR은 사용자 검토 후 제출 예정)
- 🔗 트러블슈팅: [T-2](25_트러블슈팅.md) — 루트 `.gitignore`의 `reference/` 패턴이 game-studio 소스 디렉터리와 우연히 겹침 (작업 자체엔 지장 없었음)
- ✅ Jira 자동 전환 확인 예정 — `develop` 도달 시 '완료'

<!-- 새 날짜는 이 아래에 추가 -->
