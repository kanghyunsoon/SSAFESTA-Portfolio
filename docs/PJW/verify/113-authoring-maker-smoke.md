# S15P21A604-113 — Authoring·Maker 스모크 테스트 결과

> 검증일: 2026-08-27 | 검증자: 박준우 | 대상 커밋: MR !8 (c064fb1)

## 배경

완료 조건 중 "front 통합 배포 환경에서 편집→로컬 저장→Preview smoke test"는
CI 러너 부재로 통합 배포 환경 자체가 존재하지 않아, **로컬 dev 서버(`npm run dev`,
`VITE_USE_MOCK=true`)로 대체 검증**했다.

## 검증 절차 및 결과

| 절차 | 결과 |
|---|---|
| 새 프로젝트 생성 (`/app/games/501/edit`) | ✅ 정상 — 기본 STORY형 초기 Scene 로드 |
| 시작 템플릿 선택 화면 진입 (▦ 시작 템플릿) | ✅ 정상 — 6종 템플릿(스토리·방탈출·수집·점프맵·슈팅·생존전) 카드 전부 정상 표시 |
| 템플릿 진입 — PLATFORMER("별빛 점프맵") | ✅ 정상 — 확인 다이얼로그 → 43개 오브젝트 포함 Scene으로 전환, 토스트 확인 |
| 템플릿 교체 확인 다이얼로그 — ESCAPE("비밀 방탈출") | ✅ 다이얼로그 정상 표시 (실제 교체는 미실행, 취소로 종료) |
| 오브젝트 선택 — Alt+L 오브젝트 목록에서 `jumpCheckpoint` 선택 | ✅ 정상 — 속성 패널에 체크포인트 속성 표시 |
| 오브젝트 수정 — 방향키로 위치 이동 | ✅ 정상 — "저장되지 않은 변경" 상태로 전환 확인 |
| 저장 | ✅ 정상 — "브라우저(Mock) 초안을 저장했습니다" 토스트, 새로고침 후 재로드 확인 |
| 플레이 테스트(Preview) | ✅ 정상 — PLATFORMER 전용 HUD(이동/점프 안내) 정상 렌더 |
| 콘솔 에러 | 세션 전체에서 **0건** (루트 경로(`/`) 404는 라우터 미정의로 인한 기존 동작, 신규 아님) |
| 신규 오브젝트 배치 — 클릭 방식 (팔레트 "NPC" 클릭 → 캔버스 클릭) | ✅ 정상 — 클릭 지점에 정확히 생성, 오브젝트 카운트 4→5 증가, 속성 패널 즉시 표시 |
| 신규 오브젝트 배치 — 드래그 방식 (팔레트 "벽/장애물" → 캔버스 드롭) | ✅ 정상 — 드롭 지점에 정확히 생성, 카운트 5→6 증가 |
| 배치 후 저장 | ✅ 정상 — "저장 완료" 토스트, 미니맵에 신규 오브젝트 반영 |

(위 4건은 1차 검증 이후 Claude in Chrome(실제 화면 렌더링 가능한 환경)으로 재검증하여 스크린샷 기반으로 직접 확인함 — 아래 "한계" 항목 해소.)

## 6개 템플릿 검증 범위 (참고)

실제 진입·편집까지 전량 검증한 것은 PLATFORMER 1종. 나머지 5종은 목록 표시와 ESCAPE의
확인 다이얼로그만 점검 — 동일 코드 경로(같은 템플릿 로더 컴포넌트)라 리스크는 낮다고 판단.

## 결론

**회귀 0건 발견 — 별도 Bug 티켓 없음. 완료 조건 전량 충족.**
