# 로컬 규약 스냅샷 — CLAUDE.local.md + 세션 메모리

> **왜 이 문서가 있는가**: `CLAUDE.local.md`(프로젝트 루트)는 `.git/info/exclude`로 제외돼 있고,
> 세션 메모리(`~/.claude/projects/{경로 해시}/memory/`)는 홈 디렉터리에 있어
> **둘 다 git 저장소 밖**이다. 새 머신에서 `git clone`하면 이 두 곳이 통째로 비어 있다.
> 이 파일은 `docs/LJH/`(front 브랜치에 커밋됨)에 두는 **유일하게 이전 가능한 사본**이다.
>
> ⚠️ **드리프트 경고**: 이 스냅샷 자체가 원본과 정본 2벌 관계다(#45·#51·#59와 같은 위험).
> **CLAUDE.local.md나 메모리 파일을 고치면 같은 커밋에서 이 문서도 갱신한다** — 갱신은
> 덧붙이기가 아니라 해당 섹션 전체 교체(교체 서술 원칙 그대로).
>
> **갱신일**: 2026-08-25

---

## A. CLAUDE.local.md 스냅샷

원본 경로: `/Users/colosair/projects/ssafesta/CLAUDE.local.md` (프로젝트 루트, `.git/info/exclude` 등록)

```markdown
# LJH 개인 규칙 (로컬 전용 — 팀 공유 아님)

> ⚠️ 이 파일이나 세션 메모리(`~/.claude/.../memory/`)를 고치면 **같은 커밋에서**
> `docs/LJH/26_로컬_규약_스냅샷.md`도 함께 갱신할 것 — 이 파일은 git 추적 밖이라
> 스냅샷 없이는 환경 이전 시 소실된다.

## 문서 규칙 — 상태 서술은 교체, 덧붙이기 금지

- **덧붙이기(append) 절대 금지.** `대기 → ✅ 확정`, `— 이후 #NN로 확정`, 취소선 뒤 새 값 병기 등
  낡은 서술 위에 갱신을 쌓지 않는다. 낡은 문장은 **최종 상태 문장으로 다시 쓴다**(내용 누락 없이).
  변경 이력은 Git/Issue/PR이 담당한다.
- **상태의 정본(SSOT)은 1곳.** spec의 C-xx 등 미결 항목 상태는 Clarifications 표에서만 관리.
  다른 섹션은 상태를 복제하지 않고 확정된 동작·정책만 기술.
- **상태 변경은 정본화 작업.** 확정 시 해당 ID로 파일 전체 grep → SSOT 외 상태 복제를
  최종 결정문으로 교체 → 전 occurrence 확인까지가 완료 조건.
  (근거: spec 005 자기모순 사고 #45 / spec 007 동일 패턴 #51)
- 팀 규칙 승격은 리드 승인 사안 — 공유 문서(CLAUDE.md·docs/00)에 임의로 넣지 않는다.

## 이슈·브랜치 확인 규칙

- **이슈 응답 확인은 내용까지.** 코멘트 개수·타임스탬프 비교로 "새 응답 없음" 판정 금지.
  마지막 코멘트의 작성자를 확인하고, 내가 아니면 본문 전문을 읽고 backlog와 대조한다.
  (근거: #60 즉답을 "코멘트 1개뿐"으로 오판한 사고, 2026-08-23)
- **작업 착수 전 `git fetch origin` 전 브랜치 필수.** develop·game·back·ai가 계약 정본을
  먼저 갱신하는 구조라 front만 봐서는 낡는다.

## 게시 전 대기 범위

- **원격 공개 게시(이슈·PR·코멘트·리뷰 등)는 예외 없이 항상 초안 작성 후 대기.** 사용자 승인 없이 게시 안 함.
- **내부 문서(작업일지·backlog 등)는 사용자 판단이 필요한 중요 사안일 때만 대기.** 일반적인 갱신·기록은
  바로 작성·커밋 진행 — 매번 승인 구하지 않는다.

## "브랜치 최신화"에는 공용 문서 pull이 항상 포함된다

- 사용자가 "브랜치 최신화"를 지시하면 **코드 fetch/sync뿐 아니라 develop의 공용 문서
  (docs/00·docs/17·docs/26 등 팀 규칙·결정 로그) 회수도 항상 세트**로 본다. 코드만 갱신하고
  문서는 별도 확인 없이 넘어가지 않는다.
  (근거: #59→PR#61이 develop에만 반영된 채 front가 못 받아온 것을 뒤늦게 발견, 2026-08-23)

## 자율 처리 판정 — 리드 확인은 Unity 유발 건에만

- **판정 기준은 하나: 이 작업이 Unity 파트의 코드 수정을 유발하는가.**
  유발하면 합의, 아니면 실행하고 결과를 보고한다. "제가 고쳐도 되나요"를 게시물에 쓰지 않고
  무엇을 했는지 통보한다.
- **"Unity 파일을 만지는가"가 기준이 아니다.** 2026-08-25 실판정 2건 —
  `festa-unity` 1,462파일을 develop본으로 복원한 것은 Unity 동작이 안 바뀌므로 **자체 처리**,
  `BOOTH_LAPTOP_INTERACT`에서 `url` 필드 제거는 Unity 코드 수정을 유발하므로 **합의**.
- **자체 처리 대상**: FE 내부 코드, 규약·절차 확립, 문서 정정, 내가 발견한 결함 수정.
  남이 쓴 코드라도 FE 영역이면 고친다. FE·BE 간 합의가 이미 선 사항은 그대로 진행한다
  (상대 파트가 이슈에 답을 남겼으면 그것이 합의다).
- **자율의 경계 — MR은 리뷰어를 지정하고 대기한다. 셀프 머지 금지.**
  권한 문제가 아니라 규칙 문제다: 보호 브랜치는 `main` 하나뿐이라 `develop`·`front`는
  Developer 권한으로도 머지되지만, `docs/17` §8 "최소 1명 리뷰"를 우회하게 된다
  (GitLab Free라 승인 규칙 강제 불가 — 문화로 운영, `docs/jira-gitlab-workflow.md:106`).
- **권한 제약은 주장하기 전에 실측한다.** 2026-08-25에 "MR 머지는 리드 권한"이라고
  근거 없이 반복했다가 실제로는 내 Developer 권한으로 가능함이 드러났다.
  (근거: 사용자 지시 "의미없는 리드 확인 작업을 반복하지 않고 금지한다", 2026-08-25.
  이전까지 남의 코드 수정·절차 확정마다 리드 답변을 기다린 것이 front에 들어간 결함을
  방치하고 완료 작업 5건을 Jira '진행 중'에 묶어두는 병목이 됐다)
```

---

## B. 세션 메모리 스냅샷

원본 경로: `~/.claude/projects/{프로젝트 경로 해시}/memory/`(머신·프로젝트 경로 종속, 이전 불가).
경로 해시가 OS·사용자 경로 문자열을 그대로 담기 때문에 **환경마다 다른 디렉터리를 쓰고, 어느 하나를 "현재"로 고정해 서술하지 않는다** — 이 프로젝트는 두 환경을 오가며 작업 중이다:

- macOS: `~/.claude/projects/-Users-colosair-projects-ssafesta/memory/` (하이픈)
- Windows: `C:\Users\SSAFY\.claude\projects\C--colosair-projects-ssafesta\memory\` (언더스코어)

> ⚠️ **이 문서가 경고한 드리프트가 실제로 발생했다** (2026-08-25 최초 확인, 2026-08-26 재확인).
> 머신을 이전하면 메모리가 새로 구성되며 이름 규칙까지 바뀐다(하이픈 ↔ 언더스코어) — **양쪽에
> 동시에 존재하는 법이 없다.** 08-23 macOS 사본(B-1~B-4)이 뜬 시점의 Windows에는 그 4개가
> 없었고, 반대로 08-26 macOS 재확인 시점에는 B-1~B-4가 정확히 살아 있고 Windows 전용 B-5~B-8이
> 없다. 즉 "현재 없는 쪽"은 어느 환경에서 여는지에 따라 매번 뒤바뀐다 — **B-1~B-4 / B-5~B-8은
> 각각 해당 환경의 스냅샷일 뿐, 어느 쪽도 상시 정본이 아니다.**
> **규칙 내용 자체는 유실되지 않는다** — A 섹션(CLAUDE.local.md)의 같은 이름 섹션들과 1:1
> 대응하는 사본이고, A는 두 환경 공통 정본으로 그대로 살아 있다. 새 환경에서 메모리가 비어
> 있으면 **C-2 복원 절차**로 A를 원본 삼아 재구성한다.

> 아래 **B-1~B-4**는 macOS 환경(`-Users-colosair-projects-ssafesta`)의 08-23 스냅샷.

### B-0. MEMORY.md (인덱스)

```markdown
- [문서 갱신은 교체, 덧붙이기 금지](feedback-doc-replace-not-append.md) — "대기→✅확정" 마커 절대 금지, 최종 상태 문장으로 재서술
- [이슈 확인은 내용까지](feedback-issue-check-read-content.md) — 코멘트 개수·시각 비교 금지, 마지막 코멘트 작성자+본문 확인. 착수 전 전 브랜치 fetch 필수
- [게시 전 대기 범위](feedback-publish-wait-scope.md) — 원격 공개 게시는 항상 대기, 내부 문서(작업일지·backlog)는 중요 사안만 대기·일반 갱신은 바로 진행
- [브랜치 최신화 = 공용 문서 pull 포함](feedback-branch-sync-includes-docs.md) — 코드만 fetch하고 docs/00·17·26 회수 누락하지 않기
```

### B-1. feedback-doc-replace-not-append.md

```markdown
---
name: feedback-doc-replace-not-append
description: 문서 갱신 시 "대기 → ✅ 확정" 덧붙이기 절대 금지 — 최종 상태 문장으로 교체 서술
metadata:
  type: feedback
---

문서(spec·docs)의 낡은 서술을 갱신할 때 `— ✅ 이후 #NN로 확정` 같은 마커를 **덧붙이는 방식 절대 금지**. 낡은 문장을 내용 누락 없이 **최종 상태 문장으로 다시 쓴다**. 이력은 Git/Issue가 담당.

**Why:** 2026-08-22 spec 005 정본화 작업에서 8곳을 전부 "덧붙이기"로 처리했다가 사용자 지적 — "문서를 무겁게 하면서 동시에 복잡하게 만듦. 사실상 재발 불가능할 정도로 금지한다." 덧붙이기는 자기모순(#45 사고)의 변형일 뿐이다 — 낡은 상태어가 본문에 계속 남는다.

**How to apply:**
- 갱신 = 교체. 확정 근거는 이슈 링크를 문장 안에 자연스럽게(`**막지 않고 경고**(#45로 채택)`), 화살표·✅·"이후 확정" 금지
- 날짜 박힌 검토 기록도 상태 꼬리("승인 대기")는 떼고 의견 본문만 남긴다 — 상태는 SSOT(Clarifications 표)가 담당
- 완료 조건: 해당 ID 파일 전체 grep → 전 occurrence 교체 확인
- 프로젝트 CLAUDE.md "필수: 문서 규칙" 섹션에 팀 강제 규칙으로 등록됨 (2026-08-22)
```

### B-2. feedback-issue-check-read-content.md

```markdown
---
name: feedback-issue-check-read-content
description: 이슈 응답 확인은 코멘트 개수·시각 비교가 아니라 "마지막 코멘트 작성자+내용"까지 읽어야 함. 세션 시작 시 전 브랜치 fetch 필수
metadata:
  type: feedback
---

이슈/PR 응답 확인 시 **코멘트 개수나 타임스탬프 비교만으로 "새 응답 없음" 판정 금지**. 마지막 코멘트의 **작성자와 내용**까지 읽는다 — 마지막 코멘트가 상대방이면 그게 미처리 응답일 수 있다. 그리고 **모든 작업 시작 전 `git fetch origin`(전 브랜치) 필수**.

**Why:** 2026-08-23 #60에서 재발 — 게시 직후 kanghyunsoon이 즉답(빌드 URL 확정+credential BE 이관)을 달았는데, "코멘트 1개 = 내가 아는 그 상태"로 오판해 '응답 없음' 보고. 실제로는 resolver 주석 갱신·BE 질의 이관이 필요한 실질 답변이었다. backlog #30·#34 stale도 같은 뿌리(상태 재확인 없이 기록 신뢰).

**How to apply:**
- 응답 확인 절차: 마지막 코멘트 `author` 확인 → 내가 아닌 사람이면 **본문 전문 읽기** → backlog 반영 여부 대조
- backlog의 "회신 완료—대기" 표 항목은 확인 시마다 마지막 코멘트 작성자를 실제 조회
- 세션 시작·작업 착수 전 `git fetch origin` — front만이 아니라 전 브랜치(develop·game·back·ai가 계약 정본을 먼저 갱신하는 구조)
- 관련 기존 규칙: [[feedback-doc-replace-not-append]](backlog 자체도 stale해질 수 있음 전제)
```

### B-3. feedback-publish-wait-scope.md

```markdown
---
name: feedback-publish-wait-scope
description: 게시 전 대기는 원격 공개 게시(이슈·PR·코멘트·리뷰)에만 예외 없이 적용. 내부 문서(작업일지·backlog)는 중요 사안일 때만 대기, 일반 갱신은 바로 진행
metadata:
  type: feedback
---

**원격 공개 게시**(GitHub 이슈·PR·코멘트·리뷰 등, 팀원이 보는 것)는 **예외 없이 항상** 초안 작성 후 사용자 승인 대기.

**내부 문서**(작업일지·backlog 등, 세션 간 인수인계용)는 **사용자 판단이 필요할 정도로 중요한 사안일 때만** 대기하고, 일반적인 기록·상태 갱신은 승인 없이 바로 작성·커밋 진행.

**Why:** 2026-08-23, 4건 처리 후 작업일지·backlog 갱신 초안을 만들어 대기했는데 사용자가 "이건 그냥 반영해서 커밋하라"고 정정 — 매번 대기시키는 게 오히려 비효율. 동시에 "게시는 예외 없이 항상 대기"를 재확인.

**How to apply:**
- 이슈/PR/코멘트/리뷰 게시 → 무조건 초안 먼저, 승인 후 게시(안전 규칙 그대로 유지)
- 작업일지 1줄 기록, backlog 상태 갱신, 트러블슈팅 T-번호 등록 같은 **일상 기록**은 바로 작성해 커밋
- 판단 필요 기준 예시: 문서 구조를 바꾸는 대규모 재작성(예: backlog 105→85줄 정리), 팀 규칙 신설·수정, 여러 파일에 걸친 일괄 정정 — 이런 건 여전히 방향 확인 후 진행
```

### B-4. feedback-branch-sync-includes-docs.md

```markdown
---
name: feedback-branch-sync-includes-docs
description: "브랜치 최신화" 지시에는 코드뿐 아니라 develop의 공용 문서(docs/00·docs/17·docs/26 등) 회수도 항상 포함
metadata:
  type: feedback
---

사용자가 "브랜치 최신화"를 지시하면 코드 `git fetch`/sync만이 아니라 **develop의 공용 문서(팀 규칙·결정 로그) 회수도 항상 세트**로 수행한다.

**Why:** 2026-08-23, #59 재발 방지 규칙이 PR #61로 develop에 머지됐는데 front는 이후 "브랜치 최신화"를 여러 차례 지시받고도 코드 fetch만 하고 `docs/00`·`docs/17`을 안 끌어와 규칙 3건이 front 문서 정본에 없는 채로 방치됐다. 사용자가 "브랜치 최신화에는 공용 문서 pull도 반드시 포함된 세트여야 함"이라고 명시 정정.

**How to apply:**
- "브랜치 최신화" 지시 시: `git fetch origin` 전 브랜치([[feedback-issue-check-read-content]]) + **develop 대비 공용 문서(`docs/00`·`docs/17`·`docs/26` 등) diff 확인** → front 고유 변경 없으면 파일 단위 checkout으로 회수
- front 고유 변경이 있으면 회수 전 사용자에게 보고(Block A 절차와 동일)
- 이 규칙은 [[feedback-doc-replace-not-append]]가 실제로 팀 공식 문서에 반영돼 있는지를 담보하는 전제 조건이기도 하다 — 문서가 안 오면 규칙 자체가 front에서는 "로컬 관행"에 머문다
```


> 아래 **B-5~B-8**은 Windows 환경(`C--colosair-projects-ssafesta`)의 08-25 스냅샷.

### B-5. MEMORY.md (인덱스 — Windows 08-25)

```markdown
- [Codex 검증 판단 안내](feedback_codex_verification_judgment.md) — 직접 대조가 나아 보이면 말없이 바꾸지 말고 먼저 안내
- [착수 전 브랜치 최신화](feedback_sync_before_work.md) — 내 브랜치의 공유 문서 사본은 낡았다고 가정, develop 대조 먼저
- [리드 확인 최소화 방침](feedback_autonomy_over_lead_confirmation.md) — Unity 직접 영향만 합의, 나머지는 자체 처리 후 보고
```

### B-6. feedback_autonomy_over_lead_confirmation.md

```markdown
---
name: feedback_autonomy_over_lead_confirmation
description: 리드 확인은 Unity 직접 영향 건에만 — 자체 처리 가능·규약 확립·FE/BE 합의분은 묻지 말고 실행 후 보고
metadata: 
  node_type: memory
  type: feedback
  originSessionId: a0aa6df7-054b-45fb-bd55-3adde2829f29
  modified: 2026-08-25T05:08:12.352Z
---

SSAFY FESTA에서 리드 확인을 받아야 하는 것은 **Unity에 직접 영향이 가는 변경뿐**이다. 나머지는 확인을 구하지 말고 처리한다.

- **자체 처리 후 보고**: FE 내부 코드, 규약·절차 확립, 문서 정정, 내가 발견한 결함 수정 — 남이 쓴 코드라도 FE 영역이면 고친다.
- **그대로 진행**: FE·BE 간 합의가 이미 선 사항 (상대 파트가 이슈에 답을 남겼으면 그것이 합의다).
- **합의 필요**: Unity 브리지 페이로드 변경처럼 Unity 파트 작업이 뒤따르는 건.

**판정 기준은 "Unity 파트가 코드를 고쳐야 하는가"이지 "Unity 파일을 만지는가"가 아니다.** 2026-08-25 실제 판정 2건: `festa-unity` 1462파일을 develop본으로 복원한 것은 Unity 동작이 안 바뀌므로 자체 처리했고, `BOOTH_LAPTOP_INTERACT`에서 `url` 필드를 빼는 것은 Unity 코드 수정을 유발하므로 합의로 남겼다.

**Why**: 2026-08-25 사용자 지시 — "의미없는 리드 확인 작업을 반복하지 않고 금지한다". 이전까지 남의 코드 수정·절차 확정마다 리드 답변을 기다렸는데, 그 대기가 이미 front에 들어간 결함(guard 오배치)을 방치하고 완료된 작업 5건을 Jira '진행 중'에 묶어두는 병목이 됐다.

**How to apply**: 작업 전 "이게 Unity 작업을 유발하는가"만 판정한다. 아니면 실행하고 결과를 보고한다. "제가 고쳐도 되나요" 류의 질문을 게시물에 쓰지 않는다 — 대신 무엇을 고쳤는지 통보한다. 게시물 초안은 여전히 사용자에게 승인받되, 리드에게 허락을 구하는 내용을 담지 않는다.

**자율의 경계 — MR은 리뷰어를 지정하고 대기한다.** 셀프 머지는 하지 않는다. 이건 권한 문제가 아니라 규칙 문제다: GitLab 보호 브랜치는 `main` 하나뿐이고 `develop`·`front`는 보호되지 않아 Developer 권한으로도 머지가 되지만, `docs/17` §8의 "최소 1명 리뷰"를 우회하게 된다(GitLab Free라 승인 규칙을 강제할 수 없어 문화로 운영 중 — `docs/jira-gitlab-workflow.md:106`). **권한 제약을 주장할 때는 반드시 실측하고 말한다** — 2026-08-25에 "MR 머지는 리드 권한"이라고 근거 없이 반복했다가 실제로는 내 권한으로 가능함이 드러났다.
```

### B-7. feedback_codex_verification_judgment.md

```markdown
---
name: feedback-codex-verification-judgment
description: Codex 검증을 지시받아도 직접 대조가 나을 것 같으면 바로 전환하지 말고 먼저 안내할 것
metadata: 
  node_type: memory
  type: feedback
  originSessionId: a02a47a9-f8c5-4a3a-9708-ee50cc9fe19e
  modified: 2026-08-21T01:54:32.441Z
---

Codex 검증을 지시받았을 때, 판단상 직접 대조(git show/grep/comm 등)가 더 낫다고 보이면 **말없이 전환하지 말고 사용자에게 먼저 안내**한다 — 판단 근거를 짧게 제시하고 진행 여부를 확인받는다.

**Why:** [[trouble-codex-timeout-root-cause]]에서 Codex 타임아웃의 근본 원인이 "열린 탐색 범위를 요구하는 프롬프트"(예: "모든 행을 대조하라", "파일을 훑어봐라")임을 실측 확인했다. 순수 텍스트 응답 7.6초, 도구 1회 호출 11.5초로 Codex 자체는 정상 속도이며, 모델 티어·MCP 고정비용은 원인이 아니었다. 반면 열린 지시는 스텝 수십 회를 유발해 400초 이상 걸리거나 타임아웃된다. 이 판단 기준(요청이 열린 탐색을 요구하는가)이 서 있으므로, Codex 검증 지시가 와도 그 판단을 사용자에게 공유하는 게 맞다 — 사용자가 "Codex로"를 명시했어도 그 판단 근거를 모른 채 매번 타임아웃을 겪게 두지 않는다.

**How to apply:** Codex 검증을 지시받으면, 요청할 프롬프트가 (a) 정확한 명령(git show/grep 등)으로 좁혀지는가 (b) "전체를 훑어라"류 열린 지시가 되는가를 먼저 가늠한다. (b)에 해당하면 "이건 직접 대조가 나을 것 같습니다 — 이유: …" 식으로 짧게 안내하고, 사용자가 그래도 Codex로 하라면 항목을 (a) 형태로 좁혀서 실행한다. 말없이 대체하거나 말없이 강행하지 않는다.
```

### B-8. feedback_sync_before_work.md

```markdown
---
name: feedback-sync-before-work
description: 주요 구현·문서 작업 착수 전에 반드시 origin/develop 대조로 브랜치를 최신화할 것
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 49790181-817d-4338-9b37-2247019ee341
  modified: 2026-08-21T06:05:52.988Z
---

주요 작업(spec 산출물 작성, 구현 착수, 이슈 회신 등)에 들어가기 **전에 반드시 `git fetch origin develop` 후 대상 파일을 `origin/develop`과 대조**한다. 뒤늦게 검증 단계에서 발견하지 말고 착수 전에 한다.

**Why:** 2026-08-21 spec 005 `/speckit-plan` 산출물 4종을 작성해 커밋까지 마친 뒤, #36 회신 검증 중에야 `origin/develop`의 `specs/005/spec.md`가 `front`보다 53줄 앞서 있음을 발견했다. BE가 2026-08-20에 FR-014~FR-018 신설, C-04~C-07 갱신, 검증 규칙 error/warning 분류표를 넣었는데 `front` 사본에 없었고, 산출물 전체를 구버전 기준으로 쓴 상태였다. 결과적으로 설계 근간 2건이 틀렸다 — C-04 판정 주체(FE가 아니라 서버가 `errors`/`warnings`로 응답)와 facade 범위(005 제외가 아니라 FR-018로 편입). 재작업 커밋 `2616630`이 통째로 필요했다.

이 팀은 파트별 브랜치(`front`/`back`/`game`/`ai`)에서 각자 작업하고 `develop`으로 파일 단위 누적하는 방식이라, **내 브랜치의 공유 문서 사본은 기본적으로 낡았다고 가정해야 한다.**

**How to apply:** 작업 착수 전에 `git fetch origin develop --quiet` 후 `git diff --stat <현재브랜치> origin/develop -- <대상 경로>`로 확인한다. 차이가 있으면 `git checkout origin/develop -- <path>`로 파일 단위 sync하고(전체 merge 금지 — [[trouble-full-merge-deletes-unity]] 참조), 내 변경분은 그 위에 재적용한다. 특히 `specs/`, `docs/08`, `docs/10`, `docs/26`처럼 타 파트가 함께 고치는 문서가 대상일 때 필수다.
```

---

## C. 새 환경 세팅 가이드

새 머신에서 `git clone` 직후, 또는 기존 머신에서 `~/.claude` 메모리가 소실됐을 때 복원하는 절차.

### C-1. CLAUDE.local.md 복원

1. 위 **A절** 코드블록 전체를 `/Users/<user>/projects/ssafesta/CLAUDE.local.md`(프로젝트 루트)에 그대로 저장
2. `.git/info/exclude`에 등록해 커밋 방지:
   ```bash
   grep -q "CLAUDE.local.md" .git/info/exclude || echo "CLAUDE.local.md" >> .git/info/exclude
   ```
3. `git status`로 `CLAUDE.local.md`가 안 잡히는지 확인

### C-2. 세션 메모리 복원

1. 메모리 디렉터리 확인(세션 시작 시 자동 존재 — 없으면 AI 세션이 첫 메모리 저장 시 생성):
   `~/.claude/projects/-Users-colosair-projects-ssafesta/memory/`
2. 위 **B-1~B-4** 코드블록 각각을 파일명 그대로(`feedback-doc-replace-not-append.md` 등) 저장
3. **B-0**(MEMORY.md)을 같은 디렉터리의 `MEMORY.md`로 저장 — 이미 다른 메모리가 있으면 **덮어쓰지 말고 병합**(기존 줄 유지 + 여기 4줄 추가)

### C-3. 검증

```bash
cat CLAUDE.local.md | head -1        # "# LJH 개인 규칙"이 나오는지
ls ~/.claude/projects/-Users-colosair-projects-ssafesta/memory/
git status --short CLAUDE.local.md   # 출력 없어야 정상(추적 제외됨)
```

### C-4. 이후 유지 원칙

- CLAUDE.local.md나 메모리 파일을 고칠 때마다 **같은 턴에 이 스냅샷 문서의 해당 섹션도 교체**(덧붙이기 금지 — A절 원칙을 이 문서 자체에도 적용)
- 새 로컬 규칙이 생기면 A절에 섹션 추가 + B절에 새 `feedback-*.md` 블록 추가 + MEMORY.md 인덱스(B-0) 갱신, 3곳 전부 같은 커밋

### C-5. 개인 개발도구는 프로젝트 공유 설정과 분리

**원칙**: 팀이 채택하지 않은 개인 도구(JAM 등)를 `.mcp.json`(project-scope, 커밋됨·팀 전체 자동 로드 대상)에 넣지 않는다. 2026-08-25 세션이 JAM을 project-scope로 등록했다가(`ae4aa58`) 2026-08-26 세션에서 발견·정정(`claude mcp add-json jam --scope local`로 이관 후 `.mcp.json` 삭제) — 팀은 JAM의 존재 자체를 모르는데 파일만 조용히 project-scope에 박혀 팀원 전원 세션이 자동 로드 시도하는 상태였다.

- 개인 도구는 `claude mcp add-json <name> '<json>' --scope local`(이 프로젝트+이 머신 한정, `~/.claude.json`에 저장, 커밋 안 됨) 또는 `--scope user`(전 프로젝트 공통)로 등록한다
- `.mcp.json`(project-scope)에는 팀이 합의한 도구만 넣는다 — 지금은 없음
- 개인 도구를 project-scope로 승격하려면 팀 합의가 먼저다(CLAUDE.local.md "팀 규칙 승격은 리드 승인 사안"과 같은 원칙)

**같은 원칙의 두 번째 축 — 저장소 안에 생기는 개인 도구 산출물**. 개인 도구는 MCP 등록만 남기지 않는다. 프로젝트 루트에 runtime 디렉터리·설정·작업 산출물을 만들며, 그것이 `git status`에 untracked로 뜨는 동안은 `git add .` 한 번에 커밋된다(2026-08-26 실측: 4종 노출). 막는 자리는 `.gitignore`가 아니라 `.git/info/exclude`다 — `.gitignore`는 커밋되므로 파일 자체는 안 올라가도 **팀 파일에 개인 도구 이름이 남는다.** 이것은 `.mcp.json` 건과 같은 원칙의 약한 형태다.

- `.git/info/exclude`는 clone을 따라가지 않는다 — 새 머신마다 재등록해야 한다
- **그 패턴 목록을 이 문서에 적지 않는다.** 팀 문서(front에 커밋됨)에 개인 도구 이름을 남기지 않는다는 같은 이유다 — 여기 적으면 `.gitignore`에 적는 것과 결과가 같아진다
- 목록의 이전 가능 사본은 **팀 저장소 밖**이 보관한다. 우선순위: ⑴ 각 도구 저장소가 자기 흔적을 스스로 등록한다(설치 시 자동 — 도구가 자기 산출물 경로를 아는 유일한 주체다) ⑵ 도구 저장소가 없는 산출물(host·플러그인 작업물)은 개인 설정 저장소에 둔다
- 이 문서가 담당하는 것은 **원칙과 복원 순서까지**이고, 값은 담당하지 않는다
