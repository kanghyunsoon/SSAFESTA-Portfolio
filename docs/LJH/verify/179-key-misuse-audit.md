# Jira 키 오사용 전수조사 (S15P21A604-179 사고)

> 조사일: 2026-08-27 | 조사자: 이정헌 | 대상: 내가 만든 커밋 256건 · MR 14건 · 브랜치 전량

## 사고 요약

FE 작업(`상한 guard 이관 + 서버 오류 코드 정렬`)에 **`S15P21A604-179` 를 달았는데 그 키는 황덕의 BE 티켓**(`[TEST] Game Draft 조회·오류 계약 테스트 작성`, Backend 컴포넌트)이었다. 08-25 부터 08-27 까지 브랜치·커밋·MR 3곳에 붙어 그 티켓 상태를 움직였다.

**키가 존재하기만 하면 커밋 형식 검사는 통과하고, Webhook→Jira Automation 은 그 키의 티켓을 말없이 옮긴다.** 요약만 보고 "내 작업 같다"로 넘긴 것이 원인이다.

## 조사 범위와 방법

```bash
git log --all --author=colosair --format='%s%n%b' | grep -oE 'S15P21A604-[0-9]+' | sort | uniq -c
glab api "...merge_requests?scope=created_by_me&state=all" --paginate
```

키 12종을 뽑아 JAM `jira_search` 로 **assignee·component 를 전수 대조**했다.

## 결과 1 — 소유자 오사용: 1건 (확정)

| 키 | 요약 | assignee | component | 판정 |
|---|---|---|---|---|
| **-179** | [TEST] Game Draft 조회·오류 계약 테스트 작성 | **황덕** | **Backend (Spring)** | ❌ **오사용** |
| -275 · -272 · -271 | 이 사고 조사 중 내가 만든 FE 이슈 | 미배정 | Frontend | ✅ |
| -254 · -171 · -153 · -90 · -87 | FE 구현 | 이정헌 | Frontend | ✅ |
| -247 | [FE] FE 코드베이스 develop 반입 | 미배정 | — | △ 범위 이탈(결과 2) |
| -234 | [CI][DOCS] Jira-GitLab 연동 워크플로 구축 | 미배정 | Docs/Process | △ 범위 이탈(결과 2) |
| -251 | [공통] 워크플로 규약 개정 | 미배정 | — | ✅ 주제 일치 |

**나머지 11종은 전부 내 티켓이거나 팀이 공용으로 쓰는 프로세스 키였다. 남의 티켓을 문 것은 `-179` 하나다.**

## 결과 2 — 피해 범위: `Closes` 는 develop 에 0건 도달

가장 중요한 확인이다. **잘못된 키의 `Closes` 가 develop 에 도달했으면 남의 티켓이 자동 완료됐을 것이다.**

| `Closes` 키 | develop 도달 | 내 티켓인가 |
|---|:---:|---|
| -153 · -254 · -271 · -272 · -171 | ✅ | ✅ |
| **-179** | **❌** | ❌ — MR !61 을 **머지 전에 닫았다** |
| -275 | 대기(MR !63) | ✅ |

- `front` 행 MR `!13` 에는 `Closes` 를 넣지 않았다 — 파트 브랜치행에 `Closes` 를 쓰지 않는 규칙(§12 규칙 5)이 **의도치 않게 방어벽이 됐다.**
- 잔존 흔적은 `front` 의 커밋 3건(`cf0e506`·`44472e2`·`21fd4de`)과 `-179` 에 붙은 mention 코멘트다. **history 를 고쳐 지우지 않는다**(force push 금지).
- 닫힌 MR !61 의 소스 브랜치 `fix/S15P21A604-179-develop-intake` 는 원격에서 삭제했다.
- **`-179` 의 상태는 되돌리지 않았다.** 그 티켓의 완료 조건은 MR !33 으로 충족돼 보이지만 판단은 소유자 몫이다. MR !63 본문으로 경위를 통보했다.

## 결과 3 — 더 큰 문제: 키 없는 커밋 173건

전수조사에서 **원래 찾던 것보다 큰 것이 나왔다.**

```text
내 커밋 256건 중 키 없음 173건 (68%)
날짜 분포: 08-14(3) 08-16(1) 08-17(8) 08-18(15) 08-20(23) 08-21(27) 08-22(23) 08-23(57) 08-24(16)
그 중 develop 도달: 11건
```

**전부 2026-08-24 09:59 이전이다.** 키 규칙은 `-234` 로 08-24 에 배포됐고 내 첫 키 커밋은 08-25 09:12 다 — 그 사이 **약 23시간·16건**이 규칙 발효 후의 누락이고, 나머지 157건은 규칙 이전이다.

develop 에 남은 11건은 08-18~08-24 의 문서·정본화 커밋이다(`docs/26` 결정 로그, spec 005 정본화, docs/10 라우트 정합 등). **history 로는 되돌릴 수 없다** — Jira 링크가 영구히 안 붙는다.

이미 한 번 같은 방식으로 보정한 선례가 있다: `-87` 에 남긴 *"[자동화 누락 보정] 본 이슈 구현은 front `440769a` 으로 완료"* 코멘트(08-25). **키가 빠진 구현은 Jira 코멘트로 커밋을 수동 연결하는 것이 유일한 사후 수단이다.**

## 재발 방지

1. **브랜치를 만들기 전에 키의 `assignee`·`component` 를 확인한다.** 요약이 비슷하다는 이유로 재사용하지 않는다. 없으면 새 이슈를 만든다(`-275` 가 그렇게 나왔다).
2. **남의 티켓 상태를 잘못 움직였으면 임의로 되돌리지 않는다.** 경위를 통보하고 판단은 소유자에게 맡긴다 — 그 티켓의 진짜 완료 여부를 내가 모른다.
3. **타인 티켓을 잘못 움직였으면 임의로 상태를 되돌리지 않는다** — 경위만 통보한다.
4. **형식 통과는 정확성의 증거가 아니다.** 키가 존재하기만 하면 검사는 통과하고 자동화는 조용히 돈다.
5. **공용 프로세스 키(`-247`·`-234` 류)는 공용 규칙 자체의 변경에만 쓴다.** 개인 기록·개인 도구 정비는 그 작업의 키로 분리한다 — 이번 조사에서 확인된 scope creep 이다.
6. **자동 검증을 `key exists` 수준에서 끝내지 않는다.** 최소 **assignee + component + 작업 주제 정합성**까지 보는 방향으로 강화한다.

`CLAUDE.local.md`·`docs/LJH/26_로컬_규약_스냅샷.md`·세션 메모리에 등록했다.

> **핵심 교훈: 형식 검증 통과 ≠ 올바른 Jira 키.**

## 소급 보정 판정 (2026-08-27, 조사 종결분)

**과거 커밋 전체를 억지로 Jira 화하지 않는다.** 기존 티켓과 **명확히 1:1 대응되는 것만** `[자동화 누락 보정]` 으로 연결하고, 당시 대응 티켓이 없던 작업은 이 기록만 남기고 종료한다.

| develop 도달 키 없는 커밋 | 대응 티켓 | 처리 |
|---|---|---|
| `a69df08` · `5e39013` · `429985d` (#59 정본화 3건) | **`-98` `[DOCS] spec 상태 서술 정본화 규칙 확정 (#59)`** — 요약에 `#59` 가 그대로 있다 | ✅ **소급 코멘트 등록**(2026-08-27, comment 215823). 상태는 옮기지 않았다 — 미배정 이슈 |
| `44dfd6a` · `aeccede` · `242b48b` · `3556580` (계약 회수·develop 정본화 4건) | `-66` `[DOCS] 파트 간 계약 문서 회수·develop 정본화` | ❌ **연결하지 않음.** 주제는 겹치지만 **1:1 이 아니라 범주 일치**이고, 이미 완료·미배정 티켓이다. "내 것 같다"로 넘기는 것이 이 사고의 원인이라 같은 판단을 반복하지 않는다 |
| `69a67b0`(docs/10 §3 라우트 정합) · `41b119b`(013a ownership FE.md) · `f8c47fc`·`95cd845`(front→develop 계약 통합·revert) | 없음 | ❌ **당시 대응 티켓 부재.** 이 기록만 남기고 종료 |

## 남은 판단 (내 권한 밖)

- **`-179` 상태 정리** — 황덕 (MR !63 본문으로 경위 통보 완료)
- **`-98` 상태 전환** — 미배정 이슈. #59 는 08-26 리드 확정으로 종료됐고 규칙은 `docs/jira-gitlab-workflow.md` §12 규칙 7 에 반영돼 있어 실질은 끝났지만, 상태는 소유자가 판단한다
