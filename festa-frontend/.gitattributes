# 컨테이너 안에서 실행되는 파일은 반드시 LF 여야 한다.
# core.autocrlf=true 인 Windows 에서 clone 하면 체크아웃이 CRLF 가 되고,
# `#!/bin/sh\r` 은 컨테이너에서 "bad interpreter" 로 죽는다 — 만든 사람 환경에서는
# 재현되지 않고 clone 한 사람에게서만 터지는 종류의 실패다 (S15P21A604-254).
*.sh text eol=lf
Dockerfile text eol=lf
docker/** text eol=lf
