VITE_API_BASE_URL=
VITE_GAME_STUDIO_API_ENABLED=false
# 실 BE 없이 개발 — auth·wallet·layout·facade·lease·unity loader 가 메모리 mock 으로 동작 (*.select.ts)
VITE_USE_MOCK=false
# Unity WebGL 빌드가 서빙되는 base URL — `${BASE}/manifest.json` 을 읽는다(unity/host/resolver.ts, 키 4종 #60).
# 비우면 실 빌드 로드는 실패한다(배포 경로는 #30 후속 미결) — mock 로더(VITE_USE_MOCK=true)는 이 값이 필요 없다.
VITE_UNITY_BUILD_BASE=
