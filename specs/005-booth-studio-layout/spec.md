# Feature Specification: Booth Studio / Layout 계약

**Spec**: `005-booth-studio-layout`
**Created**: 2026-08-12
**Status**: Draft — **FE·BE 검토 완료, C-04(기획 승인) 대기**
**주 담당**: Frontend(이정헌) + Backend
**선행 spec**: 004 (임대) — Mock API로 병행 착수 가능
**근거 문서**: docs/02 §2.4(STUDIO-01~13), docs/sdd/constitution.md Article I·21

> ⚠️ **이 spec은 프로젝트 최대의 파트 간 계약(Layout JSON)의 원본이다.**
> React가 만들고 Spring이 저장하고 Unity가 해석한다. 세 파트 중 하나라도 다르게 이해하면
> 통합 시점에 크게 터진다. 그래서 리드가 초안을 쓰되 **3파트 합동 확정**이 필요하다 (헌법 21조).
> Unity 측 소비자 구현(spec 006)은 이미 존재하므로 §계약 초안은 실제 동작하는 형태다.

---

## User Scenarios & Testing

### User Story 1 — 부스를 꾸며서 공개한다 (Priority: P0)

부스를 임대한 사용자가 웹 편집기에서 오브젝트를 배치하고, 저장했다가, 준비되면 공개한다.

**Why this priority**: 이 서비스의 UGC 핵심. "사용자가 Unity 없이 3D 공간을 만든다"는 프로젝트 정체성 그 자체.

**Independent Test**: 빈 부스 → 오브젝트 3개 배치 → 저장 → 새로고침 후에도 배치가 남아있음 → 공개 → 다른 사용자가 그 부스를 방문하면 배치가 보임.

**Acceptance Scenarios**:

1. **Given** 부스를 임대한 사용자, **When** 편집기를 열면, **Then** 배치 가능한 오브젝트 목록과 부스 영역이 보인다.
2. **Given** 편집기, **When** 오브젝트를 추가하고 위치·회전을 조정하면, **Then** 화면에 즉시 반영된다.
3. **Given** 편집 중인 배치, **When** 임시 저장하면, **Then** 아직 방문자에게는 보이지 않는다.
4. **Given** 임시 저장한 배치, **When** 다시 편집기를 열면, **Then** 마지막 저장 상태가 복원된다.
5. **Given** 임시 저장한 배치, **When** 공개하면, **Then** 방문자가 월드에서 그 배치를 볼 수 있다.
6. **Given** 공개된 배치가 있는 상태, **When** 새로 편집하고 저장만 하면, **Then** 방문자에게는 **여전히 이전 공개본**이 보인다.

### User Story 2 — 기능 오브젝트에 내용을 연결한다 (Priority: P0)

배치한 오브젝트(AI 직원, 영상, 설문 등)에 실제 콘텐츠를 연결한다.

**Independent Test**: AI 오브젝트 배치 → 특정 AI 직원 연결 → 공개 → 월드에서 그 오브젝트와 상호작용 시 연결한 AI가 응답.

**Acceptance Scenarios**:

1. **Given** 배치된 기능 오브젝트, **When** 속성 패널에서 콘텐츠를 연결하면, **Then** 연결이 배치 정보에 저장된다.
2. **Given** 콘텐츠가 연결되지 않은 기능 오브젝트, **When** 공개를 시도하면, **Then** 무엇이 비어 있는지 안내된다. [NEEDS CLARIFICATION: 공개를 막을지, 경고만 할지]

### User Story 3 — 실수로 공개되지 않게 한다 (Priority: P1)

**Acceptance Scenarios**:

1. **Given** 임대가 만료된 뒤 다시 임대한 부스, **When** 스튜디오에 진입하면, **Then** 이전 콘텐츠가 보존되어 있되 **자동으로 공개되지는 않는다**.
2. **Given** 유효하지 않은 배치(영역 이탈, 개수 초과), **When** 공개를 시도하면, **Then** 사유가 안내되고 공개되지 않는다.

### Edge Cases

- 두 사람(Owner와 Staff)이 같은 부스를 동시에 편집하는 경우 → C-05 확정: `version` 낙관적 잠금 + 409
- 오브젝트를 100개 배치하는 경우 → 월드 성능에 직접 영향 (헌법 4조)
- Unity가 모르는 새 오브젝트 타입이 배치된 경우 → **부스 전체가 실패하면 안 된다** (006 FR 참조)
- 공개 직후 임대가 만료되는 경우
- 편집 중 브라우저가 닫힌 경우 (Autosave는 P1)

---

## Requirements

### Functional Requirements

- **FR-001**: 부스 소유자는 오브젝트를 추가·이동·회전·삭제할 수 있어야 한다.
- **FR-002**: 시스템은 제공된 템플릿에서 부스 기본 구성을 선택할 수 있어야 한다.
- **FR-003**: 시스템은 규칙적 배치를 돕는 정렬(스냅) 수단을 제공해야 한다.
- **FR-004**: 시스템은 오브젝트별 속성을 편집하고 콘텐츠를 연결할 수 있어야 한다.
- **FR-005**: 시스템은 **작업본(Draft)과 공개본(Published)을 분리**해 저장해야 한다.
- **FR-006**: 방문자에게는 **공개본만** 노출되어야 한다.
- **FR-007**: 공개 전 유효성 검사를 수행하고, 실패 시 사유를 사용자에게 알려야 한다.
- **FR-008**: 배치 정보는 **버전을 가져야** 한다 (Unity가 스키마 변화에 대응할 수 있도록).
- **FR-009**: 배치 정보는 3D 자산이 아니라 **데이터(오브젝트 종류·위치·회전·연결 ID)** 여야 한다 (헌법 4조).
- **FR-010**: 부스당 배치 가능한 오브젝트 수는 **최대 12개**여야 한다 (헌법 22조). 성능 실측 결과 부족하면 하향한다.
- **FR-011**: 재임대 시 보존된 콘텐츠는 **작업본 상태로 시작**해야 한다 (자동 공개 금지).
- **FR-012**: 소유자·권한 있는 Staff 외에는 편집할 수 없어야 한다.
- **FR-013**: 배치 정보 스키마 변경은 **React·Spring·Unity 3파트 합의로만** 가능하다 (헌법 21조).
- **FR-014**: 작업본 저장은 **다른 편집자가 먼저 저장한 것을 조용히 덮어쓰면 안 된다.** 충돌을 감지해 거부하고 사유를 알려야 한다 (C-05). *— 2026-08-20 BE 추가*
- **FR-015**: **임대가 유효하지 않은 부스의 공개본은 일반 조회에 제공되지 않아야 한다.** 004 FR-008과 같은 술어를 쓴다 (docs/08 만료 처리 계약). *— 2026-08-20 BE 추가*
- **FR-016**: 공개 검증 결과는 **공개를 막는 사유와 막지 않는 경고를 구분해** 전달해야 한다. C-04·C-06처럼 아직 확정되지 않은 규칙은 이 구분 안에서 자리만 옮기면 되도록 한다. *— 2026-08-20 BE 추가*
- **FR-017**: 재임대로 부스가 슬롯에 다시 연결될 때 **공개본 연결은 해제된 상태로 시작해야 한다.** FR-011("작업본 상태로 시작")을 실제로 성립시키는 수단이며, 이것이 없으면 이전 공개본이 자동으로 되살아난다. *— 2026-08-20 BE 추가*
- **FR-018**: 부스 외부 표현(**facade** — 테마·대표색·간판 문구·로고)은 소유자·권한 있는 Staff가 수정할 수 있어야 하며, **자유 배치가 아니라 정해진 필드 집합**이어야 한다 (006의 "제한형 Facade"와 같은 범위). *— 2026-08-20 BE 추가, facade 설계 문서 채택에 따름*

### Key Entities

- **Booth Layout**: 한 부스의 배치. 부스 식별자, 템플릿, 버전, 오브젝트 목록, 상태(작업본/공개본)
- **Layout Object**: 배치된 오브젝트 하나. 식별자, 종류, 위치, 회전, 연결된 콘텐츠 식별자
- **Object Type**: 배치 가능한 오브젝트 종류. 기능형(AI/영상/설문/상담데스크/프로젝트패널/노트북)과 장식형

---

## 공통 계약 기준 — Layout JSON

> 아래 필드명과 Object Type은 Backend/Frontend/Unity 공통 기준이며 Unity 파싱을 검증했다. 좌표 원점·오브젝트 상한·스케일 등 아래 미결정 항목은 3파트 합의 후 확정한다.

```json
{
  "boothId": 7,
  "template": "PROJECT_EXHIBITION",
  "schemaVersion": 1,
  "objects": [
    { "objectId": "screen-1", "type": "VIDEO_SCREEN",
      "position": { "x": 2.1, "y": 0, "z": 1.4 }, "rotationY": 90, "configId": 152 },
    { "objectId": "ai-1", "type": "AI_AGENT",
      "position": { "x": 1.2, "y": 0, "z": 1.5 }, "rotationY": 0, "configId": 78 }
  ]
}
```

> **`schemaVersion` ≠ `version`** — `schemaVersion`은 Layout JSON의 **구조 버전**(현재 1, 모양이 바뀔 때만 증가)이고, `version`은 published 응답에만 실리는 **공개 회차**다. 예시가 `"version": 2`로 적혀 있던 것을 #36 합의(2026-08-21)로 정정했다.
> 좌표·실물 검증과 통행 판정의 기하 계약(타입별 bounds 실측, 회전 규칙, 래스터 파라미터)은 **`contracts/layout-api.md` §9~§10**이 원본이다 (#19, 2026-08-21 확정 — 부스는 6×6×**2.72**m).

**공통 canonical type 문자열**: `AI_AGENT`, `VIDEO_SCREEN`, `PROJECT_PANEL`, `SURVEY_KIOSK`, `RECRUITMENT_BOARD`, `CONSULTATION_DESK`, `LAPTOP`, `LIKE_VOTE`, `FURNITURE`, `DECORATION`

Unity는 v0.0.1 POC 데이터 하위 호환을 위해 `SURVEY`, `CONSULT_DESK`도 읽지만 Frontend와 Backend는 신규 Layout에 canonical 문자열만 저장한다.

### 좌표 규칙이 왜 문제인가 (C-02 배경)

React 편집기는 **위에서 내려다보는 2D 평면**에서 오브젝트를 놓고, Unity는 그 숫자로 **3D 공간**에 놓는다.
`"position": { "x": 2.1, "y": 0, "z": 3.4 }` 라는 값 하나를 두고 세 파트가 서로 다르게 해석할 수 있는 지점이 4개다.

| 논점 | 어긋나면 생기는 일 |
|---|---|
| **원점(0,0)이 어디인가** — 부스 바닥 중앙 vs 왼쪽 위 모서리 | 모든 오브젝트가 부스 크기의 절반만큼 통째로 밀린다 |
| **1.0이 얼마인가** — 1미터 vs 1픽셀 vs 1그리드칸 | 2.1이 2.1m가 아니라 2.1px로 해석되면 전부 원점에 뭉치고, 반대면 200m 밖으로 날아간다 |
| **2D 세로축이 Unity의 무엇인가** — 화면 아래로 갈수록 +z인가 −z인가 | 부스 배치가 **앞뒤로 뒤집힌다**. 입구에 둔 안내 데스크가 벽 뒤에 생긴다 |
| **rotationY 0도가 어느 방향인가** | 모든 오브젝트가 엉뚱한 쪽을 본다. 화면이 벽을 보고 NPC가 등을 돌린다 |

**✅ 확정 규칙 (2026-08-12 채택 — 헌법 21조)**

1. **좌표는 React가 Unity 기준으로 변환해서 보낸다.** JSON의 값은 항상 "부스 기준 미터(m)"다.
   편집기 내부의 픽셀·그리드 단위는 React가 저장 직전에 환산한다. *Unity는 픽셀 개념을 모르므로 추측할 수 없다.*
2. **원점은 부스 바닥의 중앙.** `y = 0`이 바닥면이다. (오브젝트가 바닥에 묻히는 문제는 Unity가 피벗 보정으로 처리한다)
3. **+Z가 부스 정면**(방문자가 부스를 바라볼 때 안쪽 방향), **+X는 그 기준 오른쪽**.
   2D 편집기에서 아래로 내려가는 것은 `-Z`로 환산한다.
4. **rotationY는 도(degree)**, `0`이면 오브젝트가 `+Z`(부스 정면)를 바라본다. 위에서 볼 때 시계 방향이 +.

**확정 방법**: 합의 후 React가 오브젝트 1개짜리 Layout을 만들어 보내고, Unity가 같은 위치에 놓이는지 눈으로 확인한다.
이 왕복 1회면 네 논점이 전부 검증된다. **말로 합의하고 넘어가지 말 것** — 부호 하나는 반드시 틀린다.

**확정이 필요한 지점**:

| 항목 | 현재 초안 | 논점 |
|---|---|---|
| 좌표계 | y=0 바닥 기준, 부스 로컬 좌표 | 원점이 부스 중앙인가 모서리인가 — **반드시 합의** |
| 단위 | Unity 단위(1 = 1m) | React 편집기의 픽셀↔미터 환산 규칙 |
| 회전 | `rotationY`만 (수평 회전) | X/Z 회전 필요한 오브젝트가 있는가 |
| `configId` | 정수 1개 | 오브젝트별로 여러 설정이 필요해지면? (예: 노트북 URL은 정수 ID가 아님 → 016 참조) |
| 스케일 | 없음 | 크기 조절을 허용할 것인가 |
| 버전 | 정수 증가 | 스키마 버전과 콘텐츠 버전을 구분할 것인가 |

---

## Success Criteria

- **SC-001**: 사용자가 편집기 사용법을 안내받지 않고도 오브젝트 3개를 배치해 공개까지 완료할 수 있다.
- **SC-002**: 공개(Publish)한 배치가 방문자에게 보이는 시점은 006 C-01에 따라 **Publish 이후
  방문자가 그 부스 구역에 진입하는 시점**이다. 이미 안에 있던 방문자의 재진입, 밖에 있던
  방문자의 진입, 첫 방문 모두 같은 경로다. Publish 이후의 진입에서 이전 공개본이 보인 사례가 0건이다.
  *(주기 폴링·서버 푸시가 없으므로 "몇 초 이내"라는 시간 목표는 성립하지 않는다 — 006 C-01에서 역산.
  서버 반영 방식(동기/캐시)은 규정하지 않되, 어느 쪽이든 이 기준을 만족해야 한다.)*
- **SC-003**: 작업 중 저장한 내용이 방문자에게 노출된 사례가 0건이다.
- **SC-004**: 같은 Layout JSON을 React 미리보기와 Unity 월드에서 열었을 때 배치가 일치한다.
- **SC-005**: Unity가 모르는 오브젝트가 포함된 배치에서도 나머지 오브젝트가 정상 표시된다.

---

## Clarifications

| # | 질문 | 담당 | 메모 |
|---|---|---|---|
| ~~C-01~~ | ~~부스 최대 오브젝트 수~~ | — | ✅ **확정: 12개** (요청 10~12 중 상한 채택, 실측 후 하향 가능) |
| ~~C-02~~ | ~~좌표 원점·단위 규칙~~ | — | ✅ **확정: 미터 / 부스 바닥 중앙 원점 / +Z 정면 / rotationY 0=+Z** (헌법 21조). **왕복 검증 1회는 여전히 필수** |
| ~~C-03~~ | ~~오브젝트 크기 조절을 허용하는가?~~ | — | ✅ **확정: 고정 크기** (2026-08-18). `scale` 필드 없음 — Layout 계약 변경 없음 |
| C-04 | 콘텐츠 미연결 오브젝트의 공개를 막는가? | FE + 기획 | ⏳ 기획 대기 — BE 구현은 검증 결과를 `errors`(차단)/`warnings`(허용)로 나눠 응답하므로, 확정은 이 항목을 두 리스트 사이에서 옮기는 것뿐이다(FR-016). 현재 기본값 **warning**(공개 허용) |
| ~~C-05~~ | ~~Owner와 Staff의 동시 편집 정책은?~~ | — | ✅ **확정: `version` 낙관적 잠금 + 409.** BE 채택([Issue #5](https://github.com/kanghyunsoon/ssafesta/issues/5), 2026-08-20). 409 응답 body는 **`{code, message, requestId}`로 확정·구현 완료** (2026-08-20, `docs/08` §1.3 — 클라이언트는 `code`로만 분기). 요청 스키마는 body `expectedRevision` 필수로 확정·구현 완료, 코드 `LAYOUT_REVISION_CONFLICT` (2026-08-20) |
| C-06 | 템플릿은 몇 종이며 무엇이 다른가? | 기획 | ⏳ 기획 대기(종수·차이) — BE는 화이트리스트 검증만 한다. **허용값은 `PROJECT_EXHIBITION` 단독** — `DEFAULT`는 셸 1종·1:1 확정으로 제거([#19](https://github.com/kanghyunsoon/ssafesta/issues/19) ④, 2026-08-21, V11 이관). footprint·상한 SSOT는 `GET /booth-layout-templates` 신설로 해소 (contracts §9) |
| C-07 | 공개 이력(과거 버전)을 보관하고 되돌릴 수 있는가? | BE | ✅ **BE 답변(2026-08-20): 보관은 한다, 되돌리기 API는 MVP 제외.** `booth_layout_published_versions`가 이미 이력 구조라 보관 비용 0. 되돌리기는 UI·권한·"되돌린 것도 새 버전인가"를 함께 정해야 하므로 지금 열지 않는다 (재임대 미리보기와 연관) |

---

## Out of Scope (이 spec에서 다루지 않음)

- Undo/Redo, Autosave, 복제 (P1 — STUDIO-10~12)
- 템플릿 저장·공유 (P2 — STUDIO-14)
- Unity 내부 3D 표현 방식 → spec 006
- 노트북 홈페이지 열람 동작 → spec 016 (단, `LAPTOP` 오브젝트의 계약 필드는 여기서 함께 확정)

---

### FE 검토 (2026-08-14) — Frontend 파트분

| # | FE 답변 |
|---|---|
| C-03 크기 조절 | **MVP 제외 의견.** `scale` 추가는 3파트 재합의를 부른다 — 기획 + Unity 결정 대기 |
| C-04 미연결 공개 | **막지 않고 경고.** FR-007은 "알린다"이지 "막는다"가 아니다. 공개 시 미연결 목록 표시 후 진행, 방문자 쪽은 016 FR-009와 동일하게 오류가 아닌 안내 — 기획 승인 대기 |
| C-05 동시 편집 | **`version` 낙관적 잠금 + 409 제안.** 현재 `PUT /layouts/draft`에 충돌 감지 수단이 없어 마지막 저장이 앞 작업을 덮는다 — 요청 스키마 변경이라 BE 합의 필요 |
| C-06 템플릿 종수 | FE 결정 불가 — 기획. 편집기는 서버 제공 목록에서 고르는 구조로 만든다. **부스 크기가 확정돼야 FR-003 스냅 간격과 영역 이탈 검증값이 정해진다** |
| C-07 공개 이력 | **되돌리기 MVP 제외 의견.** 단 `version` 단조 증가는 유지해야 한다 (C-05가 의존) — BE 결정 대기 |

**상정 시점**: C-03·C-06은 다음 Layout 계약 회의에 FE가 안건으로 올린다. C-04는 기획에 개별 확인. C-05·C-07은 BE 착수 전까지 합의한다 — BE가 Draft API 스키마를 확정하는 시점이 마지막 기한이다.

*C-01·C-02는 팀 결정으로 이미 확정 — 헌법 21·22조. Layout 필드·타입 불일치는 2026-08-14 3파트 확정으로 해소(T-92).*

### FE 지적·제안

| 구분 | 내용 |
|---|---|
| ② SC-002 | **해소** — 대응 C-xx 없이 방치되던 목표 수치를 006 C-01(부스 구역 진입 시 조회, 폴링·푸시 없음)에서 역산해 관찰 기준으로 재작성. 서버 반영 방식(동기/캐시)은 규정하지 않음 |
| ② FR-003 | 스냅 격자의 **단위·간격이 없다.** 좌표는 미터로 확정됐으나 간격은 부스 크기(C-06)에 종속된다. 설정값으로 두고 확정 후 주입 |
| ③ 신규 FR | **외부 설정(Facade) 편집** — 부스명·간판·로고·대표색·테마·영업 상태. 2026-08-14 외부/내부 분리 결정이 006에만 반영됐고 005에 요구사항이 없다. `docs/08`에 저장 API도 없다 |
| ③ 신규 FR | 편집기의 Loading / Error / Empty / **Forbidden** 상태 정의 (`parts/FE.md` 공통 비기능). 임대 만료 직후 진입이 Edge Case에 있는데 대응 요구가 없다 |
| ③ 신규 FR | **서버의 독립적 개수·영역 검증** (헌법 16조). 006 C-03의 편집기 차단은 UX 책임이지 인가가 아니다 |
| ③ 신규 FR | `objectId` **생성 주체·유일성 범위**. `docs/10` §8이 중복 검증을 들지만 누가 만드는지가 없다 |

### BE 검토 상세 (2026-08-20)

#### A. 틀렸거나 과한 요구사항

| 대상 | 지적 |
|---|---|
| FR-002 (템플릿) | C-06이 미정이라 "기본 구성 선택"의 **실체가 없다.** 1차는 `template` 문자열 저장 + 화이트리스트 검증까지만 하고, 템플릿이 실제로 무엇을 배치하는지는 기획 확정 후 FE가 채운다 |
| FR-003 (스냅) | **순수 FE 기능이다.** 저장되는 값은 이미 환산된 미터 좌표라 BE 계약에 영향이 0이다. spec에 남기되 BE 작업 목록에는 넣지 않는다 |
| SC-002 | "기다릴 만한 수준"은 측정 대상이 아니라 **전파 방식**의 문제였다 → 정정함 |
| Edge Case "오브젝트 100개" | FR-010(12개)으로 닫히지만, 검증을 **공개 시점에만** 걸면 FE 버그로 200개짜리 작업본이 저장돼 JSONB만 부푼다. **Draft 저장 시점에도 같은 상한을 건다** |

#### B. V1 실물 스키마와 docs/08·09의 불일치 — U-05의 실제 범위

U-05는 facade 하나로 기록돼 있었으나 **네 곳이다.** 아직 아무도 구현하지 않았으므로 지금이 정리 비용이 가장 싸다.

| # | V1 실물 | docs/09 | docs/08 | BE 결정 (2026-08-20) |
|---|---|---|---|---|
| 1 | `booth_layout_drafts`(PK=`booth_id`) + `booth_layout_published_versions` **2테이블** | `booth_layouts` 단일 테이블 + `state` | — | ✅ **V1 유지, docs/09 정정.** PK가 `booth_id`라 "작업본은 하나"가 스키마로 강제되고, 이력(C-07)과 낙관적 잠금 컬럼(`revision`, C-05)이 이미 있다. 단일 테이블+`state`는 **DRAFT 행이 둘 생기는 것을 막지 못한다** — T-110에서 뚫린 것과 같은 종류 |
| 2 | 공개본 포인터 **없음** | `booths.published_layout_version` | 응답 `publishedLayoutVersion` | ✅ **NULL 허용 컬럼으로 추가.** `MAX(version_no)` 유도로는 **FR-011/FR-017을 표현할 수 없다** — 재임대 시 이전 공개본이 자동으로 되살아난다 |
| 3 | `facade_code VARCHAR(50)` 단일 | `facade_theme_code`·`primary_color`·`sign_text`·`logo_url` 4컬럼 | 응답 `facade{themeCode,…}` 4필드 | ✅ **설계 문서대로 4필드 채택 (2026-08-20 결정).** V9에서 `facade_code` → `facade_theme_code` rename + 3컬럼 추가. 값이 전부 `'DEFAULT'`라 데이터 이관이 없다 |
| 4 | — | — | — | ✅ **facade 편집을 005 범위에 넣는다.** 외부 Facade는 006에만 있었고 "누가 편집하는가"가 어느 spec에도 없었다. 편집 경로가 없으면 4필드는 **영원히 기본값**이다 → FR-018 신설, `PUT /booths/{id}/facade`는 docs/08에 없으므로 **추가 통보 대상**(헌법 24조) |

#### C. 검증 규칙이 명세되어 있지 않다

FR-007이 "유효성 검사"를 요구하는데 **무엇을 검사하는지가 어디에도 없다.** 클라이언트가 보낸 값을 믿지 않으려면(헌법 16조) 목록이 필요하다. 1차 제안 — 확정은 plan/contracts에서 한다.

| 규칙 | 분류 |
|---|---|
| 오브젝트 12개 이하 (FR-010) | error |
| `objectId`가 한 배치 안에서 유일 | error |
| `type`이 canonical 문자열 화이트리스트에 있음 | error |
| `position`·`rotationY`가 유한한 수, 부스 영역 내 (`0 ≤ y ≤ 2.72` — 셸 실측, #19 ②) | error |
| **실물(회전 반영 AABB)이 부스 영역 안** — 앵커는 안인데 실물이 옆 슬롯에 걸치는 배치 차단 (#19 ③, contracts §10-2) | error `AREA_OUT_OF_BOUNDS` |
| `template`이 화이트리스트에 있음 (`PROJECT_EXHIBITION` 단독 — #19 ④) | error |
| `configId`가 가리키는 콘텐츠가 **그 부스 소유**인지 (헌법 16·17조) | error |
| 기능 오브젝트의 `configId` 미연결 (C-04) | **warning** — 확정 시 error로 옮길 수 있다 |
| **통행 판정** (공개 시점만, #19 ⑤·contracts §10-3): 관람 띠 도달 <50% → `FRONT_BLOCKED`, 고립 공간 ≥1㎡ → `ISOLATED_AREA` | **warning** — 뒷공간 활용은 소유자의 선택일 수 있어 공개를 막지 않는다 |

---

## 리뷰 — FE 검토 완료 (C-04 기획 승인 대기)

| 항목 | 결과 | 완료 |
|---|---|---|
| ① 남은 Clarification 답변 (C-03~C-07) | 5건 의견 제시 — **전부 타 파트 결정 대기** | ☑ |
| ② 틀렸거나 과한 요구사항 지적 | 2건 — 위 표 참조 | ☑ |
| ③ 빠진 요구사항 추가 | 4건 제안 — 위 표 참조 | ☑ |
| ④ **왕복 검증 1회 수행** (오브젝트 1개 Layout → Unity 위치 일치 확인) | 통과 — 실측 world 좌표·회전 기대표와 소수점까지 일치, 부호 오류 0건. 상세: `docs/LJH/verify/block1-roundtrip.md` | ☑ FE 이정헌 / Unity 강형순 (2026-08-20) |

**FE 검토자**: 이정헌 / **검토일**: 2026-08-14
**BE 검토**: 황덕 / **검토일**: 2026-08-20
**비고**: ④ 검증 대상에 `SURVEY_KIOSK`·`CONSULTATION_DESK`를 포함했다. 같은 실행에서 Unity 계약 정합 3건(`objectId` 정식화·엔드포인트 복수형·`assetCode` 경로)도 확인됨 — `origin/game 8852861`.

> **FE·BE 검토 완료** (2026-08-14 / 2026-08-20). C-03·C-05 확정, C-06 보류, C-07 후순위.
> **C-04(미연결 오브젝트 공개 차단 여부)는 기획 승인 대기** — 이 항목이 닫혀야 spec 전체가 확정되고 `/speckit-plan` 진행 가능.
