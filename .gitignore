# 외부 레퍼런스 프로젝트 — 저장소에 포함하지 않는다 (용량 + 라이선스)
# BossRoom / VampireSurvivors / IdleGameScripts는 로컬 참고용으로만 유지
reference/
references/
Reference/

# Secret / Credential — 절대 커밋 금지 (.env.example은 허용)
.env
.env.*
!.env.example
*.pem
*.p12
*.key
infra/environments/**/secrets/

# OS
.DS_Store
Thumbs.db

# 개인 에디터 설정 — 사람마다 다르고 문서 서식을 임의로 바꾼다 (Obsidian이 표를 자동 정렬해 확정 spec에 diff를 낸 적 있음)
.obsidian/

# 원격 세션이 파일을 지울 수 없을 때 임시로 옮겨두는 폴더 (수동 삭제 대상)
_to_delete/

# RAG 스파이크 평가용 원본 기획서·PDF — 결과 JSON에 원문을 넣지 않는 정책과 동일하게
# 소스 자체도 커밋하지 않는다 (spikes/ai-rag/README.md 안전 조건)
기획서 문서/
festa-ai/*.pdf
