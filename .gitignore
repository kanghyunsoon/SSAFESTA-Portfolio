# 외부 레퍼런스 프로젝트 — 저장소에 포함하지 않는다 (용량 + 라이선스)
# BossRoom / VampireSurvivors / IdleGameScripts는 로컬 참고용으로만 유지
reference/
references/
Reference/

# Secret / Credential — 절대 커밋 금지 (.env.example은 허용)
.env
.env.*
!.env.example
*.pem
*.p12
*.key

# OS
.DS_Store
Thumbs.db

# 원격 세션이 파일을 지울 수 없을 때 임시로 옮겨두는 폴더 (수동 삭제 대상)
_to_delete/

# 로컬 Unity 시각 QA 산출물 — 소스 에셋이 아니므로 커밋하지 않는다
festa-unity/Assets/Screenshots/
design-qa.md

# Claude Code 로컬 권한/개인 설정 — 팀 공유 대상이 아니다 (.claude/settings.json 은 공유)
.claude/settings.local.json
festa-unity/Assets/Screenshots.meta

# Booth 임시 편집기의 로컬 상태 — published 는 mock 픽스처라 커밋 대상이다
festa-unity/Tools/mock-api/api/v1/booths/*/layouts/draft
festa-unity/Tools/mock-api/api/v1/booths/*/facade.json
